description = "Executes the {{name}} task from the GoMad."
prompt = """
Execute the GOMAD '{{name}}' task.

TASK INSTRUCTIONS:
1. LOAD the task file from {project-root}/{{gomadFolderName}}/{{path}}
2. READ its entire contents
3. FOLLOW every instruction precisely as specified

TASK FILE: {project-root}/{{gomadFolderName}}/{{path}}
"""
