---
phase: 04-publish-and-verify
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [PUB-01, TST-04]
must_haves:
  truths:
    - "package.json declares the package as @xgent-ai/gomad@1.0.0 with public publishConfig"
    - "package.json description mentions curated Claude Code skills/agents/rules/hooks/commands AND project-local install"
    - "bin field key is still gomad (unchanged) so the CLI command name is preserved"
    - "No literal 'vitest' references remain in .planning/REQUIREMENTS.md, .planning/PROJECT.md, or .planning/ROADMAP.md"
    - "PROJECT.md no longer claims private-npm-only and no longer lists public npm publication as Out of Scope"
    - "npm pack --dry-run produces a tarball whose contents match the existing files whitelist"
  artifacts:
    - path: "package.json"
      provides: "Public-npm publish metadata for @xgent-ai/gomad@1.0.0"
      contains: "@xgent-ai/gomad"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated TST-04 wording"
      contains: "npm test"
    - path: ".planning/PROJECT.md"
      provides: "Reversed private-npm constraint per D-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 success criterion #4 reworded; vitest mentions removed"
  key_links:
    - from: "package.json"
      to: "npm registry"
      via: "publishConfig.access=public"
      pattern: "\"access\":\\s*\"public\""
---

<objective>
Configure package.json for public npm publish as `@xgent-ai/gomad@1.0.0` and clean up stale `vitest` references from planning docs to align with the locked test-runner decision (D-04, D-05).

Purpose: Phase 4 publishes to public npm under `@xgent-ai/gomad`. The package name, version, description, and `publishConfig.access` must be correct before any subsequent publish flow can succeed. Simultaneously, the planning docs must stop claiming a non-existent vitest dependency and stop listing public npm as Out of Scope.

Output: Updated `package.json`, `.planning/REQUIREMENTS.md`, `.planning/PROJECT.md`, `.planning/ROADMAP.md`, plus a verified `npm pack --dry-run` review showing the tarball contents match the existing `files` whitelist.
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-publish-and-verify/04-CONTEXT.md
@.planning/phases/04-publish-and-verify/04-RESEARCH.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update package.json publish metadata (D-02, D-09, D-10, D-12)</name>
  <files>package.json</files>
  <read_first>
    - package.json (current state, before edits)
    - .planning/phases/04-publish-and-verify/04-CONTEXT.md (locked decisions D-01..D-13)
    - .planning/phases/04-publish-and-verify/04-RESEARCH.md §"Package.json Diff (exact before/after)"
  </read_first>
  <action>
    Edit `package.json` with these EXACT changes (per D-02, D-09, D-10, D-12 and the verified diff in 04-RESEARCH.md §"Package.json Diff"):

    1. Change `"name": "gomad"` to `"name": "@xgent-ai/gomad"`.
    2. Change `"version": "0.1.0"` to `"version": "1.0.0"`.
    3. Replace the current `description` value with: `"One command installs curated Claude Code skills, agents, rules, hooks, and commands into your project's local .claude/ directory."` (this satisfies D-09: mentions "curated Claude Code skills, agents, rules, hooks, and commands" AND "project-local").
    4. Add a new top-level `publishConfig` block immediately after `version`:
       ```json
       "publishConfig": {
         "access": "public"
       },
       ```
       Placement: after `version`, before `description` (or anywhere before `scripts`; npm convention is near the top).
    5. LEAVE UNCHANGED: `bin` (the `gomad` key MUST stay — see D-02 and 04-RESEARCH.md Pitfall 2; renaming the bin key would break the CLI command), `type`, `engines`, `dependencies`, `keywords`, `author`, `license`, `files`, and `scripts` (scripts will be touched by Plan 04-02 if needed).
    6. Do NOT add `repository`, `homepage`, or `bugs` (D-11 — explicitly deferred).
    7. Do NOT modify the `files` array (D-13 — keep `["bin/", "assets/", "catalog/", "tools/", "README.md"]`).

    After saving, the file must remain valid JSON (no trailing commas, no comments).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.name!=='@xgent-ai/gomad')process.exit(1); if(p.version!=='1.0.0')process.exit(2); if(!p.publishConfig||p.publishConfig.access!=='public')process.exit(3); if(!p.bin||p.bin.gomad!=='bin/gomad-cli.js')process.exit(4); if(!p.description.includes('curated Claude Code')||!p.description.includes('.claude/'))process.exit(5); if(p.repository||p.homepage||p.bugs)process.exit(6); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` parses as valid JSON (`node -e "require('./package.json')"` exits 0)
    - `package.json` contains the literal string `"@xgent-ai/gomad"`
    - `package.json` contains the literal string `"1.0.0"`
    - `package.json` contains `"access": "public"` inside a `publishConfig` block
    - `package.json` `bin` field still maps `gomad` → `bin/gomad-cli.js` (the bin KEY is unchanged)
    - `package.json` `description` includes both "curated Claude Code" and ".claude/"
    - `package.json` has NO `repository`, `homepage`, or `bugs` fields
    - `package.json` `files` array is unchanged: `["bin/", "assets/", "catalog/", "tools/", "README.md"]`
    - `package.json` does NOT contain the unscoped string `"name": "gomad"` (only the scoped form)
  </acceptance_criteria>
  <done>
    package.json is configured for first-publish to public npm as @xgent-ai/gomad@1.0.0; bin command name is preserved; deferred fields are absent.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove stale vitest references from planning docs (D-05)</name>
  <files>.planning/REQUIREMENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (line 45, TST-04)
    - .planning/PROJECT.md (line 20, "35 unit tests with vitest" + the Out of Scope and Constraints sections)
    - .planning/ROADMAP.md (lines ~30 and ~80, Phase 1 and Phase 4 success criteria)
    - .planning/phases/04-publish-and-verify/04-CONTEXT.md (D-01, D-04, D-05)
    - .planning/phases/04-publish-and-verify/04-RESEARCH.md §"TST-04 Wording Cleanup" (exact line numbers + verified file list)
  </read_first>
  <action>
    Apply text edits to three planning docs. Per 04-RESEARCH.md, CLAUDE.md has zero literal `vitest` references — DO NOT edit CLAUDE.md.

    **Edit 1 — `.planning/REQUIREMENTS.md` line ~45 (TST-04):**
    Replace:
      `- [ ] **TST-04**: All tests pass with vitest`
    With:
      `- [ ] **TST-04**: All tests pass with the project test runner (\`npm test\`)`

    **Edit 2 — `.planning/PROJECT.md` line ~20 (Validated requirements list):**
    Replace:
      `- ✓ 35 unit tests with vitest — existing`
    With:
      `- ✓ 35 unit tests with node --test — existing`

    **Edit 3 — `.planning/PROJECT.md` (D-01 reversal — REQUIRED to reflect public-npm decision):**
    a. In the "Out of Scope" list, REMOVE the line: `- Public npm publication — private registry only`
    b. In the "Constraints" section, replace `- **Registry**: Publish to private npm (not public)` with `- **Registry**: Publish to public npm as @xgent-ai/gomad`
    c. In the "Active" requirements list, replace `- [ ] Publish to private npm registry as gomad` with `- [ ] Publish to public npm as @xgent-ai/gomad`
    d. In the "Key Decisions" table, replace the row `| Private npm publish | Internal distribution only | — Pending |` with `| Public npm publish as @xgent-ai/gomad | Standalone tool, broad distribution | — Pending (Phase 4) |`

    **Edit 4 — `.planning/REQUIREMENTS.md` "Out of Scope" table:**
    Remove the row: `| Public npm publication | Private registry only for now |` (D-01 reversal — public publication is now in scope).

    **Edit 5 — `.planning/ROADMAP.md` Phase 1 success criterion (line ~30):**
    Replace:
      `4. All tests pass with vitest after renaming (test files reference gomad, not mobmad)`
    With:
      `4. All tests pass with the project test runner (\`npm test\`) after renaming (test files reference gomad, not mobmad)`

    **Edit 6 — `.planning/ROADMAP.md` Phase 4 success criterion (line ~80):**
    Replace:
      `4. Full test suite passes with vitest (all 20 requirements verified)`
    With:
      `4. Full test suite passes with the project test runner \`npm test\` (all 20 requirements verified)`

    DO NOT touch:
    - `catalog/skills.yaml` or `catalog/presets.yaml` references to `vitest` — those are legitimate skill catalog entries (a skill gomad ships for users who use vitest in THEIR projects). 04-RESEARCH.md §"TST-04 Wording Cleanup" explicitly excludes them.
    - `.claude/get-shit-done/**`, `BMAD/**`, or any historical `.planning/phases/01-*` or `.planning/phases/03-*` files.
    - `CLAUDE.md` (no matches per 04-RESEARCH.md Open Question Q1).
  </action>
  <verify>
    <automated>bash -c 'set -e; for f in .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/ROADMAP.md; do if grep -in "vitest" "$f"; then echo "STALE vitest in $f"; exit 1; fi; done; grep -q "npm test" .planning/REQUIREMENTS.md && grep -q "node --test" .planning/PROJECT.md && grep -q "@xgent-ai/gomad" .planning/PROJECT.md && ! grep -q "Public npm publication" .planning/PROJECT.md && ! grep -q "Publish to private npm" .planning/PROJECT.md && echo ok'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c vitest .planning/REQUIREMENTS.md` returns 0
    - `grep -c vitest .planning/PROJECT.md` returns 0
    - `grep -c vitest .planning/ROADMAP.md` returns 0
    - `.planning/REQUIREMENTS.md` contains the literal string `npm test` in the TST-04 line
    - `.planning/PROJECT.md` contains the literal string `node --test`
    - `.planning/PROJECT.md` contains the literal string `@xgent-ai/gomad`
    - `.planning/PROJECT.md` does NOT contain `Public npm publication` (Out of Scope row removed)
    - `.planning/PROJECT.md` does NOT contain `Publish to private npm`
    - `CLAUDE.md` is unchanged (no edits made to it)
    - `catalog/skills.yaml` is unchanged
    - `catalog/presets.yaml` is unchanged
  </acceptance_criteria>
  <done>
    All `.planning/*.md` files reflect the locked D-01 (public npm) and D-04/D-05 (node --test, not vitest) decisions; no stale vitest mentions remain in canonical planning docs; CLAUDE.md is untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: npm pack dry-run sanity check (D-13)</name>
  <files></files>
  <read_first>
    - package.json (post-Task-1 state — `files` array, `name`, `version`)
    - .planning/phases/04-publish-and-verify/04-CONTEXT.md (D-13)
    - .planning/phases/04-publish-and-verify/04-RESEARCH.md §"Package.json Diff" (Validation step) and Pitfall 3
  </read_first>
  <action>
    Run `npm pack --dry-run` from the repo root and inspect the output. Per D-13, the `files` array stays as `["bin/", "assets/", "catalog/", "tools/", "README.md"]` — DO NOT expand it. The purpose of this task is to verify the existing whitelist is sane.

    Steps:
    1. Run: `npm pack --dry-run 2>&1`
    2. Capture the file listing in the output.
    3. Verify the listing contains entries from each of the five whitelisted top-level paths: `bin/`, `assets/`, `catalog/`, `tools/`, `README.md`, plus `package.json` (npm always includes this).
    4. Verify the listing does NOT contain: `.git/`, `node_modules/`, `.planning/`, `BMAD/`, `test/`, `.claude/`, `*.tgz`, `eslint.config.*`, or any dotfile other than `.npmignore` if present. If any of these appear, surface as a deviation in the task summary — do NOT silently expand the `files` array (per D-13: "surface as a deviation rather than silently expand").
    5. Verify the printed `name` is `@xgent-ai/gomad` and `version` is `1.0.0` (confirms Task 1 took effect).
    6. Verify the printed total tarball size is reasonable (expected 1–5 MB; flag if > 10 MB).

    No file edits in this task. If any deviation is found, document it in the task completion message and STOP — do not proceed to Plan 04-02 until the deviation is reviewed.
  </action>
  <verify>
    <automated>bash -c 'OUT=$(npm pack --dry-run 2>&1); echo "$OUT" | grep -q "@xgent-ai/gomad" && echo "$OUT" | grep -q "1.0.0" && echo "$OUT" | grep -q "bin/" && echo "$OUT" | grep -q "assets/" && echo "$OUT" | grep -q "catalog/" && echo "$OUT" | grep -q "tools/" && echo "$OUT" | grep -q "README.md" && ! echo "$OUT" | grep -qE "\\.planning/|BMAD/|node_modules/|\\.git/" && echo ok'</automated>
  </verify>
  <acceptance_criteria>
    - `npm pack --dry-run` exits 0
    - Output contains `@xgent-ai/gomad` and `1.0.0`
    - Output lists files from `bin/`, `assets/`, `catalog/`, `tools/`, and `README.md`
    - Output does NOT list any path under `.planning/`, `BMAD/`, `node_modules/`, or `.git/`
    - Output does NOT list `test/` files (test directory is not in `files` whitelist)
    - Tarball size is < 10 MB (sanity ceiling)
    - No `.tgz` artifacts left in repo root after the dry-run completes (npm pack --dry-run does not create files; verify with `ls *.tgz 2>/dev/null` returning empty)
  </acceptance_criteria>
  <done>
    npm pack --dry-run produces a sane tarball listing matching the files whitelist; no deviations found, OR deviations are surfaced and execution paused for review.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → public npm registry | After publish, anyone running `npx @xgent-ai/gomad install` will execute `bin/gomad-cli.js` from a downloaded tarball. The `files` whitelist is the integrity boundary — anything in the tarball is shipped to every consumer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | tarball contents | mitigate | Task 3 runs `npm pack --dry-run` and verifies `.planning/`, `BMAD/`, `node_modules/`, `.git/` are excluded. The `files` whitelist (D-13) is the allowlist; no `.env`, no dotfiles other than `.npmignore`. |
| T-04-02 | Tampering | package.json publish metadata | mitigate | `publishConfig.access: "public"` is explicit (D-03/D-10) so first publish does not silently fail and fall back to a private/paid path. `bin` key is unchanged so the CLI command name cannot be hijacked by a typo rename. |
| T-04-03 | Spoofing | scope ownership | accept | `@xgent-ai` scope ownership is a publish-time precondition (per 04-RESEARCH.md Open Question Q3); not a Phase 4 blocker since this phase only configures, does not run `npm publish`. Flagged for user as a manual pre-publish checklist item. |
| T-04-04 | Repudiation | none | accept | No new auth/audit surfaces in this phase. |
| T-04-05 | Denial of Service | none | accept | No runtime code changes. |
| T-04-06 | Elevation of Privilege | none | accept | No new privileged code paths; bin script is unchanged. |
</threat_model>

<verification>
- `node -e "const p=require('./package.json'); console.log(p.name, p.version, p.publishConfig.access)"` prints `@xgent-ai/gomad 1.0.0 public`
- `grep -rn vitest .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/ROADMAP.md` returns no matches
- `npm pack --dry-run` exits 0 and lists only whitelisted paths
- Existing test suite still passes: `npm test` exits 0 (no behavior change to runtime code)
</verification>

<success_criteria>
- package.json publishes as @xgent-ai/gomad@1.0.0 with public access (manual `npm publish` still deferred to post-merge)
- All vitest mentions removed from .planning/REQUIREMENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md
- PROJECT.md reflects public-npm direction (D-01 reversal complete in this plan, not deferred)
- npm pack --dry-run output matches the files whitelist with no surprising inclusions
- bin field key remains `gomad`
- No new dependencies added
- `npm test` still passes (Plan 04-02 will add the e2e test)
</success_criteria>

<output>
After completion, create `.planning/phases/04-publish-and-verify/04-01-SUMMARY.md` documenting:
- Final package.json metadata (name, version, publishConfig, description)
- List of vitest references removed (file + line)
- npm pack --dry-run results (file count, total size, any deviations)
- Confirmation that bin key is still `gomad`
- Any open questions surfaced (e.g., npm scope existence)
</output>
