---
phase: 10-story-creation-enhancements
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - src/gomad-skills/4-implementation/gm-discuss-story/SKILL.md
  - src/gomad-skills/4-implementation/gm-discuss-story/workflow.md
  - src/gomad-skills/4-implementation/gm-discuss-story/template.md
  - src/gomad-skills/4-implementation/gm-discuss-story/checklist.md
  - src/gomad-skills/4-implementation/gm-discuss-story/discover-inputs.md
autonomous: true
requirements:
  - STORY-01
  - STORY-02
  - STORY-03
tags:
  - skill
  - task-skill
  - discuss
  - elicitation
must_haves:
  truths:
    - "Developer can invoke `gm-discuss-story` for a target story and produce `{planning_artifacts}/{{story_key}}-context.md` with 5 locked XML-wrapped sections"
    - "On first invocation, `gm-discuss-story` identifies gray areas, asks user to multi-select, runs per-area focused Q&A, offers transition checks, writes per-area checkpoint JSON, and on final gate writes context.md and deletes checkpoint"
    - "On re-invocation with an existing checkpoint: skill prompts `Resume / Start fresh` and either resumes from `areas_remaining` or deletes checkpoint and starts over"
    - "On re-invocation when `{{story_key}}-context.md` exists (no checkpoint): skill prompts `Update / View / Skip`"
    - "Edge case: both checkpoint and context.md present → context.md takes precedence; stale checkpoint is auto-deleted before the Update/View/Skip prompt"
    - "Output context.md has NO token cap (per D-04); conciseness is enforced by workflow.md prompt engineering (`capture decisions, not discussion transcripts`)"
    - "Five locked sections (`<domain>`, `<decisions>`, `<canonical_refs>`, `<specifics>`, `<deferred>`) are always present in output (empty body acceptable if no content for a section)"
  artifacts:
    - path: "src/gomad-skills/4-implementation/gm-discuss-story/SKILL.md"
      provides: "Skill manifest (frontmatter + pointer to workflow.md)"
      contains: "name: gm-discuss-story"
    - path: "src/gomad-skills/4-implementation/gm-discuss-story/workflow.md"
      provides: "BMAD XML-DSL workflow with elicitation steps mirroring discuss-phase classic mode"
      contains: "<workflow>"
    - path: "src/gomad-skills/4-implementation/gm-discuss-story/template.md"
      provides: "Output template with 5 locked XML-wrapped sections"
      contains: "<domain>"
    - path: "src/gomad-skills/4-implementation/gm-discuss-story/checklist.md"
      provides: "Post-write validation checklist (verify 5 sections populated, decisions not transcripts)"
      contains: "checklist"
    - path: "src/gomad-skills/4-implementation/gm-discuss-story/discover-inputs.md"
      provides: "Shared input-load protocol (same as gm-create-story per D-01)"
      contains: "SELECTIVE_LOAD"
  key_links:
    - from: "gm-discuss-story/workflow.md"
      to: "discover-inputs.md"
      via: "`<action>Read fully and follow ./discover-inputs.md</action>`"
      pattern: "discover-inputs.md"
    - from: "gm-discuss-story/workflow.md"
      to: "{planning_artifacts}/{{story_key}}-discuss-checkpoint.json"
      via: "checkpoint read/write/delete"
      pattern: "discuss-checkpoint"
    - from: "gm-discuss-story/workflow.md"
      to: "{planning_artifacts}/{{story_key}}-context.md"
      via: "final write + template interpolation"
      pattern: "{{story_key}}-context.md"
---

<objective>
Author the `gm-discuss-story` task-skill as a 5-file mirror of `gm-create-story/` (per STORY-02), adapted for classic-discuss elicitation per D-02 and locked output shape per D-03. Implements STORY-01 (the skill itself), STORY-02 (5-file structure with re-run idempotency; token-cap clause dropped per D-04), and STORY-03 (5 locked XML-wrapped sections).

Purpose: Give developers a pre-story step that crystallizes gray areas BEFORE `gm-create-story` consumes them. Shared-input contract with `gm-create-story` (D-01) ensures both skills see the same epics/PRD/architecture/UX, keeping story scope consistent. Downstream Plan 06 patches `gm-create-story/discover-inputs.md` to auto-load `{{story_key}}-context.md`.

Output: 5 new files under `src/gomad-skills/4-implementation/gm-discuss-story/`. No installer changes needed — `ManifestGenerator.collectSkills` auto-discovers the new directory (CONTEXT.md §Reusable Assets).
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-story-creation-enhancements/10-CONTEXT.md
@.planning/phases/10-story-creation-enhancements/10-PATTERNS.md
@src/gomad-skills/4-implementation/gm-create-story/SKILL.md
@src/gomad-skills/4-implementation/gm-create-story/workflow.md
@src/gomad-skills/4-implementation/gm-create-story/template.md
@src/gomad-skills/4-implementation/gm-create-story/checklist.md
@src/gomad-skills/4-implementation/gm-create-story/discover-inputs.md
@.claude/get-shit-done/workflows/discuss-phase.md

<interfaces>
<!-- File-for-file pattern template. Executor mirrors shape, adapts content. -->

**gm-create-story/SKILL.md (6 lines, full):**
```
---
name: gm-create-story
description: 'Creates a dedicated story file with all the context the agent will need to implement it later. Use when the user says "create the next story" or "create story [story identifier]"'
---

Follow the instructions in ./workflow.md.
```
— validate-skills.js:45 `NAME_REGEX = /^gm-[a-z0-9]+(-[a-z0-9]+)*$/`; `gm-discuss-story` matches.
— SKILL-06 (validate-skills.js): description MUST start with "Use when ..."; wrap accordingly.

**gm-create-story/discover-inputs.md** (89 lines):
- Step 1: parse Input Files table (from workflow.md, not this file)
- Step 2a: try sharded (FULL_LOAD / SELECTIVE_LOAD / INDEX_GUIDED)
- Step 2b: fall back to whole file
- Step 2c: handle not found → set `{pattern_name_content}` to empty string, note "No {pattern_name} files found"
- Step 3: report discovery

**gm-create-story/template.md** (50 lines): uses `{{epic_num}}.{{story_num}}: {{story_title}}` handlebars.

**gm-create-story/checklist.md** (358 lines): quality-validator persona with "systematic re-analysis" pattern.

**gm-create-story/workflow.md:19-26** — config load pattern:
```
Load config from `{project-root}/_gomad/agile/config.yaml` and resolve:
- project_name, user_name
- communication_language, document_output_language
- user_skill_level
- planning_artifacts, implementation_artifacts
- date as system-generated current datetime
```

**discuss-phase.md:266-284** (checkpoint resume prompt):
```
Found interrupted discussion checkpoint ({N} areas completed out of {M}). Resume from where you left off?
options:
  - "Resume" — Load checkpoint, skip completed areas, continue discussion
  - "Start fresh" — Delete checkpoint, start discussion from scratch
```

**discuss-phase.md:594-620** (multi-select gray areas — AskUserQuestion with multiSelect: true):
```
header: "Discuss"
question: "Which areas do you want to discuss for [phase name]?"
options: Generate 3-4 phase-specific gray areas, each with:
  - "[Specific area]" (label)
  - [1-2 questions this covers] (description)
Do NOT include "skip" or "you decide".
```

**discuss-phase.md:917-938** (checkpoint JSON schema template — adapt keys for story-level):
```json
{
  "phase": "{PHASE_NUM}",
  "phase_name": "{phase_name}",
  "timestamp": "{ISO timestamp}",
  "areas_completed": ["Area 1", "Area 2"],
  "areas_remaining": ["Area 3", "Area 4"],
  "decisions": { "Area 1": [{"question": "...", "answer": "...", "options_presented": ["..."]}] },
  "deferred_ideas": ["..."],
  "canonical_refs": ["..."]
}
```

**Five locked sections for template.md (D-03):**
`<domain>` / `<decisions>` / `<canonical_refs>` / `<specifics>` / `<deferred>` — the pattern used in this file itself (.planning/phases/10-story-creation-enhancements/10-CONTEXT.md) plus every other `*-CONTEXT.md` under `.planning/phases/`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write `gm-discuss-story/SKILL.md` + `discover-inputs.md` (near-verbatim reuse per D-01)</name>
  <files>src/gomad-skills/4-implementation/gm-discuss-story/SKILL.md, src/gomad-skills/4-implementation/gm-discuss-story/discover-inputs.md</files>
  <read_first>
    - src/gomad-skills/4-implementation/gm-create-story/SKILL.md (full 6 lines)
    - src/gomad-skills/4-implementation/gm-create-story/discover-inputs.md (full 89 lines)
    - tools/validate-skills.js (lines 45 — NAME_REGEX; lines 318-327 — name/dirname equality check)
    - .planning/phases/10-story-creation-enhancements/10-PATTERNS.md §1 (SKILL.md) and §3 (discover-inputs.md)
    - .planning/phases/10-story-creation-enhancements/10-CONTEXT.md §D-01 (reuse rationale)
  </read_first>
  <behavior>
    - `SKILL.md` has frontmatter `name: gm-discuss-story` and a description starting with `"Use when ..."` (per SKILL-06 regex in `validate-skills.js`).
    - `SKILL.md` body is a single line: `Follow the instructions in ./workflow.md.`
    - `discover-inputs.md` is near-verbatim copy of `gm-create-story/discover-inputs.md` with ONE edit: line 34 (`Load a specific shard using a template variable. Example: used for epics with \`{{epic_num}}\`.`) updated to mention `{{story_key}}` as a second example template variable.
    - `validate-skills.js` passes when run against `src/gomad-skills/4-implementation/gm-discuss-story/` (name matches regex, dir basename matches name frontmatter, SKILL.md exists, description starts with "Use when").
  </behavior>
  <action>
    **Create `src/gomad-skills/4-implementation/gm-discuss-story/SKILL.md`** (6 lines, exact form):

    ```markdown
    ---
    name: gm-discuss-story
    description: 'Use when the user wants to crystallize gray areas for a planned story BEFORE running gm-create-story. Produces {planning_artifacts}/{{story_key}}-context.md with 5 locked sections (domain, decisions, canonical_refs, specifics, deferred) that gm-create-story auto-loads.'
    ---

    Follow the instructions in ./workflow.md.
    ```

    Constraints:
    - Name MUST exactly equal `gm-discuss-story` and MUST equal the directory basename (SKILL-05 rule in `validate-skills.js:45, 318-327`).
    - Description MUST start with `"Use when ..."` (SKILL-06).
    - Body is exactly one line, ending with a newline.

    **Create `src/gomad-skills/4-implementation/gm-discuss-story/discover-inputs.md`** by copying `src/gomad-skills/4-implementation/gm-create-story/discover-inputs.md` (all 89 lines) with ONE surgical edit on line 34:

    - BEFORE: `Load a specific shard using a template variable. Example: used for epics with \`{{epic_num}}\`.`
    - AFTER: `Load a specific shard using a template variable. Examples: epics with \`{{epic_num}}\`, story context with \`{{story_key}}\`.`

    Do NOT change any other line — the SELECTIVE_LOAD/FULL_LOAD/INDEX_GUIDED protocol is identical for both skills per D-01.

    Run `npx prettier --write src/gomad-skills/4-implementation/gm-discuss-story/*.md` + `npm run lint:md -- "src/gomad-skills/4-implementation/gm-discuss-story/*.md"`.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const p = 'src/gomad-skills/4-implementation/gm-discuss-story'; const s = fs.readFileSync(p+'/SKILL.md','utf8'); if (!/^---\n.*name: gm-discuss-story/m.test(s)) throw new Error('bad name'); if (!/description: '?Use when /.test(s)) throw new Error('description must start with Use when'); if (!/Follow the instructions in .\/workflow.md./.test(s)) throw new Error('missing workflow pointer'); const d = fs.readFileSync(p+'/discover-inputs.md','utf8'); if (!/{{story_key}}/.test(d)) throw new Error('story_key example missing'); if (!/SELECTIVE_LOAD Strategy/.test(d)) throw new Error('SELECTIVE_LOAD section missing'); console.log('OK'); process.exit(0);" && node tools/validate-skills.js --strict</automated>
  </verify>
  <done>
    - `src/gomad-skills/4-implementation/gm-discuss-story/SKILL.md` exists with correct frontmatter and body.
    - `src/gomad-skills/4-implementation/gm-discuss-story/discover-inputs.md` exists and differs from `gm-create-story/discover-inputs.md` by exactly 1 line (the SELECTIVE_LOAD example).
    - `node tools/validate-skills.js --strict` exits 0 (no regression on existing skills or new skill).
    - `npm run lint:md -- "src/gomad-skills/4-implementation/gm-discuss-story/*.md"` exits 0.
  </done>
  <acceptance_criteria>
    - `test -f src/gomad-skills/4-implementation/gm-discuss-story/SKILL.md` returns 0
    - `grep -cE "^name: gm-discuss-story$" src/gomad-skills/4-implementation/gm-discuss-story/SKILL.md` = 1
    - `grep -cE "description: '?Use when " src/gomad-skills/4-implementation/gm-discuss-story/SKILL.md` = 1
    - `grep -cE "^Follow the instructions in \./workflow\.md\.$" src/gomad-skills/4-implementation/gm-discuss-story/SKILL.md` = 1
    - `test -f src/gomad-skills/4-implementation/gm-discuss-story/discover-inputs.md` returns 0
    - `grep -c "{{story_key}}" src/gomad-skills/4-implementation/gm-discuss-story/discover-inputs.md` ≥ 1
    - `grep -c "SELECTIVE_LOAD" src/gomad-skills/4-implementation/gm-discuss-story/discover-inputs.md` ≥ 2
    - `diff src/gomad-skills/4-implementation/gm-create-story/discover-inputs.md src/gomad-skills/4-implementation/gm-discuss-story/discover-inputs.md | grep -c '^[<>]'` ≤ 4 (at most 2 changed lines × 2 diff markers; D-01 near-verbatim reuse)
    - `node tools/validate-skills.js --strict` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write `gm-discuss-story/workflow.md` (BMAD DSL + classic-discuss semantics)</name>
  <files>src/gomad-skills/4-implementation/gm-discuss-story/workflow.md</files>
  <read_first>
    - src/gomad-skills/4-implementation/gm-create-story/workflow.md (full 380 lines — DSL shape analog: INITIALIZATION, Input Files table, `<workflow>` / `<step>` / `<check>` / `<action>` / `<ask>` patterns)
    - .claude/get-shit-done/workflows/discuss-phase.md (elicitation semantics analog — gray-area identification lines 560-620, multi-select AskUserQuestion lines 594-620, checkpoint resume lines 266-284, checkpoint JSON schema lines 917-938, Update/View/Skip pattern near lines 258-262)
    - .planning/phases/10-story-creation-enhancements/10-PATTERNS.md §2 (workflow.md full guidance — 2 analogs + deviations)
    - .planning/phases/10-story-creation-enhancements/10-CONTEXT.md §Decisions D-02 through D-07 (elicitation flow + re-run idempotency contract)
  </read_first>
  <behavior>
    - workflow.md implements 7+ sequential `<step>` blocks covering: (1) determine target story, (2) load shared inputs via discover-inputs.md, (3) detect checkpoint/context.md state and branch (Resume/Start fresh OR Update/View/Skip), (4) identify gray areas grounded in story content, (5) user multi-select gray areas, (6) per-area Q&A loop with transition checks ("More questions about [area], or move to next?"), (7) final gate ("Explore more / Ready for context"), (8) write context.md + delete checkpoint.
    - Checkpoint behavior (D-05, D-07):
      - Write `{planning_artifacts}/{{story_key}}-discuss-checkpoint.json` after EACH area's Q&A completes, with keys `story_key`, `timestamp`, `areas_completed`, `areas_remaining`, `decisions`, `deferred_ideas`, `canonical_refs`.
      - On re-invocation: check for checkpoint. If BOTH checkpoint AND context.md exist → context.md wins, auto-delete stale checkpoint, then go to Update/View/Skip prompt (D-07).
      - On re-invocation: only checkpoint exists → prompt Resume / Start fresh (D-05).
      - On re-invocation: only context.md exists → prompt Update / View / Skip (D-06).
      - Delete checkpoint on successful context.md write (discuss-phase.md:949 pattern).
    - Output mapping (D-03): adaptive — gray-area content routes to one of 5 sections by semantic fit, NOT by a fixed "STORY-01 category → section" lookup table. Prose guidance in workflow.md: "Map each decision to the section that best fits: boundary/scope → `<domain>`, concrete resolutions → `<decisions>`, file-paths/links → `<canonical_refs>`, noted user ideas/refs → `<specifics>`, explicitly deferred items → `<deferred>`."
    - No token cap (D-04): workflow.md prose explicitly says "capture decisions, not discussion transcripts." NO counter, NO compression step, NO truncation logic.
  </behavior>
  <action>
    Create `src/gomad-skills/4-implementation/gm-discuss-story/workflow.md`. Structure (~300-400 lines, mirror `gm-create-story/workflow.md` shape):

    **Section 1 — Header + Role** (like gm-create-story/workflow.md:1-13):
    ```markdown
    # Discuss Story Workflow

    **Goal:** Crystallize gray areas for a planned story BEFORE gm-create-story runs, producing {planning_artifacts}/{{story_key}}-context.md with 5 locked sections (domain, decisions, canonical_refs, specifics, deferred).

    **Your Role:** Gray-area elicitation engine that surfaces acceptance-criteria edge cases, NFRs, data-model ambiguities, and downstream contracts BEFORE story drafting begins.
    - Communicate all responses in {communication_language} and generate all documents in {document_output_language}
    - Your purpose is NOT to re-ask PRD questions — it's to probe ambiguity in THIS story's scope that a coding agent would need resolved.
    - CAPTURE DECISIONS, NOT DISCUSSION TRANSCRIPTS. Be surgical. No verbose preambles. No re-statement of the question in the decision record.
    ```

    **Section 2 — INITIALIZATION** (copy gm-create-story/workflow.md:17-47 structure):
    - Config loading from `{project-root}/_gomad/agile/config.yaml`
    - Paths: `sprint_status`, `epics_file`, `prd_file`, `architecture_file`, `ux_file`, `default_output_file = {planning_artifacts}/{{story_key}}-context.md`, `checkpoint_file = {planning_artifacts}/{{story_key}}-discuss-checkpoint.json`
    - Input Files table — SAME 4 rows as gm-create-story per D-01 (prd, architecture, ux, epics), all SELECTIVE_LOAD

    **Section 3 — EXECUTION `<workflow>` block** with these steps:

    ```xml
    <workflow>

    <step n="1" goal="Determine target story">
      <!-- Mirror gm-create-story/workflow.md:54-208 story selection logic (user-provided OR sprint-status auto-discovery) -->
      <!-- Extract {{epic_num}}, {{story_num}}, {{story_key}} -->
    </step>

    <step n="2" goal="Detect existing state — checkpoint and/or context.md">
      <action>Check existence of {{checkpoint_file}} and {{default_output_file}}</action>

      <check if="both checkpoint and context.md exist">
        <!-- D-07: context.md wins, stale checkpoint deleted -->
        <action>Delete {{checkpoint_file}} (stale — a completed discussion supersedes a resume token)</action>
        <output>⚠ Stale discussion checkpoint detected (context.md already exists). Deleted checkpoint; treating as re-opening a completed discussion.</output>
        <action>GOTO step 3b (Update/View/Skip)</action>
      </check>

      <check if="only checkpoint exists">
        <!-- D-05: Resume / Start fresh -->
        <action>Load checkpoint: areas_completed[], areas_remaining[], decisions{} -->
        <ask>
          header: "Resume"
          question: "Found interrupted discussion checkpoint ({N} areas completed out of {M}). Resume where you left off?"
          options:
            - "Resume" — Load checkpoint, skip completed areas, continue from remaining
            - "Start fresh" — Delete checkpoint, re-identify gray areas from scratch
        </ask>
        <check if="user chose Resume">
          <action>Load decisions{} into in-memory accumulator</action>
          <action>GOTO step 5 (skip to remaining areas)</action>
        </check>
        <check if="user chose Start fresh">
          <action>Delete {{checkpoint_file}}</action>
          <action>GOTO step 3a (identify gray areas)</action>
        </check>
      </check>

      <check if="only context.md exists">
        <!-- D-06: Update / View / Skip -->
        <action>GOTO step 3b</action>
      </check>

      <check if="neither exists">
        <action>GOTO step 3a (fresh discussion)</action>
      </check>
    </step>

    <step n="3a" goal="Identify gray areas grounded in the target story">
      <critical>Ground every gray area in the target story's epics entry — not generic interview questions.</critical>
      <action>From {epics_content}, extract story {{epic_num}}-{{story_num}} entry: acceptance criteria, technical requirements, dependencies</action>
      <action>Cross-reference against {prd_content}, {architecture_content}, {ux_content} to surface contradictions, underspecified edges, and unresolved design questions</action>
      <action>Enumerate 3-5 gray areas with concrete labels (e.g., "AC#2 edge: empty input vs whitespace vs script tags", NOT "Input validation approach")</action>
      <note>Each gray area must cite at least one concrete source (AC number, architecture section, UX wireframe) — no speculative areas.</note>
    </step>

    <step n="3b" goal="Update / View / Skip (D-06)">
      <ask>
        header: "Existing context"
        question: "{{story_key}}-context.md already exists. What now?"
        options:
          - "Update" — Load existing decisions as pre-filled, continue discussion
          - "View" — Display current context.md, then re-prompt Update/Skip
          - "Skip" — Exit without changes
      </ask>
      <check if="user chose Update">
        <action>Parse existing context.md sections into pre-filled decision accumulator</action>
        <action>GOTO step 3a (identify new or remaining gray areas)</action>
      </check>
      <check if="user chose View">
        <output>{{display context.md content}}</output>
        <ask>Choose: Update / Skip</ask>
        <action>Re-branch per the above</action>
      </check>
      <check if="user chose Skip">
        <action>HALT — no changes</action>
      </check>
    </step>

    <step n="4" goal="User multi-selects gray areas">
      <!-- discuss-phase.md:594-620 analog -->
      <ask>
        header: "Discuss"
        question: "Which areas do you want to discuss for story {{story_key}}?"
        multiSelect: true
        options: [each gray area from step 3a as a label with 1-2 probing questions in the description]
        recommended: [highlight the area with the most cross-doc contradictions OR most acceptance-criteria edge cases]
      </ask>
      <note>Do NOT include "skip" or "you decide" options. User ran this command to discuss.</note>
      <action>Set {{areas_selected}} = user's selections; set {{areas_completed}} = []; set {{decisions}} = {}</action>
    </step>

    <step n="5" goal="Per-area Q&A loop with transition checks">
      <!-- For each selected area not in areas_completed: -->
      <!-- Ask ~4 focused questions with 2-3 concrete options each -->
      <!-- After each question, record { question, answer, options_presented } in decisions[area] -->
      <!-- After area's Q&A complete: write checkpoint, then ask transition -->

      <action>FOR EACH area IN areas_selected NOT IN areas_completed:</action>
      <action>  Ask ~4 focused questions with 2-3 concrete options each (concrete = specific values, file paths, library names — never abstract like "your preference")</action>
      <action>  Append { question, answer, options_presented } to decisions[area]</action>
      <action>  Append area to areas_completed</action>
      <action>  Write checkpoint JSON to {{checkpoint_file}} (D-05)</action>
      <ask>transition: "More questions about [area], or move to next?"</ask>
      <check if="user says more">
        <action>Continue Q&A on same area (decrement areas_completed if still in-progress)</action>
      </check>
      <check if="user says next OR area's 4 questions done">
        <action>Loop to next area</action>
      </check>
    </step>

    <step n="6" goal="Final gate">
      <ask>
        header: "Ready to close discussion?"
        question: "Ready to generate context.md, or explore more gray areas?"
        options:
          - "Ready for context" — Write context.md and exit
          - "Explore more" — Re-open step 3a to add new gray areas
      </ask>
      <check if="explore more">
        <action>GOTO step 3a</action>
      </check>
    </step>

    <step n="7" goal="Adaptive mapping to 5 sections + write context.md">
      <!-- D-03: adaptive mapping by semantic fit -->
      <action>Map each accumulated decision into one of 5 sections by content semantics:
        - domain: story boundary/scope clarifications
        - decisions: concrete resolved answers (question → answer)
        - canonical_refs: file paths, links, source citations (AC#, architecture section)
        - specifics: user-framed ideas, quotes, or references
        - deferred: items user explicitly deferred out of this story
      </action>
      <action>Initialize from template.md: {{default_output_file}}</action>
      <template-output file="{default_output_file}">context_header</template-output>
      <template-output file="{default_output_file}">domain</template-output>
      <template-output file="{default_output_file}">decisions</template-output>
      <template-output file="{default_output_file}">canonical_refs</template-output>
      <template-output file="{default_output_file}">specifics</template-output>
      <template-output file="{default_output_file}">deferred</template-output>
      <action>Save context.md</action>
      <action>Delete {{checkpoint_file}} (D-05 cleanup)</action>
    </step>

    <step n="8" goal="Validate + finalize">
      <action>Validate newly created context.md against ./checklist.md and apply any required fixes</action>
      <output>
        **✅ Discussion context captured, {user_name}!**

        - Story: {{story_key}}
        - File: {{default_output_file}}
        - Sections populated: [list of non-empty sections]

        **Next step:** Run `gm-create-story` — it will auto-load this context via discover-inputs.md.
      </output>
    </step>

    </workflow>
    ```

    **Checkpoint JSON schema** — document in prose near step 5, adapted from discuss-phase.md:917-938:
    ```json
    {
      "story_key": "{{story_key}}",
      "timestamp": "ISO-8601 timestamp",
      "areas_completed": ["Area 1 label", ...],
      "areas_remaining": ["Area 3 label", ...],
      "decisions": { "Area 1 label": [{"question": "...", "answer": "...", "options_presented": ["..."]}] },
      "deferred_ideas": ["..."],
      "canonical_refs": ["..."]
    }
    ```

    Run `npx prettier --write src/gomad-skills/4-implementation/gm-discuss-story/workflow.md` + `npm run lint:md -- "src/gomad-skills/4-implementation/gm-discuss-story/workflow.md"`.
  </action>
  <verify>
    <automated>node -e "const s = require('fs').readFileSync('src/gomad-skills/4-implementation/gm-discuss-story/workflow.md','utf8'); const musts = ['&lt;workflow&gt;', '&lt;/workflow&gt;', '{{story_key}}-context.md', '{{story_key}}-discuss-checkpoint.json', 'Resume', 'Start fresh', 'Update', 'View', 'Skip', 'multiSelect: true', 'More questions about', 'Ready for context', 'Explore more', 'checklist.md', 'discover-inputs.md', 'domain', 'decisions', 'canonical_refs', 'specifics', 'deferred', 'capture decisions, not discussion transcripts']; const missing = musts.filter(m => !s.includes(m.replace(/&lt;/g,'<').replace(/&gt;/g,'>'))); if (missing.length) { console.error('missing:', missing); process.exit(1); } const steps = (s.match(/&lt;step n=\&quot;\\d/g) || []).length + (s.match(/<step n=\"\\d/g) || []).length; if (steps &lt; 7) { console.error('need at least 7 steps, got', steps); process.exit(1); } console.log('OK'); process.exit(0);"</automated>
  </verify>
  <done>
    - `src/gomad-skills/4-implementation/gm-discuss-story/workflow.md` exists.
    - Contains `<workflow>` … `</workflow>` block with at least 7 `<step>` entries.
    - Contains explicit references to `Resume / Start fresh` (D-05), `Update / View / Skip` (D-06), stale-checkpoint auto-delete (D-07), no-token-cap prose (D-04), adaptive section mapping (D-03).
    - Checkpoint JSON schema documented in the file.
    - No token-counting, compression, or truncation logic exists (D-04).
    - No conflict-detection or warning emission logic exists (D-14 — but that's a gm-create-story concern, not gm-discuss-story; just mentioned for completeness).
    - `npm run lint:md -- "src/gomad-skills/4-implementation/gm-discuss-story/workflow.md"` exits 0.
  </done>
  <acceptance_criteria>
    - `test -f src/gomad-skills/4-implementation/gm-discuss-story/workflow.md` returns 0
    - `grep -c "<workflow>" src/gomad-skills/4-implementation/gm-discuss-story/workflow.md` = 1 AND `grep -c "</workflow>" src/gomad-skills/4-implementation/gm-discuss-story/workflow.md` = 1
    - `grep -cE "<step n=\"[0-9a-z]+\"" src/gomad-skills/4-implementation/gm-discuss-story/workflow.md` ≥ 7
    - `grep -c "{{story_key}}-context.md" src/gomad-skills/4-implementation/gm-discuss-story/workflow.md` ≥ 2
    - `grep -c "{{story_key}}-discuss-checkpoint.json" src/gomad-skills/4-implementation/gm-discuss-story/workflow.md` ≥ 2 (declare path + write + delete)
    - `grep -cE "Resume.*Start fresh|Start fresh.*Resume" src/gomad-skills/4-implementation/gm-discuss-story/workflow.md` ≥ 1 (D-05)
    - `grep -cE "Update.*View.*Skip" src/gomad-skills/4-implementation/gm-discuss-story/workflow.md` ≥ 1 (D-06)
    - `grep -c "multiSelect: true" src/gomad-skills/4-implementation/gm-discuss-story/workflow.md` ≥ 1 (gray-area selection per D-02)
    - `grep -c "capture decisions, not discussion transcripts" src/gomad-skills/4-implementation/gm-discuss-story/workflow.md` ≥ 1 (D-04 prose)
    - `grep -cE "token.?count|token.?cap|truncat|compress" src/gomad-skills/4-implementation/gm-discuss-story/workflow.md` = 0 (D-04 — no length-management logic)
    - `grep -cE "\\bdomain\\b|\\bdecisions\\b|\\bcanonical_refs\\b|\\bspecifics\\b|\\bdeferred\\b" src/gomad-skills/4-implementation/gm-discuss-story/workflow.md` ≥ 5 (5 locked sections referenced)
    - `grep -c "discover-inputs.md" src/gomad-skills/4-implementation/gm-discuss-story/workflow.md` ≥ 1
    - `grep -c "checklist.md" src/gomad-skills/4-implementation/gm-discuss-story/workflow.md` ≥ 1
    - `npm run lint:md -- "src/gomad-skills/4-implementation/gm-discuss-story/workflow.md"` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write `gm-discuss-story/template.md` + `checklist.md`</name>
  <files>src/gomad-skills/4-implementation/gm-discuss-story/template.md, src/gomad-skills/4-implementation/gm-discuss-story/checklist.md</files>
  <read_first>
    - src/gomad-skills/4-implementation/gm-create-story/template.md (full 50 lines — handlebars pattern)
    - src/gomad-skills/4-implementation/gm-create-story/checklist.md (358 lines — read header lines 1-70 for mission/persona pattern)
    - .planning/phases/10-story-creation-enhancements/10-PATTERNS.md §4 (template.md with 5-section adaptation) and §5 (checklist.md deviations)
    - .planning/phases/10-story-creation-enhancements/10-CONTEXT.md §D-03 (5 locked section names) and §D-04 (no length cap — checklist validates decisions vs transcripts instead)
    - .planning/phases/10-story-creation-enhancements/10-CONTEXT.md itself (the canonical section-wrapper idiom — `<domain>`, `<decisions>`, `<canonical_refs>`, `<specifics>`, `<deferred>` XML-wrapped-with-H2-heading pattern)
  </read_first>
  <behavior>
    - `template.md` renders `{{story_key}}-context.md` with exactly 5 XML-wrapped sections in order: `<domain>`, `<decisions>`, `<canonical_refs>`, `<specifics>`, `<deferred>`.
    - Each section contains an H2 heading AND a body placeholder (handlebars-style `{{section_content}}`).
    - `checklist.md` is a quality validator persona (mirror gm-create-story/checklist.md pattern) that checks: all 5 sections are present, each section contains decisions (bullets or concrete answers) NOT verbose prose, canonical_refs cite file paths or section numbers (not generic "see architecture"), deferred items are explicit (not "TBD").
  </behavior>
  <action>
    **Create `src/gomad-skills/4-implementation/gm-discuss-story/template.md`** (~40-60 lines, mirror the XML-wrapper pattern from `.planning/phases/10-story-creation-enhancements/10-CONTEXT.md`):

    ```markdown
    # {{story_key}} Context

    **Story:** {{epic_num}}.{{story_num}} — {{story_title}}
    **Generated:** {{date}}
    **Source:** gm-discuss-story (classic elicitation mode)

    <domain>
    ## Story Boundary

    {{domain_content}}

    </domain>

    <decisions>
    ## Resolved Decisions

    {{decisions_content}}

    </decisions>

    <canonical_refs>
    ## Canonical References

    {{canonical_refs_content}}

    </canonical_refs>

    <specifics>
    ## Specific Ideas

    {{specifics_content}}

    </specifics>

    <deferred>
    ## Deferred Ideas

    {{deferred_content}}

    </deferred>
    ```

    Placeholder names (`{{domain_content}}`, `{{decisions_content}}`, etc.) match the template-output identifiers in workflow.md step 7.

    **Create `src/gomad-skills/4-implementation/gm-discuss-story/checklist.md`** (~80-150 lines — simpler than gm-create-story/checklist.md; validates 5-section shape + content discipline):

    ```markdown
    # 🎯 Story Discussion Context Quality Checklist

    ## **Critical Mission: Verify the discussion captured decisions, not transcripts**

    You are an independent quality validator in a **FRESH CONTEXT**. Your mission is to review the `{{story_key}}-context.md` file produced by `gm-discuss-story` and verify it meets the output contract.

    ---

    ## SYSTEMATIC VALIDATION

    ### 1. Structural Checks

    - [ ] File starts with H1 heading `# {{story_key}} Context`
    - [ ] All 5 XML-wrapped sections are present (opening + closing tags):
      - [ ] `<domain>` ... `</domain>`
      - [ ] `<decisions>` ... `</decisions>`
      - [ ] `<canonical_refs>` ... `</canonical_refs>`
      - [ ] `<specifics>` ... `</specifics>`
      - [ ] `<deferred>` ... `</deferred>`
    - [ ] Section ORDER matches template: domain → decisions → canonical_refs → specifics → deferred
    - [ ] No other top-level XML wrappers exist (no inventing new sections — downstream parsers key on these 5 names)

    ### 2. Content Discipline (per D-04 — captures decisions, not transcripts)

    - [ ] `<decisions>` contains concrete resolved answers (bullets or `question → answer` form), NOT verbose paragraphs of dialogue
    - [ ] Every decision cites a specific source (AC number, architecture section, file path) where applicable
    - [ ] `<canonical_refs>` lists file paths / section anchors — NOT generic phrases like "see the architecture doc"
    - [ ] `<deferred>` items are explicit with one-sentence reason (not "TBD" or "maybe later")
    - [ ] No verbatim quotes of the user's question text padding out the decision record
    - [ ] No re-statement of PRD content that was simply copied over

    ### 3. Scope Discipline

    - [ ] Content is grounded in THIS story's epic entry (acceptance criteria, technical requirements) — not generic interview answers
    - [ ] No gray areas from OTHER stories in the same epic
    - [ ] No decisions that contradict the shared inputs (PRD/architecture/UX) without noting the contradiction

    ### 4. Re-run Safety

    - [ ] No `{{story_key}}-discuss-checkpoint.json` file exists alongside the context.md (D-05 cleanup — stale checkpoint should be deleted after successful write)

    ---

    ## IF GAPS FOUND

    If any checkbox fails, present the issues to the user:

    1. List each failed check with the specific file/line
    2. Offer: "Apply all fixes / Select fixes to apply / Skip fixes and close"
    3. On "Apply all" or "Select": return to workflow step 7 to re-render the affected sections
    4. On "Skip": leave context.md as-is; note gaps in output summary
    ```

    Run `npx prettier --write src/gomad-skills/4-implementation/gm-discuss-story/{template,checklist}.md` + `npm run lint:md -- "src/gomad-skills/4-implementation/gm-discuss-story/*.md"`.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const p = 'src/gomad-skills/4-implementation/gm-discuss-story'; const t = fs.readFileSync(p+'/template.md','utf8'); for (const s of ['<domain>','</domain>','<decisions>','</decisions>','<canonical_refs>','</canonical_refs>','<specifics>','</specifics>','<deferred>','</deferred>']) { if (!t.includes(s)) { console.error('template missing', s); process.exit(1); } } for (const v of ['{{story_key}}','{{domain_content}}','{{decisions_content}}','{{canonical_refs_content}}','{{specifics_content}}','{{deferred_content}}']) { if (!t.includes(v)) { console.error('template missing placeholder', v); process.exit(1); } } const c = fs.readFileSync(p+'/checklist.md','utf8'); for (const s of ['<domain>','<decisions>','<canonical_refs>','<specifics>','<deferred>','decisions, not transcripts','discuss-checkpoint']) { if (!c.includes(s)) { console.error('checklist missing', s); process.exit(1); } } console.log('OK'); process.exit(0);"</automated>
  </verify>
  <done>
    - `template.md` has all 5 section wrappers (opening + closing) in order: domain → decisions → canonical_refs → specifics → deferred.
    - `template.md` contains handlebars placeholders `{{domain_content}}`, `{{decisions_content}}`, `{{canonical_refs_content}}`, `{{specifics_content}}`, `{{deferred_content}}`.
    - `checklist.md` validates the 5-section shape + content discipline (decisions not transcripts per D-04) + cleanup (no stale checkpoint).
    - `npm run lint:md -- "src/gomad-skills/4-implementation/gm-discuss-story/*.md"` exits 0.
    - Running `node tools/validate-skills.js --strict` still exits 0 (entire 5-file skill passes skill validator).
  </done>
  <acceptance_criteria>
    - `test -f src/gomad-skills/4-implementation/gm-discuss-story/template.md` returns 0
    - `grep -cE "^<domain>$" src/gomad-skills/4-implementation/gm-discuss-story/template.md` = 1
    - `grep -cE "^</domain>$" src/gomad-skills/4-implementation/gm-discuss-story/template.md` = 1
    - `grep -cE "^<decisions>$" src/gomad-skills/4-implementation/gm-discuss-story/template.md` = 1
    - `grep -cE "^<canonical_refs>$" src/gomad-skills/4-implementation/gm-discuss-story/template.md` = 1
    - `grep -cE "^<specifics>$" src/gomad-skills/4-implementation/gm-discuss-story/template.md` = 1
    - `grep -cE "^<deferred>$" src/gomad-skills/4-implementation/gm-discuss-story/template.md` = 1
    - `grep -cE "\\{\\{domain_content\\}\\}|\\{\\{decisions_content\\}\\}|\\{\\{canonical_refs_content\\}\\}|\\{\\{specifics_content\\}\\}|\\{\\{deferred_content\\}\\}" src/gomad-skills/4-implementation/gm-discuss-story/template.md` ≥ 5
    - `test -f src/gomad-skills/4-implementation/gm-discuss-story/checklist.md` returns 0
    - `grep -c "decisions, not transcripts" src/gomad-skills/4-implementation/gm-discuss-story/checklist.md` ≥ 1 (D-04 discipline)
    - `grep -cE "domain|decisions|canonical_refs|specifics|deferred" src/gomad-skills/4-implementation/gm-discuss-story/checklist.md` ≥ 5
    - `grep -c "discuss-checkpoint" src/gomad-skills/4-implementation/gm-discuss-story/checklist.md` ≥ 1 (cleanup check per D-05)
    - `node tools/validate-skills.js --strict` exits 0
    - `npm run lint:md -- "src/gomad-skills/4-implementation/gm-discuss-story/*.md"` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User prompt → skill workflow | User drives gray-area selection + answers focused questions. LLM-interpreted, not parsed. |
| Checkpoint JSON file → skill state | `{{story_key}}-discuss-checkpoint.json` contains user-authored answers. Read by the skill on re-invocation. |
| Context.md output → downstream `gm-create-story` | Plan 06 will consume context.md via LLM context ordering (D-13). No mechanical parsing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Tampering (T) | Stale checkpoint left behind | mitigate | D-07 logic (Task 2): if context.md exists AND checkpoint exists → auto-delete checkpoint. D-05 logic: delete on successful write. Workflow step 7 + checklist.md check 4 both enforce. MEDIUM: leftover checkpoint would cause a spurious Resume prompt on next run. |
| T-10-03-02 | Information Disclosure (I) | Checkpoint JSON contains user's in-progress decisions | accept | Local filesystem only (`{planning_artifacts}/`); same trust level as story/PRD files. No secrets expected in discussion decisions. LOW. |
| T-10-03-03 | Denial of Service (D) | Unbounded context.md growth | accept | D-04 drops token cap; prompt engineering ("decisions, not transcripts") handles it. If empirically problematic post-v1.3, revisit via a patch. LOW — size signal is soft. |
| T-10-03-04 | Tampering (T) | Skill validation bypass | mitigate | `node tools/validate-skills.js --strict` catches: missing SKILL.md, name-regex mismatch, description missing "Use when", etc. Wired into `npm run quality`. LOW — existing gate. |
| T-10-03-05 | Repudiation (R) | No audit trail of who ran the discussion | accept | Solo-dev workflow (PROJECT.md); no multi-user contention. Checkpoint + context.md timestamps sufficient. LOW. |

**HIGH/CRITICAL findings:** None. Task-skill content is static markdown; no executable code ships in this plan. All threats are LOW/MEDIUM.
</threat_model>

<verification>
Phase-level checks for this plan:

1. All 5 files exist under `src/gomad-skills/4-implementation/gm-discuss-story/` (SKILL.md, workflow.md, template.md, checklist.md, discover-inputs.md).
2. `node tools/validate-skills.js --strict` exits 0 (new skill conforms to SKILL-01..SKILL-NN rules).
3. `npm run lint:md -- "src/gomad-skills/4-implementation/gm-discuss-story/*.md"` exits 0.
4. `npm run validate:kb-licenses` still exits 0 (no KB files shipped in this plan).
5. `npm run test:orphan-refs` exits 0 (discover-inputs.md references are present or handled-not-found).
6. `grep -rn "gm-discuss-story" src/gomad-skills/` — appears only in the new skill's own directory; no external reference yet (Plan 06 adds the gm-create-story side).
</verification>

<success_criteria>
- `gm-discuss-story/` directory contains exactly 5 files: SKILL.md, workflow.md, template.md, checklist.md, discover-inputs.md (STORY-02 mirror).
- Output template has 5 locked XML-wrapped sections (STORY-03).
- workflow.md implements classic-discuss elicitation with Resume/Start fresh (D-05), Update/View/Skip (D-06), edge-case stale-checkpoint cleanup (D-07), and adaptive section mapping (D-03).
- No token-cap / compression / truncation logic in workflow.md (D-04 respected).
- No new runtime deps.
- `node tools/validate-skills.js --strict` exits 0.
- `ManifestGenerator.collectSkills` auto-discovers the new skill on next install (no installer code change needed).
</success_criteria>

<output>
After completion, create `.planning/phases/10-story-creation-enhancements/10-03-SUMMARY.md` documenting:
- Line counts for each of the 5 files
- Which discuss-phase.md patterns were mirrored (line-range references)
- Which deviations from gm-create-story/ were required and why
- Confirm `node tools/validate-skills.js --strict` exits 0 with the new skill
- Any deviations from this plan and rationale
</output>
