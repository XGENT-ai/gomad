---
phase: 10-story-creation-enhancements
plan: 06
type: execute
wave: 4
depends_on: [10-02, 10-03, 10-04, 10-05]
files_modified:
  - src/gomad-skills/4-implementation/gm-create-story/discover-inputs.md
  - src/gomad-skills/4-implementation/gm-create-story/workflow.md
autonomous: true
requirements:
  - STORY-04
  - STORY-05
  - STORY-12
tags:
  - skill
  - integration
  - gm-create-story
must_haves:
  truths:
    - "`gm-create-story/workflow.md` Input Files table includes a new `story_context` entry with SELECTIVE_LOAD strategy and path pattern `{planning_artifacts}/{{story_key}}-context.md` (STORY-04)"
    - "`gm-create-story/discover-inputs.md` protocol's SELECTIVE_LOAD example mentions `{{story_key}}` alongside the existing `{{epic_num}}` (so the protocol naturally handles the new pattern)"
    - "Missing `{planning_artifacts}/{{story_key}}-context.md` file emits a hint (not an error) — the existing 2c handle-not-found path in discover-inputs.md (lines 70-76) satisfies STORY-04 by falling through to empty-string `{story_context_content}`"
    - "No conflict-detection logic, no visible warning emission for context.md vs user prompt disagreement (D-14 revises STORY-05: prompt-wins is satisfied naturally by LLM context ordering)"
    - "`gm-create-story/workflow.md` has a new step (between existing steps 3 and 4) that invokes `gm-domain-skill` for pre-bake of domain KB content (STORY-12 — integration point confirmed in workflow.md)"
    - "`gm-dev-story` integration is NOT added (STORY-F1 deferred per CONTEXT.md §Deferred Ideas)"
  artifacts:
    - path: "src/gomad-skills/4-implementation/gm-create-story/discover-inputs.md"
      provides: "Patched SELECTIVE_LOAD example (adds {{story_key}} alongside {{epic_num}})"
      contains: "{{story_key}}"
    - path: "src/gomad-skills/4-implementation/gm-create-story/workflow.md"
      provides: "Patched INITIALIZATION paths + Input Files table + new step invoking gm-domain-skill"
      contains: "story_context"
  key_links:
    - from: "gm-create-story/workflow.md Input Files table"
      to: "{planning_artifacts}/{{story_key}}-context.md"
      via: "SELECTIVE_LOAD entry — `story_context`"
      pattern: "story_context"
    - from: "gm-create-story/workflow.md new step"
      to: "gm-domain-skill"
      via: "`<action>Invoke gm-domain-skill ...</action>`"
      pattern: "gm-domain-skill"
    - from: "gm-create-story auto-load of context.md"
      to: "discover-inputs.md 2c handle-not-found"
      via: "missing file → empty string (not error)"
      pattern: "No story_context files found"
---

<objective>
Patch `gm-create-story/` to auto-load `{{story_key}}-context.md` produced by Plan 03's `gm-discuss-story`, and to invoke Plan 04's `gm-domain-skill` for pre-bake integration (STORY-12). Implements STORY-04 (auto-detect + hint-not-error), STORY-05 REVISED per D-14 (prompt-wins is implicit from LLM context ordering; no conflict detection or warning emission), and STORY-12 (integration point in workflow.md, gm-dev-story integration deferred).

Purpose: Closes the story-creation loop. After this plan: running `gm-discuss-story` for story X → producing `X-context.md`, then running `gm-create-story` for X → auto-loads X-context.md alongside PRD/epics/architecture/UX per D-13 (purely additive as a SELECTIVE_LOAD entry — no mechanical parsing, no field routing). If context.md is missing, `gm-create-story` proceeds without it (STORY-04 hint-not-error via the existing 2c fall-through).

Output: 2 modified files (both under `gm-create-story/`). No new files. No installer changes. No executable code changes. All diffs are markdown additions.
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-story-creation-enhancements/10-CONTEXT.md
@.planning/phases/10-story-creation-enhancements/10-PATTERNS.md
@.planning/phases/10-story-creation-enhancements/10-03-PLAN.md
@.planning/phases/10-story-creation-enhancements/10-04-PLAN.md
@.planning/phases/10-story-creation-enhancements/10-05-PLAN.md
@src/gomad-skills/4-implementation/gm-create-story/discover-inputs.md
@src/gomad-skills/4-implementation/gm-create-story/workflow.md

<interfaces>
<!-- Exact lines + patterns to patch. -->

From `gm-create-story/workflow.md` current state (verified in PATTERNS.md §12, §13):

**Lines 28-37 (Paths section under INITIALIZATION):**
```
### Paths

- `sprint_status` = `{implementation_artifacts}/sprint-status.yaml`
- `epics_file` = `{planning_artifacts}/epics.md`
- `prd_file` = `{planning_artifacts}/prd.md`
- `architecture_file` = `{planning_artifacts}/architecture.md`
- `ux_file` = `{planning_artifacts}/*ux*.md`
- `story_title` = "" (will be elicited if not derivable)
- `project_context` = `**/project-context.md` (load if exists)
- `default_output_file` = `{implementation_artifacts}/{{story_key}}.md`
```

**Lines 39-47 (Input Files table):**
```
### Input Files

| Input | Description | Path Pattern(s) | Load Strategy |
|-------|-------------|------------------|---------------|
| prd | PRD (fallback - epics file should have most content) | whole: `{planning_artifacts}/*prd*.md`, sharded: `{planning_artifacts}/*prd*/*.md` | SELECTIVE_LOAD |
| architecture | Architecture (fallback - epics file should have relevant sections) | whole: `{planning_artifacts}/*architecture*.md`, sharded: `{planning_artifacts}/*architecture*/*.md` | SELECTIVE_LOAD |
| ux | UX design (fallback - epics file should have relevant sections) | whole: `{planning_artifacts}/*ux*.md`, sharded: `{planning_artifacts}/*ux*/*.md` | SELECTIVE_LOAD |
| epics | Enhanced epics+stories file with BDD and source hints | whole: `{planning_artifacts}/*epic*.md`, sharded: `{planning_artifacts}/*epic*/*.md` | SELECTIVE_LOAD |
```
— Add a 5th row AFTER `epics` for `story_context`.

**Line 215 (step 2 action):**
```
<action>Read fully and follow `./discover-inputs.md` to load all input files</action>
```
— No change needed. `story_context` will be loaded automatically since it's now in the Input Files table.

**Lines 216-217 (step 2 note):**
```
<note>Available content: {epics_content}, {prd_content}, {architecture_content}, {ux_content},
{project_context}</note>
```
— Patch: add `{story_context_content}` to the list.

**Step 3 ends around line 269 (workflow.md currently structured 250-269 for architecture analysis; step 4 is web research starting line 271).**
— Insert new step `<step n="3b" goal="...">` between step 3 and step 4 per PATTERNS.md §13.

**discover-inputs.md line 34 (current):**
```
Load a specific shard using a template variable. Example: used for epics with `{{epic_num}}`.
```
— Patch: add `{{story_key}}` as a second example:
```
Load a specific shard using a template variable. Examples: epics with `{{epic_num}}`, story context with `{{story_key}}`.
```

**discover-inputs.md lines 70-76 (current — handle not found is ALREADY CORRECT):**
```
### 2c: Handle Not Found

If no matches were found for either sharded or whole patterns:

1. Set `{pattern_name_content}` to empty string.
2. Note in session: "No {pattern_name} files found" -- this is not an error, just unavailable. Offer the user a chance to provide the file.
```
— No change needed. This already satisfies STORY-04 "missing file = hint not error" for `story_context`. Do NOT modify this block.

**D-13 (from CONTEXT.md):** "gm-create-story does NOT mechanically parse or route context.md fields. Integration is purely: discover-inputs.md SELECTIVE_LOAD adds `{{story_key}}-context.md` as one more Input File alongside prd/architecture/epics/ux."

**D-14 (from CONTEXT.md):** "No conflict detection and no warning emission. If user prompt and context.md contradict, Claude silently prioritizes the user prompt through normal LLM context-ordering behavior. Revises STORY-05: drop 'emits a visible warning listing the conflict lines' clause; retain 'user prompt wins'."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch gm-create-story/discover-inputs.md (one-line edit)</name>
  <files>src/gomad-skills/4-implementation/gm-create-story/discover-inputs.md</files>
  <read_first>
    - src/gomad-skills/4-implementation/gm-create-story/discover-inputs.md (full 89 lines — CRITICAL: lines 32-39 for SELECTIVE_LOAD section; lines 70-76 for 2c handle-not-found — DO NOT MODIFY 2c)
    - .planning/phases/10-story-creation-enhancements/10-PATTERNS.md §12 (exact patch content)
    - .planning/phases/10-story-creation-enhancements/10-CONTEXT.md §D-01 and §D-13 (reuse rationale + "one more Input File, no mechanical parsing")
  </read_first>
  <behavior>
    - Exactly ONE line changed: line 34 (the SELECTIVE_LOAD example) gains a second example mention.
    - Lines 70-76 (handle-not-found) are NOT touched — they already satisfy STORY-04.
    - `diff src/gomad-skills/4-implementation/gm-create-story/discover-inputs.md.orig src/gomad-skills/4-implementation/gm-create-story/discover-inputs.md` shows exactly 2 line-diff markers (1 line deleted, 1 line added).
    - `node tools/validate-skills.js --strict` still passes.
  </behavior>
  <action>
    Edit `src/gomad-skills/4-implementation/gm-create-story/discover-inputs.md`.

    **Single change, line 34** (inside the `#### SELECTIVE_LOAD Strategy` section):

    BEFORE:
    ```
    Load a specific shard using a template variable. Example: used for epics with `{{epic_num}}`.
    ```

    AFTER:
    ```
    Load a specific shard using a template variable. Examples: epics with `{{epic_num}}`, story context with `{{story_key}}`.
    ```

    Do NOT modify any other line. The 2c handle-not-found block (lines 70-76) already handles missing files correctly (STORY-04 "hint not error" satisfied by "Note in session: 'No {pattern_name} files found' -- this is not an error").

    Run `npx prettier --write src/gomad-skills/4-implementation/gm-create-story/discover-inputs.md` + `npm run lint:md -- "src/gomad-skills/4-implementation/gm-create-story/discover-inputs.md"`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/gomad-skills/4-implementation/gm-create-story/discover-inputs.md','utf8'); if (!/Examples: epics with .\\{\\{epic_num\\}\\}., story context with .\\{\\{story_key\\}\\}./.test(s)) { console.error('patched SELECTIVE_LOAD example missing {{story_key}}'); process.exit(1); } if (!/No \\{pattern_name\\} files found/.test(s)) { console.error('handle-not-found was accidentally modified'); process.exit(1); } console.log('OK');" && node tools/validate-skills.js --strict</automated>
  </verify>
  <done>
    - discover-inputs.md line 34 mentions both `{{epic_num}}` and `{{story_key}}` as SELECTIVE_LOAD examples.
    - Lines 70-76 (2c handle-not-found) are unchanged.
    - `node tools/validate-skills.js --strict` exits 0.
    - `npm run lint:md` exits 0.
  </done>
  <acceptance_criteria>
    - `grep -c "{{story_key}}" src/gomad-skills/4-implementation/gm-create-story/discover-inputs.md` >= 1
    - `grep -c "{{epic_num}}" src/gomad-skills/4-implementation/gm-create-story/discover-inputs.md` >= 1 (preserved)
    - `grep -c "No {pattern_name} files found" src/gomad-skills/4-implementation/gm-create-story/discover-inputs.md` = 1 (2c block untouched)
    - `grep -c "### 2c: Handle Not Found" src/gomad-skills/4-implementation/gm-create-story/discover-inputs.md` = 1 (section intact)
    - `node tools/validate-skills.js --strict` exits 0
    - `npm run lint:md -- "src/gomad-skills/4-implementation/gm-create-story/discover-inputs.md"` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Patch gm-create-story/workflow.md (3 surgical edits: Paths, Input Files table, new step for gm-domain-skill)</name>
  <files>src/gomad-skills/4-implementation/gm-create-story/workflow.md</files>
  <read_first>
    - src/gomad-skills/4-implementation/gm-create-story/workflow.md (full 380 lines — CRITICAL: lines 28-37 for Paths; lines 39-47 for Input Files table; lines 211-248 for step 2 body; lines 250-269 for step 3 end; lines 271-291 for step 4 position)
    - .planning/phases/10-story-creation-enhancements/10-PATTERNS.md §13 (exact insertion content for the new step)
    - .planning/phases/10-story-creation-enhancements/10-CONTEXT.md §D-13, §D-14, §D-12 (integration rules + no conflict detection + gm-dev-story deferred)
    - .planning/research/PITFALLS.md §4-K (integration timing — D-13 sets pre-bake from gm-create-story as chosen integration point; gm-dev-story fresh retrieval is STORY-F1 deferred)
  </read_first>
  <behavior>
    - THREE edits total, surgical:
      1. Add `- story_context = {planning_artifacts}/{{story_key}}-context.md (load if exists per STORY-04)` to Paths section.
      2. Add a new row to Input Files table: `| story_context | Pre-discussion context captured by gm-discuss-story (optional — missing file = hint not error per STORY-04) | whole: \`{planning_artifacts}/{{story_key}}-context.md\` | SELECTIVE_LOAD |`
      3. Insert a new `<step n="3b" goal="Fetch domain KB context (pre-bake)">` BETWEEN step 3 (ends ~line 269) and step 4 (starts ~line 271) per PATTERNS.md §13.
    - NO conflict-detection logic added to ANY step (D-14).
    - NO token-counting logic added (D-04 also applies to downstream — keep gm-create-story transparent).
    - NO gm-dev-story changes (STORY-F1 deferred).
    - Existing steps 1-8 are preserved exactly as-is (only the step 2 note at line 216-217 gets a small addition to list `{story_context_content}` alongside other loaded content variables).
  </behavior>
  <action>
    Edit `src/gomad-skills/4-implementation/gm-create-story/workflow.md`. Make 4 surgical edits:

    **Edit 1 — Paths section** (current lines 28-37). Insert a new line after `default_output_file`:

    BEFORE (line 37):
    ```
    - `default_output_file` = `{implementation_artifacts}/{{story_key}}.md`
    ```

    AFTER:
    ```
    - `default_output_file` = `{implementation_artifacts}/{{story_key}}.md`
    - `story_context` = `{planning_artifacts}/{{story_key}}-context.md` (load if exists per STORY-04; produced by gm-discuss-story)
    ```

    **Edit 2 — Input Files table** (current lines 39-47). Add one new row AFTER the `epics` row:

    AFTER:
    ```markdown
    | epics | Enhanced epics+stories file with BDD and source hints | whole: `{planning_artifacts}/*epic*.md`, sharded: `{planning_artifacts}/*epic*/*.md` | SELECTIVE_LOAD |
    | story_context | Pre-discussion context produced by gm-discuss-story (optional — missing file = hint not error per STORY-04) | whole: `{planning_artifacts}/{{story_key}}-context.md` | SELECTIVE_LOAD |
    ```

    (Note: the template variable `{{story_key}}` is resolved by the existing SELECTIVE_LOAD machinery — see discover-inputs.md §2 SELECTIVE_LOAD + the handle-not-found fallback at §2c.)

    **Edit 3 — Step 2 note** (current lines 216-217). Add `{story_context_content}` to the note:

    BEFORE:
    ```
    <note>Available content: {epics_content}, {prd_content}, {architecture_content}, {ux_content},
    {project_context}</note>
    ```

    AFTER:
    ```
    <note>Available content: {epics_content}, {prd_content}, {architecture_content}, {ux_content},
    {project_context}, {story_context_content} (from gm-discuss-story per STORY-04; may be empty)</note>
    ```

    **Edit 4 — Insert new `<step n="3b">` between existing step 3 and step 4** (current: step 3 ends ~line 269, step 4 starts ~line 271). Insert verbatim:

    ```xml
    <step n="3b" goal="Fetch domain KB context (pre-bake per STORY-12)">
      <critical>📚 DOMAIN KB PRE-BAKE — Retrieve relevant reference material from installed KB packs per STORY-12. Skip gracefully if no KB packs are installed.</critical>

      <action>From {architecture_content}, {epics_content}, and {story_context_content}, identify 1-3 domains relevant to this story (e.g., "testing" if the story involves new test coverage; "architecture" if it involves new module boundaries). Match against installed slugs by reading the directory names under `<installRoot>/_config/kb/`.</action>

      <check if="no relevant domain slugs match any installed pack">
        <output>No domain KB match for story {{story_key}} — proceeding without KB pre-bake.</output>
        <action>Set {kb_snippets} to empty string and GOTO step 4</action>
      </check>

      <action>For each relevant domain slug, invoke gm-domain-skill with a focused query derived from the story scope (e.g., domain=testing, query="integration test DB strategy for Node.js").</action>
      <note>gm-domain-skill returns a single best-matching .md file's full content per D-08, or a "no match" message per D-10, or a "did you mean" fallback per D-11. Accept all three outcomes silently — no fatal branches.</note>

      <action>Append each successful retrieval to {kb_snippets} with a header line: `\n\n### Domain KB: <slug> — <relative_path>\n\n{content}`.</action>

      <check if="kb retrieval returned no match or pack not installed for all requested slugs">
        <action>Proceed without kb_snippets — this is NOT an error. Note in summary: "No KB pre-bake for this story."</action>
      </check>

      <note>This step is the integration point for STORY-12. gm-dev-story fresh-retrieval integration is deferred per STORY-F1.</note>
    </step>
    ```

    **Template-output wiring for kb_snippets (optional — can be folded into Step 5 rendering):**
    In Step 5 (currently starts ~line 293), near the existing `<template-output file="{default_output_file}">technical_requirements</template-output>` line, add a conditional:

    ```xml
          <!-- Domain KB pre-bake content (STORY-12) -->
          <check if="kb_snippets is non-empty">
            <template-output file="{default_output_file}">domain_kb_references</template-output>
          </check>
    ```

    Place this ADDITIVE block near the existing architecture/testing template-output entries (around workflow.md:305-315) without modifying existing entries.

    Run `npx prettier --write src/gomad-skills/4-implementation/gm-create-story/workflow.md` + `npm run lint:md -- "src/gomad-skills/4-implementation/gm-create-story/workflow.md"` + `node tools/validate-skills.js --strict`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/gomad-skills/4-implementation/gm-create-story/workflow.md','utf8'); const musts = [{pattern: /story_context = .*\\{planning_artifacts\\}\\/\\{\\{story_key\\}\\}-context\\.md/, name: 'Path entry for story_context'}, {pattern: /\\| story_context \\|.*SELECTIVE_LOAD/, name: 'Input Files row for story_context'}, {pattern: /\\{story_context_content\\}/, name: 'step 2 note references story_context_content'}, {pattern: /<step n=\"3b\" goal=\"Fetch domain KB context/, name: 'new step 3b inserted'}, {pattern: /gm-domain-skill/, name: 'gm-domain-skill invocation referenced'}, {pattern: /STORY-12/, name: 'STORY-12 traceability'}, {pattern: /STORY-04/, name: 'STORY-04 traceability'}, {pattern: /STORY-F1/, name: 'STORY-F1 deferred note'}]; const missing = musts.filter(m => !m.pattern.test(s)); if (missing.length) { console.error('missing:', missing.map(m=>m.name)); process.exit(1); } if (/conflict detect|emit.*warning.*listing the conflict|conflict.*lines/i.test(s) && !/D-14|revised/.test(s)) { console.error('possible conflict-detection logic leaked (D-14 forbids)'); process.exit(1); } console.log('OK');" && node tools/validate-skills.js --strict && npm run lint:md -- "src/gomad-skills/4-implementation/gm-create-story/workflow.md"</automated>
  </verify>
  <done>
    - workflow.md Paths section includes `story_context` entry.
    - workflow.md Input Files table has 5 rows (added `story_context` row with SELECTIVE_LOAD).
    - workflow.md step 2 note lists `{story_context_content}` alongside existing variables.
    - workflow.md has new `<step n="3b">` between existing step 3 and step 4.
    - New step 3b invokes gm-domain-skill, handles 3 retrieval outcomes (match, no-match, pack-not-installed) silently, and proceeds without error in all cases (STORY-04 style graceful degradation).
    - NO conflict-detection logic added (D-14 revision respected).
    - `node tools/validate-skills.js --strict` exits 0.
    - `npm run lint:md` exits 0.
  </done>
  <acceptance_criteria>
    - `grep -c "story_context = " src/gomad-skills/4-implementation/gm-create-story/workflow.md` >= 1 (Paths entry)
    - `grep -cE "\\| story_context \\|.*SELECTIVE_LOAD" src/gomad-skills/4-implementation/gm-create-story/workflow.md` = 1 (table row)
    - `grep -c "{story_context_content}" src/gomad-skills/4-implementation/gm-create-story/workflow.md` >= 1 (step 2 note)
    - `grep -cE "<step n=\"3b\"" src/gomad-skills/4-implementation/gm-create-story/workflow.md` = 1 (new step)
    - `grep -c "gm-domain-skill" src/gomad-skills/4-implementation/gm-create-story/workflow.md` >= 2 (step body + note)
    - `grep -c "STORY-04" src/gomad-skills/4-implementation/gm-create-story/workflow.md` >= 1 (traceability)
    - `grep -c "STORY-12" src/gomad-skills/4-implementation/gm-create-story/workflow.md` >= 1 (traceability)
    - `grep -c "STORY-F1" src/gomad-skills/4-implementation/gm-create-story/workflow.md` >= 1 (deferred note)
    - `grep -cE "conflict detect|emit.*warning.*listing the conflict|conflict.*lines" src/gomad-skills/4-implementation/gm-create-story/workflow.md` = 0 (D-14 — no conflict-detection logic added by this plan)
    - `grep -cE "token.?count|token.?cap|truncate" src/gomad-skills/4-implementation/gm-create-story/workflow.md` = 0 (D-04 — no token-management logic added)
    - Existing step numbers 1-8 still present: `grep -cE "<step n=\"[1-8]\"" src/gomad-skills/4-implementation/gm-create-story/workflow.md` >= 6 (plus the new 3b = at least 7 step tags)
    - `node tools/validate-skills.js --strict` exits 0
    - `npm run lint:md -- "src/gomad-skills/4-implementation/gm-create-story/workflow.md"` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {story_context_content} → LLM context for gm-create-story | Context.md is author-produced via gm-discuss-story (Plan 03); feeds into story draft via LLM context ordering (D-13). No mechanical parsing — no injection surface to parse. |
| gm-domain-skill retrieval → {kb_snippets} | Plan 04's retrieval output becomes part of gm-create-story's context. KB content is pre-validated by Plan 01 gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06-01 | Tampering (T) | Malicious context.md content | accept | Context.md is author-produced (`{planning_artifacts}/` is workspace-local, solo-dev trust boundary). Same trust level as PRD/epics. LOW. |
| T-10-06-02 | Information Disclosure (I) | context.md contains sensitive decisions | accept | Workspace-local file; same level as story files. Solo-dev trust boundary per PROJECT.md. LOW. |
| T-10-06-03 | Repudiation (R) | Silent prompt-wins (D-14, no conflict warning) | mitigate | D-14 explicitly accepts this behavior. If empirical pain arises post-v1.3, add warning in a patch release with a concrete failure mode (per CONTEXT.md §Deferred Ideas). LOW — by design decision. |
| T-10-06-04 | Elevation of Privilege (E) | gm-domain-skill invocation reading files outside `_config/kb/` | mitigate | Plan 04's threat model T-10-04-01/02 already covers this (slug validation rule in workflow.md step 1 rejects traversal). This plan simply invokes Plan 04's workflow — no new EoP surface. MEDIUM — already mitigated in Plan 04. |
| T-10-06-05 | Denial of Service (D) | Large context.md bloats gm-create-story's LLM context | accept | D-04 drops token cap; prompt engineering in Plan 03's workflow.md enforces "decisions not transcripts". If context.md is huge, gm-create-story slows but does not fail. LOW. |

**HIGH/CRITICAL findings:** None. This is a markdown-only integration plan; no executable code changes.
</threat_model>

<verification>
Phase-level checks for this plan:

1. `diff` between pre-plan and post-plan `gm-create-story/discover-inputs.md` shows exactly 2 line-diff markers (1 removed, 1 added).
2. `diff` between pre-plan and post-plan `gm-create-story/workflow.md` shows:
   - 1 line added in Paths section
   - 1 line added in Input Files table
   - 1 line modified in step 2 note
   - ~15-25 lines added for new step 3b
   - ~3-5 lines added for conditional template-output of domain_kb_references
3. `node tools/validate-skills.js --strict` exits 0 (gm-create-story skill still valid).
4. `npm run lint:md` exits 0 for both modified files.
5. End-to-end: with Plans 03, 04, 05 landed AND this plan landed, a fresh `gomad install` → run `gm-discuss-story 1-2-example` → run `gm-create-story 1-2-example` succeeds, and the story draft includes context.md content + domain KB snippets (if relevant slugs match).
6. `npm run test:orphan-refs` exits 0 — the new story_context path pattern is handled by discover-inputs.md §2c.
7. No D-14 violations: `grep -rn "conflict.*detect\|emit.*warning.*listing" src/gomad-skills/4-implementation/gm-create-story/` returns zero hits.
</verification>

<success_criteria>
- `gm-create-story/discover-inputs.md` mentions `{{story_key}}` as a SELECTIVE_LOAD example (1-line change).
- `gm-create-story/workflow.md` has:
  - Paths entry for `story_context`
  - Input Files table row for `story_context` (SELECTIVE_LOAD)
  - Step 2 note listing `{story_context_content}`
  - New step 3b invoking gm-domain-skill (STORY-12 integration)
- Missing context.md does not error — falls through discover-inputs.md §2c handle-not-found (STORY-04).
- Missing KB pack slugs do not error — step 3b handles no-match / pack-not-installed silently (STORY-04 style graceful degradation).
- No conflict-detection logic added (D-14 respected).
- No token-counting logic added (D-04 respected).
- No gm-dev-story changes (STORY-F1 deferred).
- `node tools/validate-skills.js --strict` exits 0.
- `npm run lint:md` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/10-story-creation-enhancements/10-06-SUMMARY.md` documenting:
- Exact line numbers of the 4 edits in workflow.md
- Confirmation that discover-inputs.md diff is exactly 2 lines (1 removed, 1 added)
- End-to-end manual check (if available): invoke the full `gm-discuss-story → gm-create-story` chain in a test workspace
- Confirm `node tools/validate-skills.js --strict` exits 0
- Confirm no D-14 or D-04 violations (no conflict-detection, no token-counting) per the grep checks in verification
</output>
