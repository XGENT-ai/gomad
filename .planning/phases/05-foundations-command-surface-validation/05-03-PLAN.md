---
phase: 05-foundations-command-surface-validation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
autonomous: true
requirements:
  - REL-01
tags:
  - docs
  - project-contract
  - key-decisions
  - factual-correction

must_haves:
  truths:
    - "`.planning/PROJECT.md` line 77 (in §Context) no longer contains the phrase `type: module`; language is corrected to describe CommonJS with `require()`-based loading"
    - "`.planning/PROJECT.md` line 94 (in §Constraints) no longer contains the phrase `type: module`; same CommonJS correction applied"
    - "`.planning/PROJECT.md` Key Decisions table (lines 98-119 range) has a NEW row logging the launcher-form architecture decision (D-08) with wording matching the D-08 contract"
    - "The new launcher decision row is formatted as a valid 3-column Markdown table row (Decision | Rationale | Outcome)"
    - "No other changes to `.planning/PROJECT.md` — line count increases by exactly 1 (new table row) and two existing lines are edited in place"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "CommonJS factual correction (D-13) + launcher-form Key Decisions row (D-08)"
      contains: "CommonJS"
  key_links:
    - from: ".planning/PROJECT.md line 77 (Context tech stack)"
      to: "runtime reality in tools/installer/gomad-cli.js + core/installer.js"
      via: "docs statement matches code (CommonJS `require()`)"
      pattern: "CommonJS.*require"
    - from: ".planning/PROJECT.md line 94 (Constraints tech stack)"
      to: "same runtime reality"
      via: "docs statement matches code (CommonJS `require()`)"
      pattern: "CommonJS.*require"
    - from: ".planning/PROJECT.md Key Decisions table"
      to: "Phase 6 agent-command-generator.js contract"
      via: "new row locks launcher-form decision as phase-5-committed architecture"
      pattern: "Launcher-form slash commands"
---

<objective>
Correct the two factually-wrong `type: module` statements in `.planning/PROJECT.md` (lines 77 and 94, per D-13) — the installer is CommonJS with `require()`-based loading, evidenced by `package.json` having NO `"type"` field and `tools/installer/gomad-cli.js` line 3 (`const { program } = require('commander')`).

Simultaneously log the launcher-form slash commands architecture decision (D-08) as a new row in the Key Decisions table (PROJECT.md lines 98-119), formally committing that `.claude/commands/gm/agent-*.md` is a thin launcher that loads persona from `_gomad/gomad/agents/*.md` at runtime.

Purpose: Close REL-01 (ROADMAP SC#4 + CONTEXT D-13) and the second half of ROADMAP SC#2 (launcher decision logged). Both are docs-only, low-risk edits to a single file.

Output: edited `.planning/PROJECT.md` with exactly two in-place replacements and one appended table row.
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-foundations-command-surface-validation/05-CONTEXT.md
@.planning/phases/05-foundations-command-surface-validation/05-PATTERNS.md
@.planning/PROJECT.md
@package.json
@tools/installer/gomad-cli.js
@tools/installer/core/installer.js

<interfaces>
<!-- Exact current text being edited (verbatim from PROJECT.md at read time) -->

Line 77 (in §Context, bullet list):
```
- Tech stack: Node.js / JavaScript (`type: module`), inherited from BMAD.
```

Line 94 (in §Constraints, bullet list with **bold** label):
```
- **Tech stack**: Node.js / JavaScript (`type: module`), inherited from BMAD.
```

Key Decisions table header (line 100):
```
| Decision                                                                                                                                | Rationale                                                                                                              | Outcome                                                  |
| --------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- |
```

Last existing row (line 119):
```
| E2E test verifies tarball structurally (not via interactive `gomad install`)                                                            | Avoids `@clack/prompts` hang in non-TTY test environment                                                               | ✓ Good                                                   |
```

Evidence for D-13 correction (do NOT include this in the doc; just confirms the edit is correct):
- `package.json` (this repo): no `"type"` field — confirmed via cat of package.json lines 1-123
- `tools/installer/gomad-cli.js` line 3: `const { program } = require('commander');` — CommonJS
- `tools/installer/core/installer.js`: uses `require('node:path')` — CommonJS
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Correct `type: module` factual error at lines 77 + 94 (D-13)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (FULL FILE — confirm current line 77 and line 94 contents match the verbatim shapes in <interfaces>; line numbers may drift if the file has changed since 05-PATTERNS.md was written, so match on the unique `type: module` phrase rather than trusting line numbers blindly)
    - package.json (lines 1-30 — confirm no `"type"` field exists, validating the correction)
    - tools/installer/gomad-cli.js (line 3 — confirm `require('commander')`)
    - tools/installer/core/installer.js (head — confirm `require('node:path')`)
    - .planning/phases/05-foundations-command-surface-validation/05-CONTEXT.md (D-13: exact reasoning and evidence citations)
    - .planning/phases/05-foundations-command-surface-validation/05-PATTERNS.md §3 "Edit A — D-13 factual correction" (proposed corrected phrasing)
  </read_first>
  <behavior>
    - Line containing `Tech stack: Node.js / JavaScript (`type: module`), inherited from BMAD.` (unbolded, §Context) is replaced with the corrected form
    - Line containing `**Tech stack**: Node.js / JavaScript (`type: module`), inherited from BMAD.` (bolded, §Constraints) is replaced with the corrected form
    - Both corrected lines preserve their existing markdown shape (bullet prefix, bold formatting, trailing phrase "inherited from BMAD.")
    - No other content in PROJECT.md changes as part of this task
    - Post-edit, `grep -c "type: module" .planning/PROJECT.md` returns 0
    - Post-edit, `grep -c "CommonJS" .planning/PROJECT.md` returns >= 2
  </behavior>
  <action>
**Step 1 — Locate both occurrences using the unique phrase (NOT by hardcoded line numbers):**

```bash
grep -n 'type: module' .planning/PROJECT.md
```

Expected output: exactly 2 matches. If fewer or more, stop and surface to the user — the file has drifted from the mapped state.

**Step 2 — Apply the two in-place edits. Use the Edit tool with these EXACT old_string → new_string pairs:**

Edit A (unbolded, §Context version — likely line 77):

OLD:
```
- Tech stack: Node.js / JavaScript (`type: module`), inherited from BMAD.
```

NEW:
```
- Tech stack: Node.js / JavaScript (CommonJS, `require()`-based loading), inherited from BMAD.
```

Edit B (bolded, §Constraints version — likely line 94):

OLD:
```
- **Tech stack**: Node.js / JavaScript (`type: module`), inherited from BMAD.
```

NEW:
```
- **Tech stack**: Node.js / JavaScript (CommonJS, `require()`-based loading), inherited from BMAD.
```

The two OLD strings differ only by the `**` bold markers around "Tech stack" — the Edit tool will match each uniquely because they are distinct full-line strings.

**Step 3 — Verify post-edit:**

```bash
# Zero occurrences of the wrong phrase
grep -c "type: module" .planning/PROJECT.md   # expect: 0

# Two occurrences of the corrected phrase
grep -c "CommonJS, \`require()\`-based loading" .planning/PROJECT.md   # expect: 2
```

If verification fails, revert and investigate.

**Constraint:** Do NOT add a third mention of CommonJS elsewhere in the document (e.g., do NOT edit the Key Decisions table as part of this task — that's Task 2). Scope is strictly the two existing `type: module` lines.
  </action>
  <verify>
    <automated>test $(grep -c "type: module" .planning/PROJECT.md) -eq 0 && test $(grep -cF 'CommonJS, `require()`-based loading' .planning/PROJECT.md) -eq 2</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "type: module" .planning/PROJECT.md` returns exactly 0
    - `grep -cF "CommonJS, \`require()\`-based loading" .planning/PROJECT.md` returns exactly 2
    - The unbolded §Context version is preserved as a bullet: `grep -c '^- Tech stack: Node.js / JavaScript (CommonJS' .planning/PROJECT.md` returns exactly 1
    - The bolded §Constraints version is preserved as a bullet: `grep -c '^- \*\*Tech stack\*\*: Node.js / JavaScript (CommonJS' .planning/PROJECT.md` returns exactly 1
    - Both corrected lines end with `, inherited from BMAD.` — `grep -cE "^- (\*\*)?Tech stack(\*\*)?: Node.js / JavaScript \(CommonJS, \`require\(\)\`-based loading\), inherited from BMAD\.$" .planning/PROJECT.md` returns exactly 2
    - No unintended deletions: `git diff .planning/PROJECT.md | grep "^-" | grep -vE "^---|^- Tech stack:.*type: module|^- \*\*Tech stack\*\*:.*type: module"` returns no lines
  </acceptance_criteria>
  <done>
    Both `type: module` occurrences replaced with `CommonJS, \`require()\`-based loading`; markdown formatting (bullets, bold, trailing "inherited from BMAD.") preserved; no other lines in PROJECT.md changed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append launcher-form decision row to Key Decisions table (D-08)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (FULL FILE — confirm Key Decisions table structure; find the LAST existing row via `grep -n "E2E test verifies tarball structurally" .planning/PROJECT.md` so we append immediately after it regardless of line drift)
    - .planning/phases/05-foundations-command-surface-validation/05-CONTEXT.md (D-08: exact wording specification)
    - .planning/phases/05-foundations-command-surface-validation/05-PATTERNS.md §3 "Edit B — D-08 Key Decisions row" (proposed row contents with locked Decision + Rationale cells)
    - .planning/STATE.md §"Accumulated Context → Decisions" (launcher-form bullet — verify no conflict with new row wording)
  </read_first>
  <behavior>
    - A single new row is appended to the Key Decisions table
    - The new row is placed IMMEDIATELY AFTER the existing `E2E test verifies tarball structurally` row
    - The new row is the LAST row of the Key Decisions table — no content inserted between it and the `## Evolution` section header
    - The row is a valid 3-column Markdown table row: `| Decision | Rationale | Outcome |`
    - The Decision cell contains the exact phrase from D-08: `Launcher-form slash commands (not self-contained)` AND `.claude/commands/gm/agent-*.md` AND `_gomad/gomad/agents/*.md`
    - The Rationale cell mentions `SKILL.md` as the single source of truth
    - The Outcome cell indicates "contract set in Phase 5" / "extractor lands in Phase 6"
    - Post-edit, `grep -c "^| " .planning/PROJECT.md` (count of table rows across the whole file) increases by exactly 1 vs pre-edit
  </behavior>
  <action>
**Step 1 — Capture pre-edit table-row count for audit:**

```bash
ROWS_BEFORE=$(grep -c "^| " .planning/PROJECT.md)
```

**Step 2 — Append the new row using the Edit tool, with the last existing row as the match anchor:**

OLD string (the full last row — unique phrase `E2E test verifies tarball structurally`):

```
| E2E test verifies tarball structurally (not via interactive `gomad install`)                                                            | Avoids `@clack/prompts` hang in non-TTY test environment                                                               | ✓ Good                                                   |
```

NEW string (the same row followed by a newline and the new row):

```
| E2E test verifies tarball structurally (not via interactive `gomad install`)                                                            | Avoids `@clack/prompts` hang in non-TTY test environment                                                               | ✓ Good                                                   |
| Launcher-form slash commands (not self-contained) — `.claude/commands/gm/agent-*.md` is a thin stub loading persona from `_gomad/gomad/agents/*.md` at runtime | Preserves `SKILL.md` as single source of truth (persona body extracted at install time per D-06); no hand-authored duplication; Claude Code sees rich metadata via launcher body | — Contract set in Phase 5; extractor lands in Phase 6    |
```

This preserves the existing row verbatim and adds exactly ONE new row below it. The column widths don't need to match the visual padding of earlier rows — Markdown tables render correctly regardless.

**Step 3 — Verify post-edit:**

```bash
# Row count increased by exactly 1
ROWS_AFTER=$(grep -c "^| " .planning/PROJECT.md)
test $((ROWS_AFTER - ROWS_BEFORE)) -eq 1

# New row present with required phrases
grep -q "Launcher-form slash commands (not self-contained)" .planning/PROJECT.md
grep -q "loading persona from \`_gomad/gomad/agents/\*.md\`" .planning/PROJECT.md
grep -q "Contract set in Phase 5" .planning/PROJECT.md

# Evolution section still follows (new row did not overwrite the section header)
grep -q "^## Evolution" .planning/PROJECT.md
```

If any verification fails, revert and surface to the user.

**Do NOT:**
- Add more than one new row.
- Edit any other existing row.
- Change the table header or the separator row.
- Add a new section, subsection, or paragraph elsewhere.
  </action>
  <verify>
    <automated>grep -qF "Launcher-form slash commands (not self-contained)" .planning/PROJECT.md && grep -qF "\`_gomad/gomad/agents/*.md\`" .planning/PROJECT.md && grep -qF "Contract set in Phase 5" .planning/PROJECT.md && grep -q "^## Evolution" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - PROJECT.md contains exactly one line with literal `Launcher-form slash commands (not self-contained)` — `grep -cF "Launcher-form slash commands (not self-contained)" .planning/PROJECT.md` returns exactly 1
    - The new row references the launcher path — `grep -qF ".claude/commands/gm/agent-*.md" .planning/PROJECT.md`
    - The new row references the persona source path — `grep -qF "_gomad/gomad/agents/*.md" .planning/PROJECT.md`
    - The new row references SKILL.md as source of truth — `grep -qE "SKILL\.md.*single source of truth" .planning/PROJECT.md`
    - The new row references Phase 5 contract + Phase 6 extractor — `grep -qE "Contract set in Phase 5.*extractor lands in Phase 6" .planning/PROJECT.md`
    - `## Evolution` section header still exists AFTER the Key Decisions table — `grep -n "^## Evolution" .planning/PROJECT.md` returns one match, and the line number is strictly greater than the line number of the new `Launcher-form slash commands` row
    - Exactly one new table row added — `git diff --stat .planning/PROJECT.md` shows additions >= 1 and the specific new-row line is present in `git diff` output
    - Existing `E2E test verifies tarball structurally` row still exists and is unchanged — `grep -qF "E2E test verifies tarball structurally (not via interactive \`gomad install\`)" .planning/PROJECT.md`
    - No rows deleted from Key Decisions — `git diff .planning/PROJECT.md | grep "^-| " | grep -v "^---" | wc -l` returns 0
  </acceptance_criteria>
  <done>
    Key Decisions table has the new launcher-form row positioned immediately after the `E2E test verifies tarball structurally` row; row contains all required phrases; no other changes to the Key Decisions section; `## Evolution` section header unchanged and still follows the table.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs edit → repo commit | Edits to `.planning/PROJECT.md` become part of git history; no runtime effect, but affects downstream agents that read PROJECT.md as the project contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Information Disclosure | Key Decisions row content | accept | Contents are project architecture — intended to be public. No secrets. |
| T-05-03-02 | Tampering | Accidental over-edit (e.g., wiping Key Decisions table) | mitigate | Edit tool's old_string/new_string matching requires exact anchors; acceptance criteria verify existing rows preserved (count + specific row grep); `git diff` reviewed. |
| T-05-03-03 | Repudiation | Claim of what PROJECT.md said before D-13 correction | mitigate | Git history retains the pre-edit `type: module` text; commit message will reference D-13 for traceability. |
| T-05-03-04 | Tampering | Mis-replace `type: module` in an unintended third location if pattern grows | mitigate | Action step 1 explicitly counts `grep -c "type: module"` as 2 before proceeding; halts if count differs. |
</threat_model>

<verification>
1. `grep -c "type: module" .planning/PROJECT.md` returns 0.
2. `grep -cF 'CommonJS, \`require()\`-based loading' .planning/PROJECT.md` returns 2.
3. `grep -cF "Launcher-form slash commands (not self-contained)" .planning/PROJECT.md` returns 1.
4. `## Evolution` section still present and positioned after the Key Decisions table.
5. `git diff .planning/PROJECT.md` shows only the 2 replacements + 1 appended row; no unrelated edits.
</verification>

<success_criteria>
- REL-01 closed: PROJECT.md no longer contains the `type: module` factual error.
- ROADMAP SC#2 launcher-decision-logged half closed: Key Decisions table now has a formal row committing the launcher-form architecture per D-08.
- ROADMAP SC#4 closed: PROJECT.md language corrected to CommonJS with `require()`-based loading, consistent with repo reality.
- Zero unrelated edits; markdown structure preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/05-foundations-command-surface-validation/05-03-SUMMARY.md`. Include:
- Truths verified (5)
- Artifact modified: `.planning/PROJECT.md` (2 in-place corrections + 1 appended Key Decisions row)
- Key links established (3)
- Requirements closed: REL-01
- Partial closure: ROADMAP Phase 5 SC#2 (launcher decision logged in PROJECT.md — the D-05/D-06/D-07 contract detail lands in Phase 6)
- Git diff summary captured for audit trail
</output>
