---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - PKG-01
  - PKG-02
  - PKG-03
  - PKG-04
  - PKG-05

must_haves:
  truths:
    - "npm pkg get name returns @xgent-ai/gomad"
    - "npm pkg get version returns 1.1.0"
    - "npm pkg get bin shows gomad binary pointing to tools/installer/bmad-cli.js"
    - "npm scripts include gomad:install, gomad:uninstall, install:gomad and do NOT include bmad:install, bmad:uninstall, install:bmad, or rebundle"
    - "publishConfig.access is public"
    - "xml2js, @kayvan/markdown-tree-parser, and ignore are NOT in dependencies"
  artifacts:
    - path: "package.json"
      provides: "GoMad package identity and clean dependency list"
      contains: "@xgent-ai/gomad"
  key_links:
    - from: "package.json bin.gomad"
      to: "tools/installer/bmad-cli.js"
      via: "bin field path"
      pattern: "\"gomad\": \"tools/installer/bmad-cli.js\""
---

<objective>
Update package.json to establish GoMad package identity and remove unused dependencies.

Purpose: Transform the package from `bmad-method@6.2.2` to `@xgent-ai/gomad@1.1.0` with correct metadata, renamed scripts, and a clean dependency list. This is the foundational identity change that all subsequent phases build on.

Output: A package.json that declares itself as GoMad with no dead dependencies.
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update package.json identity, bin, scripts</name>
  <files>package.json</files>
  <read_first>
    - package.json (current state of all fields being modified)
    - .planning/phases/01-foundation/01-CONTEXT.md (locked decisions D-01 through D-11, including D-08 REVISED 2026-04-08 and D-09 REVISED 2026-04-08)
  </read_first>
  <action>
Edit package.json to set the following fields (per D-01 through D-11):

**Identity fields:**
- `"name": "@xgent-ai/gomad"` (per D-01)
- `"version": "1.1.0"` (per D-01)
- `"description": "GOMAD Orchestration Method for Agile Development"` (per D-02)
- `"keywords": ["gomad", "agile", "ai", "agents", "orchestrator", "development", "methodology"]` (per D-03 -- note "gomad" first, "bmad" removed)
- `"repository": { "type": "git", "url": "git+https://github.com/xgent-ai/gomad.git" }` (per D-04)
- `"author": "Rockie Guo <rockie@kitmi.com.au>"` (per D-05)
- `"homepage": "https://gomad.xgent.ai"` (per D-06)
- `"bugs": { "url": "https://github.com/xgent-ai/gomad/issues" }` (per D-06)

**Binary and main:**
- `"bin": { "gomad": "tools/installer/bmad-cli.js" }` (per **D-08 REVISED 2026-04-08**: the `gomad` binary name is set now, but the file path stays as `bmad-cli.js` in Phase 1. Phase 2 FS-04 will atomically rename the file AND update this path to `tools/installer/gomad-cli.js`. Both `bmad` and `bmad-method` aliases are removed entirely.)
- `"main": "tools/installer/bmad-cli.js"` (per **D-09 REVISED 2026-04-08**: same staged transition rationale as D-08 — Phase 2 updates path atomically with the file rename.)

**Scripts:**
- Rename `"bmad:install"` to `"gomad:install"` with value `"node tools/installer/bmad-cli.js install"` (per D-10; path stays on bmad-cli.js in Phase 1 per D-08 REVISED)
- Rename `"bmad:uninstall"` to `"gomad:uninstall"` with value `"node tools/installer/bmad-cli.js uninstall"` (per D-10)
- Rename `"install:bmad"` to `"install:gomad"` with value `"node tools/installer/bmad-cli.js install"` (per D-10)
- DELETE the `"rebundle"` script entirely (per D-11; references non-existent `tools/installer/bundlers/bundle-web.js`)

**Verify preserved:** `"publishConfig": { "access": "public" }` must remain (per D-07).

After editing, run `npx prettier --write package.json` to normalize field ordering via `prettier-plugin-packagejson`.
  </action>
  <verify>
    <automated>cd /Users/rockie/Documents/GitHub/xgent/gomad && [ "$(npm pkg get name)" = '"@xgent-ai/gomad"' ] && [ "$(npm pkg get version)" = '"1.1.0"' ] && [ "$(npm pkg get description)" = '"GOMAD Orchestration Method for Agile Development"' ] && [ "$(npm pkg get author)" = '"Rockie Guo <rockie@kitmi.com.au>"' ] && [ "$(npm pkg get homepage)" = '"https://gomad.xgent.ai"' ] && [ "$(npm pkg get repository.url)" = '"git+https://github.com/xgent-ai/gomad.git"' ] && [ "$(npm pkg get bugs.url)" = '"https://github.com/xgent-ai/gomad/issues"' ] && npm pkg get keywords | grep -q gomad && npm pkg get bin | grep -q 'gomad' && npm pkg get bin | grep -q 'bmad-cli.js' && ! npm pkg get bin | grep -q 'bmad-method' && [ "$(npm pkg get publishConfig.access)" = '"public"' ] && npm pkg get scripts | grep -q 'gomad:install' && npm pkg get scripts | grep -q 'gomad:uninstall' && npm pkg get scripts | grep -q 'install:gomad' && ! npm pkg get scripts | grep -q 'bmad:install' && ! npm pkg get scripts | grep -q 'bmad:uninstall' && ! npm pkg get scripts | grep -q 'install:bmad' && ! npm pkg get scripts | grep -q 'rebundle' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `npm pkg get name` outputs `"@xgent-ai/gomad"`
    - `npm pkg get version` outputs `"1.1.0"`
    - `npm pkg get description` outputs `"GOMAD Orchestration Method for Agile Development"`
    - `npm pkg get author` outputs `"Rockie Guo <rockie@kitmi.com.au>"`
    - `npm pkg get bin` contains `"gomad": "tools/installer/bmad-cli.js"` and does NOT contain `bmad-method`
    - `npm pkg get scripts` contains `gomad:install`, `gomad:uninstall`, `install:gomad`
    - `npm pkg get scripts` does NOT contain `bmad:install`, `bmad:uninstall`, `install:bmad`, or `rebundle`
    - `npm pkg get publishConfig.access` outputs `"public"`
    - `npm pkg get homepage` outputs `"https://gomad.xgent.ai"`
    - `npm pkg get repository.url` outputs `"git+https://github.com/xgent-ai/gomad.git"`
    - `npm pkg get bugs.url` outputs `"https://github.com/xgent-ai/gomad/issues"`
  </acceptance_criteria>
  <done>package.json declares @xgent-ai/gomad@1.1.0 with correct metadata, gomad binary, renamed scripts, no rebundle, and publishConfig.access preserved</done>
</task>

<task type="auto">
  <name>Task 2: Remove unused production dependencies</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (verify current dependency list after Task 1 edits)
    - .planning/phases/01-foundation/01-RESEARCH.md (Dependency Audit Findings section -- lists xml2js, @kayvan/markdown-tree-parser, ignore as unused)
  </read_first>
  <action>
Remove three unused production dependencies identified by research (per D-12, D-13):

1. `npm uninstall xml2js` -- was for legacy XML import; no imports found in source or tools
2. `npm uninstall @kayvan/markdown-tree-parser` -- likely used by removed builder features; no imports found
3. `npm uninstall ignore` -- likely used by bundler/builder features; no imports found (only JS keyword `ignore` appears)

These commands will update both `package.json` (remove from `dependencies`) and `package-lock.json` (remove lockfile entries).

After removal, run `npx prettier --write package.json` to normalize formatting.

IMPORTANT: Do NOT remove any devDependencies -- research confirmed all devDeps are actively used by build/lint/test/docs pipeline.
  </action>
  <verify>
    <automated>cd /Users/rockie/Documents/GitHub/xgent/gomad && node -e "const d=require('./package.json').dependencies||{};process.exit(d.xml2js||d['@kayvan/markdown-tree-parser']||d.ignore?1:0)" && node -e "const d=require('./package.json').dependencies||{};process.exit(d.chalk&&d.commander&&d['fs-extra']&&d.glob&&d['js-yaml']&&d.yaml&&d.semver?0:1)" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - package.json `dependencies` does NOT contain `xml2js`
    - package.json `dependencies` does NOT contain `@kayvan/markdown-tree-parser`
    - package.json `dependencies` does NOT contain `ignore`
    - package.json `dependencies` DOES contain `chalk`, `commander`, `csv-parse`, `fs-extra`, `glob`, `js-yaml`, `yaml`, `semver`, `picocolors`, `@clack/core`, `@clack/prompts` (all verified as used)
    - package-lock.json does not reference the three removed packages as direct dependencies
  </acceptance_criteria>
  <done>Three unused production dependencies removed; all used dependencies preserved; lockfile updated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json -> npm registry | Package metadata determines what npm publishes and how users discover/install it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | package.json name/version | mitigate | Verify `npm pkg get name` and `npm pkg get version` return exact expected values after edit; commit reviewed before push |
| T-01-02 | Information Disclosure | package.json | accept | No secrets in package.json; only public metadata fields are changed |
| T-01-03 | Tampering | Supply chain -- scoped package name | mitigate | `@xgent-ai/gomad` scope is owned by the user; `publishConfig.access: "public"` verified preserved; npm 2FA required for publish (Phase 4 concern) |
| T-01-04 | Denial of Service | Removing used dependency | mitigate | Each removed dep verified via codebase grep to have zero imports; `npm run test:install` validates no runtime require errors |
</threat_model>

<verification>
After both tasks complete:
1. `npm pkg get name` returns `@xgent-ai/gomad`
2. `npm pkg get version` returns `1.1.0`
3. `npm pkg get bin` shows `gomad` pointing to `tools/installer/bmad-cli.js` (per D-08 REVISED; path updated to gomad-cli.js atomically in Phase 2)
4. `npm pkg get scripts` has gomad-prefixed scripts, no bmad-prefixed scripts, no rebundle
5. `node -e "const d=require('./package.json').dependencies||{};process.exit(d.xml2js||d['@kayvan/markdown-tree-parser']||d.ignore?1:0)"` exits 0
6. `node tools/installer/bmad-cli.js install --help` runs without require errors (validates no removed dep was actually needed)
</verification>

<success_criteria>
- package.json fully reflects GoMad identity per all PKG requirements
- No unused production dependencies remain
- CLI still loads without errors (no accidental removal of used deps)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
