- Pre-existing prettier formatting issue in tools/installer/ide/templates/agent-command-template.md (unrelated to plan 12-01 — exists before plan changes; out of scope per Rule 3 scope boundary)
- Pre-existing test/test-cleanup-planner.js failures (4 assertions: "LEGACY_AGENT_SHORT_NAMES: has exactly 7 entries", expected order, "legacy: 7 snapshots", "legacy: 7 removals") — solo-dev was registered as the 8th canonical agent (commit 8c754b1, 2026-04-24) but test expectations were not updated. Pre-existing failures verified via `git stash && npm run test:cleanup-all` before plan 12-02 edits — out of scope per Rule 3 scope boundary; logged for future test fix.
