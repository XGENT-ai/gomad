---
phase: 11-docs-site-content-authoring
plan: 10
type: execute
wave: 6
gap_closure: true
depends_on:
  - 11-08
  - 11-09
files_modified:
  - .planning/phases/11-docs-site-content-authoring/11-10-SUMMARY.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04
  - DOCS-05
  - DOCS-06

must_haves:
  truths:
    - "Full `npm run docs:build` exits 0 end-to-end after Plans 11-08 (tooling fixes) and 11-09 (zh-cn upgrade-recovery translation) land. All 6 stages execute (platform check + banner → injectReferenceTables → checkDocLinks → cleanBuildDirectory → generateArtifacts → buildAstroSite); no stage aborts."
    - "Re-running `npm run docs:build` produces zero git diff under `docs/reference/` + `docs/zh-cn/reference/` (end-to-end idempotency — proves the injector + Astro build are both byte-stable post-gap-closure)."
    - "`build/site/index.html` exists and was emitted from the post-gap-closure working tree (mtime newer than 11-07 integration-gate run; build/site reflects all 14 Phase 11 page targets, not the stale 2026-04-25 artifact)."
    - "`build/site/llms.txt` exists and contains the 6 corrected v1.3 page URLs from Plan 11-08 Task 2 — `tutorials/install/`, `tutorials/quick-start/`, `explanation/architecture/`, `reference/agents/`, `reference/skills/`, `how-to/contributing/` — with NO BMAD-era URLs (`tutorials/getting-started/`, `reference/modules/`, etc.) and the corrected `https://github.com/xgent-ai/gomad` REPO_URL."
    - "Phase 11 success criterion 1 — visitor can read tutorials at `gomad.xgent.ai/tutorials/install` and `/tutorials/quick-start` via the auto-deploy pipeline — is mechanically verified at the integration level. The same `npm run docs:build` that the GH Actions docs.yaml workflow runs on push to main now exits 0."
  artifacts:
    - path: ".planning/phases/11-docs-site-content-authoring/11-10-SUMMARY.md"
      provides: "Final integration-gate record post-gap-closure — confirms Phase 11 DOCS-01..06 are end-to-end verified"
      contains: "npm run docs:build"
  key_links:
    - from: "Plans 11-08 + 11-09 outputs"
      to: "Full docs:build pipeline"
      via: "execSync chain — injectReferenceTables → checkDocLinks → cleanBuildDirectory → generateArtifacts → buildAstroSite"
      pattern: "Build complete"
    - from: "tools/build-docs.mjs Plan 11-08 Task 2 patches"
      to: "build/site/llms.txt"
      via: "generateLlmsTxt() emission"
      pattern: "tutorials/install/|tutorials/quick-start/|explanation/architecture/|reference/agents/|reference/skills/|how-to/contributing/"
---

<objective>
Re-run the full `npm run docs:build` end-to-end as the FINAL post-gap-closure integration gate. After Plans 11-08 (tooling fixes for Gap A + WR-01 + WR-02) and 11-09 (Gap B zh-cn upgrade-recovery translation) land, this plan asserts:

1. The link-check stage now passes (Gap A + Gap B both closed).
2. The full pipeline runs to completion: injector → link-check → cleanup → artifact generation → Astro build.
3. End-to-end idempotency holds (re-run produces zero diff under reference pages).
4. The emitted `build/site/llms.txt` contains the 6 v1.3 URLs from Plan 11-08 Task 2 (WR-01 closed) and the corrected REPO_URL (WR-02 closed).
5. `build/site/index.html` is fresh (post-gap-closure mtime, not the stale 2026-04-25 artifact).
6. Phase 11 DOCS-01..06 are end-to-end verified at the integration level.

Purpose: Plan 11-07 was the original integration gate but failed (link-check exit 1 with 5 issues, pipeline aborted before stages 4-6). Plans 11-08 and 11-09 close the 5 link-check failures and the 2 bundled review findings (WR-01 stale BMAD URLs, WR-02 wrong REPO_URL). This plan re-runs the same gate to confirm the closure in the integrated state — the equivalent of `/gsd-verify-phase 11` re-verification but mechanized at the build-pipeline level.

Why a separate plan from Plan 11-07: Plan 11-07 has its own SUMMARY documenting the failure surface and it's the wave-4 baseline integration gate; rerunning it in place would conflate "first integration attempt that surfaced gaps" with "post-gap-closure verification." A separate Plan 11-10 with its own SUMMARY makes the audit trail clean and keeps Plan 11-07 a faithful record of the original gate failure.

Why Wave 6 not Wave 5: Plans 11-08 and 11-09 are independent of each other (different files — `tools/` vs `docs/zh-cn/`) and run in parallel in Wave 5. Plan 11-10 depends on BOTH closure plans landing because the link-check stage will still fail if either gap remains. Wave 6 sequencing forces Plan 11-10 to wait until 11-08 and 11-09 are both committed.

Output: 1 SUMMARY artifact (`11-10-SUMMARY.md`). No source-tree modifications — this plan is read-only against `docs/`, `tools/`, `website/`, `package.json` except for writing the SUMMARY (mirrors Plan 11-07's posture).
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-docs-site-content-authoring/11-CONTEXT.md
@.planning/phases/11-docs-site-content-authoring/11-VERIFICATION.md
@.planning/phases/11-docs-site-content-authoring/11-07-SUMMARY.md
@.planning/phases/11-docs-site-content-authoring/11-08-SUMMARY.md
@.planning/phases/11-docs-site-content-authoring/11-09-SUMMARY.md
@tools/build-docs.mjs
@tools/validate-doc-links.js
@tools/inject-reference-tables.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run npm run docs:build end-to-end post-gap-closure and assert exit 0</name>
  <files>
    - .planning/phases/11-docs-site-content-authoring/11-10-SUMMARY.md (NEW — written by execute-plan via Output section)
  </files>
  <read_first>
    - tools/build-docs.mjs (full pipeline ordering: injectReferenceTables → checkDocLinks → cleanBuildDirectory → generateArtifacts → buildAstroSite; this is the file Plan 11-08 Task 2 patched — confirm REPO_URL = xgent-ai/gomad and v1.3 URLs in generateLlmsTxt)
    - tools/validate-doc-links.js (Plan 11-08 Task 1 patched — confirm URL-scheme guard present)
    - .planning/phases/11-docs-site-content-authoring/11-07-SUMMARY.md (the ORIGINAL gate failure surface — this plan re-asserts the same checks)
    - .planning/phases/11-docs-site-content-authoring/11-08-SUMMARY.md (must exist; documents Plan 11-08 closure of Gap A + WR-01 + WR-02)
    - .planning/phases/11-docs-site-content-authoring/11-09-SUMMARY.md (must exist; documents Plan 11-09 closure of Gap B)
    - All 14 Phase 11 page targets must still exist (post-gap-closure):
      - docs/index.md, docs/zh-cn/index.md (Plan 01)
      - docs/reference/agents.md, docs/reference/skills.md (Plan 03)
      - docs/zh-cn/reference/agents.md, docs/zh-cn/reference/skills.md (Plan 03)
      - docs/tutorials/install.md, docs/tutorials/quick-start.md (Plan 04)
      - docs/zh-cn/tutorials/install.md, docs/zh-cn/tutorials/quick-start.md (Plan 04)
      - docs/explanation/architecture.md, docs/zh-cn/explanation/architecture.md (Plan 05)
      - docs/how-to/contributing.md, docs/zh-cn/how-to/contributing.md (Plan 06)
      - PLUS the new docs/zh-cn/upgrade-recovery.md (Plan 11-09)
  </read_first>
  <action>
    Pre-flight: confirm closure plans landed and all page targets exist.

    1. Assert `.planning/phases/11-docs-site-content-authoring/11-08-SUMMARY.md` exists (proves Plan 11-08 ran).
    2. Assert `.planning/phases/11-docs-site-content-authoring/11-09-SUMMARY.md` exists (proves Plan 11-09 ran).
    3. Assert `docs/zh-cn/upgrade-recovery.md` exists (proves Plan 11-09 created the missing file).
    4. Re-grep tools/validate-doc-links.js for the URL-scheme guard introduced by Plan 11-08 Task 1.
    5. Re-grep tools/build-docs.mjs for the corrected REPO_URL (`xgent-ai/gomad`) and absence of `gomad-code-org`/`GOMAD-METHOD`.

    If any pre-flight check fails, FAIL FAST with a clear message naming which closure plan didn't land — do NOT attempt `npm run docs:build`.

    Then re-run the full integration gate:

    1. Run `rm -rf build/` to clear any stale artifact (the existing `build/site/index.html` is from 2026-04-25 per 11-VERIFICATION.md).
    2. Run `npm run docs:build` once. Capture stdout/stderr. Expected stages execute in order:
       - Platform check + banner.
       - `Injecting auto-generated reference tables` — `node tools/inject-reference-tables.cjs` succeeds; "✓ Reference tables injected".
       - `Checking documentation links` — `node tools/validate-doc-links.js` exits 0 (Gap A closed by Plan 11-08 URL-scheme guard; Gap B closed by Plan 11-09 zh-cn upgrade-recovery file).
       - `Cleaning previous build` — `build/` recreated.
       - `Generating LLM files` — `llms.txt` + `llms-full.txt` written to `build/artifacts/`.
       - `Building Astro + Starlight site` — `build/site/index.html` and the locale subtrees emitted.
    3. Assert `npm run docs:build` exits 0.
    4. Assert `build/site/index.html` exists.
    5. Assert `build/site/llms.txt` exists. Inspect content:
       - Contains all 6 v1.3 URL paths (substring matches): `tutorials/install/`, `tutorials/quick-start/`, `explanation/architecture/`, `reference/agents/`, `reference/skills/`, `how-to/contributing/`.
       - Does NOT contain BMAD-era paths: `tutorials/getting-started/`, `how-to/install-gomad/`, `explanation/quick-flow/`, `explanation/party-mode/`, `reference/workflow-map/`, `reference/modules/`, `BMM, BMB, BMGD`.
       - REPO_URL is `https://github.com/xgent-ai/gomad` (not `gomad-code-org/GOMAD-METHOD`).
    6. Assert `build/site/llms-full.txt` exists.
    7. Spot-check that select expected pages exist as static HTML under `build/site/`:
       - `build/site/index.html`
       - `build/site/tutorials/install/index.html`
       - `build/site/tutorials/quick-start/index.html`
       - `build/site/reference/agents/index.html`
       - `build/site/reference/skills/index.html`
       - `build/site/explanation/architecture/index.html`
       - `build/site/how-to/contributing/index.html`
       - `build/site/upgrade-recovery/index.html`
       - `build/site/zh-cn/index.html`
       - `build/site/zh-cn/upgrade-recovery/index.html` (NEW from Plan 11-09)

    8. Run `npm run docs:build` a SECOND time and assert exit 0 again.
    9. Assert `git diff --quiet docs/reference/ docs/zh-cn/reference/` succeeds (end-to-end idempotency — injector is byte-stable on second run; reference-table marker blocks unchanged across rebuilds).

    Do NOT modify any files under `docs/`, `tools/`, `website/`, or `package.json` in this plan. The integration gate is read-only against Wave 1-3 + closure plans except for writing the SUMMARY artifact and the build artifact under `build/` (which is `.gitignore`d and ephemeral).

    If `npm run docs:build` fails despite Plans 11-08 + 11-09 having landed, do NOT attempt to fix the failure inline. Surface the failure with the exact stage that broke and the offending diagnostic. The orchestrator will route the failure back through the planner for a follow-up plan rather than letting an integration gate silently patch authoring/tooling.

    Capture into `.planning/phases/11-docs-site-content-authoring/11-10-SUMMARY.md`:
    - Date of integration run.
    - `npm run docs:build` exit code (must be 0) for both runs.
    - Stage-by-stage success table (mirroring Plan 11-07 SUMMARY format).
    - Sample of expected build/site/ paths confirmed to exist (the 10 listed above).
    - llms.txt + llms-full.txt sizes (in chars, mirroring the format printed by generateLlmsTxt/generateLlmsFullTxt).
    - llms.txt URL audit: list the 6 v1.3 URLs found and confirm BMAD-era URLs absent.
    - Confirmation that DOCS-01..06 are now end-to-end verified by the wave-level gate.
    - Pointer to `11-VERIFICATION.md` for re-verification (recommend running `/gsd-verify-phase 11` after this plan lands to flip the verification state from `gaps_found` to `passed`).
    - Pointer to `11-VALIDATION.md` for any remaining manual-only verification items (page-reads-coherently checks, deployed-site reproducibility checks).
  </action>
  <verify>
    <automated>
      # Pre-flight — closure plans landed.
      [ -f .planning/phases/11-docs-site-content-authoring/11-08-SUMMARY.md ] && \
      [ -f .planning/phases/11-docs-site-content-authoring/11-09-SUMMARY.md ] && \
      # Pre-flight — Plan 11-09 created the missing zh-cn page.
      [ -f docs/zh-cn/upgrade-recovery.md ] && \
      # Pre-flight — Plan 11-08 Task 1 patch present in validate-doc-links.js.
      grep -q 'Skip absolute URLs' tools/validate-doc-links.js && \
      # Pre-flight — Plan 11-08 Task 2 patches present in build-docs.mjs.
      grep -q "const REPO_URL = 'https://github.com/xgent-ai/gomad';" tools/build-docs.mjs && \
      ! grep -q 'gomad-code-org' tools/build-docs.mjs && \
      ! grep -q 'GOMAD-METHOD' tools/build-docs.mjs && \
      grep -q 'tutorials/install/' tools/build-docs.mjs && \
      grep -q 'reference/agents/' tools/build-docs.mjs && \
      ! grep -q 'tutorials/getting-started/' tools/build-docs.mjs && \
      ! grep -q 'reference/modules/' tools/build-docs.mjs && \
      # Pre-flight — Wave 1-3 page targets all present.
      [ -f docs/index.md ] && \
      [ -f docs/zh-cn/index.md ] && \
      [ -f docs/reference/agents.md ] && \
      [ -f docs/reference/skills.md ] && \
      [ -f docs/zh-cn/reference/agents.md ] && \
      [ -f docs/zh-cn/reference/skills.md ] && \
      [ -f docs/tutorials/install.md ] && \
      [ -f docs/tutorials/quick-start.md ] && \
      [ -f docs/zh-cn/tutorials/install.md ] && \
      [ -f docs/zh-cn/tutorials/quick-start.md ] && \
      [ -f docs/explanation/architecture.md ] && \
      [ -f docs/zh-cn/explanation/architecture.md ] && \
      [ -f docs/how-to/contributing.md ] && \
      [ -f docs/zh-cn/how-to/contributing.md ] && \
      # Pre-flight — Plan 02 pipeline ordering still intact.
      grep -q 'function injectReferenceTables' tools/build-docs.mjs && \
      [ "$(grep -n 'injectReferenceTables();' tools/build-docs.mjs | head -1 | cut -d: -f1)" -lt "$(grep -n 'checkDocLinks();' tools/build-docs.mjs | head -1 | cut -d: -f1)" ] && \
      # Clear stale build artifact (build/site from 2026-04-25 per 11-VERIFICATION.md).
      rm -rf build/ && \
      # Full pipeline runs end-to-end (link-check + injector + Astro build green).
      npm run docs:build && \
      # First-run artifacts emitted.
      [ -f build/site/index.html ] && \
      [ -f build/site/llms.txt ] && \
      [ -f build/site/llms-full.txt ] && \
      # llms.txt content audit — v1.3 URLs present (WR-01 closed).
      grep -q 'tutorials/install/' build/site/llms.txt && \
      grep -q 'tutorials/quick-start/' build/site/llms.txt && \
      grep -q 'explanation/architecture/' build/site/llms.txt && \
      grep -q 'reference/agents/' build/site/llms.txt && \
      grep -q 'reference/skills/' build/site/llms.txt && \
      grep -q 'how-to/contributing/' build/site/llms.txt && \
      # llms.txt content audit — BMAD-era URLs absent (WR-01 closed).
      ! grep -q 'tutorials/getting-started/' build/site/llms.txt && \
      ! grep -q 'how-to/install-gomad/' build/site/llms.txt && \
      ! grep -q 'reference/modules/' build/site/llms.txt && \
      ! grep -q 'reference/workflow-map/' build/site/llms.txt && \
      # llms.txt content audit — REPO_URL corrected (WR-02 closed).
      grep -q 'https://github.com/xgent-ai/gomad' build/site/llms.txt && \
      ! grep -q 'gomad-code-org' build/site/llms.txt && \
      ! grep -q 'GOMAD-METHOD' build/site/llms.txt && \
      # Sample of expected static HTML pages — EN.
      [ -f build/site/tutorials/install/index.html ] && \
      [ -f build/site/tutorials/quick-start/index.html ] && \
      [ -f build/site/reference/agents/index.html ] && \
      [ -f build/site/reference/skills/index.html ] && \
      [ -f build/site/explanation/architecture/index.html ] && \
      [ -f build/site/how-to/contributing/index.html ] && \
      [ -f build/site/upgrade-recovery/index.html ] && \
      # Sample of expected static HTML pages — zh-cn.
      [ -f build/site/zh-cn/index.html ] && \
      [ -f build/site/zh-cn/upgrade-recovery/index.html ] && \
      # End-to-end idempotency: re-run produces zero diff under reference/ pages.
      npm run docs:build && \
      git diff --quiet docs/reference/ docs/zh-cn/reference/ && \
      # Final: no Phase 11 source files modified by this plan (only build/ + the SUMMARY).
      git diff --quiet docs/ tools/ website/ package.json && \
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - Pre-flight: 11-08-SUMMARY.md and 11-09-SUMMARY.md both exist (proves closure plans landed).
    - Pre-flight: `docs/zh-cn/upgrade-recovery.md` exists (Plan 11-09 created it).
    - Pre-flight: tools/validate-doc-links.js has the URL-scheme guard (Plan 11-08 Task 1 landed).
    - Pre-flight: tools/build-docs.mjs has corrected REPO_URL and v1.3 URL list, no BMAD-era leaks (Plan 11-08 Task 2 landed).
    - Pre-flight: All 14 original Wave 1-3 page targets exist.
    - Pre-flight: Plan 02 pipeline ordering intact (`injectReferenceTables` before `checkDocLinks`).
    - `rm -rf build/` runs without error.
    - `npm run docs:build` exits 0 on first run.
    - `build/site/index.html` exists post-build.
    - `build/site/llms.txt` exists; contains all 6 v1.3 URL paths (`tutorials/install/`, `tutorials/quick-start/`, `explanation/architecture/`, `reference/agents/`, `reference/skills/`, `how-to/contributing/`); contains the corrected `https://github.com/xgent-ai/gomad`; does NOT contain any of the 6 BMAD-era URL paths or the `gomad-code-org`/`GOMAD-METHOD` strings.
    - `build/site/llms-full.txt` exists.
    - 7 EN static HTML pages exist under `build/site/{tutorials,reference,explanation,how-to,upgrade-recovery}/<page>/index.html`.
    - 2 zh-cn static HTML pages spot-checked: `build/site/zh-cn/index.html` and `build/site/zh-cn/upgrade-recovery/index.html` (the latter is the Plan 11-09 output rendered).
    - `npm run docs:build` exits 0 on second run.
    - `git diff --quiet docs/reference/ docs/zh-cn/reference/` succeeds after the second run (idempotency).
    - `git diff --quiet docs/ tools/ website/ package.json` succeeds (no source-tree modifications by this plan).
    - `[ -f .planning/phases/11-docs-site-content-authoring/11-10-SUMMARY.md ]` succeeds with the integration record.
  </acceptance_criteria>
  <done>Full `npm run docs:build` exits 0 end-to-end post-gap-closure. The link-check, injector, Astro build, and LLM-flat artifact stages all run cleanly. `build/site/llms.txt` contains the correct v1.3 URLs and REPO_URL. Re-run idempotency confirmed. `11-10-SUMMARY.md` records the integration result. Phase 11 DOCS-01..06 are mechanically verified at the integration level — gaps from `11-VERIFICATION.md` are closed. Recommend running `/gsd-verify-phase 11` next to flip the verification state from `gaps_found` to `passed` and unblock Phase 12 release-gate dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Closure plans (11-08, 11-09) → integrated build | Both upstream gap-closure plans authored their files; this plan only invokes the integrated pipeline post-closure. |
| `build/site/llms.txt` content → Phase 12 release artifact | The corrected llms.txt content is the AI-facing artifact that ships to `gomad.xgent.ai/llms.txt` once Phase 11 merges to main. Wrong content here silently degrades AI integration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10-01 | Denial of Service | Closure plan landed but link-check still fails because of an unanticipated third gap | mitigate | Pre-flight grep checks confirm Plan 11-08 patches present in `tools/` and Plan 11-09 file exists in `docs/zh-cn/`. If `npm run docs:build` still fails, plan surfaces it as a gap (per Plan 11-07's pattern) for orchestrator routing rather than inline-patching. |
| T-11-10-02 | Tampering | Inline-fix temptation in this final integration plan | mitigate | Action explicitly forbids modifications to `docs/`, `tools/`, `website/`, `package.json`. `git diff --quiet` assertion at the end of `<verify>` proves no source-tree modifications. |
| T-11-10-03 | Information Disclosure | `build/site/llms.txt` ships with stale content because Plan 11-08 Task 2 was incomplete or partially applied | mitigate | Acceptance criteria audit `build/site/llms.txt` directly (not the source `tools/build-docs.mjs`). 6 positive grep checks (v1.3 URLs present) + 4 negative grep checks (BMAD URLs absent) + 2 REPO_URL checks (corrected + old absent). Catches any discrepancy between source patch and emitted artifact. |
| T-11-10-04 | Information Disclosure | `build/site/zh-cn/upgrade-recovery/index.html` not emitted because Plan 11-09 file has rendering issues (frontmatter typo, malformed markdown) | mitigate | Acceptance criterion includes `[ -f build/site/zh-cn/upgrade-recovery/index.html ]`. If Astro build renders the page, frontmatter is well-formed and content parses. |
| T-11-10-05 | Repudiation | Re-run idempotency check fails because gap-closure plans introduced injector non-determinism | mitigate | Acceptance criterion runs `npm run docs:build` TWICE and asserts `git diff --quiet docs/reference/ docs/zh-cn/reference/` succeeds. Plan 11-08 + 11-09 don't touch the injector or its inputs — Plan 11-09 adds `docs/zh-cn/upgrade-recovery.md` which does NOT contain AUTO markers, so the injector's persona/skill table generation is untouched. |
| T-11-10-06 | Spoofing | Stale 2026-04-25 `build/site/index.html` confuses the integration check (file exists but isn't from this run) | mitigate | Action explicitly runs `rm -rf build/` BEFORE the first `npm run docs:build`. The acceptance criterion's `[ -f build/site/index.html ]` check is on the freshly emitted artifact, not the stale one. |
</threat_model>

<verification>
- Pre-flight checks confirm Plans 11-08 + 11-09 landed (their SUMMARYs exist; their patches are present in source files).
- Stale `build/` directory cleared before run.
- `npm run docs:build` exits 0 on first run; emits `build/site/{index.html,llms.txt,llms-full.txt}` and 9 spot-checked static HTML pages.
- llms.txt content audit: 6 v1.3 URLs present, 4 BMAD-era URLs absent, REPO_URL corrected, old REPO_URL absent.
- `npm run docs:build` exits 0 on second run; `git diff --quiet docs/reference/ docs/zh-cn/reference/` succeeds (idempotency).
- `git diff --quiet docs/ tools/ website/ package.json` succeeds (this plan made no source modifications).
- 11-10-SUMMARY.md captures the integration result with stage-by-stage success and llms.txt URL audit.
</verification>

<success_criteria>
1. `npm run docs:build` exits 0 end-to-end on first run after Plans 11-08 + 11-09 land. All 6 stages run; pipeline does not abort.
2. Re-run idempotency: second `npm run docs:build` exits 0 with zero diff under `docs/reference/` + `docs/zh-cn/reference/`.
3. `build/site/index.html` exists; `build/site/llms.txt` and `build/site/llms-full.txt` exist; 7 EN page HTMLs and 2 zh-cn page HTMLs spot-checked under `build/site/`.
4. `build/site/llms.txt` content audit: contains 6 v1.3 URLs (`tutorials/install/`, `tutorials/quick-start/`, `explanation/architecture/`, `reference/agents/`, `reference/skills/`, `how-to/contributing/`), the corrected `https://github.com/xgent-ai/gomad` REPO_URL, and NO BMAD-era URLs or the `gomad-code-org`/`GOMAD-METHOD` strings.
5. `build/site/zh-cn/upgrade-recovery/index.html` exists (Plan 11-09 file rendered to static HTML through the Astro build).
6. No source modifications outside `.planning/phases/11-docs-site-content-authoring/11-10-SUMMARY.md`. `git diff --quiet docs/ tools/ website/ package.json` succeeds.
7. Phase 11 DOCS-01..06 are end-to-end verified; verifier should re-run `/gsd-verify-phase 11` to flip state from `gaps_found` to `passed`.
</success_criteria>

<output>
After completion, create `.planning/phases/11-docs-site-content-authoring/11-10-SUMMARY.md` documenting:
- Date of post-gap-closure integration run.
- `npm run docs:build` exit code (0) and stage-by-stage success table (6 stages all green) — mirror the Plan 11-07 SUMMARY's stage table format for direct comparison.
- Pre-flight check results (closure plans landed, page targets present, tooling patches in place).
- Sample of expected build/site/ paths confirmed (10 paths including the new zh-cn/upgrade-recovery rendering).
- llms.txt + llms-full.txt sizes (chars) and llms.txt URL audit (6 v1.3 URLs present; BMAD-era URLs absent; REPO_URL corrected).
- Re-run idempotency result (second `npm run docs:build` exit 0 + `git diff --quiet docs/reference/ docs/zh-cn/reference/` succeeds).
- Confirmation that Plans 11-08 (Gap A + WR-01 + WR-02) and 11-09 (Gap B) closure is reflected in the build artifact.
- Confirmation that DOCS-01..06 are now end-to-end verified by the wave-level gate.
- Recommendation: run `/gsd-verify-phase 11` next to flip phase verification state from `gaps_found` to `passed`.
- Pointer to `11-VALIDATION.md` for any remaining manual-only verification items (page-reads-coherently, reproducible install) that fall outside automated verification scope.
</output>
