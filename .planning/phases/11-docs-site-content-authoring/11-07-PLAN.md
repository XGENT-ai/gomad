---
phase: 11-docs-site-content-authoring
plan: 07
type: execute
wave: 4
depends_on:
  - 11-01
  - 11-02
  - 11-03
  - 11-04
  - 11-05
  - 11-06
files_modified:
  - .planning/phases/11-docs-site-content-authoring/11-07-SUMMARY.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04
  - DOCS-05
  - DOCS-06

must_haves:
  truths:
    - "After all Wave 1-3 plans land, `npm run docs:build` exits 0 — link-check passes against the now-authored cross-linked sibling pages, the injector populates all marker blocks, the Astro build emits the static site, and the LLM-flat artifact (llms-full.txt) generates without errors."
    - "Re-running `npm run docs:build` produces zero git diff under docs/reference/ + docs/zh-cn/reference/ (end-to-end idempotency — proves the injector + Astro build are both byte-stable)."
    - "All 6 DOCS-01..06 page sets exist and are linked from docs/index.md (forward references from Plan 01 now resolve)."
  artifacts:
    - path: ".planning/phases/11-docs-site-content-authoring/11-07-SUMMARY.md"
      provides: "Wave 4 integration summary — confirms full pipeline green + records build output for Phase 12 release plan reference"
      contains: "npm run docs:build"
  key_links:
    - from: "tools/build-docs.mjs"
      to: "All Wave 1-3 outputs"
      via: "execSync chain — injectReferenceTables → checkDocLinks → cleanBuildDirectory → generateArtifacts → buildAstroSite"
      pattern: "injectReferenceTables|checkDocLinks|buildAstroSite"
---

<objective>
Run the full `npm run docs:build` end-to-end as a wave-level integration gate. Asserts that all Wave 1-3 outputs (cleanup, injector + pipeline patch, 4 reference + 2 tutorial + 1 architecture + 1 contributing pages with their zh-cn siblings) compose correctly: link-check passes, the injector populates every marker block, the Astro build emits the static site, and the LLM-flat artifact generates.

Purpose: Wave 1-3 plans intentionally deferred `npm run docs:build` because cross-links between not-yet-authored sibling pages would break the link-check stage. This plan exists to close that integration gate exactly once, after every cross-linked page has landed.

Output: 1 summary artifact (`11-07-SUMMARY.md`) recording build output and any warnings.

Wave 4 sequencing: This plan is the LAST plan of Phase 11 and depends on every prior plan. It must run only after 11-01..11-06 all complete; the orchestrator's `--gaps` flag is NOT relevant here — this is part of the standard Phase 11 plan set.

Why a dedicated wave: Wave 1 (cleanup) rewrote `docs/index.md` with forward links to 6 Wave 3 pages. Wave 2 (injector) wired `injectReferenceTables` BEFORE `checkDocLinks` in `build-docs.mjs`. Wave 3 plans (03/04/05/06) authored cross-linked sibling pages in parallel. None of Waves 1-3 could run `npm run docs:build` themselves because the link-check stage would chase forward references to pages that hadn't yet shipped at that point. This Wave 4 plan asserts the integrated state — all pages present, all cross-links resolve, all markers populated.
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-docs-site-content-authoring/11-CONTEXT.md
@.planning/phases/11-docs-site-content-authoring/11-RESEARCH.md
@.planning/phases/11-docs-site-content-authoring/11-PATTERNS.md
@.planning/phases/11-docs-site-content-authoring/11-VALIDATION.md
@.planning/phases/11-docs-site-content-authoring/11-01-SUMMARY.md
@.planning/phases/11-docs-site-content-authoring/11-02-SUMMARY.md
@.planning/phases/11-docs-site-content-authoring/11-03-SUMMARY.md
@.planning/phases/11-docs-site-content-authoring/11-04-SUMMARY.md
@.planning/phases/11-docs-site-content-authoring/11-05-SUMMARY.md
@.planning/phases/11-docs-site-content-authoring/11-06-SUMMARY.md
@tools/build-docs.mjs
@tools/inject-reference-tables.cjs
@docs/index.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run npm run docs:build end-to-end and assert exit 0</name>
  <files>
    - .planning/phases/11-docs-site-content-authoring/11-07-SUMMARY.md (NEW — written by execute-plan via Output section)
  </files>
  <read_first>
    - tools/build-docs.mjs (full pipeline ordering: injectReferenceTables → checkDocLinks → cleanBuildDirectory → generateArtifacts → buildAstroSite per Plan 02 patch)
    - docs/index.md (Plan 01 rewrite — forward refs to all 6 Wave 3 pages)
    - All 6 page targets must exist before this task runs:
      - docs/reference/agents.md, docs/reference/skills.md (Plan 03)
      - docs/zh-cn/reference/agents.md, docs/zh-cn/reference/skills.md (Plan 03)
      - docs/tutorials/install.md, docs/tutorials/quick-start.md (Plan 04)
      - docs/zh-cn/tutorials/install.md, docs/zh-cn/tutorials/quick-start.md (Plan 04)
      - docs/explanation/architecture.md, docs/zh-cn/explanation/architecture.md (Plan 05)
      - docs/how-to/contributing.md, docs/zh-cn/how-to/contributing.md (Plan 06)
  </read_first>
  <action>
    Pre-flight check: confirm every page target from Wave 1-3 plans exists in the working tree. If any file is missing, FAIL FAST with a clear message naming which plan owns the missing page — do NOT attempt `npm run docs:build` because the link-check stage will produce noisier output than a direct file-existence assertion.

    Then run `npm run docs:build` once. Capture stdout/stderr. The expected stages and their relative ordering (per Plan 02's patch + the existing pipeline):
    1. Platform check + banner.
    2. `Injecting auto-generated reference tables` (Plan 02's `injectReferenceTables()` step) — runs `node tools/inject-reference-tables.cjs`. Expect "✓ Reference tables injected" line in output.
    3. `Checking documentation links` (`checkDocLinks()` step) — runs `node tools/validate-doc-links.js`. Expect zero broken links because all Wave 3 pages now exist.
    4. `Cleaning build directory`.
    5. `Generating LLM artifacts` (llms.txt + llms-full.txt for AI discovery).
    6. `Building Astro site` (`pnpm/npm exec astro build` or equivalent — emits static site to `build/site/`).

    Assert `npm run docs:build` exits 0.

    Then run `npm run docs:build` a SECOND time and assert no diff under `docs/reference/` + `docs/zh-cn/reference/`. This proves end-to-end idempotency (the injector + Astro build are both byte-stable; no churn-on-rebuild).

    Then capture the build artifact summary into `.planning/phases/11-docs-site-content-authoring/11-07-SUMMARY.md`:
    - Date of integration run.
    - `npm run docs:build` exit code (must be 0).
    - List of pages emitted under `build/site/` (sample 5-10 expected paths — `index.html`, `tutorials/install/index.html`, `reference/agents/index.html`, `zh-cn/index.html`, etc.).
    - llms.txt + llms-full.txt sizes.
    - Any warnings (e.g., Starlight slug collisions, image-asset path warnings) — note for later cleanup but not blocking.
    - Confirmation that DOCS-01..06 are now end-to-end verified.
    - Pointer to `11-VALIDATION.md` for any remaining manual-only verification items.

    Do NOT modify any files under `docs/`, `tools/`, `website/`, or `package.json` in this plan. The integration gate is read-only against Wave 1-3 outputs except for writing the SUMMARY artifact.

    If `npm run docs:build` fails, do NOT attempt to fix the failure inline. Surface the failure with the exact stage that broke (link-check, injector, Astro build) and the offending page/path. The orchestrator will route the failure back through the planner for a gap-closure plan rather than letting an integration gate silently patch authoring plans.
  </action>
  <verify>
    <automated>
      # Pre-flight: every Wave 1-3 page target exists.
      [ -f docs/index.md ] && \
      [ -f docs/zh-cn/index.md ] && \
      [ -f docs/reference/agents.md ] && \
      [ -f docs/reference/skills.md ] && \
      [ -f docs/zh-cn/reference/agents.md ] && \
      [ -f docs/zh-cn/reference/skills.md ] && \
      [ -f docs/tutorials/install.md ] && \
      [ -f docs/tutorials/quick-start.md ] && \
      [ -f docs/zh-cn/tutorials/install.md ] && \
      [ -f docs/zh-cn/tutorials/quick-start.md ] && \
      [ -f docs/explanation/architecture.md ] && \
      [ -f docs/zh-cn/explanation/architecture.md ] && \
      [ -f docs/how-to/contributing.md ] && \
      [ -f docs/zh-cn/how-to/contributing.md ] && \
      # Wave 1 cleanup is intact (no BMAD pages came back):
      [ "$(find docs/tutorials docs/how-to docs/reference docs/explanation -maxdepth 1 -name '*.md' ! -name 'install.md' ! -name 'quick-start.md' ! -name 'agents.md' ! -name 'skills.md' ! -name 'architecture.md' ! -name 'contributing.md' 2>/dev/null | wc -l | tr -d ' ')" = "0" ] && \
      # Plan 02 pipeline patch present:
      grep -q "function injectReferenceTables" tools/build-docs.mjs && \
      [ "$(grep -n 'injectReferenceTables();' tools/build-docs.mjs | head -1 | cut -d: -f1)" -lt "$(grep -n 'checkDocLinks();' tools/build-docs.mjs | head -1 | cut -d: -f1)" ] && \
      # Full pipeline runs end-to-end (link-check + injector + Astro build green):
      npm run docs:build && \
      # End-to-end idempotency: re-run produces zero diff under reference/ pages:
      npm run docs:build && \
      git diff --quiet docs/reference/ docs/zh-cn/reference/ && \
      # Build artifact emitted:
      [ -d build/site ] && \
      [ -f build/site/index.html ]
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - All 14 Wave 1-3 page targets exist (`docs/index.md` + 6 EN pages + 6 zh-cn pages + `docs/zh-cn/index.md`).
    - `npm run docs:build` exits 0 on first run.
    - `npm run docs:build` exits 0 on second run.
    - `git diff --quiet docs/reference/ docs/zh-cn/reference/` succeeds after the second run (end-to-end idempotency — no churn under reference pages).
    - `build/site/index.html` exists (Astro build emitted).
    - `[ -f .planning/phases/11-docs-site-content-authoring/11-07-SUMMARY.md ]` succeeds with the integration record.
    - No new files written under `docs/`, `tools/`, `website/`, or `package.json` (this plan is integration-only; if it tries to fix anything inline, that's a planner-level violation and must be surfaced as a gap).
  </acceptance_criteria>
  <done>Full `npm run docs:build` exits 0 end-to-end. Re-run idempotency confirmed. 11-07-SUMMARY.md records the integration result, build artifact paths, and any warnings. Phase 11 DOCS-01..06 closure is mechanically verified — the integrated state matches the per-plan promises.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wave 1-3 outputs → integrated build | All upstream plans authored their files; this plan only invokes the integrated pipeline. No new content boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07-01 | Denial of Service | Cross-link gap surfaces only at the integration gate | mitigate | Pre-flight file-existence check fails fast with a clear message naming the missing page's owning plan, so debug starts at the source rather than at the link-check error. Acceptance criterion enforces all 14 page targets exist before `npm run docs:build` runs. |
| T-11-07-02 | Tampering | Inline-fix temptation in the integration plan | mitigate | Action explicitly forbids modifications to `docs/`, `tools/`, `website/`, `package.json`. If the build fails, the failure surfaces as a gap to the orchestrator for routing through `/gsd-plan-phase --gaps`, not patched in this plan. Acceptance criterion asserts no new file writes outside the SUMMARY. |
| T-11-07-03 | Information Disclosure | Spurious diff after re-run (non-idempotent build emits churn) | mitigate | End-to-end idempotency assertion (`npm run docs:build` twice + `git diff --quiet docs/reference/ docs/zh-cn/reference/`) catches injector or Astro non-determinism. If this fires, the failure routes back as a gap (likely against Plan 02's injector implementation). |
| T-11-07-04 | Repudiation | Wave 1-3 plans pass per-plan verify but fail at integration | accept | This is exactly why Wave 4 exists. The wave-level integration gate is the canonical check; per-plan verify blocks are intentionally narrower (idempotency + file content). Per VALIDATION.md, manual verification of "page reads coherently to a human" is also captured here for the closure check. |
</threat_model>

<verification>
- All 14 Wave 1-3 page targets exist in the working tree.
- `npm run docs:build` exits 0 end-to-end.
- Re-running `npm run docs:build` produces zero diff under `docs/reference/` + `docs/zh-cn/reference/`.
- `build/site/index.html` exists; LLM-flat artifacts (llms.txt + llms-full.txt) generated.
- No file modifications outside `.planning/phases/11-docs-site-content-authoring/11-07-SUMMARY.md`.
</verification>

<success_criteria>
1. `npm run docs:build` exits 0 (full pipeline: injector → link-check → Astro build → LLM-flat artifacts).
2. Re-run idempotency: second `npm run docs:build` produces zero diff under `docs/reference/` + `docs/zh-cn/reference/`.
3. `build/site/index.html` exists; sample of expected output paths verified.
4. `11-07-SUMMARY.md` records the integration result (exit code, sample emitted paths, llms-full.txt size, any warnings, DOCS-01..06 closure).
5. No file modifications outside the SUMMARY artifact.
</success_criteria>

<output>
After completion, create `.planning/phases/11-docs-site-content-authoring/11-07-SUMMARY.md` documenting:
- Date of integration run.
- `npm run docs:build` exit code and stage-by-stage success.
- List of expected build/site/ output paths confirmed (5-10 samples).
- llms.txt + llms-full.txt sizes (KB).
- Any warnings encountered (non-blocking; flag for milestone-close cleanup).
- Confirmation that DOCS-01..06 are now end-to-end verified by the wave-level gate.
- Pointer to `11-VALIDATION.md` for any remaining manual-only verification items (page-reads-coherently checks).
</output>
