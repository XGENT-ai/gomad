---
phase: 11-docs-site-content-authoring
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - docs/reference/agents.md
  - docs/reference/skills.md
  - docs/zh-cn/reference/agents.md
  - docs/zh-cn/reference/skills.md
autonomous: true
requirements:
  - DOCS-02
  - DOCS-03
  - DOCS-06

must_haves:
  truths:
    - "Visitor opening /reference/agents sees all 8 gm-agent-* personas listed with persona name, slash command (/gm:agent-*), phase, and purpose (DOCS-02)."
    - "Visitor opening /reference/skills sees skills cataloged with 5 sections — Analysis, Planning, Solutioning, Implementation, Core — each populated by the injector (DOCS-03)."
    - "Each EN page has a zh-cn sibling at docs/zh-cn/reference/{agents,skills}.md authored in the same plan, EN-first per page per D-13 (DOCS-06)."
    - "All path references use the v1.3 layout `<installRoot>/_config/agents/<shortName>.md` — zero v1.2 path leaks (D-10)."
    - "After running the injector, the rendered tables between markers contain exactly 8 persona rows + the 5 skill sections; re-running the injector twice produces zero git diff under docs/reference/ + docs/zh-cn/reference/ (idempotency proved without invoking the full docs:build pipeline; full build is Wave 4 plan 11-07's responsibility)."
  artifacts:
    - path: "docs/reference/agents.md"
      provides: "Agents reference EN — hand-authored intro + AUTO:agents-table-* markers"
      contains: "<!-- AUTO:agents-table-start -->"
    - path: "docs/reference/skills.md"
      provides: "Skills reference EN — 5 sections with AUTO:skills-table-{phase}-* marker pairs"
      contains: "<!-- AUTO:skills-table-analysis-start -->"
    - path: "docs/zh-cn/reference/agents.md"
      provides: "Agents reference zh-cn — translated prose, same markers (English data per D-14)"
      contains: "<!-- AUTO:agents-table-start -->"
    - path: "docs/zh-cn/reference/skills.md"
      provides: "Skills reference zh-cn — translated prose, same 5-section markers"
      contains: "<!-- AUTO:skills-table-analysis-start -->"
  key_links:
    - from: "docs/reference/agents.md"
      to: "tools/inject-reference-tables.cjs (Plan 02)"
      via: "AUTO:agents-table-{start,end} markers populated at build time"
      pattern: "AUTO:agents-table-(start|end)"
    - from: "docs/reference/skills.md"
      to: "tools/inject-reference-tables.cjs"
      via: "five marker pairs — analysis/planning/solutioning/implementation/core"
      pattern: "AUTO:skills-table-(analysis|planning|solutioning|implementation|core)-(start|end)"
---

<objective>
Author the two reference pages (`docs/reference/agents.md` and `docs/reference/skills.md`) plus their zh-cn siblings. Each page contains hand-authored intro narrative + grouping commentary + the HTML comment markers that Plan 02's injector populates at build time. EN authored first then zh-cn (per D-13).

Purpose: Deliver DOCS-02 (Agents Reference) and DOCS-03 (Skills Reference) with the hybrid content model from D-05 — auto-gen tables for structural data, hand-authored prose for narrative. zh-cn parity for DOCS-06.

Output: 4 markdown pages (~80-120 lines each EN, similar zh-cn).

Wave 3: Depends on Plan 01 (which deletes the BMAD-era `docs/reference/agents.md` + `docs/reference/commands.md` etc., otherwise this plan can't write to those paths cleanly) AND Plan 02 (which ships the injector that populates the markers — without it, the rendered tables stay empty).

Wave 3 isolation note: Task 3 verify deliberately does NOT run `npm run docs:build`. The pages authored here cross-link to `../explanation/architecture.md` (Plan 05) and `../how-to/contributing.md` (Plan 06), which are parallel-wave siblings and may not exist when this plan runs. Plus, Plan 01's `docs/index.md` cross-links to all 6 Wave 3 pages — the link-check stage cannot be expected to pass within Wave 3. Task 3 asserts injector idempotency via direct `node tools/inject-reference-tables.cjs && git diff` rather than the `docs:build` pipeline. Wave 4 integration plan `11-07-PLAN.md` runs the full `npm run docs:build` once all Wave 3 plans complete.
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-docs-site-content-authoring/11-CONTEXT.md
@.planning/phases/11-docs-site-content-authoring/11-RESEARCH.md
@.planning/phases/11-docs-site-content-authoring/11-PATTERNS.md
@.planning/phases/11-docs-site-content-authoring/11-01-SUMMARY.md
@.planning/phases/11-docs-site-content-authoring/11-02-SUMMARY.md
@docs/_STYLE_GUIDE.md
@docs/upgrade-recovery.md
@tools/inject-reference-tables.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/reference/agents.md (EN) with AUTO:agents-table markers</name>
  <files>
    - docs/reference/agents.md (NEW)
  </files>
  <read_first>
    - docs/_STYLE_GUIDE.md (reference catalog template, header budget 8-12 `##`, no `####`, no horizontal rules outside frontmatter, admonition syntax `:::tip[]`/`:::note[]`/`:::caution[]`/`:::danger[]`)
    - docs/upgrade-recovery.md (shipped reference-style prose for tone match)
    - .planning/phases/11-docs-site-content-authoring/11-PATTERNS.md (lines 144-185 — exact frontmatter, intro paragraph, marker block format)
    - .planning/phases/11-docs-site-content-authoring/11-CONTEXT.md (D-05, D-06, D-07, D-09, D-10)
    - tools/inject-reference-tables.cjs (Plan 02 — note the marker name pattern: `AUTO:agents-table-start` / `AUTO:agents-table-end`)
  </read_first>
  <action>
    Create `docs/reference/agents.md`. Use this EXACT frontmatter:

    ```yaml
    ---
    title: "Agents Reference"
    description: All eight gm-agent-* personas with purpose, phase, and slash-command invocation.
    sidebar:
      order: 1
    ---
    ```

    Body MUST contain (in this exact order):

    1. Intro paragraph (no header) — 2-3 sentences explaining: GoMad ships eight agent personas spanning the four workflow phases (Analysis → Planning → Solutioning → Implementation); each persona is a launcher-form slash command that loads the persona body from `<installRoot>/_config/agents/<shortName>.md` at runtime. Mention runtime resolution (the persona body is loaded fresh on each invocation — not bundled into the slash command stub).

    2. `## All personas` — section header, then the marker block:
    ```markdown
    <!-- AUTO:agents-table-start -->
    <!-- This block is auto-generated from src/gomad-skills/*/gm-agent-*/SKILL.md.
         Do not edit between the markers — run `npm run docs:build` to regenerate. -->
    <!-- AUTO:agents-table-end -->
    ```
    Note: Plan 02's injector will replace content between the markers with the 4-column table `Persona | Slash command | Phase | Purpose`. Author leaves only the marker comments — the content fills in at build time.

    3. `## Choosing a persona` — 3-4 short paragraphs of hand-authored guidance:
       - When to use Analysis personas (Mary the Analyst, Paige the Tech Writer) — early discovery / requirements / docs.
       - When to use Planning personas (John the PM, Sally the UX Designer) — PRD + UX work.
       - When to use Solutioning personas (Winston the Architect) — system design before implementation.
       - When to use Implementation personas (Amelia the Dev, Bob the SM, Barry the Solo Dev) — sprint mechanics + coding. Specifically call out Barry as the "8th persona" (a single-developer-mode persona introduced post-fork).

    4. `## How invocation works` — 2-3 paragraphs on the launcher-form slash command contract:
       - Slash command shape: `/gm:agent-<shortName>` (e.g., `/gm:agent-pm`).
       - Stub location: `.claude/commands/gm/agent-<shortName>.md`.
       - Persona body location: `<installRoot>/_config/agents/<shortName>.md` (D-10).
       - Runtime behavior: stub loads body fresh on each invocation.
       - Cross-link in prose to `[GoMad architecture](../explanation/architecture.md)` for deeper detail.

    5. `## Customizing personas` — single paragraph + admonition:
       ```markdown
       :::note[User overrides]
       Persona files at `<installRoot>/_config/agents/*.md` are regenerated by `gomad install`.
       To customize a persona without losing changes on the next install, see the
       customization guide in `<installRoot>/_config/files-manifest.csv` and the
       `.customize.yaml` user-override semantics.
       :::
       ```

    Constraints:
    - Frontmatter is the FIRST content. Body starts with the intro paragraph (no header).
    - 4 `##` headers total (within budget 8-12).
    - 1 admonition (`:::note[User overrides]`).
    - No `####` headers anywhere.
    - No `---` horizontal rules outside frontmatter.
    - No "Related" / "Next:" sections.
    - DO NOT mention BMM/BMB/BMGD modules.
    - DO NOT use the v1.2 install path `<installRoot>/gomad/agents/` — every persona body path reference must be `<installRoot>/_config/agents/<shortName>.md` (D-10).
    - DO NOT use trigger codes (CP/VP/EP) — those were BMAD-era and are gone in gomad's `/gm:agent-*` slash-command surface (RESEARCH "Deprecated/outdated" line 636).
    - DO NOT hand-author any markdown table content between the AUTO markers; the injector populates them.
    - DO NOT reorder the marker comments or change their text — they must match the regex in `tools/inject-reference-tables.cjs` (`AUTO:agents-table-start` / `AUTO:agents-table-end` exactly).

    After writing, run `node tools/inject-reference-tables.cjs` once and confirm the file gets populated with the 8-row table between markers (the injector exits 0 only when persona count === 8).
  </action>
  <verify>
    <automated>
      [ -f docs/reference/agents.md ] && \
      grep -q '^title: "Agents Reference"' docs/reference/agents.md && \
      grep -q '^sidebar:' docs/reference/agents.md && \
      # Markers present (exact text, no whitespace drift):
      grep -F '<!-- AUTO:agents-table-start -->' docs/reference/agents.md && \
      grep -F '<!-- AUTO:agents-table-end -->' docs/reference/agents.md && \
      [ "$(grep -c '<!-- AUTO:agents-table-start -->' docs/reference/agents.md)" = "1" ] && \
      [ "$(grep -c '<!-- AUTO:agents-table-end -->' docs/reference/agents.md)" = "1" ] && \
      # 4 ## headers expected:
      [ "$(grep -c '^## ' docs/reference/agents.md)" = "4" ] && \
      # Required section headers:
      grep -q '^## All personas' docs/reference/agents.md && \
      grep -q '^## Choosing a persona' docs/reference/agents.md && \
      grep -q '^## How invocation works' docs/reference/agents.md && \
      grep -q '^## Customizing personas' docs/reference/agents.md && \
      # No #### headers, no horizontal rules outside frontmatter, no Next/Related:
      ! grep -E '^####' docs/reference/agents.md && \
      [ "$(grep -c '^---$' docs/reference/agents.md)" = "2" ] && \
      ! grep -E '^## (Related|Next:?)' docs/reference/agents.md && \
      # v1.3 path layout — no v1.2 leak:
      grep -F '<installRoot>/_config/agents/' docs/reference/agents.md && \
      ! grep -F '<installRoot>/gomad/agents/' docs/reference/agents.md && \
      # No deprecated BMAD module names:
      ! grep -wE '\bBMM\b|\bBMB\b|\bBMGD\b' docs/reference/agents.md && \
      # After running injector, the table is populated — exactly 8 persona rows + 1 header + 1 separator = 10 lines starting with '|':
      node tools/inject-reference-tables.cjs && \
      [ "$(awk '/<!-- AUTO:agents-table-start -->/{flag=1; next} /<!-- AUTO:agents-table-end -->/{flag=0} flag' docs/reference/agents.md | grep -c '^|')" = "10" ]
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - `[ -f docs/reference/agents.md ]` succeeds.
    - `grep -c '^## ' docs/reference/agents.md` outputs `4`.
    - `grep -c '<!-- AUTO:agents-table-start -->' docs/reference/agents.md` outputs `1`.
    - `grep -c '<!-- AUTO:agents-table-end -->' docs/reference/agents.md` outputs `1`.
    - `grep -F '<installRoot>/_config/agents/' docs/reference/agents.md` succeeds.
    - `! grep -F '<installRoot>/gomad/agents/' docs/reference/agents.md` succeeds (no v1.2 path).
    - `! grep -E '^####' docs/reference/agents.md` succeeds.
    - `! grep -wE '\bBMM\b|\bBMB\b|\bBMGD\b' docs/reference/agents.md` succeeds.
    - After `node tools/inject-reference-tables.cjs`: between the markers, exactly 10 lines start with `|` (header + separator + 8 persona rows).
    - Structural row-count assertion (durable against persona-name iteration per D-14): `[ "$(grep -c '/gm:agent-' docs/reference/agents.md)" = "8" ]` succeeds — 8 slash-command rows match the source-of-truth catalog. (Persona display names like Mary/Paige/John/Sally/Winston/Amelia/Bob/Barry are derived from `skill-manifest.yaml > displayName` and may iterate post-publish; the slash-command pattern `/gm:agent-<shortName>` is structural and stable.)
  </acceptance_criteria>
  <done>EN agents reference page authored with frontmatter, 4 `##` sections, 1 admonition, marker block in place, all path refs at v1.3 layout, no BMAD module names, no `####`. After Plan 02 injector runs, table populates with 8 slash-command rows (structural assertion; persona display names allowed to iterate per D-14).</done>
</task>

<task type="auto">
  <name>Task 2: Author docs/reference/skills.md (EN) with 5 AUTO:skills-table-{phase} marker pairs</name>
  <files>
    - docs/reference/skills.md (NEW)
  </files>
  <read_first>
    - docs/_STYLE_GUIDE.md (header budget, admonition syntax)
    - docs/reference/agents.md (Task 1 — uses the same hybrid auto-gen + hand-authored prose pattern)
    - .planning/phases/11-docs-site-content-authoring/11-PATTERNS.md (lines 188-220 — grouping pattern, marker naming per phase)
    - .planning/phases/11-docs-site-content-authoring/11-CONTEXT.md (D-08 grouping per src/gomad-skills/ structure; D-09 invocation column phrasing for task-skills vs personas)
    - tools/inject-reference-tables.cjs (Plan 02 — confirms marker names: `AUTO:skills-table-{analysis,planning,solutioning,implementation,core}-{start,end}`)
  </read_first>
  <action>
    Create `docs/reference/skills.md`. Use this EXACT frontmatter:

    ```yaml
    ---
    title: "Skills Reference"
    description: All GoMad skills cataloged by workflow phase — agent personas, task skills, and core skills.
    sidebar:
      order: 2
    ---
    ```

    Body MUST contain (in this exact order):

    1. Intro paragraph (no header) — 2-3 sentences: skills are atomic capabilities organized by the four workflow phases (Analysis, Planning, Solutioning, Implementation) plus a Core layer of cross-cutting utilities. Note that 8 of the catalog entries are agent personas (full slash-command launchers) — full details on `[Agents](./agents.md)`. Task skills and core skills are invoked via the persona that owns them, not directly (D-09).

    2. `## How to read this catalog` — single paragraph explaining the column shape used in each phase table, plus an admonition:
       ```markdown
       :::note[Source of truth]
       The tables below are auto-generated from `src/gomad-skills/*/SKILL.md` and
       `src/core-skills/*/SKILL.md` at build time. To add or rename a skill, edit
       the source SKILL.md and re-run `npm run docs:build`.
       :::
       ```

    3. `## 1. Analysis` — section header followed by the marker pair:
       ```markdown
       <!-- AUTO:skills-table-analysis-start -->
       <!-- AUTO:skills-table-analysis-end -->
       ```
       Then a 1-sentence prose follow-up: "Analysis-phase skills focus on early discovery, market research, and requirements gathering."

    4. `## 2. Planning` — same shape:
       ```markdown
       <!-- AUTO:skills-table-planning-start -->
       <!-- AUTO:skills-table-planning-end -->
       ```
       1-sentence follow-up: "Planning-phase skills produce PRDs, UX design specs, and validated planning artifacts."

    5. `## 3. Solutioning` — same shape with marker pair `AUTO:skills-table-solutioning-{start,end}`. 1-sentence follow-up: "Solutioning-phase skills cover architecture design, epic + story decomposition, and implementation-readiness checks."

    6. `## 4. Implementation` — same shape with marker pair `AUTO:skills-table-implementation-{start,end}`. 1-sentence follow-up: "Implementation-phase skills drive sprint mechanics — story creation, dev execution, code review, retrospectives, and the new domain knowledge framework."

    7. `## Core` — same shape with marker pair `AUTO:skills-table-core-{start,end}`. 1-sentence follow-up: "Core skills are cross-cutting utilities — review helpers, brainstorming, document sharding, and elicitation primitives — usable by any persona in any phase."

    Constraints:
    - 6 `##` headers total (within budget 8-12).
    - Exactly 5 marker pairs (one per phase + Core); marker names are EXACT — `AUTO:skills-table-analysis-start`, `AUTO:skills-table-analysis-end`, `AUTO:skills-table-planning-start`, etc.
    - 1 admonition (`:::note[Source of truth]`).
    - No `####` headers.
    - No `---` horizontal rules outside frontmatter.
    - No "Related" / "Next:" sections.
    - Cross-link to `./agents.md` in the intro paragraph.
    - Do NOT hand-author any table content between markers.
    - DO NOT mention BMM/BMB/BMGD; DO NOT use v1.2 install paths.

    After writing, run `node tools/inject-reference-tables.cjs` once and confirm all 5 marker blocks get populated.
  </action>
  <verify>
    <automated>
      [ -f docs/reference/skills.md ] && \
      grep -q '^title: "Skills Reference"' docs/reference/skills.md && \
      # 6 ## headers (How to read + 4 phases + Core):
      [ "$(grep -c '^## ' docs/reference/skills.md)" = "6" ] && \
      grep -q '^## How to read this catalog' docs/reference/skills.md && \
      grep -q '^## 1. Analysis' docs/reference/skills.md && \
      grep -q '^## 2. Planning' docs/reference/skills.md && \
      grep -q '^## 3. Solutioning' docs/reference/skills.md && \
      grep -q '^## 4. Implementation' docs/reference/skills.md && \
      grep -q '^## Core' docs/reference/skills.md && \
      # 5 start markers + 5 end markers, exact names:
      [ "$(grep -c '<!-- AUTO:skills-table-analysis-start -->' docs/reference/skills.md)" = "1" ] && \
      [ "$(grep -c '<!-- AUTO:skills-table-analysis-end -->' docs/reference/skills.md)" = "1" ] && \
      [ "$(grep -c '<!-- AUTO:skills-table-planning-start -->' docs/reference/skills.md)" = "1" ] && \
      [ "$(grep -c '<!-- AUTO:skills-table-planning-end -->' docs/reference/skills.md)" = "1" ] && \
      [ "$(grep -c '<!-- AUTO:skills-table-solutioning-start -->' docs/reference/skills.md)" = "1" ] && \
      [ "$(grep -c '<!-- AUTO:skills-table-solutioning-end -->' docs/reference/skills.md)" = "1" ] && \
      [ "$(grep -c '<!-- AUTO:skills-table-implementation-start -->' docs/reference/skills.md)" = "1" ] && \
      [ "$(grep -c '<!-- AUTO:skills-table-implementation-end -->' docs/reference/skills.md)" = "1" ] && \
      [ "$(grep -c '<!-- AUTO:skills-table-core-start -->' docs/reference/skills.md)" = "1" ] && \
      [ "$(grep -c '<!-- AUTO:skills-table-core-end -->' docs/reference/skills.md)" = "1" ] && \
      # No #### or stray horizontal rules:
      ! grep -E '^####' docs/reference/skills.md && \
      [ "$(grep -c '^---$' docs/reference/skills.md)" = "2" ] && \
      # Cross-link to ./agents.md present:
      grep -F './agents.md' docs/reference/skills.md && \
      # No v1.2 paths or BMAD modules:
      ! grep -F '<installRoot>/gomad/agents/' docs/reference/skills.md && \
      ! grep -wE '\bBMM\b|\bBMB\b|\bBMGD\b' docs/reference/skills.md && \
      # After injector, the implementation section gets a populated table (14 task-skills):
      node tools/inject-reference-tables.cjs && \
      # Implementation phase has 14 task-skills (per RESEARCH.md A4 + interfaces block in Plan 02);
      # table = 1 header + 1 separator + 14 rows = 16 lines starting with '|':
      [ "$(awk '/<!-- AUTO:skills-table-implementation-start -->/{flag=1; next} /<!-- AUTO:skills-table-implementation-end -->/{flag=0} flag' docs/reference/skills.md | grep -c '^|')" = "16" ] && \
      # Core section has 11 core-skills:
      [ "$(awk '/<!-- AUTO:skills-table-core-start -->/{flag=1; next} /<!-- AUTO:skills-table-core-end -->/{flag=0} flag' docs/reference/skills.md | grep -c '^|')" = "13" ]
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - `[ -f docs/reference/skills.md ]` succeeds.
    - `grep -c '^## ' docs/reference/skills.md` outputs `6`.
    - All 10 marker comments present exactly once each (5 start + 5 end pairs).
    - `grep -F './agents.md' docs/reference/skills.md` succeeds (cross-link to agents page).
    - `! grep -F '<installRoot>/gomad/agents/' docs/reference/skills.md` succeeds.
    - After running `node tools/inject-reference-tables.cjs`:
      - Analysis section: 6 task-skill rows + header + separator = 8 `|` lines.
      - Planning section: 4 + 2 = 6 `|` lines.
      - Solutioning section: 4 + 2 = 6 `|` lines.
      - Implementation section: 14 + 2 = 16 `|` lines.
      - Core section: 11 + 2 = 13 `|` lines.
  </acceptance_criteria>
  <done>EN skills reference page authored with 6 `##` sections, 5 marker pairs (one per phase + core), 1 admonition. After Plan 02 injector runs, all 5 sections populate with the correct task-skill / core-skill counts (28 task-skills + 11 core-skills = 39 entries split across 5 sections).</done>
</task>

<task type="auto">
  <name>Task 3: Translate to zh-cn — docs/zh-cn/reference/agents.md and skills.md (EN-first per D-13)</name>
  <files>
    - docs/zh-cn/reference/agents.md (NEW)
    - docs/zh-cn/reference/skills.md (NEW)
  </files>
  <read_first>
    - docs/reference/agents.md (Task 1 EN sibling — primary translation source; per D-13 EN MUST land first)
    - docs/reference/skills.md (Task 2 EN sibling — primary translation source)
    - .planning/phases/11-docs-site-content-authoring/11-CONTEXT.md (D-13, D-14 zh-cn drafting policy — Claude commits without pre-review; iterate-and-patch)
    - .planning/phases/11-docs-site-content-authoring/11-PATTERNS.md (lines 567-590 — zh-cn translation rules: translate frontmatter title/description, translate prose, KEEP code blocks/paths/marker text in English; AUTO marker block stays in English (D-14 acceptance — injector populates English data; future patch can localize))
  </read_first>
  <action>
    Translate the two EN reference pages to Simplified Chinese.

    For `docs/zh-cn/reference/agents.md`:
    - Frontmatter: translate `title:` (e.g., `title: "Agents 参考"` or `title: "代理参考"`) and `description:`. Keep `sidebar.order: 1` identical (so localized sidebar matches EN order).
    - Translate every `##` section header to Chinese (e.g., `## All personas` → `## 所有 Agent`).
    - Translate every prose paragraph to Chinese.
    - DO NOT translate:
      - Code blocks (shell commands, paths like `<installRoot>/_config/agents/<shortName>.md`, slash commands like `/gm:agent-pm`).
      - The marker comments `<!-- AUTO:agents-table-start -->` / `<!-- AUTO:agents-table-end -->` (must match injector regex exactly).
      - File paths (`.claude/commands/gm/agent-*.md`).
      - Persona file names / `gm-agent-*` slugs.
      - npm package names.
    - Cross-link `[GoMad architecture](../explanation/architecture.md)` becomes `[GoMad 架构](../explanation/architecture.md)` (translate label, keep relative path).
    - Translate the admonition block content (`:::note[User overrides]` → `:::note[用户覆盖]`).
    - Per D-14: the injector populates the table with English persona data (Mary/John/Winston etc. as displayName). That's accepted; future patch can localize.

    For `docs/zh-cn/reference/skills.md`:
    - Same translation rules.
    - Section headers `## 1. Analysis` → `## 1. 分析阶段`, `## 2. Planning` → `## 2. 规划阶段`, etc.
    - All 10 marker comments unchanged (`AUTO:skills-table-{phase}-{start,end}`).
    - Cross-link `[Agents](./agents.md)` → `[Agents 参考](./agents.md)` (or similar — keep the relative path identical to EN).

    Constraints:
    - Same `##` header count as EN siblings (4 for agents, 6 for skills).
    - Same marker pair count and exact marker names.
    - No `####` headers (style guide is identical for zh-cn).
    - No `---` horizontal rules outside frontmatter.
    - No translation glossary lock per D-14 — terminology drift between pages is accepted; user is bilingual and will patch post-publish.

    After writing, run `node tools/inject-reference-tables.cjs` again and confirm zh-cn pages also get populated tables (the injector handles both EN + zh-cn paths in `main()` per Plan 02 implementation).
  </action>
  <verify>
    <automated>
      [ -f docs/zh-cn/reference/agents.md ] && \
      [ -f docs/zh-cn/reference/skills.md ] && \
      # zh-cn agents has 4 ## headers (mirrors EN):
      [ "$(grep -c '^## ' docs/zh-cn/reference/agents.md)" = "4" ] && \
      # zh-cn skills has 6 ## headers (mirrors EN):
      [ "$(grep -c '^## ' docs/zh-cn/reference/skills.md)" = "6" ] && \
      # Markers preserved exactly (still in English):
      grep -F '<!-- AUTO:agents-table-start -->' docs/zh-cn/reference/agents.md && \
      grep -F '<!-- AUTO:agents-table-end -->' docs/zh-cn/reference/agents.md && \
      grep -F '<!-- AUTO:skills-table-analysis-start -->' docs/zh-cn/reference/skills.md && \
      grep -F '<!-- AUTO:skills-table-implementation-start -->' docs/zh-cn/reference/skills.md && \
      grep -F '<!-- AUTO:skills-table-core-start -->' docs/zh-cn/reference/skills.md && \
      # Frontmatter title differs from EN (translated):
      ! grep -q '^title: "Agents Reference"' docs/zh-cn/reference/agents.md && \
      ! grep -q '^title: "Skills Reference"' docs/zh-cn/reference/skills.md && \
      # sidebar.order matches EN:
      grep -A1 '^sidebar:' docs/zh-cn/reference/agents.md | grep -q 'order: 1' && \
      grep -A1 '^sidebar:' docs/zh-cn/reference/skills.md | grep -q 'order: 2' && \
      # Code blocks and paths stay in English:
      grep -F '<installRoot>/_config/agents/' docs/zh-cn/reference/agents.md && \
      ! grep -F '<installRoot>/gomad/agents/' docs/zh-cn/reference/agents.md && \
      grep -F '/gm:agent-' docs/zh-cn/reference/agents.md && \
      # No #### or stray rules:
      ! grep -E '^####' docs/zh-cn/reference/agents.md && \
      ! grep -E '^####' docs/zh-cn/reference/skills.md && \
      [ "$(grep -c '^---$' docs/zh-cn/reference/agents.md)" = "2" ] && \
      [ "$(grep -c '^---$' docs/zh-cn/reference/skills.md)" = "2" ] && \
      # Body has CJK characters (proves translation happened, not copy of EN):
      grep -P '[\x{4e00}-\x{9fff}]' docs/zh-cn/reference/agents.md && \
      grep -P '[\x{4e00}-\x{9fff}]' docs/zh-cn/reference/skills.md && \
      # After injector, zh-cn tables also populated:
      node tools/inject-reference-tables.cjs && \
      [ "$(awk '/<!-- AUTO:agents-table-start -->/{flag=1; next} /<!-- AUTO:agents-table-end -->/{flag=0} flag' docs/zh-cn/reference/agents.md | grep -c '^|')" = "10" ] && \
      # Idempotency check: re-run injector, confirm no diff under docs/reference/ + docs/zh-cn/reference/.
      # This proves marker substitution is byte-stable WITHOUT invoking the full docs:build pipeline
      # (which would link-check Plan 05's architecture.md / Plan 06's contributing.md / Plan 01's index.md
      # forward-references that may not exist in this Wave 3 plan's execution slice).
      node tools/inject-reference-tables.cjs && \
      git diff --quiet docs/reference/agents.md docs/reference/skills.md docs/zh-cn/reference/agents.md docs/zh-cn/reference/skills.md
      echo OK
      # NOTE: `npm run docs:build` is INTENTIONALLY NOT run here. Wave 3 plans 03/04/05/06
      # cross-link to each other's siblings; the link-check stage cannot pass mid-wave.
      # Wave 4 plan 11-07-PLAN.md runs the full pipeline once all Wave 3 plans complete.
    </automated>
  </verify>
  <acceptance_criteria>
    - `[ -f docs/zh-cn/reference/agents.md ] && [ -f docs/zh-cn/reference/skills.md ]` succeeds.
    - `grep -c '^## ' docs/zh-cn/reference/agents.md` outputs `4` (mirrors EN structure).
    - `grep -c '^## ' docs/zh-cn/reference/skills.md` outputs `6`.
    - All marker pairs preserved (`grep -F '<!-- AUTO:' docs/zh-cn/reference/agents.md` finds 2 lines; `grep -F '<!-- AUTO:' docs/zh-cn/reference/skills.md` finds at least 10 lines for the 5 pairs).
    - `! grep -q '^title: "Agents Reference"' docs/zh-cn/reference/agents.md` succeeds (title translated).
    - `grep -P '[\x{4e00}-\x{9fff}]' docs/zh-cn/reference/agents.md` finds CJK characters (translation happened).
    - `grep -F '<installRoot>/_config/agents/' docs/zh-cn/reference/agents.md` succeeds (paths kept in English).
    - `! grep -F '<installRoot>/gomad/agents/' docs/zh-cn/reference/{agents,skills}.md` succeeds (no v1.2 leak).
    - After running `node tools/inject-reference-tables.cjs` twice in a row: `git diff --quiet docs/reference/ docs/zh-cn/reference/` succeeds (idempotency, proved without invoking the full docs:build pipeline).
    - Full `npm run docs:build` is NOT asserted in this plan — Wave 4 integration plan `11-07-PLAN.md` owns end-to-end build verification.
  </acceptance_criteria>
  <done>zh-cn siblings authored with translated frontmatter + prose + section headers, English markers/paths/slash commands preserved. Injector populates both EN + zh-cn pages with English table data (per D-14). Idempotency proved via direct injector re-run + git diff. Full `npm run docs:build` deferred to Wave 4 plan 11-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Marker comments in source `.md` → injector regex | Strict text match required. Author-introduced marker drift (e.g., `<!--AUTO:agents-table-start-->` without space) breaks injection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Tampering | Persona-name translation drift | accept | Per D-14, injector populates English persona data into both EN + zh-cn tables. User is bilingual and will patch terminology if surfaced post-publish. Acceptance criterion does NOT enforce zh-cn name localization, and the EN row-count assertion is structural (`/gm:agent-` count) rather than persona-name-based — durable against displayName iteration. |
| T-11-03-02 | Tampering | Marker text drift | mitigate | Acceptance criteria assert exact marker text (`<!-- AUTO:agents-table-start -->` literal); regex in tools/inject-reference-tables.cjs uses `\\s*` between `<!--` and marker name to allow minor whitespace tolerance. |
| T-11-03-03 | Information Disclosure | v1.2 install path leakage in prose | mitigate | Acceptance criteria assert `! grep -F '<installRoot>/gomad/agents/' docs/reference/{agents,skills}.md docs/zh-cn/reference/{agents,skills}.md` succeeds. DOCS-07 in Phase 12 is the enforcement gate; this plan prevents leaks in the first place per D-10. |
| T-11-03-04 | Denial of Service | Build fails because injector + page out of sync | mitigate | Per Plan 02 contract, injector exits 1 only if persona count != 8. Pages skip silently if missing. Idempotency asserted via direct injector re-run + `git diff --quiet docs/reference/` (Wave 4 plan 11-07 closes the full-pipeline link-check gate once all Wave 3 plans land their cross-linked siblings). |
</threat_model>

<verification>
- 4 reference pages authored with correct frontmatter + section headers + AUTO markers.
- v1.3 path layout used everywhere; zero v1.2 leaks.
- After injector run, all marker blocks populated with expected row counts.
- Re-running the injector twice leaves `docs/reference/` + `docs/zh-cn/reference/` byte-stable (`git diff --quiet`).
- Full `npm run docs:build` is intentionally NOT run in this plan; Wave 4 integration plan `11-07-PLAN.md` enforces the link-check gate once all Wave 3 cross-linked siblings exist.
</verification>

<success_criteria>
1. `docs/reference/agents.md` exists with 4 `##` sections, 1 AUTO marker pair, populated 8-row table after injector (8 `/gm:agent-` rows; persona display names allowed to iterate per D-14).
2. `docs/reference/skills.md` exists with 6 `##` sections, 5 AUTO marker pairs, populated tables (Analysis 6, Planning 4, Solutioning 4, Implementation 14, Core 11).
3. `docs/zh-cn/reference/agents.md` and `docs/zh-cn/reference/skills.md` exist with translated prose, identical marker structure, English paths/slash commands preserved.
4. Re-running `node tools/inject-reference-tables.cjs` twice produces zero git diff under `docs/reference/` + `docs/zh-cn/reference/` (idempotency proved without `npm run docs:build`).
5. No v1.2 path leaks (`! grep -F '<installRoot>/gomad/agents/' docs/{reference,zh-cn/reference}/*.md`).
</success_criteria>

<output>
After completion, create `.planning/phases/11-docs-site-content-authoring/11-03-SUMMARY.md` documenting: 4 page paths, marker names used, table row counts after injection, zh-cn translation policy notes (per D-14), and the deferral of full `npm run docs:build` to Wave 4 plan 11-07.
</output>
