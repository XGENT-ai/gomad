---
phase: 01-rename
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - test/test-installation.js
  - test/test-module.js
  - test/test-presets.js
  - test/test-catalogs.js
autonomous: true
requirements:
  - TST-01

must_haves:
  truths:
    - "All 35 tests pass with node --test"
    - "No test file contains the string 'mobmad'"
    - "No test file contains 'bmad-' prefixed agent names or directory references"
    - "Test assertions validate gomad names, not mobmad names"
  artifacts:
    - path: "test/test-installation.js"
      provides: "Integration tests referencing gomad paths and names"
    - path: "test/test-module.js"
      provides: "Module structure tests referencing gomad package name and bin path"
    - path: "test/test-presets.js"
      provides: "Preset resolution tests with 'enhanced' preset (was bmad-enhanced)"
    - path: "test/test-catalogs.js"
      provides: "Catalog validation tests (may need bmad- reference cleanup)"
  key_links:
    - from: "test/test-installation.js"
      to: "bin/gomad-cli.js"
      via: "CLI path assertion"
      pattern: "gomad-cli.js"
    - from: "test/test-module.js"
      to: "package.json"
      via: "package name assertion"
      pattern: "gomad"
    - from: "test/test-presets.js"
      to: "catalog/presets.yaml"
      via: "preset name resolution"
      pattern: "enhanced"
---

<objective>
Update all test files to reference gomad instead of mobmad, update bmad- agent assertions to use new names, and verify the full test suite passes.

Purpose: Complete TST-01 (all existing tests updated to reference gomad) and validate the rename from Plan 01 did not break any functionality.
Output: All 35 tests pass with `node --test 'test/test-*.js'` using gomad references throughout.
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rename/01-CONTEXT.md
@.planning/phases/01-rename/01-RESEARCH.md
@.planning/phases/01-rename/01-01-SUMMARY.md

<interfaces>
<!-- Test patterns that need updating. Extracted from RESEARCH.md analysis. -->

From test/test-installation.js:
- CLI path references: "bin/mobmad-cli.js" -> "bin/gomad-cli.js"
- Lockfile path: "mobmad.lock.yaml" -> "gomad.lock.yaml"
- Status output strings checking for "mobmad" -> "gomad"
- bmad-skill-manifest.yaml existence check (line 58-64) -> skill-manifest.yaml

From test/test-module.js:
- Package name assertion: expects "mobmad" -> expects "gomad"
- Bin field assertion: expects "mobmad" key -> expects "gomad" key
- CLI path: "bin/mobmad-cli.js" -> "bin/gomad-cli.js"
- peerDependencies assertion for "bmad-method" -> LEAVE UNTOUCHED (Phase 3 scope)
- Agent subdirectory checks: "bmad-analysis", "bmad-planning", etc. -> "analysis", "planning", etc.
- Preset list reference: "bmad-enhanced" -> "enhanced"

From test/test-presets.js:
- Preset name "bmad-enhanced" -> "enhanced" in test descriptions and assertions
- `presets['bmad-enhanced']` -> `presets['enhanced']`
- `resolvePreset('bmad-enhanced', ...)` -> `resolvePreset('enhanced', ...)`
- Agent filter: `a.startsWith('bmad-')` must be rewritten to:
  `a.startsWith('analysis/') || a.startsWith('planning/') || a.startsWith('research/') || a.startsWith('review/')`
- Assertion message: "10+ BMAD agents" -> "10+ specialized agents"

From test/test-catalogs.js:
- May have bmad- references in catalog validation -- check and update if present
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update test file string references from mobmad/bmad to gomad (per D-08, D-09, D-10, TST-01)</name>
  <files>
    test/test-installation.js,
    test/test-module.js,
    test/test-presets.js,
    test/test-catalogs.js
  </files>
  <read_first>
    test/test-installation.js,
    test/test-module.js,
    test/test-presets.js,
    test/test-catalogs.js
  </read_first>
  <action>
Per D-08 (keep Node.js built-in test runner), D-09 (update all internal strings), D-10 (check filenames):

**D-10 check:** Verify no test files have "mobmad" in their filename. Expected: none do (confirmed in research A1).

**test/test-installation.js:**
- Replace all "mobmad-cli.js" with "gomad-cli.js" in path strings
- Replace "mobmad.lock.yaml" with "gomad.lock.yaml" in path/assertion strings
- Replace any console output checks for "mobmad" with "gomad"
- Replace "bmad-skill-manifest.yaml" with "skill-manifest.yaml" in file existence checks (lines ~58-64)
- Replace ".mobmad-manifest" with ".gomad-manifest" if referenced
- Replace any "mobmad" in test descriptions with "gomad"

**test/test-module.js:**
- Package name assertion: change expected value from `'mobmad'` to `'gomad'`
- Bin field assertion: change expected key from `'mobmad'` to `'gomad'`
- CLI path assertion: "bin/mobmad-cli.js" -> "bin/gomad-cli.js"
- Agent subdirectory assertions: replace `'bmad-analysis'` with `'analysis'`, `'bmad-planning'` with `'planning'`, `'bmad-research'` with `'research'`, `'bmad-review'` with `'review'`
- Preset list reference: `'bmad-enhanced'` -> `'enhanced'`
- LEAVE the `bmad-method` peerDependency assertion UNTOUCHED -- this is Phase 3 scope (BMA-01)

**test/test-presets.js (CRITICAL -- logic change needed, not just string replace):**
- All `'bmad-enhanced'` strings -> `'enhanced'` in test descriptions, object lookups, and resolvePreset calls
- The agent filter assertion (research: line 101) must be rewritten:
  OLD: `const bmadAgents = result.agents.filter((a) => a.startsWith('bmad-'))`
  NEW: `const specialAgents = result.agents.filter((a) => a.startsWith('analysis/') || a.startsWith('planning/') || a.startsWith('research/') || a.startsWith('review/'))`
- Update the assertion variable name: `bmadAgents` -> `specialAgents`
- Update assertion message: `expected 10+ BMAD agents` -> `expected 10+ specialized agents`
- Any other "bmad-" references in test descriptions -> updated descriptive text

**test/test-catalogs.js:**
- Read the file and check for any "mobmad" or "bmad-" references
- Update if found (research suggests this file validates catalog format, may have header/comment references)
- If no mobmad/bmad references exist, no changes needed
  </action>
  <verify>
    <automated>grep -r "mobmad" test/ | wc -l | xargs -I{} test {} -eq 0 && grep -c "bmad-analysis\|bmad-planning\|bmad-research\|bmad-review\|bmad-enhanced\|bmad-skill-manifest" test/test-presets.js test/test-module.js test/test-installation.js 2>/dev/null | grep -v ":0$" | wc -l | xargs -I{} test {} -eq 0 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of "mobmad" in any file under test/
    - Zero occurrences of "bmad-analysis", "bmad-planning", "bmad-research", "bmad-review" in test files
    - Zero occurrences of "bmad-enhanced" in test files
    - Zero occurrences of "bmad-skill-manifest" in test files
    - test/test-module.js contains `'gomad'` as expected package name
    - test/test-module.js contains `'gomad'` as expected bin key
    - test/test-module.js contains `'bin/gomad-cli.js'` as expected bin path
    - test/test-presets.js contains `presets['enhanced']` (not `presets['bmad-enhanced']`)
    - test/test-presets.js contains `resolvePreset('enhanced',` (not `resolvePreset('bmad-enhanced',`)
    - test/test-presets.js contains filter using `a.startsWith('analysis/')` or similar (not `a.startsWith('bmad-')`)
    - test/test-installation.js contains `gomad-cli.js` (not `mobmad-cli.js`)
    - test/test-installation.js contains `skill-manifest.yaml` (not `bmad-skill-manifest.yaml`)
    - peerDependencies assertion for bmad-method in test-module.js is UNCHANGED
  </acceptance_criteria>
  <done>All test files reference gomad names and stripped agent prefixes. No traces of mobmad or bmad- agent prefix in test code.</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and verify all 35 tests pass (per D-02 atomic commit)</name>
  <files>
    test/test-catalogs.js,
    test/test-presets.js,
    test/test-module.js,
    test/test-installation.js
  </files>
  <read_first>
    package.json
  </read_first>
  <action>
Run the full test suite to verify the rename is complete and nothing is broken:

```bash
npm test
```

This executes: `node --test 'test/test-*.js'` (35 tests across 4 files).

**Expected result:** All 35 tests pass (0 failures).

**If any tests fail:**
1. Read the failing test's error output carefully
2. Identify if the failure is a missed string replacement (fix in the test or source file)
3. Identify if the failure is a logic change needed (e.g., the bmad- agent filter in test-presets.js)
4. Fix the specific issue and re-run `npm test`
5. Repeat until all 35 tests pass

**After all tests pass, run the final comprehensive verification:**
```bash
grep -r "mobmad" --include="*.js" --include="*.yaml" --include="*.json" --include="*.csv" --include="*.md" --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning --exclude-dir=BMAD --exclude=package-lock.json .
```
This must return zero results.

```bash
node bin/gomad-cli.js --help
```
This must show "gomad" in the output with no mention of "mobmad".

Per D-02, all changes from Plan 01 and Plan 02 should be committed together as a single atomic commit. The executor should stage ALL changed files and commit with message: `feat(01): rename mobmad to gomad and strip bmad- prefix`
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5 && node bin/gomad-cli.js --help 2>&1 | head -3 | grep -q "gomad" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits with code 0
    - `npm test` output shows 35 tests passed, 0 failed
    - `node bin/gomad-cli.js --help` outputs text containing "gomad"
    - `node bin/gomad-cli.js --help` output does NOT contain "mobmad"
    - `grep -r "mobmad" --include="*.js" --include="*.yaml" --include="*.json" --include="*.csv" --include="*.md" --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning --exclude-dir=BMAD --exclude=package-lock.json .` returns zero results
  </acceptance_criteria>
  <done>All 35 tests pass. CLI help shows gomad branding. Zero traces of mobmad in codebase. Phase 1 rename is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Test updates are internal code changes with no trust boundary crossings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | Test assertions | accept | Tests are being updated to match renamed code. The test suite itself validates integrity -- passing tests prove correctness. |
</threat_model>

<verification>
1. `npm test` passes all 35 tests with exit code 0
2. `grep -r "mobmad" test/` returns zero results
3. `grep -r "bmad-analysis\|bmad-planning\|bmad-research\|bmad-review\|bmad-enhanced\|bmad-skill-manifest" test/` returns zero results
4. `node bin/gomad-cli.js --help` shows "gomad" and not "mobmad"
</verification>

<success_criteria>
- All 35 tests pass with Node.js built-in test runner
- Test files reference gomad paths, gomad names, stripped agent prefixes
- CLI runs correctly under new name
- Zero grep hits for old names anywhere in the codebase (excluding .planning/, BMAD/, node_modules/)
</success_criteria>

<output>
After completion, create `.planning/phases/01-rename/01-02-SUMMARY.md`
</output>
