---
phase: 01-rename
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [mobmad.lock.yaml]
autonomous: true
gap_closure: true
requirements: [REN-04, REN-06]

must_haves:
  truths:
    - "No file named mobmad.lock.yaml exists in the project root"
    - "find . -maxdepth 1 -name '*mobmad*' returns zero results"
  artifacts: []
  key_links: []
---

<objective>
Delete the leftover mobmad.lock.yaml file from the project root.

Purpose: Close the single remaining gap from Phase 01 verification. Decision D-03 required deletion of this file ("gomad starts fresh, no migration"). Plan 01-03 incorrectly reported it as already absent.
Output: The file is removed from the working directory.
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-rename/01-VERIFICATION.md
@.planning/phases/01-rename/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete mobmad.lock.yaml from project root</name>
  <files>mobmad.lock.yaml</files>
  <read_first>mobmad.lock.yaml</read_first>
  <action>
Delete the file mobmad.lock.yaml from the project root directory per decision D-03 ("Ignore old mobmad.lock.yaml and .mobmad-manifest.yaml files entirely -- gomad starts fresh").

Run: rm /Users/rockie/Documents/GitHub/xgent/gomad/mobmad.lock.yaml

This file is untracked by git (confirmed in verification report), so no git rm is needed -- a simple filesystem delete suffices.
  </action>
  <verify>
    <automated>test ! -f /Users/rockie/Documents/GitHub/xgent/gomad/mobmad.lock.yaml && echo "PASS: mobmad.lock.yaml deleted" || echo "FAIL: file still exists"</automated>
  </verify>
  <acceptance_criteria>
- `ls /Users/rockie/Documents/GitHub/xgent/gomad/mobmad.lock.yaml 2>&1` returns "No such file or directory"
- `find /Users/rockie/Documents/GitHub/xgent/gomad -maxdepth 1 -name "*mobmad*"` returns zero results
- `npm test` still passes (35/35 tests, file was not referenced by any test)
  </acceptance_criteria>
  <done>mobmad.lock.yaml no longer exists in the project root. The verification truth "No file in the project contains the string 'mobmad'" is now satisfied (excluding the one acceptable historical reference in CLAUDE.md).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected -- this is a file deletion of an unused lockfile.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | I (Information Disclosure) | mobmad.lock.yaml | mitigate | Delete the file to remove any old configuration data from the working directory |
</threat_model>

<verification>
1. `test ! -f mobmad.lock.yaml` -- file no longer exists
2. `find . -maxdepth 1 -name "*mobmad*"` -- zero results
3. `npm test` -- 35/35 tests pass (no regression)
</verification>

<success_criteria>
- mobmad.lock.yaml is deleted from project root
- No file or directory with "mobmad" in its name exists at project root level
- All 35 tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-rename/01-04-SUMMARY.md`
</output>
