---
phase: 01-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/gomad-cli.js
  - package.json
  - tools/global-installer.js
  - tools/curator.js
  - tools/sync-upstream.js
  - tools/package-skills.js
  - catalog/agents.yaml
  - catalog/presets.yaml
  - catalog/skills.yaml
  - catalog/commands.yaml
  - catalog/hooks.yaml
  - src/module/module.yaml
  - src/module/module-help.csv
  - src/module/agents/skill-manifest.yaml
  - src/module/agents/analysis/api-documenter.md
  - src/module/agents/analysis/codebase-analyzer.md
  - src/module/agents/analysis/data-analyst.md
  - src/module/agents/analysis/pattern-detector.md
  - src/module/agents/planning/epic-optimizer.md
  - src/module/agents/planning/requirements-analyst.md
  - src/module/agents/planning/technical-decisions-curator.md
  - src/module/agents/planning/trend-spotter.md
  - src/module/agents/planning/user-journey-mapper.md
  - src/module/agents/planning/user-researcher.md
  - src/module/agents/planning/dependency-mapper.md
  - src/module/agents/research/market-researcher.md
  - src/module/agents/research/tech-debt-auditor.md
  - src/module/agents/review/document-reviewer.md
  - src/module/agents/review/technical-evaluator.md
  - src/module/agents/review/test-coverage-analyzer.md
  - README.md
autonomous: true
requirements:
  - REN-01
  - REN-02
  - REN-03
  - REN-04
  - REN-05
  - REN-06

must_haves:
  truths:
    - "Running node bin/gomad-cli.js --help shows 'gomad' in program name and description with zero 'mobmad' mentions"
    - "package.json name is 'gomad' and bin field maps 'gomad' to 'bin/gomad-cli.js'"
    - "No file in the project contains the string 'mobmad' (excluding .planning/, BMAD/, node_modules/, .git/, package-lock.json)"
    - "No agent directory or catalog entry contains 'bmad-' prefix"
    - "Lock file constant references gomad.lock.yaml and manifest constant references .gomad-manifest.yaml"
  artifacts:
    - path: "bin/gomad-cli.js"
      provides: "CLI entry point renamed from mobmad-cli.js"
    - path: "package.json"
      provides: "Package identity as gomad"
      contains: "\"name\": \"gomad\""
    - path: "src/module/agents/analysis/"
      provides: "Renamed agent directory (was bmad-analysis/)"
    - path: "src/module/agents/planning/"
      provides: "Renamed agent directory (was bmad-planning/)"
    - path: "src/module/agents/research/"
      provides: "Renamed agent directory (was bmad-research/)"
    - path: "src/module/agents/review/"
      provides: "Renamed agent directory (was bmad-review/)"
  key_links:
    - from: "package.json bin field"
      to: "bin/gomad-cli.js"
      via: "bin.gomad entry"
      pattern: "\"gomad\": \"bin/gomad-cli.js\""
    - from: "tools/global-installer.js"
      to: "gomad.lock.yaml"
      via: "LOCKFILE_PATH constant"
      pattern: "gomad\\.lock\\.yaml"
    - from: "tools/global-installer.js"
      to: ".gomad-manifest.yaml"
      via: "MANIFEST_PATH constant"
      pattern: "\\.gomad-manifest\\.yaml"
---

<objective>
Rename all mobmad references to gomad and strip bmad- prefix from agent directories and catalog entries across the entire codebase.

Purpose: Complete rebranding from mobmad to gomad with no trace of old names in source, config, catalogs, or documentation (per D-01 through D-07).
Output: All source files, catalogs, module files, and documentation reference "gomad" exclusively; agent directories use plain descriptive names (analysis/, planning/, research/, review/).
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rename/01-CONTEXT.md
@.planning/phases/01-rename/01-RESEARCH.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Key constants and paths that must be updated. Extracted from codebase. -->

From bin/mobmad-cli.js (lines 14-16):
```javascript
program
  .name('mobmad')
  .description('My Own BMAD — personalized BMAD-METHOD distribution')
```

From tools/global-installer.js (lines 15-18):
```javascript
const CLAUDE_DIR = join(homedir(), '.claude');
const MANIFEST_PATH = join(CLAUDE_DIR, '.mobmad-manifest.yaml');
const GLOBAL_SRC = join(PROJECT_ROOT, 'global');
const LOCKFILE_PATH = join(PROJECT_ROOT, 'mobmad.lock.yaml');
```

From package.json:
```json
{
  "name": "mobmad",
  "description": "My Own BMAD — a personalized distribution of BMAD-METHOD with curated skills, agents, rules, and hooks",
  "bin": { "mobmad": "bin/mobmad-cli.js" },
  "keywords": ["bmad", "claude-code", "ai-development", "skills", "agents"]
}
```

Agent directory structure:
```
src/module/agents/bmad-analysis/   -> analysis/
src/module/agents/bmad-planning/   -> planning/
src/module/agents/bmad-research/   -> research/
src/module/agents/bmad-review/     -> review/
src/module/agents/bmad-skill-manifest.yaml -> skill-manifest.yaml
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename files and directories (per D-01, D-05)</name>
  <files>
    bin/gomad-cli.js,
    src/module/agents/analysis/,
    src/module/agents/planning/,
    src/module/agents/research/,
    src/module/agents/review/,
    src/module/agents/skill-manifest.yaml,
    src/module/skills/*/skill-manifest.yaml
  </files>
  <read_first>
    bin/mobmad-cli.js,
    src/module/agents/bmad-skill-manifest.yaml,
    src/module/agents/bmad-analysis/api-documenter.md,
    src/module/agents/bmad-planning/epic-optimizer.md,
    src/module/agents/bmad-research/market-researcher.md,
    src/module/agents/bmad-review/document-reviewer.md
  </read_first>
  <action>
Per D-01 (delete old, create new -- clean break) and D-05 (strip bmad- prefix from agent dirs):

1. **Rename CLI entry point:**
   - Read bin/mobmad-cli.js content
   - Write to bin/gomad-cli.js (exact same content for now -- string updates happen in Task 2)
   - Delete bin/mobmad-cli.js

2. **Rename agent directories (4 dirs):**
   - Copy src/module/agents/bmad-analysis/ to src/module/agents/analysis/ (all 4 .md files inside)
   - Copy src/module/agents/bmad-planning/ to src/module/agents/planning/ (all 7 .md files inside)
   - Copy src/module/agents/bmad-research/ to src/module/agents/research/ (all 2 .md files inside)
   - Copy src/module/agents/bmad-review/ to src/module/agents/review/ (all 3 .md files inside)
   - Delete the 4 old bmad-* directories recursively

3. **Rename agent skill manifest:**
   - Copy src/module/agents/bmad-skill-manifest.yaml to src/module/agents/skill-manifest.yaml
   - Delete src/module/agents/bmad-skill-manifest.yaml

4. **Rename 14 skill manifest files (per D-07):**
   - For each directory under src/module/skills/ that contains bmad-skill-manifest.yaml:
     Copy bmad-skill-manifest.yaml to skill-manifest.yaml within that same directory, then delete the old file.
   - The 14 directories are discoverable via: `find src/module/skills -name "bmad-skill-manifest.yaml"`

5. **Delete old lockfile (per D-03):**
   - Delete mobmad.lock.yaml from project root (gomad starts fresh, no migration)

Do NOT update any string content inside files in this task -- that is Task 2.
  </action>
  <verify>
    <automated>test ! -f bin/mobmad-cli.js && test -f bin/gomad-cli.js && test ! -d src/module/agents/bmad-analysis && test -d src/module/agents/analysis && test ! -d src/module/agents/bmad-planning && test -d src/module/agents/planning && test ! -d src/module/agents/bmad-research && test -d src/module/agents/research && test ! -d src/module/agents/bmad-review && test -d src/module/agents/review && test ! -f src/module/agents/bmad-skill-manifest.yaml && test -f src/module/agents/skill-manifest.yaml && test ! -f mobmad.lock.yaml && test $(find src/module/skills -name "bmad-skill-manifest.yaml" | wc -l) -eq 0 && test $(find src/module/skills -name "skill-manifest.yaml" | wc -l) -ge 14 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - bin/mobmad-cli.js does not exist
    - bin/gomad-cli.js exists
    - src/module/agents/bmad-analysis/ does not exist
    - src/module/agents/analysis/ exists with 4 .md files (api-documenter.md, codebase-analyzer.md, data-analyst.md, pattern-detector.md)
    - src/module/agents/bmad-planning/ does not exist
    - src/module/agents/planning/ exists with 7 .md files
    - src/module/agents/bmad-research/ does not exist
    - src/module/agents/research/ exists with 2 .md files
    - src/module/agents/bmad-review/ does not exist
    - src/module/agents/review/ exists with 3 .md files
    - src/module/agents/bmad-skill-manifest.yaml does not exist
    - src/module/agents/skill-manifest.yaml exists
    - mobmad.lock.yaml does not exist in project root
    - Zero files named bmad-skill-manifest.yaml under src/module/skills/
    - At least 14 files named skill-manifest.yaml under src/module/skills/
  </acceptance_criteria>
  <done>All file/directory renames complete. Old paths deleted. No bmad-prefixed directories or bmad-skill-manifest.yaml files remain.</done>
</task>

<task type="auto">
  <name>Task 2: Update all string content across source, catalogs, module, and docs (per D-04, D-06, D-07, REN-01 through REN-06)</name>
  <files>
    bin/gomad-cli.js,
    package.json,
    tools/global-installer.js,
    tools/curator.js,
    tools/sync-upstream.js,
    tools/package-skills.js,
    catalog/agents.yaml,
    catalog/presets.yaml,
    catalog/skills.yaml,
    catalog/commands.yaml,
    catalog/hooks.yaml,
    src/module/module.yaml,
    src/module/module-help.csv,
    src/module/agents/skill-manifest.yaml,
    README.md
  </files>
  <read_first>
    bin/gomad-cli.js,
    package.json,
    tools/global-installer.js,
    tools/curator.js,
    tools/sync-upstream.js,
    tools/package-skills.js,
    catalog/agents.yaml,
    catalog/presets.yaml,
    catalog/skills.yaml,
    catalog/commands.yaml,
    catalog/hooks.yaml,
    src/module/module.yaml,
    src/module/module-help.csv,
    src/module/agents/skill-manifest.yaml,
    README.md
  </read_first>
  <action>
Update ALL string content in the following files. Every occurrence of "mobmad" becomes "gomad". Every "bmad-" prefix on agent names/dirs is stripped. Details per file:

**bin/gomad-cli.js (7 mobmad + 1 bmad occurrences):**
- `.name('mobmad')` -> `.name('gomad')`
- `.description('My Own BMAD — personalized BMAD-METHOD distribution')` -> `.description('GOMAD — curated Claude Code environment')`
- All help text strings: replace "mobmad" with "gomad" (install description, curate description, status/update/uninstall descriptions)
- Preset option help text: replace "bmad-enhanced" with "enhanced" in preset list string
- Any console.log or chalk strings referencing "mobmad" -> "gomad"

**package.json (per REN-01, REN-02):**
- `"name": "mobmad"` -> `"name": "gomad"`
- `"description": "My Own BMAD — a personalized distribution of BMAD-METHOD with curated skills, agents, rules, and hooks"` -> `"description": "GOMAD Orchestration Method for Agile Development — curated Claude Code skills, agents, rules, hooks, and commands"`
- `"bin": { "mobmad": "bin/mobmad-cli.js" }` -> `"bin": { "gomad": "bin/gomad-cli.js" }`
- `"keywords": ["bmad", ...]` -> `"keywords": ["gomad", "claude-code", "ai-development", "skills", "agents"]`
- Leave `"peerDependencies": { "bmad-method": "^6.x" }` untouched (Phase 3 scope, BMA-01)
- After editing package.json, run `npm install` to regenerate package-lock.json

**tools/global-installer.js (12 mobmad occurrences, per REN-04, REN-05):**
- `const MANIFEST_PATH = join(CLAUDE_DIR, '.mobmad-manifest.yaml')` -> `const MANIFEST_PATH = join(CLAUDE_DIR, '.gomad-manifest.yaml')`
- `const LOCKFILE_PATH = join(PROJECT_ROOT, 'mobmad.lock.yaml')` -> `const LOCKFILE_PATH = join(PROJECT_ROOT, 'gomad.lock.yaml')`
- All console.log/chalk strings: "mobmad" -> "gomad" (e.g., "Updating mobmad..." -> "Updating gomad...")
- Backup path pattern: ".mobmad-backup-" -> ".gomad-backup-"
- JSDoc comments: "mobmad" -> "gomad"

**tools/curator.js (4 mobmad occurrences, per REN-04):**
- Lockfile path string: "mobmad.lock.yaml" -> "gomad.lock.yaml"
- Intro/outro text: "mobmad" -> "gomad"
- JSDoc: "mobmad" -> "gomad"

**tools/sync-upstream.js (4 mobmad occurrences):**
- `LOCKFILE_PATH` referencing "mobmad.lock.yaml" -> "gomad.lock.yaml"
- console.log strings: "mobmad" -> "gomad"

**tools/package-skills.js (3 mobmad + 2 bmad occurrences):**
- LOCKFILE_PATH: "mobmad.lock.yaml" -> "gomad.lock.yaml"
- console.log: "mobmad" -> "gomad"
- "bmad-skill-manifest.yaml" references -> "skill-manifest.yaml"

**catalog/agents.yaml (per D-06 -- 16 agent names to strip bmad- prefix):**
- Every agent name like `bmad-analysis/api-documenter` becomes `analysis/api-documenter`
- Every agent name like `bmad-planning/epic-optimizer` becomes `planning/epic-optimizer`
- Every agent name like `bmad-research/market-researcher` becomes `research/market-researcher`
- Every agent name like `bmad-review/document-reviewer` becomes `review/document-reviewer`
- Header comment: "mobmad" -> "gomad" if present

**catalog/presets.yaml (per D-07 -- 32 agent refs + preset name):**
- Preset name `bmad-enhanced` -> `enhanced`
- All 32 agent references: strip "bmad-" prefix (e.g., "bmad-analysis/api-documenter" -> "analysis/api-documenter")
- Header comment: "mobmad" -> "gomad" if present

**catalog/skills.yaml, catalog/commands.yaml, catalog/hooks.yaml:**
- Header comment only: "mobmad" -> "gomad" if present

**src/module/module.yaml (3 occurrences):**
- Preset name reference: "bmad-enhanced" -> "enhanced"
- Help text strings: "mobmad" -> "gomad"

**src/module/module-help.csv (per D-07):**
- All agent entries with "bmad-" prefix: strip prefix
- Any "mobmad" references -> "gomad"

**src/module/agents/skill-manifest.yaml (complete rewrite per D-07):**
- All 16 keys that have "bmad-" prefix: strip prefix (e.g., "bmad-analysis/api-documenter" -> "analysis/api-documenter")
- Any "mobmad" references -> "gomad"

**README.md (25 occurrences):**
- All "mobmad" -> "gomad"
- All "bmad-" prefixed names -> stripped prefix
- Update description to match new package.json description
- Update CLI examples: `mobmad install` -> `gomad install`, etc.

After all file updates, run: `npm install` to regenerate package-lock.json with the new package name.
  </action>
  <verify>
    <automated>grep -r "mobmad" --include="*.js" --include="*.yaml" --include="*.json" --include="*.csv" --include="*.md" --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning --exclude-dir=BMAD --exclude=package-lock.json . | grep -v "Binary" | wc -l | xargs -I{} test {} -eq 0 && grep -q '"name": "gomad"' package.json && grep -q '"gomad": "bin/gomad-cli.js"' package.json && grep -q "gomad.lock.yaml" tools/global-installer.js && grep -q ".gomad-manifest.yaml" tools/global-installer.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of "mobmad" in any .js, .yaml, .json (except package-lock.json), .csv, or .md file (excluding .planning/, BMAD/, node_modules/)
    - package.json contains `"name": "gomad"`
    - package.json contains `"gomad": "bin/gomad-cli.js"` in bin field
    - package.json contains `"description": "GOMAD Orchestration Method for Agile Development`
    - package.json contains `"keywords": ["gomad",`
    - tools/global-installer.js contains `gomad.lock.yaml`
    - tools/global-installer.js contains `.gomad-manifest.yaml`
    - tools/global-installer.js contains `.gomad-backup-`
    - tools/curator.js contains `gomad.lock.yaml`
    - catalog/agents.yaml contains `analysis/api-documenter` (not `bmad-analysis/api-documenter`)
    - catalog/presets.yaml contains `enhanced:` as a preset key (not `bmad-enhanced:`)
    - src/module/agents/skill-manifest.yaml has no `bmad-` prefixed keys
    - bin/gomad-cli.js contains `.name('gomad')`
    - bin/gomad-cli.js contains `enhanced` in preset list (not `bmad-enhanced`)
    - Zero occurrences of agent name pattern `bmad-analysis/` or `bmad-planning/` or `bmad-research/` or `bmad-review/` in any source file
  </acceptance_criteria>
  <done>All string content updated. Zero traces of "mobmad" or "bmad-" prefix in source, catalogs, module files, or documentation. package-lock.json regenerated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This is a pure rename phase with no runtime behavior changes, no user input handling, no network calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | package.json name field | accept | Rename is intentional; package name change from mobmad to gomad is the goal of this phase. No security implications -- private registry only. |
| T-01-02 | Information Disclosure | String replacement scope | accept | No secrets, credentials, or sensitive data involved. All changes are to branding strings in source code. |
</threat_model>

<verification>
1. `grep -r "mobmad" --include="*.js" --include="*.yaml" --include="*.json" --include="*.csv" --include="*.md" --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning --exclude-dir=BMAD --exclude=package-lock.json .` returns zero results
2. `grep -r "bmad-analysis\|bmad-planning\|bmad-research\|bmad-review\|bmad-skill-manifest" --include="*.js" --include="*.yaml" --include="*.json" --include="*.csv" --include="*.md" --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning --exclude-dir=BMAD .` returns zero results
3. `node bin/gomad-cli.js --help` runs without error and shows "gomad" in output
4. `find . -name "*mobmad*" -not -path "./.planning/*" -not -path "./BMAD/*" -not -path "./node_modules/*" -not -path "./.git/*"` returns zero results
5. `find src/module/agents -name "bmad-*" -type d` returns zero results
</verification>

<success_criteria>
- The CLI entry point is bin/gomad-cli.js and runs correctly
- package.json identifies the project as "gomad" with correct bin mapping
- All constants (LOCKFILE_PATH, MANIFEST_PATH) reference gomad names
- All 48 agent catalog entries use plain names (no bmad- prefix)
- All 6 presets reference agents by their new names
- README.md documents "gomad" exclusively
- Zero grep hits for "mobmad" or "bmad-" agent prefix in source/config/docs
</success_criteria>

<output>
After completion, create `.planning/phases/01-rename/01-01-SUMMARY.md`
</output>
