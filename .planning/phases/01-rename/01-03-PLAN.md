---
phase: 01-rename
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
gap_closure: true
requirements:
  - REN-06

must_haves:
  truths:
    - "CLAUDE.md contains zero occurrences of the string 'mobmad'"
    - "No file named mobmad.lock.yaml exists in the project root"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Accurate developer documentation with gomad references"
      contains: "bin/gomad-cli.js"
  key_links:
    - from: "CLAUDE.md"
      to: "bin/gomad-cli.js"
      via: "CLI entry point reference"
      pattern: "bin/gomad-cli.js"
---

<objective>
Close 2 verification gaps from Phase 01 rename: update 16 stale mobmad references in CLAUDE.md and delete the leftover mobmad.lock.yaml file.

Purpose: CLAUDE.md is injected into every Claude Code session as project context. Stale mobmad references cause AI assistants to suggest incorrect commands, binary names, and file paths. The mobmad.lock.yaml file was explicitly required for deletion by D-03.
Output: CLAUDE.md with all mobmad references replaced by gomad; mobmad.lock.yaml removed from working directory.
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rename/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all mobmad references in CLAUDE.md and delete mobmad.lock.yaml</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (current state with 16 stale mobmad references)
    - .planning/phases/01-rename/01-VERIFICATION.md (gap details)
  </read_first>
  <action>
Perform the following replacements in CLAUDE.md (line numbers reference current file state):

1. Line 6: Keep "Previously called "mobmad"" as-is — this is historical context, not an active instruction.

2. Line 29: `bin/mobmad-cli.js` -> `bin/gomad-cli.js`
   Full: "commander 13.1.0 - Command-line interface framework (`bin/gomad-cli.js`)"

3. Line 43: `mobmad mcp enable` -> `gomad mcp enable`
   Full: "Secrets: MCP configs with API keys are explicitly NOT auto-installed (user must opt in via `gomad mcp enable`)"

4. Line 47: `bin/mobmad-cli.js` -> `bin/gomad-cli.js` AND `bin.mobmad` -> `bin.gomad`
   Full: "- `bin/gomad-cli.js` - CLI executable registered in package.json as `bin.gomad`"

5. Line 48: `npm install -g mobmad` -> `npm install -g gomad`
   Full: "- Installed globally via `npm install -g gomad` or locally via `npm install`"

6. Line 58: `bin/mobmad-cli.js` -> `bin/gomad-cli.js`
   Full: "- CLI commands: Dynamic imports in `bin/gomad-cli.js` load tools on demand"

7. Line 67: `mobmad-cli.js` -> `gomad-cli.js`
   Full: "- kebab-case for CLI and tool scripts: `gomad-cli.js`, `curator.js`, `sync-upstream.js`"

8. Line 114: `'Updating mobmad...'` -> `'Updating gomad...'`
   Full: "- `console.log()` for status updates: `console.log('Updating gomad...')`"

9. Line 119: `'\\nmobmad sync...\\n'` -> `'\\ngomad sync...\\n'`
   Full: "- Tool initialization and completion: `console.log(chalk.bold('\\ngomad sync...\\n'))`"

10. Line 152: `bin/mobmad-cli.js` -> `bin/gomad-cli.js`
    Full: "- Top-level entry: `bin/gomad-cli.js` dynamically imports submodules"

11. Line 171: `bin/mobmad-cli.js` -> `bin/gomad-cli.js`
    Full: "- Location: `bin/gomad-cli.js`"

12. Line 228: `~/.claude/.mobmad-manifest.yaml` -> `~/.claude/.gomad-manifest.yaml`
    Full: "- Location: `~/.claude/.gomad-manifest.yaml`"

13. Line 231: `bin/mobmad-cli.js` -> `bin/gomad-cli.js`
    Full: "- Location: `bin/gomad-cli.js`"

14. Line 232: `npx mobmad install` -> `npx gomad install`
    Full: "- Triggers: `npm run curate`, `npx gomad install`, user terminal invocation"

15. Line 249: `No authentication in mobmad itself` -> `No authentication in gomad itself`
    Full: "- No authentication in gomad itself"

16. Line 251: `mobmad mcp enable` -> `gomad mcp enable`
    Full: "- User must manually enable MCP configs via `gomad mcp enable <name>`"

Additionally, delete the file `mobmad.lock.yaml` from the project root using `rm mobmad.lock.yaml`. This file is untracked by git and was required for deletion per D-03.

NOTE on line 6: The phrase "Previously called 'mobmad'" is historical context describing the project's origin. This is acceptable to keep — it describes what the project WAS, not what it IS. All other 15 occurrences are active instructions/references that must change to gomad.
  </action>
  <verify>
    <automated>grep -c "mobmad" CLAUDE.md | grep -q "^1$" && echo "PASS: exactly 1 mobmad (historical context)" || echo "FAIL: expected exactly 1 mobmad occurrence"; test ! -f mobmad.lock.yaml && echo "PASS: mobmad.lock.yaml deleted" || echo "FAIL: mobmad.lock.yaml still exists"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mobmad" CLAUDE.md` returns exactly 1 (the historical "Previously called 'mobmad'" on line 6)
    - `grep "bin/gomad-cli.js" CLAUDE.md` returns 6 matches (lines referencing CLI entry point)
    - `grep "gomad mcp enable" CLAUDE.md` returns 2 matches
    - `grep "npm install -g gomad" CLAUDE.md` returns 1 match
    - `grep ".gomad-manifest.yaml" CLAUDE.md` returns 1 match
    - `grep "bin.gomad" CLAUDE.md` returns 1 match
    - `grep "npx gomad install" CLAUDE.md` returns 1 match
    - `grep "Updating gomad" CLAUDE.md` returns 1 match
    - `grep "gomad sync" CLAUDE.md` returns 1 match
    - `grep "No authentication in gomad" CLAUDE.md` returns 1 match
    - `test ! -f mobmad.lock.yaml` succeeds (file deleted)
    - `npm test` still passes (CLAUDE.md changes do not affect tests)
  </acceptance_criteria>
  <done>CLAUDE.md contains exactly 1 occurrence of "mobmad" (historical context only), all active developer instructions reference "gomad", and mobmad.lock.yaml is deleted from the project root.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected — this is a documentation update and file deletion only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | I (Information Disclosure) | CLAUDE.md | accept | CLAUDE.md is already committed to the repo and contains no secrets. Documentation update only. |
</threat_model>

<verification>
1. `grep -c "mobmad" CLAUDE.md` returns exactly 1
2. `find . -maxdepth 1 -name "*mobmad*"` returns no results
3. `npm test` passes (35/35 tests)
</verification>

<success_criteria>
- REN-06 unblocked: all active string literals, comments, logs, help text in CLAUDE.md reference "gomad" instead of "mobmad"
- Zero files named "mobmad" exist in project root
- Phase 01 verification truth 7 ("No file in the project contains the string 'mobmad'") is now satisfiable with only the historical context exception
</success_criteria>

<output>
After completion, create `.planning/phases/01-rename/01-03-SUMMARY.md`
</output>
