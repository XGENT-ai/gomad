---
phase: 03-bmad-decoupling
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - assets/agents/api-documenter.md
  - assets/agents/codebase-analyzer.md
  - assets/agents/data-analyst.md
  - assets/agents/pattern-detector.md
  - assets/agents/dependency-mapper.md
  - assets/agents/epic-optimizer.md
  - assets/agents/requirements-analyst.md
  - assets/agents/technical-decisions-curator.md
  - assets/agents/trend-spotter.md
  - assets/agents/user-journey-mapper.md
  - assets/agents/user-researcher.md
  - assets/agents/market-researcher.md
  - assets/agents/tech-debt-auditor.md
  - assets/agents/document-reviewer.md
  - assets/agents/technical-evaluator.md
  - assets/agents/test-coverage-analyzer.md
  - catalog/agents.yaml
  - catalog/presets.yaml
autonomous: true
requirements: [BMA-04]
must_haves:
  truths:
    - "All 16 ex-BMAD agent .md files exist under assets/agents/ as flat files (no analysis/, planning/, research/, review/ subdirs)"
    - "Every migrated agent .md frontmatter has name field equal to its bare unprefixed filename (no bmm- or mob- prefix)"
    - "Every migrated agent .md frontmatter has zero of: module, canonicalId, displayName, title"
    - "catalog/agents.yaml lists all 16 agents with flat unprefixed names and scope: project"
    - "catalog/presets.yaml enterprise.additional_agents and enhanced.additional_agents reference the 16 agents by flat unprefixed names"
    - "npm test still passes (catalog and preset validation tests stay green)"
  artifacts:
    - path: "assets/agents/codebase-analyzer.md"
      provides: "Migrated codebase-analyzer agent (representative)"
      contains: "name: codebase-analyzer"
    - path: "catalog/agents.yaml"
      provides: "Flat-name catalog entries for all 16 ex-BMAD agents"
      contains: "name: codebase-analyzer"
    - path: "catalog/presets.yaml"
      provides: "Updated preset references"
      contains: "- codebase-analyzer"
  key_links:
    - from: "catalog/agents.yaml"
      to: "assets/agents/{name}.md"
      via: "name field matches filename"
      pattern: "name: [a-z][a-z0-9-]+$"
    - from: "catalog/presets.yaml (enhanced/enterprise additional_agents)"
      to: "catalog/agents.yaml entries"
      via: "preset entry name must exist as a catalog entry name"
      pattern: "- [a-z][a-z0-9-]+$"
---

<objective>
Migrate the 16 ex-BMAD agents from `src/module/agents/{analysis,planning,research,review}/*.md` to flat `assets/agents/*.md`. Rewrite each agent's `name:` frontmatter field to the bare unprefixed form (strip both `bmm-` and `mob-` legacy prefixes per researcher correction to D-03). Update `catalog/agents.yaml` and `catalog/presets.yaml` to reference the flat names. Source files remain in `src/module/agents/` for now — Plan 03 deletes the entire `src/module/` tree afterward.

Purpose: Satisfies BMA-04 (BMAD-specific agents converted to regular agents) and lays the groundwork for BMA-02 (Plan 03 then deletes src/module/). Catalog/preset updates are MANDATORY — without them preset resolution breaks.

Output: 16 flat agent files in `assets/agents/`, an updated `catalog/agents.yaml` with 16 flat entries, and an updated `catalog/presets.yaml` with `enhanced.additional_agents` and `enterprise.additional_agents` referencing the new flat names.
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-bmad-decoupling/03-CONTEXT.md
@.planning/phases/03-bmad-decoupling/03-RESEARCH.md

<interfaces>
<!-- Existing agent frontmatter template — every migrated agent must conform to this shape. -->
<!-- From assets/agents/code-reviewer.md (representative existing flat agent): -->
```yaml
# (frontmatter delimiters omitted to keep this plan parseable)
name: code-reviewer
description: Expert code review specialist. ...
tools: ["Read", "Grep", "Glob", "Bash"]
model: sonnet
```

<!-- The 16 source files currently look like this (verified from src/module/agents/analysis/codebase-analyzer.md): -->
```yaml
# (frontmatter delimiters omitted)
name: bmm-codebase-analyzer
description: Performs comprehensive codebase analysis ...
tools:
```

<!-- Target shape after rewrite (conservative — keep tools empty list to match the stripped source): -->
```yaml
# (frontmatter delimiters omitted)
name: codebase-analyzer
description: Performs comprehensive codebase analysis ...
tools: []
```

<!-- catalog/agents.yaml entry pattern (existing flat agents at lines 5-148 use this shape): -->
```yaml
- name: codebase-analyzer
  description: Comprehensive codebase analysis and architecture
  scope: project
  default_include: true
```

<!-- The 16 source -> destination mapping (from research, VERIFIED): -->
src/module/agents/analysis/api-documenter.md          -> assets/agents/api-documenter.md
src/module/agents/analysis/codebase-analyzer.md       -> assets/agents/codebase-analyzer.md
src/module/agents/analysis/data-analyst.md            -> assets/agents/data-analyst.md
src/module/agents/analysis/pattern-detector.md        -> assets/agents/pattern-detector.md
src/module/agents/planning/dependency-mapper.md       -> assets/agents/dependency-mapper.md
src/module/agents/planning/epic-optimizer.md          -> assets/agents/epic-optimizer.md
src/module/agents/planning/requirements-analyst.md    -> assets/agents/requirements-analyst.md
src/module/agents/planning/technical-decisions-curator.md -> assets/agents/technical-decisions-curator.md
src/module/agents/planning/trend-spotter.md           -> assets/agents/trend-spotter.md
src/module/agents/planning/user-journey-mapper.md     -> assets/agents/user-journey-mapper.md
src/module/agents/planning/user-researcher.md         -> assets/agents/user-researcher.md
src/module/agents/research/market-researcher.md       -> assets/agents/market-researcher.md
src/module/agents/research/tech-debt-auditor.md       -> assets/agents/tech-debt-auditor.md
src/module/agents/review/document-reviewer.md         -> assets/agents/document-reviewer.md
src/module/agents/review/technical-evaluator.md       -> assets/agents/technical-evaluator.md
src/module/agents/review/test-coverage-analyzer.md    -> assets/agents/test-coverage-analyzer.md

<!-- No filename collisions with existing flat agents (architect, build-error-resolver, code-reviewer, planner, security-reviewer, tdd-guide). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy 16 agents to assets/agents/ with rewritten frontmatter</name>
  <files>
    assets/agents/api-documenter.md, assets/agents/codebase-analyzer.md, assets/agents/data-analyst.md,
    assets/agents/pattern-detector.md, assets/agents/dependency-mapper.md, assets/agents/epic-optimizer.md,
    assets/agents/requirements-analyst.md, assets/agents/technical-decisions-curator.md,
    assets/agents/trend-spotter.md, assets/agents/user-journey-mapper.md, assets/agents/user-researcher.md,
    assets/agents/market-researcher.md, assets/agents/tech-debt-auditor.md, assets/agents/document-reviewer.md,
    assets/agents/technical-evaluator.md, assets/agents/test-coverage-analyzer.md
  </files>
  <read_first>
    - src/module/agents/analysis/codebase-analyzer.md (representative source — confirm `bmm-` prefix in name)
    - src/module/agents/planning/requirements-analyst.md (second sample to confirm consistency)
    - assets/agents/code-reviewer.md (target template — frontmatter shape to match)
    - .planning/phases/03-bmad-decoupling/03-RESEARCH.md (## Architecture Patterns and Code Examples sections — frontmatter rewrite pattern)
  </read_first>
  <action>
For each of the 16 source files in the mapping table in `<interfaces>`:

1. Read the source file content (UTF-8).
2. Parse the YAML frontmatter using `yaml` package: extract the block between the first `^---\n` and the next `\n---\n`.
3. Mutate the parsed frontmatter object:
   - Set `fm.name = <bare-filename-without-prefix>` (e.g. for `codebase-analyzer.md`, set `fm.name = 'codebase-analyzer'`). The new name MUST equal the destination filename without the `.md` extension. Strip ANY leading `bmm-` or `mob-` prefix that may be present.
   - `delete fm.module`
   - `delete fm.canonicalId`
   - `delete fm.displayName`
   - `delete fm.title`
   - If `fm.tools` is undefined or null, set `fm.tools = []` (matches the stripped-empty convention; do NOT invent tool lists).
   - Keep `fm.description` unchanged.
4. Stringify the new frontmatter with `yaml.stringify(fm)` and reconstruct the file as: `---\n${stringified}---\n${body}` where `body` is the original content after the closing `---\n`.
5. Write to the destination path under `assets/agents/`.

Use a small Node script (or inline ESM in a one-shot file) following the rewrite pattern from `tools/package-skills.js:34-52` and the example in `03-RESEARCH.md` ## Code Examples. Use the existing `yaml` package (^2.7.1) — do NOT hand-roll a parser.

Do NOT delete the source files in `src/module/agents/` yet — Plan 03 removes the entire `src/module/` tree.

Concrete bare names (must match destination filenames exactly):
api-documenter, codebase-analyzer, data-analyst, pattern-detector, dependency-mapper, epic-optimizer, requirements-analyst, technical-decisions-curator, trend-spotter, user-journey-mapper, user-researcher, market-researcher, tech-debt-auditor, document-reviewer, technical-evaluator, test-coverage-analyzer
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const yaml=require('yaml');const names=['api-documenter','codebase-analyzer','data-analyst','pattern-detector','dependency-mapper','epic-optimizer','requirements-analyst','technical-decisions-curator','trend-spotter','user-journey-mapper','user-researcher','market-researcher','tech-debt-auditor','document-reviewer','technical-evaluator','test-coverage-analyzer'];let ok=true;for(const n of names){const p='assets/agents/'+n+'.md';if(!fs.existsSync(p)){console.error('MISSING',p);ok=false;continue;}const c=fs.readFileSync(p,'utf8');const m=c.match(/^---\n([\s\S]*?)\n---\n/);if(!m){console.error('NO FM',p);ok=false;continue;}const fm=yaml.parse(m[1]);if(fm.name!==n){console.error('BAD NAME',p,fm.name);ok=false;}for(const k of ['module','canonicalId','displayName','title']){if(k in fm){console.error('LEFTOVER',p,k);ok=false;}}}process.exit(ok?0:1);"</automated>
  </verify>
  <acceptance_criteria>
    - All 16 files exist under `assets/agents/` (verifiable: `ls assets/agents/ | wc -l` returns at least 22 — the 6 pre-existing plus the 16 new)
    - `grep -l "^name: bmm-" assets/agents/*.md` returns no matches
    - `grep -l "^name: mob-" assets/agents/*.md` returns no matches
    - `grep -l "^module:" assets/agents/*.md` returns no matches
    - `grep -l "^canonicalId:" assets/agents/*.md` returns no matches
    - For each new file, `name:` value in frontmatter equals the filename without `.md`
    - Source files in `src/module/agents/` are NOT deleted (still present for Plan 03 to handle)
  </acceptance_criteria>
  <done>16 ex-BMAD agents live as regular flat agents under assets/agents/ with stripped frontmatter, satisfying BMA-04.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite catalog/agents.yaml entries to flat names + fix count comments</name>
  <files>catalog/agents.yaml</files>
  <read_first>
    - catalog/agents.yaml (full file — confirm current entries at lines 150-222 and total comment at line 223)
    - .planning/phases/03-bmad-decoupling/03-RESEARCH.md (## Concrete File Inventory > catalog/agents.yaml entries to update)
  </read_first>
  <action>
Edit `catalog/agents.yaml`:

1. For each of the 16 entries currently at lines ~150-221 with prefixed names (`analysis/api-documenter`, `analysis/codebase-analyzer`, ..., `review/test-coverage-analyzer`), rewrite the `name:` value to the flat unprefixed form. Keep `description`, `scope: project`, and `default_include: true` unchanged. Example:

   BEFORE:
   ```yaml
     - name: analysis/codebase-analyzer
       description: Comprehensive codebase analysis and architecture
       scope: project
       default_include: true
   ```

   AFTER:
   ```yaml
     - name: codebase-analyzer
       description: Comprehensive codebase analysis and architecture
       scope: project
       default_include: true
   ```

2. Update the section header comment at line 150 from `# === Project-Specific Agents (15) — Project Scope ===` to `# === Project-Specific Agents (16) — Project Scope ===` (the count was already wrong — 16 entries, not 15).

3. You MAY remove the `# analysis/`, `# planning/`, `# research/`, `# review/` subgroup comments since the names are now flat — alphabetize within the project scope block instead. Keep the subgroup comments only if they aid readability without misleading anyone.

4. Update the trailer comment at line 223 from `# Total: 48 agents (33 global + 15 project)` to a corrected count. Recount by hand: count entries with `scope: global` and entries with `scope: project` in the actual file after edits, then write `# Total: <total> agents (<global> global + 16 project)`. If the existing global count of 33 was wrong, fix it.

The 16 new flat names that MUST appear, one per `- name:` line:
api-documenter, codebase-analyzer, data-analyst, pattern-detector, dependency-mapper, epic-optimizer, requirements-analyst, technical-decisions-curator, trend-spotter, user-journey-mapper, user-researcher, market-researcher, tech-debt-auditor, document-reviewer, technical-evaluator, test-coverage-analyzer

Validate the file is still parseable YAML before writing (use `yaml.parse` round-trip).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const yaml=require('yaml');const c=fs.readFileSync('catalog/agents.yaml','utf8');const data=yaml.parse(c);const names=new Set(data.agents.map(a=>a.name));const need=['api-documenter','codebase-analyzer','data-analyst','pattern-detector','dependency-mapper','epic-optimizer','requirements-analyst','technical-decisions-curator','trend-spotter','user-journey-mapper','user-researcher','market-researcher','tech-debt-auditor','document-reviewer','technical-evaluator','test-coverage-analyzer'];const missing=need.filter(n=>!names.has(n));const stillPrefixed=[...names].filter(n=>n.startsWith('analysis/')||n.startsWith('planning/')||n.startsWith('research/')||n.startsWith('review/'));if(missing.length){console.error('missing',missing);process.exit(1);}if(stillPrefixed.length){console.error('still prefixed',stillPrefixed);process.exit(1);}console.log('ok',names.size,'agents');"</automated>
  </verify>
  <acceptance_criteria>
    - `catalog/agents.yaml` parses as valid YAML (no errors from `yaml.parse`)
    - All 16 flat names listed above appear as `- name:` entries
    - No entry name starts with `analysis/`, `planning/`, `research/`, or `review/`
    - Section header at the project-agents block reads `(16)` not `(15)`
    - Trailer total comment reflects accurate counts (no `15 project`)
    - Total entry count for `scope: project` equals 16
  </acceptance_criteria>
  <done>catalog/agents.yaml uses flat names matching the new file locations, count comments are accurate, and the file is still valid YAML.</done>
</task>

<task type="auto">
  <name>Task 3: Update catalog/presets.yaml enterprise + enhanced additional_agents to flat names</name>
  <files>catalog/presets.yaml</files>
  <read_first>
    - catalog/presets.yaml (lines 200-290 — enterprise.additional_agents around 204-219 and enhanced.additional_agents around 269-284)
    - .planning/phases/03-bmad-decoupling/03-RESEARCH.md (## Concrete File Inventory > catalog/presets.yaml references to update)
  </read_first>
  <action>
Edit `catalog/presets.yaml`:

1. In the `enterprise.additional_agents` block (currently around lines 204-219), rewrite the 16 entries that use `analysis/`, `planning/`, `research/`, `review/` prefixes to flat unprefixed names. Example:

   BEFORE:
   ```yaml
         - analysis/codebase-analyzer
         - planning/dependency-mapper
   ```

   AFTER:
   ```yaml
         - codebase-analyzer
         - dependency-mapper
   ```

2. In the `enhanced.additional_agents` block (currently around lines 269-284), do the same for the same 16 entries. Leave the unrelated entries (`doc-updater`, `docs-lookup`, `refactor-cleaner`) untouched.

3. Validate the file is still parseable YAML and that every preset's referenced agent name now exists as a `name:` entry in `catalog/agents.yaml` (cross-check). If any preset references an agent name not present in catalog/agents.yaml after Task 2, that is a defect — fix the preset entry.

The 16 names that must appear in BOTH enterprise.additional_agents and enhanced.additional_agents (as flat strings, no prefix):
api-documenter, codebase-analyzer, data-analyst, pattern-detector, dependency-mapper, epic-optimizer, requirements-analyst, technical-decisions-curator, trend-spotter, user-journey-mapper, user-researcher, market-researcher, tech-debt-auditor, document-reviewer, technical-evaluator, test-coverage-analyzer
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const yaml=require('yaml');const presets=yaml.parse(fs.readFileSync('catalog/presets.yaml','utf8'));const agents=yaml.parse(fs.readFileSync('catalog/agents.yaml','utf8'));const catalogNames=new Set(agents.agents.map(a=>a.name));const need=['api-documenter','codebase-analyzer','data-analyst','pattern-detector','dependency-mapper','epic-optimizer','requirements-analyst','technical-decisions-curator','trend-spotter','user-journey-mapper','user-researcher','market-researcher','tech-debt-auditor','document-reviewer','technical-evaluator','test-coverage-analyzer'];const ent=presets.presets.enterprise.additional_agents||[];const enh=presets.presets.enhanced.additional_agents||[];for(const n of need){if(!ent.includes(n)){console.error('enterprise missing',n);process.exit(1);}if(!enh.includes(n)){console.error('enhanced missing',n);process.exit(1);}}for(const a of [...ent,...enh]){if(a.includes('/')){console.error('still prefixed',a);process.exit(1);}if(!catalogNames.has(a)){console.error('preset references unknown agent',a);process.exit(1);}}console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `catalog/presets.yaml` parses as valid YAML
    - `enterprise.additional_agents` contains all 16 flat names
    - `enhanced.additional_agents` contains all 16 flat names
    - Neither block contains any string with `/` in it for the 16 migrated agents
    - Every entry in `enterprise.additional_agents` and `enhanced.additional_agents` resolves to an existing `name:` in `catalog/agents.yaml`
    - `npm test` passes (catalog and preset validation tests must stay green)
  </acceptance_criteria>
  <done>Preset resolution still works end-to-end with the new flat agent names; catalog and preset tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Refactor-only file moves and metadata edits. No new attack surface; no untrusted input handling, no auth, no network.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering | catalog YAML edits | mitigate | Round-trip parse with `yaml` package before write; cross-check preset references against catalog agent names; npm test gates the change |
| T-03-03 | (none) | agent .md migration | accept | Pure text/file copy with metadata strip; agents are static prompt files with no executable surface |
</threat_model>

<verification>
- 16 flat agent files exist under assets/agents/ with stripped frontmatter
- catalog/agents.yaml has 16 flat entries and accurate count comments
- catalog/presets.yaml enterprise + enhanced presets reference flat names only
- npm test still passes (catalog and preset tests catch any drift)
</verification>

<success_criteria>
1. `ls assets/agents/*.md | wc -l` returns ≥ 22 (6 existing + 16 new)
2. `grep -E "^name: (bmm|mob)-" assets/agents/*.md` returns no matches
3. `grep -E "^- name: (analysis|planning|research|review)/" catalog/agents.yaml` returns no matches
4. `grep -E "^      - (analysis|planning|research|review)/" catalog/presets.yaml` returns no matches
5. `npm test` exits 0
6. Source files under `src/module/agents/` still present (Plan 03 will delete them)
</success_criteria>

<output>
After completion, create `.planning/phases/03-bmad-decoupling/03-02-SUMMARY.md` listing migrated files, catalog updates, and preset updates.
</output>
