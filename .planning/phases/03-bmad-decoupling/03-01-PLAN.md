---
phase: 03-bmad-decoupling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-bmad-decoupling/03-01-AUDIT.md
autonomous: true
requirements: [BMA-02]
must_haves:
  truths:
    - "Repo-wide audit confirms whether anything outside tools/package-skills.js and test/test-installation.js still references src/module/skills/"
    - "Audit result is recorded as a written artifact (AUDIT.md) so downstream plans cite a known state, not an assumption"
  artifacts:
    - path: ".planning/phases/03-bmad-decoupling/03-01-AUDIT.md"
      provides: "Documented audit result for D-07"
      contains: "src/module/skills"
  key_links:
    - from: "03-01-AUDIT.md"
      to: "Plan 03 (deletion of src/module/skills/)"
      via: "audit conclusion authorizes or blocks deletion"
      pattern: "SAFE TO DELETE|DEVIATION"
---

<objective>
Execute the D-07 audit: scan the repo for any reference to `src/module/skills/` outside the files already slated for deletion (`tools/package-skills.js`, the BMAD test block in `test/test-installation.js`). Record the findings as a small AUDIT.md so Plan 03 has a written authorization to delete the directory.

Purpose: D-07 explicitly forbids assuming `src/module/skills/` is unused. Researcher already did a high-confidence audit and recommended SAFE TO DELETE, but this plan re-verifies on the working tree at execution time and produces a citable artifact.

Output: `.planning/phases/03-bmad-decoupling/03-01-AUDIT.md` with grep results and a SAFE-TO-DELETE or DEVIATION verdict.
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-bmad-decoupling/03-CONTEXT.md
@.planning/phases/03-bmad-decoupling/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep audit src/module/skills/ references and write AUDIT.md</name>
  <files>.planning/phases/03-bmad-decoupling/03-01-AUDIT.md</files>
  <read_first>
    - .planning/phases/03-bmad-decoupling/03-CONTEXT.md (D-07 wording)
    - .planning/phases/03-bmad-decoupling/03-RESEARCH.md (## src/module/skills/ Audit section)
    - tools/package-skills.js (the only known writer — confirm it's the only one)
    - bin/gomad-cli.js (confirm only line 53-59 references src/module/skills via `package` command description)
  </read_first>
  <action>
Run two greps from project root, scoped to source dirs only (exclude BMAD/, node_modules/, .planning/, package-lock.json):

  grep -rn "src/module/skills" bin/ tools/ src/ test/ assets/ catalog/ package.json README.md CLAUDE.md
  grep -rn "module/skills" bin/ tools/ src/ test/ assets/ catalog/

Record EVERY hit verbatim in `.planning/phases/03-bmad-decoupling/03-01-AUDIT.md` using this exact template:

```markdown
# Phase 3 — D-07 Audit: src/module/skills/ References

**Audited:** {ISO date}
**Scope:** bin/ tools/ src/ test/ assets/ catalog/ package.json README.md CLAUDE.md
**Excluded:** BMAD/, node_modules/, .planning/, package-lock.json

## Hits

```
{paste raw grep output here, every line}
```

## Classification

| File:Line | Reference | Survives Plan 03? | Notes |
|-----------|-----------|-------------------|-------|
| {file}:{line} | {snippet} | yes/no | {why} |

## Verdict

{ONE OF:}
- **SAFE TO DELETE** — every reference is inside a file Plan 03 already deletes (tools/package-skills.js, BMAD test block in test/test-installation.js, bin/gomad-cli.js `package` command lines 53-59). Plan 03 is authorized to `rm -rf src/module/skills/` as part of `src/module/` removal.
- **DEVIATION** — the following references survive Plan 03 deletions and must be addressed first: {list}
```

Fill in the verdict based on the actual grep output. The expected verdict per researcher (HIGH confidence) is SAFE TO DELETE; if any unexpected reference appears (e.g., a new consumer added since research), mark DEVIATION and list specifics.

Do NOT delete any files in this task. Audit only.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-bmad-decoupling/03-01-AUDIT.md && grep -E "SAFE TO DELETE|DEVIATION" .planning/phases/03-bmad-decoupling/03-01-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/03-bmad-decoupling/03-01-AUDIT.md` exists
    - File contains the literal string `src/module/skills` at least once
    - File contains exactly one of `SAFE TO DELETE` or `DEVIATION` in the Verdict section
    - File contains the raw grep output (not paraphrased) under the `## Hits` heading
    - No files under `src/module/skills/` were deleted by this task: `ls src/module/skills/ | wc -l` returns the same count as before the task ran (≥14)
  </acceptance_criteria>
  <done>AUDIT.md committed with a verdict; Plan 03 has written authorization (or a documented blocker) before deleting src/module/skills/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Refactor-only audit; no new attack surface. No untrusted input crosses any boundary in this plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | (none) | audit script | accept | Read-only grep of repo files; no execution of external code; no new attack surface introduced |
</threat_model>

<verification>
- AUDIT.md exists, has a verdict, and lists every grep hit verbatim
- No files deleted in this plan
- src/module/skills/ directory still present and intact
</verification>

<success_criteria>
A single committed `03-01-AUDIT.md` whose verdict authorizes (or blocks with reasons) the deletion of `src/module/skills/` in Plan 03.
</success_criteria>

<output>
After completion, create `.planning/phases/03-bmad-decoupling/03-01-SUMMARY.md` summarizing the verdict and listing any deviations discovered.
</output>
