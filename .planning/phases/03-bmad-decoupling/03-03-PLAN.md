---
phase: 03-bmad-decoupling
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - package.json
  - bin/gomad-cli.js
  - tools/package-skills.js
  - src/module/module.yaml
  - src/module/module-help.csv
  - src/module/agents/skill-manifest.yaml
  - src/module/agents/analysis/api-documenter.md
  - src/module/agents/analysis/codebase-analyzer.md
  - src/module/agents/analysis/data-analyst.md
  - src/module/agents/analysis/pattern-detector.md
  - src/module/agents/planning/dependency-mapper.md
  - src/module/agents/planning/epic-optimizer.md
  - src/module/agents/planning/requirements-analyst.md
  - src/module/agents/planning/technical-decisions-curator.md
  - src/module/agents/planning/trend-spotter.md
  - src/module/agents/planning/user-journey-mapper.md
  - src/module/agents/planning/user-researcher.md
  - src/module/agents/research/market-researcher.md
  - src/module/agents/research/tech-debt-auditor.md
  - src/module/agents/review/document-reviewer.md
  - src/module/agents/review/technical-evaluator.md
  - src/module/agents/review/test-coverage-analyzer.md
  - src/module/skills/
autonomous: true
requirements: [BMA-01, BMA-02, BMA-03, BMA-05]
must_haves:
  truths:
    - "package.json has no peerDependencies key and no bmad-method in dependencies"
    - "package.json files array contains assets/ instead of global/ (Phase 2 carryover bug fixed)"
    - "src/module/ directory does not exist on disk"
    - "tools/package-skills.js does not exist on disk"
    - "bin/gomad-cli.js no longer registers a `package` command and no longer imports package-skills.js"
    - "No file under bin/, tools/, src/, assets/, catalog/ contains the literal string `bmad-method` or imports it"
    - "node_modules/bmad-method/ is removed after a fresh npm install"
  artifacts:
    - path: "package.json"
      provides: "Cleaned manifest with no BMAD coupling and correct files array"
      contains: "\"assets/\""
    - path: "bin/gomad-cli.js"
      provides: "CLI without `package` command"
  key_links:
    - from: "package.json files array"
      to: "assets/ directory"
      via: "npm publish includes assets/"
      pattern: "\"assets/\""
    - from: "bin/gomad-cli.js"
      to: "(deleted) tools/package-skills.js"
      via: "import removed"
      pattern: "package-skills"
---

<objective>
Execute the irreversible deletions and edits that complete BMAD decoupling: remove `peerDependencies` from `package.json`, fix the `package.json` files-array carryover bug from Phase 2 (`global/` → `assets/`), delete `tools/package-skills.js`, delete the `package` CLI command from `bin/gomad-cli.js`, and delete the entire `src/module/` directory tree (module.yaml, module-help.csv, agents/ subtree including skill-manifest.yaml, and skills/ — authorized by Plan 01's audit). Then run `npm install` to regenerate the lockfile and purge `node_modules/bmad-method/`.

Purpose: Satisfies BMA-01, BMA-02, BMA-03, BMA-05 in one coherent surgical pass. Plan 02 already moved the 16 agents to `assets/agents/`, so deleting `src/module/agents/` is now safe.

Output: A repo with zero BMAD-METHOD coupling at runtime and a corrected publish manifest.
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-bmad-decoupling/03-CONTEXT.md
@.planning/phases/03-bmad-decoupling/03-RESEARCH.md
@.planning/phases/03-bmad-decoupling/03-01-AUDIT.md

<interfaces>
<!-- package.json BEFORE (verified — current state at /Users/rockie/Documents/GitHub/xgent/gomad/package.json): -->
```json
{
  "name": "gomad",
  ...
  "keywords": [...],
  "author": "Rockie Guo",
  "license": "MIT",
  "peerDependencies": {
    "bmad-method": "^6.x"
  },
  "dependencies": {...},
  ...
  "files": [
    "bin/",
    "src/",
    "global/",
    "catalog/",
    "tools/",
    "README.md"
  ]
}
```

<!-- package.json AFTER target shape: -->
<!-- - peerDependencies key entirely removed (NOT set to {}) -->
<!-- - files array entry "global/" replaced with "assets/" -->
<!-- - "src/" entry removed because src/module/ is the only thing under src/ and we're deleting it (verify with `ls src/` after deletion; if src/ becomes empty, drop the "src/" line too) -->

<!-- bin/gomad-cli.js BEFORE — lines 53-59 (verified): -->
```js
program
  .command('package')
  .description('Package selected skills into src/module/skills/ with manifests')
  .action(async () => {
    const { packageSkills } = await import('../tools/package-skills.js');
    packageSkills();
  });
```
<!-- AFTER: those 7 lines deleted, with the surrounding blank lines tidied so the file remains well-formatted. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit package.json — drop peerDependencies, fix files array global/ -> assets/</name>
  <files>package.json</files>
  <read_first>
    - package.json (full file — confirm peerDependencies block at lines 23-25 and files array at lines 36-43)
    - .planning/phases/03-bmad-decoupling/03-RESEARCH.md (## Concrete File Inventory > package.json edits — pitfall #4 about deleting key vs setting to {})
  </read_first>
  <action>
Edit `package.json` with two precise changes:

1. **Remove the `peerDependencies` block entirely.** The current block (lines 23-25) is:
   ```json
     "peerDependencies": {
       "bmad-method": "^6.x"
     },
   ```
   Delete the key, the object, and the trailing comma. Do NOT leave `"peerDependencies": {}`. After this edit, `JSON.parse(...).peerDependencies` MUST be `undefined`.

2. **Fix the files array.** The current array at lines 36-43 is:
   ```json
     "files": [
       "bin/",
       "src/",
       "global/",
       "catalog/",
       "tools/",
       "README.md"
     ]
   ```
   Replace `"global/"` with `"assets/"`. Also evaluate whether `"src/"` should remain: this plan deletes `src/module/` which is the only thing currently under `src/`. Run `ls src/` after Task 4 (deletion task) — if `src/` becomes empty or is removed entirely, also remove the `"src/"` line from the files array. If `src/` still has content, keep `"src/"`. Final array should look like:
   ```json
     "files": [
       "bin/",
       "assets/",
       "catalog/",
       "tools/",
       "README.md"
     ]
   ```
   (with `"src/"` retained only if `src/` has surviving contents)

3. Validate by running `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` — must exit 0.

4. After the JSON edits AND after Tasks 2-4 below have completed, run `npm install` from the project root to regenerate `package-lock.json` and purge `node_modules/bmad-method/`. NOTE: order matters — see Task ordering note below. The `npm install` step is part of Task 5, not this task.
  </action>
  <verify>
    <automated>node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8'));if(p.peerDependencies!==undefined){console.error('peerDependencies still present');process.exit(1);}if(p.dependencies&&p.dependencies['bmad-method']){console.error('bmad-method in dependencies');process.exit(1);}if(!p.files.includes('assets/')){console.error('files missing assets/');process.exit(1);}if(p.files.includes('global/')){console.error('files still has global/');process.exit(1);}console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` parses as valid JSON
    - `JSON.parse(...).peerDependencies === undefined` (key absent, not `{}`)
    - `JSON.parse(...).dependencies['bmad-method']` is `undefined`
    - `JSON.parse(...).files` contains `"assets/"`
    - `JSON.parse(...).files` does NOT contain `"global/"`
    - `grep -n "bmad-method" package.json` returns no matches
  </acceptance_criteria>
  <done>package.json is decoupled from bmad-method and the publish files array points at the actual asset directory.</done>
</task>

<task type="auto">
  <name>Task 2: Delete `package` command from bin/gomad-cli.js</name>
  <files>bin/gomad-cli.js</files>
  <read_first>
    - bin/gomad-cli.js (full file — confirm `package` command at lines 53-59 and that no other code references package-skills)
  </read_first>
  <action>
Delete the entire `program.command('package')...` block from `bin/gomad-cli.js`. The exact lines to remove (currently 53-59):

```js
program
  .command('package')
  .description('Package selected skills into src/module/skills/ with manifests')
  .action(async () => {
    const { packageSkills } = await import('../tools/package-skills.js');
    packageSkills();
  });
```

Also remove the blank line that separated this block from the next (so the file does not end up with a double blank line). The next remaining command after deletion should be `program.command('mcp')`.

Verify nothing else in `bin/gomad-cli.js` references `package-skills` or `packageSkills`. If any stray reference remains, remove it.
  </action>
  <verify>
    <automated>! grep -n "package-skills\|packageSkills\|command('package')" bin/gomad-cli.js && node bin/gomad-cli.js --help | grep -v "^\s*package\b" > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "package-skills" bin/gomad-cli.js` returns no matches
    - `grep -n "packageSkills" bin/gomad-cli.js` returns no matches
    - `grep -n "command('package')" bin/gomad-cli.js` returns no matches
    - `node bin/gomad-cli.js --help` exits 0 and the output does NOT contain a standalone `package` command line (it may still contain the substring "package" inside other text, e.g., "Package selected skills" should be GONE)
    - `node bin/gomad-cli.js --help` still shows `install`, `curate`, `status`, `uninstall`, `mcp`
  </acceptance_criteria>
  <done>The `gomad package` command no longer exists; CLI surface area is reduced; nothing imports tools/package-skills.js.</done>
</task>

<task type="auto">
  <name>Task 3: Delete tools/package-skills.js</name>
  <files>tools/package-skills.js</files>
  <read_first>
    - tools/package-skills.js (confirm contents one last time before deletion — file exists and is the BMAD skill manifest generator)
    - bin/gomad-cli.js (confirm Task 2 already removed the only known import; if not, fail)
  </read_first>
  <action>
After confirming Task 2 has removed all references from `bin/gomad-cli.js`, delete `tools/package-skills.js`:

  rm tools/package-skills.js

Verify no other source file under `bin/`, `tools/`, `src/`, `test/`, `assets/`, `catalog/` still imports or names `package-skills.js`. If any do, remove or update them. Note: Plan 04 will handle the test file edits, so existing references inside `test/test-installation.js` lines 21-67 and `test/test-module.js` are EXPECTED to still exist at this point and will be cleaned up in Plan 04. Do NOT touch the test files in this plan.
  </action>
  <verify>
    <automated>! test -f tools/package-skills.js && ! grep -rn "package-skills" bin/ tools/ src/ assets/ catalog/ 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `tools/package-skills.js` does not exist (`test -f tools/package-skills.js` returns false)
    - `grep -rn "package-skills" bin/ tools/ src/ assets/ catalog/` returns no matches (test/ may still reference it — Plan 04 cleans that up)
    - `grep -rn "packageSkills" bin/ tools/ src/ assets/ catalog/` returns no matches
  </acceptance_criteria>
  <done>The BMAD skill packager is gone from the repo; only test/ still references it (cleaned in Plan 04).</done>
</task>

<task type="auto">
  <name>Task 4: Delete src/module/ directory tree (after audit authorization)</name>
  <files>src/module/</files>
  <read_first>
    - .planning/phases/03-bmad-decoupling/03-01-AUDIT.md (verdict MUST be SAFE TO DELETE; if DEVIATION, STOP and surface the blocker)
    - .planning/phases/03-bmad-decoupling/03-CONTEXT.md (D-06 — full deletion authorized)
    - assets/agents/codebase-analyzer.md (sanity check — Plan 02 already migrated this; deleting src/module/agents/ is safe)
  </read_first>
  <action>
1. **Gate check:** Read `.planning/phases/03-bmad-decoupling/03-01-AUDIT.md`. If the Verdict section does NOT contain "SAFE TO DELETE", STOP this task immediately and surface the audit's DEVIATION list as a blocker. Do not proceed.

2. **Sanity check Plan 02:** Verify all 16 migrated agents exist in `assets/agents/` before deleting their source. Run:
   ```
   for n in api-documenter codebase-analyzer data-analyst pattern-detector dependency-mapper epic-optimizer requirements-analyst technical-decisions-curator trend-spotter user-journey-mapper user-researcher market-researcher tech-debt-auditor document-reviewer technical-evaluator test-coverage-analyzer; do
     test -f "assets/agents/$n.md" || echo "MISSING: $n"
   done
   ```
   If any are missing, STOP — Plan 02 was incomplete.

3. **Delete the entire `src/module/` directory:**
   ```
   rm -rf src/module
   ```
   This removes:
   - `src/module/module.yaml` (BMAD module registration — D-06)
   - `src/module/module-help.csv` (BMAD help registration — D-06)
   - `src/module/agents/skill-manifest.yaml` (BMAD canonical-ID manifest — D-05)
   - `src/module/agents/{analysis,planning,research,review}/*.md` (16 source files now duplicated in assets/agents/)
   - `src/module/skills/` (14 packaged skill duplicates — authorized by D-07 audit)

4. **Check if `src/` itself is now empty.** Run `ls src/`. If empty, also remove the now-empty `src/` directory: `rmdir src`. (If `src/` still has content from something else, leave it.)

5. **Verify nothing under bin/, tools/, assets/, catalog/ still references `src/module/`.** Run:
   ```
   grep -rn "src/module" bin/ tools/ assets/ catalog/ package.json
   ```
   The expected result is zero hits. (Test files under test/ are handled by Plan 04 — do not touch them here.)
  </action>
  <verify>
    <automated>! test -e src/module && ! grep -rn "src/module" bin/ tools/ assets/ catalog/ package.json 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `test -e src/module` returns false (directory does not exist)
    - `test -e src/module/skills` returns false
    - `test -e src/module/agents` returns false
    - `test -e src/module/module.yaml` returns false
    - `grep -rn "src/module" bin/ tools/ assets/ catalog/ package.json` returns no matches
    - All 16 migrated agents still exist under `assets/agents/` (deletion did not collateral-damage them)
  </acceptance_criteria>
  <done>src/module/ is gone; BMAD module registration artifacts are erased; src/module/skills/ packaged duplicates removed per audit.</done>
</task>

<task type="auto">
  <name>Task 5: Run npm install to regenerate lockfile and purge node_modules/bmad-method/</name>
  <files>package-lock.json</files>
  <read_first>
    - package.json (confirm Task 1 already removed peerDependencies — read after Task 1 completes)
    - package-lock.json (sanity check — confirm bmad-method appears in lockfile before npm install runs)
  </read_first>
  <action>
After Tasks 1-4 are complete, run a fresh npm install to regenerate the lockfile and remove the now-unreferenced bmad-method package:

  npm install

Expected outcome:
- `package-lock.json` is rewritten and no longer contains `bmad-method` entries
- `node_modules/bmad-method/` is removed (npm prunes it because it is no longer in `peerDependencies` and no `package.json` references it)
- `npm install` exits 0

If `npm install` reports errors related to the deletions, surface them — do NOT manually edit `package-lock.json`. Likely failure modes:
- A test or tool still imports `bmad-method` somewhere unaccounted for → grep `bmad-method` across `bin/ tools/ src/ test/` and fix.
- Network issues → retry once; if still failing, surface to user.

After the install succeeds, verify:
  grep -l "bmad-method" package-lock.json && echo "FAIL: lockfile still references bmad-method" || echo "ok"
  test -d node_modules/bmad-method && echo "FAIL: node_modules still has bmad-method" || echo "ok"

Note: the existing vendored `BMAD/` directory at the repo root is NOT touched by this task — it is a reference sub-repo, not an npm package. Plan 04 will scope its decoupling test to exclude `BMAD/` so it does not generate false positives.
  </action>
  <verify>
    <automated>npm install --silent && ! grep -l "\"bmad-method\"" package-lock.json && ! test -d node_modules/bmad-method</automated>
  </verify>
  <acceptance_criteria>
    - `npm install` exits 0
    - `grep "\"bmad-method\"" package-lock.json` returns no matches
    - `test -d node_modules/bmad-method` returns false
    - `npm test` exits 0 (sanity — Plan 04 will add the decoupling test, but existing tests must still pass after this plan)
  </acceptance_criteria>
  <done>node_modules and lockfile are fully purged of bmad-method; the project installs cleanly without the BMAD-METHOD peer dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Refactor + deletion only. No new attack surface; no untrusted input crosses any boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | package.json edits | mitigate | JSON.parse round-trip validation; npm install gates the change |
| T-03-05 | Denial of Service | accidental over-deletion (rm -rf src/module) | mitigate | Audit gate (Plan 01) + sanity check that all 16 migrated agents exist in assets/agents/ before deleting their source |
| T-03-06 | (none) | CLI surface reduction | accept | Removing a command reduces, never expands, the attack surface |
</threat_model>

<verification>
- package.json has no peerDependencies and files array uses assets/
- bin/gomad-cli.js has no `package` command
- tools/package-skills.js does not exist
- src/module/ does not exist
- node_modules/bmad-method/ does not exist
- package-lock.json has no bmad-method references
- npm test still passes (with old test files — Plan 04 cleans them up)
</verification>

<success_criteria>
1. `node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8'));console.log(p.peerDependencies===undefined)"` prints `true`
2. `grep -n "bmad-method" package.json` returns no matches
3. `test -f tools/package-skills.js` returns false
4. `test -e src/module` returns false
5. `grep -rn "bmad-method" bin/ tools/ src/ assets/ catalog/ 2>/dev/null` returns no matches (test/ excluded — Plan 04)
6. `test -d node_modules/bmad-method` returns false
7. `node bin/gomad-cli.js --help` exits 0 and lists install/curate/status/uninstall/mcp but not `package`
</success_criteria>

<output>
After completion, create `.planning/phases/03-bmad-decoupling/03-03-SUMMARY.md` listing every deletion, the package.json edits, and the npm install outcome.
</output>
