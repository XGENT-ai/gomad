---
phase: 08-prd-product-brief-content-refinement
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gomad-skills/2-plan-workflows/gm-create-prd/templates/prd-template.md
  - src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md
  - src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md
  - src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md
autonomous: true
requirements:
  - PRD-02
  - PRD-03
  - PRD-04
  - PRD-06
user_setup: []

must_haves:
  truths:
    - "step-04-journeys.md §2 rewrites persona+journey framework from narrative-story format to action-oriented format (name+role+goal; 'Actor performs X, system responds Y, outcome Z'); §2 header preserved; §3-§7 numbering preserved"
    - "step-09-functional.md emits FR-NN format (2-digit zero-padded, dash-separated) with inline `- AC: Given [state], when [action], then [observable outcome]` sub-bullets (2–4 per FR floor-of-2); Content Structure code fence updated; Self-Validation Quality Check gains AC + OOS validation bullets"
    - "step-09-functional.md Content Structure emits a `## Out of Scope` section with OOS-NN entries in the format `- **OOS-NN**: <capability> — Reason: <why>`"
    - "step-11-polish.md has new §2c `Coding-Agent Readiness Review` with the banned-phrase checklist from D-61, placed between §2b and §3"
    - "templates/prd-template.md frontmatter unchanged; the template stays minimal (no body-section additions)"
    - "All 4 files preserve MANDATORY EXECUTION RULES (step files), A/P/C menu (step files), SUCCESS/FAILURE METRICS rubrics, stepsCompleted frontmatter contract"
  artifacts:
    - path: "src/gomad-skills/2-plan-workflows/gm-create-prd/templates/prd-template.md"
      provides: "PRD output template (minimal, frontmatter-driven)"
      min_lines: 7
    - path: "src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md"
      provides: "Action-oriented journey mapping step (persona stripped to name+role+goal)"
      contains: "Actor performs"
    - path: "src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md"
      provides: "FR format with inline AC sub-bullets + OOS emission"
      contains: "FR-NN"
    - path: "src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md"
      provides: "Polish step with Coding-Agent Readiness Review §2c"
      contains: "Coding-Agent Readiness Review"
  key_links:
    - from: "src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md"
      to: "downstream validator (gm-validate-prd/steps-v/step-v-12)"
      via: "Content Structure emits '## Out of Scope' as H2 header scannable by step-v-12"
      pattern: "## Out of Scope"
    - from: "src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md"
      to: "downstream epics (gm-create-epics-and-stories/steps/step-01)"
      via: "FR-NN format matches 'FR1: or similar' clause"
      pattern: "FR-\\d{2}:"
    - from: "src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md"
      to: "downstream architect (gm-create-architecture/steps/step-02-context.md:64)"
      via: "AC sub-bullets extractable from FR blocks"
      pattern: "- AC: Given"
---

<objective>
Implement the three content-shape changes that define the sharpened PRD output:

1. **step-04-journeys.md** — rewrite §2 persona/journey framework from narrative-story-based to action-oriented (D-59, PRD-02). Preserve §2 header for numbering stability; rewrite body in place.
2. **step-09-functional.md** — emit FR-NN format with inline AC sub-bullets (Given/When/Then, floor=2, cap=4) and add a top-level `## Out of Scope` section to the Content Structure (D-44, D-45, D-46, D-47, D-58, PRD-04).
3. **step-11-polish.md** — add §2c `Coding-Agent Readiness Review` with the D-61 banned-phrase checklist, parallel to existing §2b (PRD-02, PRD-03 facilitator gate).
4. **templates/prd-template.md** — confirm minimal template shape is preserved; no body-section additions (per RESEARCH.md Q4 + PATTERNS.md Shared Patterns — template stays header-only; step-09 Content Structure is the source of the OOS section emission).

Purpose: Deliver the dev-ready requirement shape (PRD-04) and close residual strip targets in step-04/step-11 (PRD-02). Structural compatibility preserved: `## Out of Scope` H2 closes the existing validator gap in gm-validate-prd/steps-v/step-v-12; FR-NN matches the "or similar" clause in gm-create-epics-and-stories/step-01-validate-prerequisites.md:80; inline AC sub-bullets are extractable by gm-create-architecture/steps/step-02-context.md:64 (PRD-06).

Output: 4 edited markdown files. No new files. No new deps. No downstream-skill changes.
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-prd-product-brief-content-refinement/08-CONTEXT.md
@.planning/phases/08-prd-product-brief-content-refinement/08-RESEARCH.md
@.planning/phases/08-prd-product-brief-content-refinement/08-PATTERNS.md
@.planning/REQUIREMENTS.md
@src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md
@src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md
@src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md
@src/gomad-skills/2-plan-workflows/gm-create-prd/templates/prd-template.md
@src/gomad-skills/2-plan-workflows/gm-validate-prd/steps-v/step-v-12-completeness-validation.md
@src/gomad-skills/3-solutioning/gm-create-epics-and-stories/steps/step-01-validate-prerequisites.md
@src/gomad-skills/3-solutioning/gm-create-architecture/steps/step-02-context.md

<interfaces>
<!-- Downstream contracts this plan must NOT break (PRD-06) -->

<!-- 1. gm-validate-prd/steps-v/step-v-12 scans for these H2 headers -->
Required PRD sections (step-v-12 header scan):
- ## Executive Summary
- ## Success Criteria
- ## Product Scope
- ## User Journeys
- ## Functional Requirements
- ## Out of Scope           ← NEW per D-58 (closes the "in-scope and out-of-scope defined" validator gap)
- ## Non-Functional Requirements

<!-- 2. gm-create-epics-and-stories/step-01-validate-prerequisites.md:80 -->
"Look for numbered items like 'FR1:', 'Functional Requirement 1:', or similar"
← D-44's `FR-01:` matches the "or similar" clause.

<!-- 3. gm-create-architecture/step-02-context.md:64 -->
"Extract acceptance criteria for technical implications"
← D-45 inline AC sub-bullets surface through this path:
- FR-01: [Actor] can [capability]
    - AC: Given [state], when [action], then [observable outcome]

<!-- 4. REQ-ID house style (.planning/REQUIREMENTS.md) -->
PRD-01, REL-02, INSTALL-05, CMD-01 — 2-digit zero-padded, dash-separated, UPPER prefix.
D-44's FR-NN / NFR-NN / OOS-NN follow identical convention.

<!-- 5. Canonical §2b in step-11-polish.md (D-61 parallel for §2c) -->
### 2b. Brainstorming Reconciliation (if brainstorming input exists)

**Check the PRD frontmatter `inputDocuments` for any brainstorming document** [...]

1. **Load the brainstorming document** [...]
2. **Cross-reference against the PRD** [...]
[...]

**Why this matters**: [...]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite step-04-journeys.md §2 from narrative-story framework to action-oriented journeys (D-59)</name>
  <files>src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md</files>
  <read_first>
    - src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md (full file; §2 "Create Narrative Story-Based Journeys" currently L60–88; §5 Minimum Coverage L189–195 MUST be preserved)
    - .planning/phases/08-prd-product-brief-content-refinement/08-CONTEXT.md D-59 (persona framework strip semantics; keep name+role+goal)
    - .planning/phases/08-prd-product-brief-content-refinement/08-PATTERNS.md (pattern for §2 header preservation / in-place body rewrite per assumption A7)
    - .planning/phases/08-prd-product-brief-content-refinement/08-RESEARCH.md Pitfall 5 (renumbering hazard — §2 body rewrite preserves §3-§7 numbering intact)
  </read_first>
  <action>
In-place body replacement of §2 in `src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md`.

**Locate the block** starting with `### 2. Create Narrative Story-Based Journeys` (currently line 60) and ending before `### 3. Guide Journey Exploration` (currently line 90).

**Replace the entire §2 block** (from the `### 2.` heading through the blank line immediately before `### 3. Guide Journey Exploration`) with:

```markdown
### 2. Create Action-Oriented Journeys

For each user type, create action-oriented journeys with **name + role + goal** only. Persona framework is minimal — coding agents implement behaviors, not emotional arcs.

#### Journey Creation Process:

**Persona fields (minimal):**
- **Name** — identifier only (no demographic markers, no backstory, no personality traits)
- **Role** — how they relate to the system (e.g., "admin", "end-user", "API consumer")
- **Goal** — observable outcome they need from the system

**If Using Existing Persona from Input Documents:**
Extract name + role + goal only; discard any backstory, emotional-arc probes, or demographic markers from the source material. If the input persona has no explicit role, derive it from the user's relationship to the system in the brief.

**If Creating New Persona:**
Define name, role, and goal in a single line each. Do NOT prompt the user for personality, situation, obstacle, or solution-narrative — those are implementation-agnostic and belong in FRs downstream.

**Journey prose shape (mandatory form):**

"Actor performs X, system responds Y, outcome Z."

For each journey:
- Describe the triggering action (what the actor does)
- Describe the system's observable response
- Describe the resulting state / outcome
- Flag edge cases and error paths as separate journey entries (not emotional asides)

This form maps 1:1 to FR acceptance-criteria candidates in step 9 (Given/When/Then).

**Why this shape:** Downstream coding agents extract capability requirements from journey prose. Narrative structure (emotional arc, climax, resolution) adds noise; action-oriented prose maps directly to testable behaviors.
```

**Do NOT touch** sections §3 `Guide Journey Exploration` (starting at what is currently L90), §4 `Connect Journeys to Requirements` (L99), §5 `Aim for Comprehensive Coverage` with its Minimum Coverage list (L107–115), §6 `Generate User Journey Content` with its Content Structure code fence (L117–133), §7 `Present MENU OPTIONS` (L135+). These are preserved verbatim because downstream capability extraction depends on §5's Minimum Coverage list (Primary happy / Primary edge / Admin / Support / API).

**Do NOT renumber any section.** §2 header retained; §3-§7 keep their existing numbers. This preserves the internal cross-reference `using structure from step 6` in the A/P/C menu block and anywhere else (RESEARCH.md A7).

**Update the step's `## YOUR TASK:` statement** (currently line 34) from:

```
Create compelling narrative user journeys that leverage existing personas from product briefs and identify additional user types needed for comprehensive coverage.
```

to:

```
Create action-oriented user journeys with minimal personas (name + role + goal), identifying all user types needed for comprehensive capability coverage.
```

**Update FAILURE MODES section** (currently lines 171–181) — remove narrative-specific failure modes that are no longer relevant:
- Remove: `❌ Creating generic journeys without rich persona details and narrative`
- Remove: `❌ Missing emotional storytelling elements that make journeys compelling`

Keep all other failure modes (including `❌ Not mapping secondary users`, `❌ Not connecting journeys to capabilities`, etc.).

**Update SUCCESS METRICS section** (currently lines 162–169) — reword narrative-specific success metrics:
- Change: `✅ Rich narrative storytelling for each persona and journey` → `✅ Action-oriented journey prose for each persona (Actor performs X, system responds Y, outcome Z)`
- Change: `✅ Complete story-based journey mapping with emotional arc` → `✅ Complete action-oriented journey mapping covering all user types`
- Change: `✅ Minimum 3-4 compelling narrative journeys covering different user types` → `✅ Minimum 3-4 journeys covering different user types per the Minimum Coverage list`

**Preservation invariants:**
- `## MANDATORY EXECUTION RULES (READ FIRST):` block verbatim (L5–16)
- `## CONTEXT BOUNDARIES:` block verbatim
- §3, §4, §5 (Minimum Coverage), §6 (Content Structure code fence), §7 (menu) verbatim
- A/P/C menu block verbatim
- `## APPEND TO DOCUMENT:` block verbatim
- `## JOURNEY TYPES TO ENSURE:` block at end of file verbatim (this IS the Minimum Coverage list restated — Primary User Success Path, Edge Case, Admin/Operations, Support/Troubleshooting, API/Integration)
- `## NEXT STEP:` block verbatim
  </action>
  <verify>
    <automated>grep -q '### 2\. Create Action-Oriented Journeys' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md && ! grep -q 'Create Narrative Story-Based Journeys' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md && ! grep -q 'Opening Scene' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md && ! grep -q 'Rising Action' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md && ! grep -q 'realistic name and personality' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md && ! grep -q 'emotional journey' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md && grep -q 'Actor performs' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md && grep -q '### 3\. Guide Journey Exploration' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md && grep -q '### 5\. Aim for Comprehensive Coverage' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md && grep -q 'Primary user.*happy path\|Primary User.*Success Path' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md && grep -q '## MANDATORY EXECUTION RULES' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md && grep -qE '(Select.*\[A\].*\[P\].*\[C\])' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md && npm run lint:md -- src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '^### 2\. Create Action-Oriented Journeys$' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md` returns one line (new header)
    - `! grep -q 'Create Narrative Story-Based Journeys' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md` exits 0 (old title gone)
    - `! grep -qE '(Opening Scene|Rising Action|Climax|Resolution)' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md` exits 0 (narrative framework gone)
    - `! grep -q 'realistic name and personality' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md` exits 0
    - `! grep -q 'emotional journey\|emotional arc' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md` exits 0
    - `grep -q 'Actor performs' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md` exits 0 (new prose shape)
    - `grep -q 'name + role + goal' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md` exits 0 (minimal persona spec)
    - `grep -q '### 3\. Guide Journey Exploration' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md` exits 0 (numbering stable)
    - `grep -q '### 4\. Connect Journeys to Requirements' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md` exits 0
    - `grep -q '### 5\. Aim for Comprehensive Coverage' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md` exits 0
    - `grep -q '### 6\. Generate User Journey Content' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md` exits 0
    - `grep -q '### 7\. Present MENU OPTIONS' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md` exits 0
    - `grep -q '## JOURNEY TYPES TO ENSURE' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md` exits 0 (end-of-file Minimum Coverage restated preserved)
    - `grep -q '## MANDATORY EXECUTION RULES' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md` exits 0
    - `grep -qE '(Select.*\[A\].*\[P\].*\[C\])' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md` exits 0
    - `grep -q 'SUCCESS METRICS\|SYSTEM SUCCESS/FAILURE METRICS' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md` exits 0
    - `grep -c '^### [0-9]' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md` returns exactly 7 (§1–§7 count preserved; Pitfall 5 guard)
    - `npm run lint:md -- src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md` exits 0
  </acceptance_criteria>
  <done>
step-04 §2 body rewritten in place; §3-§7 numbering intact; action-oriented journey shape documented; Minimum Coverage list preserved; markdownlint passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Emit FR-NN format with inline Given/When/Then AC sub-bullets + OOS section in step-09-functional.md (D-44, D-45, D-46, D-47, D-58)</name>
  <files>src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md</files>
  <read_first>
    - src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md (full file; L94–107 Format block; L127–131 Quality Check; L137–156 Content Structure code fence)
    - .planning/phases/08-prd-product-brief-content-refinement/08-CONTEXT.md D-44, D-45, D-46, D-47, D-58 (locked semantics)
    - .planning/phases/08-prd-product-brief-content-refinement/08-PATTERNS.md (patterns 9–11: new FR format + Content Structure + Self-Validation Quality Check additions)
    - .planning/phases/08-prd-product-brief-content-refinement/08-RESEARCH.md Pitfall 1 (don't deviate from D-44 bullet hierarchy — tables break sub-bullet extraction)
    - .planning/phases/08-prd-product-brief-content-refinement/08-RESEARCH.md Pitfall 3 (template + step-09 emission both must be updated; verify via integration test in Plan 08-05)
    - src/gomad-skills/2-plan-workflows/gm-validate-prd/steps-v/step-v-12-completeness-validation.md (confirm `## Out of Scope` H2 header closes the "in-scope and out-of-scope defined" gap without downstream-skill edits)
    - src/gomad-skills/3-solutioning/gm-create-epics-and-stories/steps/step-01-validate-prerequisites.md:80 (confirm `FR-01:` matches "or similar" clause)
    - src/gomad-skills/3-solutioning/gm-create-architecture/steps/step-02-context.md:64 (confirm inline AC sub-bullets extractable)
    - .planning/REQUIREMENTS.md:70–84 (OOS entry format reference: `- **OOS-NN**: <capability> — Reason: <why>`)
  </read_first>
  <action>
Four edits to `src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md`.

**Edit 1 — Replace the Format block** (currently lines 94–107). Current text:

```markdown
**Format:**

- FR#: [Actor] can [capability] [context/constraint if needed]
- Number sequentially (FR1, FR2, FR3...)
- Aim for 20-50 FRs for typical projects

**Altitude Check:**
Each FR should answer "WHAT capability exists?" NOT "HOW it's implemented?"

**Examples:**

- ✅ "Users can customize appearance settings"
- ❌ "Users can toggle light/dark theme with 3 font size options stored in LocalStorage"
```

Replace with:

```markdown
**Format:**

Each FR is a top-level bullet with 2–4 inline AC sub-bullets in Given/When/Then form:

```
- FR-NN: [Actor] can [capability] [context/constraint if needed]
    - AC: Given [state], when [action], then [observable outcome]
    - AC: Given [state], when [action], then [observable outcome]
```

**FR numbering rules:**

- Use 2-digit zero-padded dash-separated IDs: `FR-01`, `FR-02`, ..., `FR-99`
- Number sequentially across all capability areas (do not restart per area)
- Aim for 20–50 FRs for typical projects

**AC rules:**

- Every FR MUST have ≥2 AC sub-bullets (floor: 2)
- Every FR MUST have ≤4 AC sub-bullets (cap: 4 — more belongs in downstream stories)
- AC wording is **Given / When / Then** (Gherkin-shaped, machine-verifiable by construction)
- AC sub-bullets indent 4 spaces under the FR bullet (markdown list continuation)

**Altitude Check:**
Each FR should answer "WHAT capability exists?" NOT "HOW it's implemented?"

**Examples:**

- ✅ `- FR-01: Users can customize appearance settings`
    - `- AC: Given a logged-in user on the settings page, when they toggle the theme, then the preference is persisted and applied immediately`
    - `- AC: Given no user session, when /settings is accessed, then the UI presents read-only defaults`
- ❌ `- FR1: Users can toggle light/dark theme with 3 font size options stored in LocalStorage` (implementation leakage; unpadded ID; missing AC)
```

Note: the inner code-fence in the replacement block uses triple-backticks. Escape them to four backticks or use indented-code form if the outer step-09 file uses triple-backticks. In practice, step-09 does not have nested fences, so this triple-backtick form is safe.

**Edit 2 — Extend the Self-Validation Quality Check block** (currently lines 127–131). Current text:

```markdown
**Quality Check:**

1. "Is each FR clear enough that someone could test whether it exists?"
2. "Is each FR independent (not dependent on reading other FRs to understand)?"
3. "Did I avoid vague terms like 'good', 'fast', 'easy'?" (Use NFRs for quality attributes)
```

Add two more numbered checks:

```markdown
**Quality Check:**

1. "Is each FR clear enough that someone could test whether it exists?"
2. "Is each FR independent (not dependent on reading other FRs to understand)?"
3. "Did I avoid vague terms like 'good', 'fast', 'easy'?" (Use NFRs for quality attributes)
4. "Does each FR have 2–4 AC sub-bullets in Given / When / Then form?"
5. "Is there an `## Out of Scope` section with at least one `OOS-NN` entry?"
```

**Edit 3 — Replace the Content Structure code fence** (currently lines 137–156). Current text (inside the fenced code block):

```markdown
## Functional Requirements

### [Capability Area Name]

- FR1: [Specific Actor] can [specific capability]
- FR2: [Specific Actor] can [specific capability]
- FR3: [Specific Actor] can [specific capability]

### [Another Capability Area]

- FR4: [Specific Actor] can [specific capability]
- FR5: [Specific Actor] can [specific capability]

[Continue for all capability areas discovered in conversation]
```

Replace the code-fenced content with:

```markdown
## Functional Requirements

### [Capability Area Name]

- FR-01: [Specific Actor] can [specific capability]
    - AC: Given [state], when [action], then [observable outcome]
    - AC: Given [state], when [action], then [observable outcome]

- FR-02: [Specific Actor] can [specific capability]
    - AC: Given [state], when [action], then [observable outcome]
    - AC: Given [state], when [action], then [observable outcome]

### [Another Capability Area]

- FR-03: [Specific Actor] can [specific capability]
    - AC: Given [state], when [action], then [observable outcome]
    - AC: Given [state], when [action], then [observable outcome]

[Continue for all capability areas discovered in conversation]

## Out of Scope

- **OOS-01**: <excluded capability> — Reason: <why excluded>
- **OOS-02**: <excluded capability> — Reason: <why excluded>
```

The `## Out of Scope` section lives OUTSIDE the `### [Capability Area]` hierarchy — it is a peer H2 to `## Functional Requirements`. Keep the enclosing ```markdown fence markers untouched.

**Edit 4 — Update the step intro / `## YOUR TASK:`.** Locate the `## YOUR TASK:` line (around line 42) or the equivalent task statement. No rewrite needed — the existing statement is already code-behavior-observable. BUT: add a one-line precursor above the "FUNCTIONAL REQUIREMENTS SYNTHESIS SEQUENCE:" header noting that OOS is now part of step-09's output:

Before `## FUNCTIONAL REQUIREMENTS SYNTHESIS SEQUENCE:`, append this block after the existing `## CRITICAL IMPORTANCE:` block (which currently ends around L42):

```markdown
## SCOPE BOUNDARY:

Step 9 emits BOTH the functional-requirement inventory (FR-NN with inline AC sub-bullets) AND an explicit `## Out of Scope` section (OOS-NN entries). The validator (gm-validate-prd/step-v-12) scans for both headers. The architect uses OOS entries to prevent over-building. Keep OOS concise — capability + reason per entry.
```

**Preservation invariants:**
- `## MANDATORY EXECUTION RULES` verbatim
- `## CRITICAL IMPORTANCE:` block (L34–42) verbatim — this is the capability-contract framing downstream skills depend on
- Steps 1, 2, 3 (`Understand FR Purpose`, `Review Existing Content`, `Organize Requirements by Capability Area`) verbatim — only Format (§4), Quality Check (§5), Content Structure (§6) are edited
- `## APPEND TO DOCUMENT:` block verbatim
- A/P/C menu block verbatim
- `## SUCCESS METRICS:` / `## FAILURE MODES:` rubrics verbatim — BUT add one success metric and one failure mode:
  - Add to SUCCESS METRICS: `✅ Every FR has 2–4 AC sub-bullets in Given/When/Then form`
  - Add to SUCCESS METRICS: `✅ ## Out of Scope section emitted with OOS-NN entries`
  - Add to FAILURE MODES: `❌ FRs missing AC sub-bullets or AC sub-bullets fewer than 2`
  - Add to FAILURE MODES: `❌ ## Out of Scope section missing or empty`
- `## CAPABILITY CONTRACT REMINDER:` verbatim
- `## NEXT STEP:` verbatim
  </action>
  <verify>
    <automated>grep -q 'FR-NN' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md && grep -q 'FR-01' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md && grep -q 'AC: Given' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md && grep -q 'Given / When / Then\|Given/When/Then' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md && grep -q '## Out of Scope' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md && grep -q '\*\*OOS-01\*\*' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md && ! grep -qE '^- FR#:' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md && grep -q "2.4 AC sub-bullets\|≥2 AC sub-bullets\|2-4 AC sub-bullets" src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md && grep -q '## MANDATORY EXECUTION RULES' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md && grep -q '## CRITICAL IMPORTANCE' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md && grep -q '## APPEND TO DOCUMENT' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md && grep -qE '(Select.*\[A\].*\[P\].*\[C\])' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md && grep -q '## SUCCESS METRICS' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md && grep -q '## FAILURE MODES' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md && grep -q '## CAPABILITY CONTRACT REMINDER' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md && npm run lint:md -- src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'FR-NN\|FR-01' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md` exits 0 (new ID format present)
    - `! grep -qE '^- FR#:|Number sequentially \(FR1, FR2' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md` exits 0 (legacy unpadded format gone)
    - `grep -q 'AC: Given' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md` exits 0 (inline AC pattern present)
    - `grep -qE 'Given.*when.*then|Given / When / Then|Given/When/Then' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md` exits 0 (Gherkin framing)
    - `grep -q '## Out of Scope' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md` exits 0 (OOS H2 in Content Structure)
    - `grep -q '\*\*OOS-01\*\*' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md` exits 0 (OOS entry format)
    - `grep -qE '(2.4|2-4|floor.*2|cap.*4)' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md` exits 0 (AC density rule)
    - `grep -q 'Does each FR have' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md` exits 0 (new Quality Check bullet)
    - `grep -q 'Is there an \`## Out of Scope\`' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md` exits 0 (new Quality Check bullet)
    - `grep -q '## MANDATORY EXECUTION RULES' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md` exits 0
    - `grep -q '## CRITICAL IMPORTANCE' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md` exits 0 (capability-contract framing preserved)
    - `grep -q '## CAPABILITY CONTRACT REMINDER' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md` exits 0
    - `grep -qE '(Select.*\[A\].*\[P\].*\[C\])' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md` exits 0
    - `grep -q '## SUCCESS METRICS' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md` exits 0
    - `grep -q '## FAILURE MODES' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md` exits 0
    - `npm run lint:md -- src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md` exits 0
  </acceptance_criteria>
  <done>
step-09 emits FR-NN format with inline Given/When/Then AC sub-bullets (floor=2, cap=4), has updated Quality Check validations, and includes `## Out of Scope` with OOS-NN entries in the Content Structure code fence. All contract blocks intact. markdownlint passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add §2c "Coding-Agent Readiness Review" to step-11-polish.md + confirm prd-template.md minimality (D-61, PRD-02, template verification)</name>
  <files>src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md, src/gomad-skills/2-plan-workflows/gm-create-prd/templates/prd-template.md</files>
  <read_first>
    - src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md (full file; §2b Brainstorming Reconciliation currently L89–103 — use this as the parallel structural pattern for new §2c)
    - src/gomad-skills/2-plan-workflows/gm-create-prd/templates/prd-template.md (full file — confirm minimal 10-line shape is preserved)
    - .planning/phases/08-prd-product-brief-content-refinement/08-CONTEXT.md D-61 (banned-phrase checklist + gate semantics)
    - .planning/phases/08-prd-product-brief-content-refinement/08-PATTERNS.md (pattern 12: §2c parallel to §2b)
    - .planning/phases/08-prd-product-brief-content-refinement/08-RESEARCH.md Q4 (template minimality decision: header-only)
  </read_first>
  <action>
**Edit A — `step-11-polish.md`**: Insert a new `### 2c. Coding-Agent Readiness Review` section immediately after the existing `### 2b. Brainstorming Reconciliation (if brainstorming input exists)` block and before `### 3. Optimization Actions` (currently line ~106).

Locate the `### 2b.` block — it ends with `**Why this matters**: Brainstorming documents are often long, and the PRD's structured template has an implicit bias toward concrete/structural ideas. Soft ideas (tone, philosophy, interaction feel) frequently get silently dropped because they don't map cleanly to FR/NFR format.` (currently line 103).

After the blank line following that paragraph, and before `### 3. Optimization Actions` (currently line 106), insert:

```markdown
### 2c. Coding-Agent Readiness Review

**Scan the PRD for residual human-founder framing that leaked past earlier steps.** Run the banned-phrase checklist below against every section of the PRD document loaded in step 1:

**Banned phrases (flag every occurrence):**

- "why now?"
- "investor" / "investors"
- "ARR" / "CAC" / "LTV" / "MRR" / "DAU" / "MAU" / "retention rate" / "churn"
- "go-to-market" / "GTM"
- "stakeholder pitch" / "elevator pitch"
- persona demographic markers (e.g. "35-year-old", "mother of two", "emotional journey")
- time-window estimation phrases ("in 3 months", "by Q4", "within 18 months")
- "business hours uptime" (reframe to "system uptime")
- "expert peer" (if referring to the user — reword to "product owner driving a coding-agent-built product")

**For each occurrence found:**

1. **Flag the exact line and section** to the user — quote the surrounding sentence
2. **Propose strip-or-rephrase** — show the concrete edit you'd make (delete / reword / keep-with-rationale)
3. **Wait for user approval** before applying — facilitator gates, user decides

**If zero occurrences are found**, state this explicitly: "No banned phrases detected. PRD is coding-agent-ready."

**Why this matters**: Earlier steps may have drifted. Coding-agent consumers parse the PRD without the human-PM context these phrases assume. §2c is the last gate before PRD finalization — it catches regressions and ensures the PRD reads as a specification for code, not a pitch deck.
```

**Preservation invariants for step-11-polish.md:**
- §1 `Load Context and Document` (L39–55) verbatim, including the `Read ../data/prd-purpose.md` reference (this is the load that surfaces the new `## Coding-Agent Consumer Mindset` section added by Plan 08-01)
- §2 `Document Quality Review` (L56–87) verbatim
- §2b `Brainstorming Reconciliation` (L89–103) verbatim
- §3 `Optimization Actions` (L105–131) verbatim — new §2c is INSERTED BEFORE §3, not merged into it
- §4 `Preserve Critical Information` verbatim — ADD one bullet `- Out of Scope block (OOS-NN entries)` to the "Must Preserve" list so polish retains OOS alongside FRs/NFRs
- §5 `Generate Optimized Document` verbatim
- §6 `Present MENU OPTIONS` verbatim
- `## MANDATORY EXECUTION RULES (READ FIRST):` verbatim
- A/P/C menu block verbatim
- `## SUCCESS METRICS:` verbatim — ADD one bullet `✅ Coding-Agent Readiness Review completed (banned phrases flagged or absence confirmed)`
- `## FAILURE MODES:` verbatim — ADD one bullet `❌ Banned-phrase checklist skipped; residual human-founder framing ships in the final PRD`

**Edit B — `templates/prd-template.md`**: Confirm the file is UNCHANGED from its current 10-line form:

```yaml
---
stepsCompleted: []
inputDocuments: []
workflowType: 'prd'
---

# Product Requirements Document - {{project_name}}

**Author:** {{user_name}}
**Date:** {{date}}
```

Per RESEARCH.md Open Question 4 + Pitfall 3: template stays header-only. The `## Out of Scope` H2 is emitted by step-09's Content Structure (Task 2 of this plan), NOT pre-baked into the template body. Step-v-12's validator does header-scanning at output time, not template time, so the template doesn't need to include the new section header.

**DO NOT ADD** any `## Executive Summary`, `## Functional Requirements`, `## Out of Scope`, or other body-section placeholders to prd-template.md. The template's only role is frontmatter + title — everything else is append-flow from step content.

**DO NOT CHANGE** the frontmatter keys: `stepsCompleted`, `inputDocuments`, `workflowType`. These three keys are the frontmatter contract read by every step file and by the `stepsCompleted`-append flow. Mutating any of them breaks every step.

Verify the file is truly unchanged by comparing to its current content: the file MUST have exactly these 10 lines (including the trailing newline per CONVENTIONS.md). If there is any whitespace drift (tabs, CRLF, extra blank lines), normalize to the canonical 10-line form.
  </action>
  <verify>
    <automated>grep -q '### 2c\. Coding-Agent Readiness Review' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md && grep -q 'Banned phrases' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md && grep -q 'business hours uptime' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md && grep -q '### 2b\. Brainstorming Reconciliation' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md && grep -q '### 3\. Optimization Actions' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md && grep -q 'Out of Scope block' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md && grep -q 'Coding-Agent Readiness Review completed' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md && grep -q "Read \`\.\./data/prd-purpose\.md\`" src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md && grep -q '## MANDATORY EXECUTION RULES' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md && grep -qE '(Select.*\[A\].*\[P\].*\[C\])' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md && test $(wc -l < src/gomad-skills/2-plan-workflows/gm-create-prd/templates/prd-template.md) -le 12 && grep -q "^workflowType: 'prd'$" src/gomad-skills/2-plan-workflows/gm-create-prd/templates/prd-template.md && grep -q '^stepsCompleted: \[\]$' src/gomad-skills/2-plan-workflows/gm-create-prd/templates/prd-template.md && grep -q '^inputDocuments: \[\]$' src/gomad-skills/2-plan-workflows/gm-create-prd/templates/prd-template.md && grep -q '# Product Requirements Document -' src/gomad-skills/2-plan-workflows/gm-create-prd/templates/prd-template.md && npm run lint:md -- src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md src/gomad-skills/2-plan-workflows/gm-create-prd/templates/prd-template.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '^### 2c\. Coding-Agent Readiness Review$' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md` returns one line
    - §2c appears BETWEEN §2b and §3 — verify with `awk '/^### /{print NR,$0}' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md` showing: `2b. Brainstorming Reconciliation` < `2c. Coding-Agent Readiness Review` < `3. Optimization Actions`
    - `grep -q 'Banned phrases' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md` exits 0
    - Banned-phrase list contains all D-61 items: `grep -q '"why now?"' && grep -q 'ARR.*CAC.*LTV\|ARR.*CAC' && grep -q 'go-to-market' && grep -q 'stakeholder pitch' && grep -q 'demographic markers' && grep -q 'time-window estimation' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md` exits 0
    - `grep -q 'Wait for user approval' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md` exits 0 (facilitator gate semantics)
    - §2b + §3 preserved: `grep -q '### 2b\. Brainstorming Reconciliation' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md && grep -q '### 3\. Optimization Actions' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md`
    - `grep -q 'Read.*\.\./data/prd-purpose\.md' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md` exits 0 (§1 prd-purpose.md load preserved — surfaces new Mindset section from Plan 08-01)
    - "Must Preserve" list includes OOS: `grep -q 'Out of Scope' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md | wc -l ` ≥ 2 (appears in §2c list AND in §4 Must Preserve list)
    - `grep -q 'Coding-Agent Readiness Review completed' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md` exits 0 (SUCCESS METRICS addition)
    - `grep -q '## MANDATORY EXECUTION RULES' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md` exits 0
    - `grep -qE '(Select.*\[A\].*\[P\].*\[C\])' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md` exits 0
    - `grep -q '## SUCCESS METRICS\|## 🚨 SYSTEM SUCCESS/FAILURE METRICS' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md` exits 0
    - Template unchanged: `test $(wc -l < src/gomad-skills/2-plan-workflows/gm-create-prd/templates/prd-template.md) -le 12` exits 0
    - `grep -q "^workflowType: 'prd'$" src/gomad-skills/2-plan-workflows/gm-create-prd/templates/prd-template.md` exits 0 (frontmatter contract preserved)
    - `grep -q '^stepsCompleted: \[\]$' src/gomad-skills/2-plan-workflows/gm-create-prd/templates/prd-template.md` exits 0
    - `grep -q '^inputDocuments: \[\]$' src/gomad-skills/2-plan-workflows/gm-create-prd/templates/prd-template.md` exits 0
    - Template has NO body sections: `! grep -qE '^## (Functional Requirements|Out of Scope|Executive Summary)' src/gomad-skills/2-plan-workflows/gm-create-prd/templates/prd-template.md` exits 0
    - `npm run lint:md -- src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md src/gomad-skills/2-plan-workflows/gm-create-prd/templates/prd-template.md` exits 0
  </acceptance_criteria>
  <done>
step-11-polish.md has the new §2c Coding-Agent Readiness Review between §2b and §3, banned-phrase checklist matches D-61 in full, facilitator-gate semantics preserved, OOS added to "Must Preserve" list. Template is unchanged / minimal. markdownlint passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Markdown-only content edits to existing prompt/template files. No new code, no network, no filesystem egress. The `## Out of Scope` H2 emission is data structure, not a privilege boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering | step-04, step-09, step-11, prd-template.md | accept | Repo-local markdown; extensive grep-based acceptance_criteria catch structural drift; integration test (Plan 08-05) exercises the downstream-compatibility contracts. |
| T-08-03-02 | Information Disclosure | step content | accept | No secrets, no PII. Prompts are intentional public documentation. |
| T-08-03-03 | Denial of Service | markdown renderers / validator header scan | mitigate | `npm run lint:md` per-file gate; integration test in Plan 08-05 exercises step-v-12 header scan. |
| T-08-03-04 | Elevation of Privilege | — | N/A | No privilege surface. |
| T-08-03-05 | Repudiation | — | accept | Git history. |
| T-08-03-06 | Spoofing | — | N/A | No identity surface. |

Secrets/PII: none. Injection vectors: none — OOS entries are author-typed prose, not structured user input. Untrusted inputs: none. Privilege changes: none. Dependency additions: none (STATE.md zero-new-runtime-deps locked). Side channels: none.
</threat_model>

<verification>
After all three tasks complete:

1. `npm run lint:md -- <4 files>` exits 0
2. step-04 §2 rewrite verified: `! grep -qE '(Opening Scene|Rising Action|Climax|Resolution|realistic name and personality|emotional journey)' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-04-journeys.md` exits 0
3. step-09 new format verified: `grep -qE 'FR-NN|FR-01' && grep -q 'AC: Given' && grep -q '## Out of Scope' && grep -q 'OOS-01' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-09-functional.md`
4. step-11 §2c verified: `grep -q '### 2c\. Coding-Agent Readiness Review' src/gomad-skills/2-plan-workflows/gm-create-prd/steps-c/step-11-polish.md`
5. Template unchanged: `test $(wc -l < src/gomad-skills/2-plan-workflows/gm-create-prd/templates/prd-template.md) -le 12`
6. Downstream-skill dirs byte-for-byte unchanged (PRD-06 guard): `git diff --name-only HEAD | grep -E '(gm-validate-prd|gm-create-architecture|gm-create-epics-and-stories|gm-check-implementation-readiness)/' ; test $? -ne 0`
7. `npm run validate:skills` exits 0
8. Cross-plan sanity: Plan 08-01's and Plan 08-02's files are unchanged by this plan's edits — `git diff --name-only HEAD | grep -vE 'step-04-journeys\.md|step-09-functional\.md|step-11-polish\.md|prd-template\.md' ; test $? -ne 0` (only this plan's 4 files in the diff)
</verification>

<success_criteria>
- step-04 §2 is action-oriented; §3–§7 numbering intact; Minimum Coverage list preserved; no narrative-framework vestiges
- step-09 Format block specifies FR-NN with inline Given/When/Then AC sub-bullets (floor 2, cap 4)
- step-09 Content Structure code fence emits FR-01/FR-02/FR-03 example + `## Out of Scope` with OOS-01/OOS-02 entries
- step-09 Self-Validation Quality Check gains AC-count and OOS-presence checks
- step-11 has §2c `Coding-Agent Readiness Review` between §2b and §3 with the full D-61 banned-phrase checklist and facilitator gate semantics
- step-11 Must Preserve list includes `Out of Scope block (OOS-NN entries)`
- prd-template.md stays at 10 lines with the existing frontmatter keys (`stepsCompleted`, `inputDocuments`, `workflowType`) unchanged
- All 4 files preserve MANDATORY EXECUTION RULES (step files), A/P/C menu (step files), SUCCESS/FAILURE METRICS rubrics
- `npm run lint:md` passes on all 4 files
- No downstream-skill files modified
</success_criteria>

<output>
After completion, create `.planning/phases/08-prd-product-brief-content-refinement/08-03-SUMMARY.md` with:
- step-04 §2 before/after diff
- step-09 Format + Quality Check + Content Structure diffs (with line counts)
- step-11 §2c insert location and full inserted block
- Confirmation that prd-template.md is unchanged (hash comparison or wc -l = 10)
- Structural compatibility proof: screenshots/greps showing `## Out of Scope` H2 scannable by step-v-12; `FR-01:` matching epics "or similar" clause; inline `- AC: Given` sub-bullets extractable via markdown indent
- npm run lint:md / validate:skills output
</output>
