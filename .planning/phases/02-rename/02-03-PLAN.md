---
phase: 02-rename
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - test/fixtures/file-refs-csv/valid/bmm-style.csv
  - test/fixtures/file-refs-csv/valid/gomad-style.csv
  - test/fixtures/file-refs-csv/valid/core-style.csv
  - test/test-file-refs-csv.js
autonomous: true
requirements: [TXT-04]

must_haves:
  truths:
    - "test/fixtures/file-refs-csv/valid/gomad-style.csv exists; bmm-style.csv does not"
    - "test/fixtures/file-refs-csv/valid/core-style.csv still exists (core-skills top-level not renamed)"
    - "Fixture CSV content uses gm-* skill IDs matching Plan 01's renamed directories"
    - "test/test-file-refs-csv.js references the renamed fixture path"
    - "node test/test-file-refs-csv.js exits 0"
    - "npm run test:install exits 0"
    - "npm run validate:refs exits 0"
  artifacts:
    - path: "test/fixtures/file-refs-csv/valid/gomad-style.csv"
      provides: "Renamed test fixture (TXT-04, D-13)"
    - path: "test/test-file-refs-csv.js"
      provides: "Updated consumer referencing new fixture path and gm-* IDs (D-15)"
  key_links:
    - from: "test/test-file-refs-csv.js"
      to: "test/fixtures/file-refs-csv/valid/gomad-style.csv"
      via: "file path string literal"
      pattern: "gomad-style\\.csv"
    - from: "test/fixtures/file-refs-csv/valid/gomad-style.csv"
      to: "src/gomad-skills/**/gm-*"
      via: "CSV rows referencing renamed skill paths"
      pattern: "gm-"
---

<objective>
Rename the `bmm-style.csv` test fixture to `gomad-style.csv`, update its CSV rows so every `bmad-*` skill ID reference becomes `gm-*`, update the `test-file-refs-csv.js` consumer, and run the final phase gate validations. Covers TXT-04 and closes the Phase 2 verification loop (D-17 after 02-03).

Purpose: Prove the full Phase 2 rename is consistent end-to-end by running the tests that validate cross-reference integrity.
Output: Renamed fixture + updated consumer + passing validation suite.
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-rename/02-CONTEXT.md
@.planning/phases/02-rename/02-RESEARCH.md
@.planning/phases/02-rename/02-01-SUMMARY.md
@.planning/phases/02-rename/02-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- D-13: rename bmm-style.csv → gomad-style.csv via git mv -->
<!-- D-14: rewrite CSV content so every bmad-<x> becomes gm-<x>                  -->
<!-- D-15: update test/test-file-refs-csv.js to reference the renamed path       -->
<!-- Note: core-style.csv is NOT renamed — core-skills top-level directory       -->
<!-- stays as `core-skills`, only the `bmad-*` prefix INSIDE it was renamed.     -->
<!-- core-style.csv contents still need CSV row updates if it references bmad-*. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename bmm-style.csv fixture and reconcile CSV contents</name>
  <files>test/fixtures/file-refs-csv/valid/bmm-style.csv, test/fixtures/file-refs-csv/valid/gomad-style.csv, test/fixtures/file-refs-csv/valid/core-style.csv, test/test-file-refs-csv.js</files>
  <read_first>
    - test/fixtures/file-refs-csv/valid/bmm-style.csv (current rows — note: may have been touched by Plan 02's sweep; confirm current state)
    - test/fixtures/file-refs-csv/valid/core-style.csv (current rows)
    - test/test-file-refs-csv.js (current fixture path references and expected-output strings)
  </read_first>
  <action>
    1. Rename the fixture with git mv (D-13):
       `git mv test/fixtures/file-refs-csv/valid/bmm-style.csv test/fixtures/file-refs-csv/valid/gomad-style.csv`

    2. Open `test/fixtures/file-refs-csv/valid/gomad-style.csv` and reconcile content (D-14):
       - Plan 02's sweep should already have replaced `bmad-*` → `gm-*` text within the file.
       - Verify every row points at a real renamed directory under `src/gomad-skills/` (cross-check with `ls src/gomad-skills/**/gm-*`).
       - Fix any row that is still stale or now points at a non-existent path.

    3. Open `test/fixtures/file-refs-csv/valid/core-style.csv` and do the same reconciliation against `src/core-skills/gm-*`.

    4. Update `test/test-file-refs-csv.js` (D-15):
       - Replace any string literal `bmm-style.csv` with `gomad-style.csv`.
       - Update any expected-output strings, snapshot data, or assertions that still reference `bmad-*` or `bmm-*` to their `gm-*` / `gomad-*` counterparts.
       - Plan 02's sweep should already have caught text references, but `fs.existsSync('.../bmm-style.csv')` or similar path-literal needs manual verification.

    5. Run `node test/test-file-refs-csv.js` — MUST exit 0.

    6. Commit: `test(02-03): rename bmm-style fixture to gomad-style + update consumer (TXT-04)`.
  </action>
  <verify>
    <automated>test ! -f test/fixtures/file-refs-csv/valid/bmm-style.csv &amp;&amp; test -f test/fixtures/file-refs-csv/valid/gomad-style.csv &amp;&amp; test -f test/fixtures/file-refs-csv/valid/core-style.csv &amp;&amp; node test/test-file-refs-csv.js &amp;&amp; test "$(grep -rn 'bmm-style\|bmad-style' test/ 2&gt;/dev/null | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `test/fixtures/file-refs-csv/valid/gomad-style.csv` exists
    - `test/fixtures/file-refs-csv/valid/bmm-style.csv` does not exist
    - `test/fixtures/file-refs-csv/valid/core-style.csv` still exists
    - `node test/test-file-refs-csv.js` exits 0
    - Zero hits for `bmm-style` or `bmad-style` under `test/`
    - Commit preserves `git log --follow` history for the renamed fixture
  </acceptance_criteria>
  <done>Test fixture renamed, CSV rows reconciled to gm-* IDs, consumer updated, fixture-specific test green.</done>
</task>

<task type="auto">
  <name>Task 2: Final phase-gate validation run</name>
  <files>(no file modifications — validation only)</files>
  <read_first>
    - .planning/phases/02-rename/02-CONTEXT.md D-17 (phase gate checks)
    - .planning/phases/02-rename/02-VALIDATION.md (sign-off checklist)
  </read_first>
  <action>
    Run the full Phase 2 gate sequence. Any failure blocks the phase and requires investigation.

    1. `npm run validate:skills` — manifest discovery + id/name integrity
    2. `npm run validate:refs` — cross-file reference integrity (TXT-02 gate)
    3. `npm run test:install` — installer component smoke tests
    4. `node test/test-file-refs-csv.js` — fixture gate (TXT-04)
    5. `node tools/installer/gomad-cli.js --help` — CLI entry point smoke test
    6. `node tools/dev/rename-sweep.js` — idempotency re-check (must report `Files touched: 0`)
    7. Final zero-hit grep:
       `grep -rn 'bmad\|BMAD\|bmm' src/ tools/ test/ docs/ website/ 2>/dev/null | wc -l`
       MUST be `0`.
    8. Phase 3 exclude-list integrity check:
       `git status -- LICENSE CHANGELOG.md TRADEMARK.md CONTRIBUTORS.md README.md README_CN.md`
       MUST show no modifications since the start of Phase 2.

    If all checks are green: Phase 2 is complete and ready for `/gsd-verify-work`.

    If any check fails: stop, diagnose, and either fix in this plan (if scoped) or open a follow-up in Plan 02-01/02-02 as appropriate. Do NOT run `npm run quality` — that is Phase 4's gate (D-18).
  </action>
  <verify>
    <automated>npm run validate:skills &amp;&amp; npm run validate:refs &amp;&amp; npm run test:install &amp;&amp; node test/test-file-refs-csv.js &amp;&amp; node tools/installer/gomad-cli.js --help &gt; /dev/null &amp;&amp; node tools/dev/rename-sweep.js 2&gt;&amp;1 | grep -q "Files touched: 0" &amp;&amp; test "$(grep -rn 'bmad\|BMAD\|bmm' src/ tools/ test/ docs/ website/ 2&gt;/dev/null | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run validate:skills` exits 0
    - `npm run validate:refs` exits 0
    - `npm run test:install` exits 0
    - `node test/test-file-refs-csv.js` exits 0
    - `node tools/installer/gomad-cli.js --help` exits 0
    - `node tools/dev/rename-sweep.js` reports `Files touched: 0` (idempotency)
    - Zero-hit grep across `src/ tools/ test/ docs/ website/` for bmad/BMAD/bmm
    - Phase 3-owned files show no Phase 2 modifications
  </acceptance_criteria>
  <done>Phase 2 gate is green across all D-17 checks. Phase 2 is ready for verify-work and phase-complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → validator | Test consumes CSV fixture as source of truth for cross-ref validation |
| phase gate → shipped state | Any validation that passes despite broken refs would allow a broken package to reach Phase 4 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Tampering | test-file-refs-csv consumer path literal | mitigate | Task 1 explicitly greps `test/` for stale `bmm-style`/`bmad-style` strings after edit |
| T-02-12 | Denial of Service | CI gate passing with stale skill IDs | mitigate | Task 2 runs validate:skills AND validate:refs AND test:install — three independent validators; any one catching a stale ref blocks the phase |
| T-02-13 | Repudiation | Fixture rename loses history | mitigate | D-13 uses `git mv` to preserve blame history for the fixture file |
</threat_model>

<verification>
Full phase gate (D-17 after 02-03):
1. `npm run validate:skills` — 0
2. `npm run validate:refs` — 0
3. `npm run test:install` — 0
4. `node test/test-file-refs-csv.js` — 0
5. `node tools/installer/gomad-cli.js --help` — 0
6. `node tools/dev/rename-sweep.js` — `Files touched: 0`
7. `grep -rn 'bmad\|BMAD\|bmm' src/ tools/ test/ docs/ website/` — 0 hits
8. Phase 3-owned files untouched by Phase 2
</verification>

<success_criteria>
- TXT-04: `bmm-style.csv` renamed to `gomad-style.csv`, CSV rows use `gm-*` IDs, `test-file-refs-csv.js` consumer updated, test exits 0
- All D-17 phase gates green
- Idempotency of rename-sweep proven (second run = zero changes)
- Phase 3 exclude-list integrity preserved (LICENSE/CHANGELOG/TRADEMARK/CONTRIBUTORS/README/README_CN untouched)
- Phase 2 is complete and ready for `/gsd-verify-work`
</success_criteria>

<output>
After completion, create `.planning/phases/02-rename/02-03-SUMMARY.md` documenting:
- Fixture rename commit SHA
- Full phase-gate command outputs (or `PASS` per check)
- Confirmation of rename-sweep idempotency on final tree
- Phase 2 completion sign-off line
</output>
