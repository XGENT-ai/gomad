---
phase: 02-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bmm-skills/
  - src/gomad-skills/
  - src/core-skills/
  - tools/installer/bmad-cli.js
  - tools/installer/gomad-cli.js
  - tools/installer/ide/shared/bmad-artifacts.js
  - tools/installer/ide/shared/artifacts.js
  - tools/installer/ide/shared/skill-manifest.js
  - tools/installer/core/manifest-generator.js
  - tools/installer/modules/official-modules.js
  - tools/installer/project-root.js
  - package.json
  - docs/mobmad-plan.md
autonomous: true
requirements: [FS-01, FS-02, FS-03, FS-04, FS-05]

must_haves:
  truths:
    - "src/gomad-skills/ exists and src/bmm-skills/ no longer exists"
    - "All bmad-* skill directories under core-skills/ and gomad-skills/ are renamed to gm-*"
    - "Manifest files are named skill-manifest.yaml and manifest.json (no bmad- prefix)"
    - "tools/installer/gomad-cli.js exists and runs; bmad-cli.js is gone"
    - "tools/installer/ide/shared/artifacts.js exists; bmad-artifacts.js is gone"
    - "package.json bin and main point at gomad-cli.js"
    - "npm run validate:skills passes after all renames"
    - "node tools/installer/gomad-cli.js --help exits 0"
    - "docs/mobmad-plan.md is deleted"
  artifacts:
    - path: "src/gomad-skills/"
      provides: "Renamed top-level skill tree (FS-01)"
    - path: "tools/installer/gomad-cli.js"
      provides: "Renamed CLI entry point (FS-04)"
    - path: "tools/installer/ide/shared/artifacts.js"
      provides: "Renamed artifacts helper (FS-05)"
    - path: "package.json"
      provides: "bin/main pointing at gomad-cli.js"
      contains: "gomad-cli.js"
  key_links:
    - from: "package.json"
      to: "tools/installer/gomad-cli.js"
      via: "bin and main fields"
      pattern: "gomad-cli\\.js"
    - from: "tools/installer/ide/shared/skill-manifest.js"
      to: "src/gomad-skills/**/skill-manifest.yaml"
      via: "glob pattern"
      pattern: "skill-manifest\\.yaml"
    - from: "tools/installer/core/manifest-generator.js"
      to: "manifest.json"
      via: "manifest discovery"
      pattern: "manifest\\.json"
---

<objective>
Execute all filesystem renames required for Phase 2 atomically with their consumer updates so the installer never enters a broken state between commits. This plan owns FS-01 through FS-05: the `bmm-skills/` tree rename, ~41 `bmad-*` → `gm-*` skill directory renames, manifest filename prefix drop, CLI entry point rename, and artifacts file rename. It also completes the Phase 1 D-08/D-09 staged CLI binary handoff and deletes the dead `docs/mobmad-plan.md` artifact.

Purpose: Establish the final path layout that downstream plans (content sweep, test fixtures) will reference.
Output: Renamed directory tree, updated installer readers, updated package.json bin/main, working CLI.
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-rename/02-CONTEXT.md
@.planning/phases/02-rename/02-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Key consumer contracts. Executors must preserve these while renaming. -->

From tools/installer/ide/shared/skill-manifest.js:
- Reads a manifest file for a skill directory. Currently globs `bmad-skill-manifest.yaml`.
- After FS-03: must glob `skill-manifest.yaml` instead.

From tools/installer/core/manifest-generator.js:
- Central manifest discovery/generation. Any hard-coded `bmad-manifest.json` becomes `manifest.json`.

From tools/installer/modules/official-modules.js:
- Enumerates official modules; may reference manifest filenames or skill directory prefixes.

From tools/installer/project-root.js:
- Resolves project paths; may reference manifest filenames.

From package.json (post Phase 1):
- `bin`: currently staged pointing at `tools/installer/bmad-cli.js` (Phase 1 D-08/D-09)
- `main`: currently staged pointing at `tools/installer/bmad-cli.js`
- After this plan: both fields MUST point at `tools/installer/gomad-cli.js`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomic CLI + artifacts + manifest-filename handoff</name>
  <files>tools/installer/bmad-cli.js, tools/installer/gomad-cli.js, tools/installer/ide/shared/bmad-artifacts.js, tools/installer/ide/shared/artifacts.js, tools/installer/ide/shared/skill-manifest.js, tools/installer/core/manifest-generator.js, tools/installer/modules/official-modules.js, tools/installer/project-root.js, package.json, docs/mobmad-plan.md</files>
  <read_first>
    - tools/installer/bmad-cli.js (current CLI entry, shebang + requires)
    - tools/installer/ide/shared/bmad-artifacts.js (current artifacts helper + its importers)
    - tools/installer/ide/shared/skill-manifest.js (current manifest glob pattern)
    - tools/installer/core/manifest-generator.js (manifest discovery logic)
    - tools/installer/modules/official-modules.js (any manifest filename refs)
    - tools/installer/project-root.js (any manifest filename refs)
    - package.json (current bin, main, scripts fields — verify Phase 1 staged state)
    - docs/mobmad-plan.md (confirm it is the stale `ad2434b` artifact before deleting)
  </read_first>
  <action>
    Execute in a single commit (D-11, D-12, D-09, D-16). No intermediate broken state.

    1. `git mv tools/installer/bmad-cli.js tools/installer/gomad-cli.js` (FS-04, D-11).
    2. `git mv tools/installer/ide/shared/bmad-artifacts.js tools/installer/ide/shared/artifacts.js` (FS-05, D-12).
    3. Update `tools/installer/gomad-cli.js` internals:
       - If shebang present keep `#!/usr/bin/env node`.
       - Update any self-referential strings/comments that named the file `bmad-cli.js` to `gomad-cli.js`.
       - Update any `require('./ide/shared/bmad-artifacts')` or similar to `require('./ide/shared/artifacts')`.
    4. Grep-update every importer of `bmad-artifacts` to `artifacts`:
       `grep -rln "ide/shared/bmad-artifacts" tools/` then edit each to `ide/shared/artifacts`.
    5. Update `package.json`:
       - `bin`: `{ "gomad": "tools/installer/gomad-cli.js" }` (replace any bmad-cli reference).
       - `main`: `tools/installer/gomad-cli.js`.
    6. Manifest filename reader updates (FS-03, D-09) — land in the SAME commit:
       - In `tools/installer/ide/shared/skill-manifest.js`: replace any glob/string literal `bmad-skill-manifest.yaml` with `skill-manifest.yaml`.
       - In `tools/installer/core/manifest-generator.js`: replace any literal `bmad-manifest.json` with `manifest.json` and any `bmad-skill-manifest.yaml` with `skill-manifest.yaml`.
       - In `tools/installer/modules/official-modules.js` and `tools/installer/project-root.js`: same literal replacements.
       Do NOT rename actual manifest files yet (that happens in Task 2/3 alongside the skill dir renames).
    7. Delete `docs/mobmad-plan.md` with `git rm docs/mobmad-plan.md` (D-16).

    Commit message: `refactor(02-01): atomic CLI + artifacts + manifest-filename handoff (FS-03/04/05)`.

    Then run the CLI smoke test from D-17 to prove the handoff is clean:
    `node tools/installer/gomad-cli.js --help`.

    CRITICAL: This task's commit does NOT rename any manifest files on disk yet. The installer readers are now expecting `skill-manifest.yaml` / `manifest.json`, so `npm run validate:skills` will FAIL until Task 3 renames them. That is acceptable because Task 2 and Task 3 run in this same plan before the plan's final gate. The plan-level gate is the one that must be green.
  </action>
  <verify>
    <automated>node tools/installer/gomad-cli.js --help &gt; /dev/null &amp;&amp; test ! -f tools/installer/bmad-cli.js &amp;&amp; test ! -f tools/installer/ide/shared/bmad-artifacts.js &amp;&amp; test ! -f docs/mobmad-plan.md &amp;&amp; node -e "const p=require('./package.json'); if(!p.bin||!p.bin.gomad||!p.bin.gomad.includes('gomad-cli.js'))process.exit(1); if(!p.main||!p.main.includes('gomad-cli.js'))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `tools/installer/gomad-cli.js` exists, `tools/installer/bmad-cli.js` does not
    - `tools/installer/ide/shared/artifacts.js` exists, `bmad-artifacts.js` does not
    - `node tools/installer/gomad-cli.js --help` exits 0
    - `node -e "require('./tools/installer/ide/shared/artifacts.js')"` exits 0
    - `package.json` bin.gomad and main both contain `gomad-cli.js`
    - `grep -rn "bmad-artifacts\\|bmad-cli" tools/ package.json` returns zero hits
    - `grep -rn "bmad-skill-manifest\\.yaml\\|bmad-manifest\\.json" tools/installer/` returns zero hits
    - `docs/mobmad-plan.md` does not exist
    - All changes in a single git commit
  </acceptance_criteria>
  <done>CLI binary handoff completed atomically, manifest readers expect new filenames, dead mobmad plan deleted. Installer readers now point at post-rename paths; manifest file renames happen in Task 3.</done>
</task>

<task type="auto">
  <name>Task 2: git mv core-skills/bmad-* to gm-* (rename-only commit)</name>
  <files>src/core-skills/bmad-*/ (11 directories)</files>
  <read_first>
    - src/core-skills/ (list all bmad-* subdirectories to build the exact rename list)
    - `.planning/research/RENAME_MECHANICS.md` skill inventory (if present) for canonical list
  </read_first>
  <action>
    Pure rename-only commit per D-03 and D-04. No content edits.

    1. Enumerate: `ls -1 src/core-skills | grep '^bmad-'` — expect ~11 directories.
    2. For each directory, run `git mv src/core-skills/bmad-<name> src/core-skills/gm-<name>`.
    3. Commit with message: `refactor(02-01): git mv core-skills/bmad-* to gm-* (FS-02 part 1)`.
    4. Verify git rename detection worked: `git log --follow --oneline src/core-skills/gm-<first-renamed>/SKILL.md` should show history prior to rename.

    Do NOT edit any file contents in this commit. Manifest filename renames and ID edits come in Task 4.
  </action>
  <verify>
    <automated>test "$(find src/core-skills -maxdepth 1 -type d -name 'bmad-*' | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(find src/core-skills -maxdepth 1 -type d -name 'gm-*' | wc -l | tr -d ' ')" -gt "0"</automated>
  </verify>
  <acceptance_criteria>
    - Zero `bmad-*` directories remain under `src/core-skills/`
    - At least one `gm-*` directory exists under `src/core-skills/`
    - Commit is rename-only: `git show --stat HEAD` shows only renames, zero line changes in content
    - `git log --follow` works across the rename for at least one file
  </acceptance_criteria>
  <done>core-skills/bmad-* directories renamed to gm-* via pure git mv; blame history preserved.</done>
</task>

<task type="auto">
  <name>Task 3: git mv bmm-skills tree, bmad-* subdirs, and manifest filenames (rename-only)</name>
  <files>src/bmm-skills/ (entire tree), src/gomad-skills/ (entire tree), **/bmad-skill-manifest.yaml, **/bmad-manifest.json, **/skill-manifest.yaml, **/manifest.json</files>
  <read_first>
    - Output of `find src/bmm-skills -type d -name 'bmad-*'` (enumerate all bmad-* subdirs, expect ~30)
    - Output of `find src -name 'bmad-skill-manifest.yaml' -o -name 'bmad-manifest.json'` (enumerate all manifest files to rename)
  </read_first>
  <action>
    Pure rename-only commit per D-03. Three groups of git mv in a single commit so the installer readers (updated in Task 1) match the on-disk tree:

    1. `git mv src/bmm-skills src/gomad-skills` (FS-01).
    2. Enumerate and rename each `bmad-*` skill subdirectory:
       ```
       find src/gomad-skills -type d -name 'bmad-*' -print | while read d; do
         git mv "$d" "$(dirname "$d")/gm-$(basename "$d" | sed 's/^bmad-//')"
       done
       ```
       (Run once; no nested recursion expected since skill dirs are leaf-level inside phase subtrees.)
    3. Rename manifest filenames (FS-03) in every renamed skill directory:
       ```
       find src -name 'bmad-skill-manifest.yaml' | while read f; do
         git mv "$f" "$(dirname "$f")/skill-manifest.yaml"
       done
       find src -name 'bmad-manifest.json' | while read f; do
         git mv "$f" "$(dirname "$f")/manifest.json"
       done
       ```
    4. Commit with message: `refactor(02-01): git mv bmm-skills → gomad-skills, bmad-* → gm-*, drop bmad- manifest prefix (FS-01/02/03)`.

    If commit diff is too large for git rename detection, split into subtrees per D-04 (e.g., `1-analysis`, `2-plan-workflows`, `3-solutioning`, `4-implementation` as separate rename-only commits). Decision is executor discretion per CONTEXT.md.

    Do NOT edit file contents in this task.
  </action>
  <verify>
    <automated>test "$(find src -type d -name 'bmm-skills' | wc -l | tr -d ' ')" = "0" &amp;&amp; test -d src/gomad-skills &amp;&amp; test "$(find src -type d -name 'bmad-*' | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(find src -name 'bmad-skill-manifest.yaml' -o -name 'bmad-manifest.json' | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `src/bmm-skills/` does not exist
    - `src/gomad-skills/` exists
    - Zero directories matching `bmad-*` under `src/`
    - Zero files named `bmad-skill-manifest.yaml` or `bmad-manifest.json` anywhere in repo
    - At least one `skill-manifest.yaml` exists under `src/gomad-skills/`
    - Commits in this task are rename-only (git show --stat shows no line-content changes)
  </acceptance_criteria>
  <done>Full skill tree renamed to gomad-skills/, all bmad-* skill dirs are gm-*, manifest filename prefix dropped. Blame history preserved via pure git mv. Installer readers (updated in Task 1) now match on-disk layout.</done>
</task>

<task type="auto">
  <name>Task 4: Reference-update commit — skill IDs inside manifests</name>
  <files>src/gomad-skills/**/skill-manifest.yaml, src/gomad-skills/**/manifest.json, src/core-skills/**/skill-manifest.yaml, src/core-skills/**/manifest.json</files>
  <read_first>
    - At least one `src/gomad-skills/**/skill-manifest.yaml` (understand id/name field structure)
    - At least one `src/gomad-skills/**/manifest.json` (understand id/name field structure)
    - .planning/phases/02-rename/02-CONTEXT.md D-10 (manifest ID edit rule)
  </read_first>
  <action>
    Reference-update commit (D-03 step 2, D-10). Edit content of the renamed manifest files so their internal `id` and `name` fields match their new `gm-*` directory names.

    For each `skill-manifest.yaml` under `src/gomad-skills/` and `src/core-skills/`:
    - Replace `id: bmad-<x>` → `id: gm-<x>`
    - Replace `name: bmad-<x>` → `name: gm-<x>` (only if the name field was the identifier form, not a human-readable title)

    For each `manifest.json` under `src/gomad-skills/` and `src/core-skills/`:
    - Replace any `"id": "bmad-<x>"` → `"id": "gm-<x>"`
    - Replace any `"name": "bmad-<x>"` (identifier-form only) → `"name": "gm-<x>"`

    Do NOT sweep SKILL.md prose, module.yaml, or module-help.csv here — those belong to Plan 02's content sweep. This task is narrowly the manifest id/name fields so `npm run validate:skills` can pass after Plan 01.

    Commit message: `refactor(02-01): update manifest id/name fields to gm-* (FS-02/D-10)`.

    After commit, run:
    - `npm run validate:skills` — MUST pass
    - `node tools/installer/gomad-cli.js --help` — MUST exit 0
    (These are the D-17 post-02-01 smoke checks.)
  </action>
  <verify>
    <automated>npm run validate:skills &amp;&amp; node tools/installer/gomad-cli.js --help &gt; /dev/null &amp;&amp; test "$(grep -rn 'id: bmad-\|\"id\": \"bmad-' src/core-skills src/gomad-skills 2&gt;/dev/null | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run validate:skills` exits 0
    - `node tools/installer/gomad-cli.js --help` exits 0
    - Zero `id: bmad-` or `"id": "bmad-` hits inside `src/core-skills/` or `src/gomad-skills/` manifest files
    - All manifest id/name identifier fields use `gm-` prefix
  </acceptance_criteria>
  <done>Manifest IDs match renamed skill directory names; installer skill validation is green; CLI entry point is functional. FS-01..FS-05 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev shell → filesystem | Developer running git mv commands on a large directory tree |
| installer runtime → shipped manifests | Installer globs/reads manifests at user install time |
| npm publish → user install | Broken bin/main field would cause `npm install -g` to fail silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | package.json bin/main fields | mitigate | Task 1 atomicity: rename file and update package.json in same commit; smoke test `gomad-cli.js --help` gates the commit |
| T-02-02 | Denial of Service | Installer manifest discovery | mitigate | Task 1 updates all four reader files (`skill-manifest.js`, `manifest-generator.js`, `official-modules.js`, `project-root.js`) atomically with the filename change in Task 3; plan gate runs `npm run validate:skills` to prove discovery works post-rename |
| T-02-03 | Information Disclosure | Trademark — residual `bmad` strings in shipped CLI | accept | Content sweep is Plan 02's responsibility; this plan only handles structural renames and is explicitly scoped not to touch LICENSE/TRADEMARK/CHANGELOG (D-06) |
| T-02-04 | Repudiation | Git blame history loss across ~41 skill renames | mitigate | D-03 rename-only commits (Task 2 and Task 3) contain pure `git mv` with zero content changes so git's rename detection preserves `git log --follow` and `git blame` |
| T-02-05 | Elevation of Privilege | N/A for rename phase | accept | Pure rename with no new code paths, no new dependencies, no new network calls |
</threat_model>

<verification>
Plan-level gate (run after Task 4):
1. `npm run validate:skills` — exits 0
2. `node tools/installer/gomad-cli.js --help` — exits 0
3. `find src -type d -name 'bmad-*' | wc -l` — returns 0
4. `find src -name 'bmad-skill-manifest.yaml' -o -name 'bmad-manifest.json' | wc -l` — returns 0
5. `test ! -f tools/installer/bmad-cli.js && test ! -f tools/installer/ide/shared/bmad-artifacts.js && test ! -f docs/mobmad-plan.md`
</verification>

<success_criteria>
- FS-01: `src/gomad-skills/` exists, `src/bmm-skills/` does not
- FS-02: Zero `bmad-*` skill directories remain; `gm-*` replacements exist
- FS-03: Manifest files named `skill-manifest.yaml` / `manifest.json` (no `bmad-` prefix)
- FS-04: `tools/installer/gomad-cli.js` is the CLI entry point; `bmad-cli.js` is gone
- FS-05: `tools/installer/ide/shared/artifacts.js` exists; `bmad-artifacts.js` is gone
- Installer discovery works: `npm run validate:skills` passes
- CLI functional: `node tools/installer/gomad-cli.js --help` exits 0
- `docs/mobmad-plan.md` is deleted
</success_criteria>

<output>
After completion, create `.planning/phases/02-rename/02-01-SUMMARY.md` documenting:
- Exact list of directories renamed (with counts)
- Exact list of manifest files renamed
- Git commit SHAs for each rename-only and reference-update commit
- Confirmation of plan-level gate green state
</output>
