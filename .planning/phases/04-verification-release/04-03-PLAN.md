---
phase: 04-verification-release
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified: []
autonomous: false
requirements:
  - REL-01
  - REL-02
user_setup:
  - service: npm
    why: "Publishing @xgent-ai/gomad@1.1.0 and deprecating v1.0.0"
    env_vars:
      - name: NPM_TOKEN
        source: "npm access token with publish scope, or run `npm login` interactively"
    dashboard_config:
      - task: "Create granular access token with Bypass 2FA enabled"
        location: "npmjs.com -> Access Tokens -> Generate New Token -> Granular Access Token"
must_haves:
  truths:
    - "@xgent-ai/gomad@1.1.0 is installable from npm registry"
    - "@xgent-ai/gomad@1.0.0 shows deprecation notice pointing to @latest"
    - "v1.1.0 git tag exists on main branch"
    - "next branch is merged into main"
  artifacts: []
  key_links: []
---

<objective>
Merge next to main, publish @xgent-ai/gomad@1.1.0 to npm, deprecate v1.0.0, and tag v1.1.0 on main.

Purpose: This is the shipping step. All verification passed in Plans 01 and 02. Now we execute the manual publish sequence per D-01, D-03, D-08, D-09, D-10.
Output: Published package on npm, deprecated v1.0.0, git tag on main.
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-verification-release/04-CONTEXT.md
@.planning/phases/04-verification-release/04-01-SUMMARY.md
@.planning/phases/04-verification-release/04-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Merge, publish, deprecate, and tag</name>
  <read_first>
    - package.json (confirm version is 1.1.0, confirm files allowlist is present)
    - .planning/phases/04-verification-release/04-02-SUMMARY.md (confirm all quality gates passed)
  </read_first>
  <action>
This task requires npm authentication which Claude cannot perform. The operator must execute these steps manually.

**Pre-flight (Claude runs these checks first):**
```bash
# Verify we're on next branch with clean working tree
git status
# Verify version
node -e "console.log(require('./package.json').version)"
# Verify quality passed recently
npm run quality && npm run test:tarball && npm run test:e2e
```

**If pre-flight passes, present these manual steps to the operator:**

**Step 1: Authenticate with npm** (per D-01)
```bash
npm login
# OR
export NPM_TOKEN=<your-granular-access-token>
```
Verify: `npm whoami` returns your username.

**Step 2: Merge next -> main** (per D-09)
```bash
git checkout main
git merge next
```

**Step 3: Publish from main** (per D-01, REL-02)
```bash
npm publish
```
Verify: `npm view @xgent-ai/gomad@1.1.0 version` returns `1.1.0`.

**Step 4: Confirm installable** (per D-08 -- must confirm before deprecating)
```bash
cd $(mktemp -d)
npm init -y
npm install @xgent-ai/gomad@1.1.0
ls node_modules/@xgent-ai/gomad/src/
cd -
```

**Step 5: Deprecate v1.0.0** (per D-08, D-10, REL-01)
```bash
npm deprecate "@xgent-ai/gomad@1.0.0" "Use @xgent-ai/gomad@latest instead."
```
Verify: `npm view @xgent-ai/gomad@1.0.0` shows deprecation notice.

**Step 6: Tag v1.1.0** (per D-03)
```bash
git tag v1.1.0
git push origin main --tags
```
  </action>
  <what-built>All verification scripts and quality gates from Plans 01 and 02 have passed. The package is ready to publish.</what-built>
  <how-to-verify>
    1. Run `npm whoami` -- should return your npm username
    2. Run `npm view @xgent-ai/gomad@1.1.0 version` -- should return `1.1.0`
    3. Run `npm view @xgent-ai/gomad@1.0.0` -- should show deprecation notice containing "Use @xgent-ai/gomad@latest instead."
    4. Run `git tag -l v1.1.0` -- should show `v1.1.0`
    5. Run `git branch` -- should show you are on `main`
  </how-to-verify>
  <resume-signal>Type "published" after completing all steps, or describe any issues encountered</resume-signal>
  <acceptance_criteria>
    - `npm view @xgent-ai/gomad@1.1.0 version` outputs `1.1.0`
    - `npm view @xgent-ai/gomad@1.0.0` output contains `DEPRECATED` or `deprecated`
    - `git tag -l v1.1.0` outputs `v1.1.0`
    - `git log --oneline -1 main` shows the merge commit or latest next commit
  </acceptance_criteria>
  <done>@xgent-ai/gomad@1.1.0 published on npm, v1.0.0 deprecated with "Use @xgent-ai/gomad@latest instead.", v1.1.0 tag on main, next merged to main</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local machine -> npm registry | npm publish sends tarball to public registry; requires auth token |
| npm token -> local env | Token must not be committed to git or logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Information Disclosure | npm auth token | mitigate | Use `npm login` interactively or `NPM_TOKEN` env var; never commit .npmrc with token; .npmrc is in .gitignore |
| T-04-07 | Tampering | published package | mitigate | Verify tarball contents with `npm pack --dry-run` before publish; confirm installability after publish before deprecating old version (D-08) |
| T-04-08 | Denial of Service | deprecate before publish | mitigate | D-08 explicitly requires confirming v1.1.0 is installable from npm before deprecating v1.0.0 |
</threat_model>

<verification>
1. `npm view @xgent-ai/gomad@1.1.0` returns package metadata
2. `npm view @xgent-ai/gomad@1.0.0` shows deprecation notice
3. `git tag -l v1.1.0` returns the tag
4. Fresh `npm install @xgent-ai/gomad@1.1.0` in a temp directory succeeds
</verification>

<success_criteria>
- @xgent-ai/gomad@1.1.0 is live on npm
- @xgent-ai/gomad@1.0.0 is deprecated with redirect message
- v1.1.0 git tag exists on main
- next is merged into main
</success_criteria>

<output>
After completion, create `.planning/phases/04-verification-release/04-03-SUMMARY.md`
</output>
