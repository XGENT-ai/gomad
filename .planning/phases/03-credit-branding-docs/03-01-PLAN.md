---
phase: 03-credit-branding-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - test/fixtures/license-bmad-baseline.txt
  - test/fixtures/contributors-bmad-baseline.txt
  - test/validate-phase-03.js
  - LICENSE
  - TRADEMARK.md
  - CONTRIBUTORS.md
  - CONTRIBUTING.md
  - SECURITY.md
  - AGENTS.md
  - CHANGELOG.md
  - CNAME
  - .npmignore
  - banner-bmad-method.png
autonomous: true
requirements:
  - CREDIT-01
  - CREDIT-02
  - CREDIT-03
  - CREDIT-04
  - BRAND-01
  - DOCS-03
  - DOCS-04
  - DOCS-05
  - DOCS-06
  - DOCS-07
must_haves:
  truths:
    - "REQUIREMENTS.md and ROADMAP.md reflect the three Phase 3 requirement-delta relaxations (CREDIT-02 intro removal, BRAND-01 banner removal, DOCS-05 delete option)"
    - "LICENSE preserves BMAD MIT text byte-identical (lines 1-31 diff cleanly against upstream fixture) with GoMad copyright block + non-affiliation disclaimer appended below a horizontal rule"
    - "CONTRIBUTORS.md preserves BMAD contributor list byte-identical with GoMad contributors section appended"
    - "TRADEMARK.md is a full GoMad rewrite with five-point defensive posture (D-21) and canonical non-affiliation disclaimer"
    - "CHANGELOG.md is truncated to start at v1.1.0 with Summary / Added / Changed / Removed / BREAKING CHANGES / Version numbering note sections"
    - "CONTRIBUTING.md contains no Discord links, no BMAD community processes, and has rockie@kitmi.com.au maintainer contact"
    - "SECURITY.md and AGENTS.md are deleted"
    - "CNAME file content is exactly 'gomad.xgent.ai\\n'"
    - "banner-bmad-method.png does not exist on disk; .npmignore line referencing it is removed"
    - "The canonical non-affiliation disclaimer sentence is drafted once and reused verbatim across LICENSE, TRADEMARK.md (and is exported in the plan summary for Plan 03-02 to reuse verbatim in README.md and README_CN.md)"
    - "test/validate-phase-03.js exists and asserts every Phase 3 invariant (21 checks from 03-VALIDATION.md)"
  artifacts:
    - path: test/fixtures/license-bmad-baseline.txt
      provides: "Byte-identical snapshot of BMAD MIT block (current LICENSE lines 1-31) for preservation verification"
    - path: test/fixtures/contributors-bmad-baseline.txt
      provides: "Byte-identical snapshot of BMAD contributor acknowledgment (current CONTRIBUTORS.md lines 1-32) for preservation verification"
    - path: test/validate-phase-03.js
      provides: "Phase 3 invariant checker - grep/diff/file-exists asserts mapped to 21 invariants in 03-VALIDATION.md"
      exports: ["validate()"]
    - path: LICENSE
      provides: "Composed MIT license: BMAD block (byte-identical) + HR + GoMad copyright + re-license statement + non-affiliation disclaimer"
      contains: "Copyright (c) 2026 Rockie Guo"
    - path: TRADEMARK.md
      provides: "GoMad defensive trademark posture with nominative BMAD acknowledgment"
      contains: "GoMad"
    - path: CONTRIBUTORS.md
      provides: "Preserved BMAD acknowledgment + GoMad Contributors section"
      contains: "## GoMad Contributors"
    - path: CHANGELOG.md
      provides: "Fresh v1.1.0 entry with BREAKING CHANGES subsection"
      contains: "## [1.1.0]"
    - path: CNAME
      provides: "GitHub Pages custom domain"
      contains: "gomad.xgent.ai"
  key_links:
    - from: LICENSE
      to: test/fixtures/license-bmad-baseline.txt
      via: "head -n 31 LICENSE | diff - fixture"
      pattern: "diff.*license-bmad-baseline"
    - from: CONTRIBUTORS.md
      to: test/fixtures/contributors-bmad-baseline.txt
      via: "head -n 32 CONTRIBUTORS.md | diff - fixture"
      pattern: "diff.*contributors-bmad-baseline"
    - from: test/validate-phase-03.js
      to: LICENSE, CONTRIBUTORS.md, TRADEMARK.md, CHANGELOG.md, CONTRIBUTING.md, CNAME, .npmignore, banner-bmad-method.png
      via: "grep/diff/file-exists assertions"
      pattern: "validate-phase-03"
---

<objective>
Phase 3 "Legal + Identity" slice. Applies the three CONTEXT.md requirement-delta relaxations to REQUIREMENTS.md and ROADMAP.md, establishes Wave 0 byte-preservation baselines, writes the Phase 3 invariant checker, and executes every legal/meta-doc file edit in the phase: LICENSE composition, TRADEMARK.md rewrite, CONTRIBUTORS.md append, CHANGELOG truncation, CONTRIBUTING.md rewrite, SECURITY.md + AGENTS.md deletion, CNAME fix, banner-bmad-method.png deletion (with .npmignore paired edit).

Purpose: Establish the canonical non-affiliation disclaimer sentence (single source of truth, reused verbatim in LICENSE + TRADEMARK.md now and in Plan 03-02's README + README_CN Credits sections), land all byte-identical preservation invariants, and close out the entire "Legal + Identity" column of the Phase 3 file inventory (§10 Category 1 of 03-RESEARCH.md) in one plan so Plan 03-02 can focus exclusively on surfaces and assets.

Output: 10 substantive file edits, 2 file deletions, 4 new files (2 baseline fixtures + 1 invariant checker + nothing else new in this plan), plus REQUIREMENTS/ROADMAP delta update. Exports the canonical disclaimer sentence in the plan summary for Plan 03-02 to reuse.
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-credit-branding-docs/03-CONTEXT.md
@.planning/phases/03-credit-branding-docs/03-RESEARCH.md
@.planning/phases/03-credit-branding-docs/03-VALIDATION.md
@CLAUDE.md

<canonical_disclaimer>
<!-- Single source of truth. Reused verbatim in LICENSE, TRADEMARK.md, README.md, README_CN.md. -->
xgent-ai is not affiliated with, endorsed by, or sponsored by BMad Code, LLC.
BMad™, BMad Method™, and BMad Core™ remain trademarks of BMad Code, LLC and are
referenced here solely for attribution purposes.
</canonical_disclaimer>

<license_composition_template>
<!-- This is what the final LICENSE looks like. Lines 1-31 come from the current LICENSE file VERBATIM (byte-identical). -->

[LINES 1-31 BYTE-IDENTICAL FROM CURRENT LICENSE — DO NOT EDIT]

----------------------------------------------------------------

GoMad is a hard fork of BMAD Method, distributed under the MIT License.

Copyright (c) 2026 Rockie Guo / xgent-ai

Modifications and additions made by xgent-ai are likewise released under the
MIT License above. The original BMAD Method copyright notice and license terms
are preserved in their entirety as required by the MIT License.

xgent-ai is not affiliated with, endorsed by, or sponsored by BMad Code, LLC.
BMad™, BMad Method™, and BMad Core™ remain trademarks of BMad Code, LLC and are
referenced here solely for attribution purposes.
</license_composition_template>

<trademark_md_template>
<!-- Five-point defensive posture per D-21. Full rewrite — old TRADEMARK.md is overwritten entirely. -->

# GoMad Trademark Policy

## 1. Nominative acknowledgment of BMAD

GoMad is a hard fork of BMAD Method (https://github.com/bmad-code-org/BMAD-METHOD),
originally authored by Brian (BMad) Madison and contributors. "BMAD", "BMad",
"BMad Method", and "BMad Core" are trademarks of BMad Code, LLC. GoMad references
these marks only in attribution, compatibility, and historical context — a use
recognized as nominative fair use under U.S. trademark law. GoMad is a product of
xgent-ai and is not a product of, nor derived from ongoing contributions by,
BMad Code, LLC.

## 2. Non-affiliation

xgent-ai is not affiliated with, endorsed by, or sponsored by BMad Code, LLC.
BMad™, BMad Method™, and BMad Core™ remain trademarks of BMad Code, LLC and are
referenced here solely for attribution purposes.

## 3. GoMad as a trademark

"GoMad" is an unregistered common-law trademark of xgent-ai (operated by Rockie
Guo). All rights in the GoMad name, logo, and wordmark are reserved. xgent-ai
asserts trademark rights in "GoMad" arising from commercial use, independent of
any formal registration.

## 4. Prohibited uses of the GoMad mark

Without prior written permission from xgent-ai, you may not:

- Use "GoMad", "Gomad", "Go-Mad", "GOMad", or any confusingly similar name as the
  name of a product, service, company, organization, or internet domain.
- Use the GoMad wordmark, logo, or visual identity in a way that implies
  endorsement, affiliation, or sponsorship by xgent-ai.
- Distribute a modified fork of this codebase under the name "GoMad" or any
  confusingly similar name.

## 5. Permitted uses

You may:

- Fork this codebase under the MIT License and distribute your fork under a
  different name.
- Reference "GoMad" in articles, documentation, reviews, tutorials, and other
  descriptive writing, in accordance with nominative fair use.
- State that your software is "built on GoMad" or "compatible with GoMad" where
  such statements are factually accurate.

## Contact

Trademark inquiries: Rockie Guo <rockie@kitmi.com.au>
</trademark_md_template>

<contributors_md_append>
<!-- Appended below byte-identical existing CONTRIBUTORS.md content. -->

----------------------------------------------------------------

## GoMad Contributors

GoMad is maintained by xgent-ai. Initial maintainer: Rockie Guo
<rockie@kitmi.com.au>.

New contributors are acknowledged via git history and future editions of this
file.
</contributors_md_append>

<changelog_v110_template>
<!-- Full content of CHANGELOG.md after truncation. Everything below replaces the entire current CHANGELOG.md file. -->

# Changelog

All notable changes to GoMad are documented in this file. This changelog starts
fresh at v1.1.0 and does not include the upstream BMAD Method history. See
[LICENSE](LICENSE) and [README.md](README.md#credits) for the fork's origin.

## [1.1.0] - 2026-04-09

### Summary

v1.1.0 re-bases `@xgent-ai/gomad` as a hard fork of BMAD Method.
`@xgent-ai/gomad@1.0.0` (a Claude Code skills installer, wrong product direction)
is deprecated on npm.

### Added

- Hard fork of BMAD Method as the new baseline for `@xgent-ai/gomad`.
- `gomad` CLI binary at `tools/installer/gomad-cli.js`.
- `src/gomad-skills/` four-phase workflow (1-analysis → 2-plan-workflows →
  3-solutioning → 4-implementation) inherited from BMAD Method.
- `src/core-skills/gm-*` shared infrastructure skills.
- LICENSE with preserved BMAD MIT notice and appended GoMad copyright block.
- TRADEMARK.md with GoMad defensive posture.

### Changed

- Package name: `@xgent-ai/gomad@1.0.0` → `@xgent-ai/gomad@1.1.0`.
- All skill prefixes: `bmad-*` → `gm-*`.
- Module directory: `src/bmm-skills/` → `src/gomad-skills/`.

### Removed

- All BMAD Method upstream changelog entries (v6.2.2 and earlier). Lineage is
  preserved via LICENSE, README Credits, and this entry.
- `banner-bmad-method.png` (no replacement banner).
- `README_VN.md`, `docs/cs/`, `docs/fr/`, `docs/vi-vn/`.
- External modules manifest and consumer code.
- `SECURITY.md`, `AGENTS.md` (BMAD governance boilerplate not retained).

### BREAKING CHANGES

Nothing from `@xgent-ai/gomad@1.0.0` carries forward. v1.1.0 is effectively a
new product reusing the package name. If you installed `@xgent-ai/gomad@1.0.0`,
uninstall it before upgrading.

### Version numbering note

Version numbers reset: BMAD Method's upstream versioning (v6.x) is unrelated to
`@xgent-ai/gomad`'s versioning (1.x). See LICENSE and Credits for origin.
</changelog_v110_template>

<contributing_md_outline>
<!-- Full rewrite of CONTRIBUTING.md. No Discord links, no BMAD community processes. -->

# Contributing to GoMad

Thanks for your interest in GoMad. GoMad is a small, focused fork of BMAD
Method maintained by xgent-ai. Contributions are welcome via pull request.

## How to contribute

1. Fork `https://github.com/xgent-ai/gomad` on GitHub.
2. Create a feature branch from `next` (milestone work) or `main` (hotfixes).
3. Make your changes, following the project conventions in `CLAUDE.md` and the
   code style enforced by `eslint.config.mjs` / `prettier.config.mjs`.
4. Run `npm run lint` and `npm test` locally before opening a PR.
5. Open a pull request against the appropriate base branch with a clear
   description of the change and its motivation.

## What we accept

- Bug fixes with a minimal reproduction.
- Documentation improvements.
- New agents or skills that follow the `gm-*` naming convention and fit the
  `1-analysis → 2-plan-workflows → 3-solutioning → 4-implementation` workflow.

## What we defer

- Large architectural changes — please open an issue for discussion first.
- Renames or moves across the skill directory tree — coordinate with the
  maintainer first, as these affect downstream installer behavior.

## Contact

Maintainer: Rockie Guo <rockie@kitmi.com.au>

Issues and PRs: https://github.com/xgent-ai/gomad
</contributing_md_outline>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply requirement deltas + establish Wave 0 baselines + write invariant checker</name>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md,
    test/fixtures/license-bmad-baseline.txt,
    test/fixtures/contributors-bmad-baseline.txt,
    test/validate-phase-03.js
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md (current CREDIT-02 line 41, BRAND-01 line 47, DOCS-05 line 56)
    - .planning/ROADMAP.md (current Phase 3 success criteria lines 58-64)
    - .planning/phases/03-credit-branding-docs/03-CONTEXT.md `<requirement_deltas>` section (lines 73-84)
    - .planning/phases/03-credit-branding-docs/03-VALIDATION.md (all 21 invariants §Phase 3 Invariants)
    - LICENSE (entire file - ~31 lines - to snapshot as baseline fixture)
    - CONTRIBUTORS.md (entire file - ~32 lines - to snapshot as baseline fixture)
  </read_first>
  <action>
    **Step 1 — Apply requirement deltas to REQUIREMENTS.md:**

    Edit `.planning/REQUIREMENTS.md`:
    - Line 41 (CREDIT-02): Replace the phrase "contain a `## Credits` section with: factual fork statement" context so the bullet reads: `**CREDIT-02**: `README.md` and `README_CN.md` contain a `## Credits` footer section with: factual fork statement ("GoMad is a hard fork of BMAD Method by Brian (BMad) Madison"), link to upstream repo, and the same non-affiliation disclaimer. No fork-statement in intro (per Phase 3 CONTEXT D-04).`
    - Line 47 (BRAND-01): Replace entirely with: `**BRAND-01**: `banner-bmad-method.png` is deleted, NO replacement banner image is created, `Wordmark.png` is regenerated for GoMad, and the CLI startup banner shows "GoMad" (per Phase 3 CONTEXT D-05).`
    - Line 56 (DOCS-05): Replace with: `**DOCS-05**: `CONTRIBUTING.md` rewritten with GoMad identity; `SECURITY.md` and `AGENTS.md` deleted (per Phase 3 CONTEXT D-24).`

    **Step 2 — Apply requirement deltas to ROADMAP.md:**

    Edit `.planning/ROADMAP.md` Phase 3 Success Criteria (lines 58-64):
    - Criterion 2: Replace "a factual fork statement in the intro and a `## Credits` section" with "a `## Credits` footer section (fork statement lives in Credits only, not intro — per CONTEXT D-04)"
    - Criterion 4: Replace entirely with: "`banner-bmad-method.png` is deleted (no replacement banner), `Wordmark.png` regenerated for GoMad, and CLI startup banner shows 'GoMad' (per CONTEXT D-05)"
    - Criterion 5: Replace "`SECURITY.md`, `AGENTS.md`, and `docs/` site content reflect GoMad identity" with "`SECURITY.md` and `AGENTS.md` are deleted; `docs/` site content reflects GoMad identity (per CONTEXT D-24)"

    **Step 3 — Create Wave 0 baseline fixtures:**

    Create directory `test/fixtures/` if it doesn't exist (it does — contains file-refs-csv).

    Create `test/fixtures/license-bmad-baseline.txt` as a byte-identical copy of the current LICENSE file (entire content, including all trailing whitespace and final newline). Use `fs.readFileSync`/`fs.writeFileSync` with no encoding transformations. This snapshot is the immutable reference for Invariant #1 of VALIDATION.md.

    Create `test/fixtures/contributors-bmad-baseline.txt` as a byte-identical copy of the current CONTRIBUTORS.md file (entire content). This is the reference for Invariant #2.

    **Step 4 — Create test/validate-phase-03.js invariant checker:**

    Create `test/validate-phase-03.js` as a CommonJS Node script (shebang `#!/usr/bin/env node`, `require`/`module.exports` style — matches project convention per CLAUDE.md conventions section). It must:

    - Load fixtures from `test/fixtures/license-bmad-baseline.txt` and `test/fixtures/contributors-bmad-baseline.txt`.
    - Implement each of the 21 invariants listed in `.planning/phases/03-credit-branding-docs/03-VALIDATION.md` §"Phase 3 Invariants". Every invariant gets a named `check()` function. Example implementation pattern:
      ```js
      function checkLicenseByteIdentical() {
        const baseline = fs.readFileSync('test/fixtures/license-bmad-baseline.txt', 'utf8');
        const current = fs.readFileSync('LICENSE', 'utf8');
        if (!current.startsWith(baseline)) {
          return { id: 1, pass: false, msg: 'LICENSE does not start with byte-identical BMAD MIT block' };
        }
        return { id: 1, pass: true };
      }
      ```
    - Run all checks, collect results, print a summary table to stdout, exit 0 if all pass, exit 1 if any fail.
    - **Initial state:** Many checks will FAIL because the corresponding files have not been edited yet. This is expected — the script is built once, then individual task commits progressively turn it green. The script's job is to be a reliable feedback signal, not to pass on first run.
    - Mark Invariant #7 (CLI banner TTY + degrades), #12 (Astro build), and #20 (CLI BMAD-free) as `skipOnWave1 = true` — they are verified in Plan 03-02 tasks. The script prints `SKIPPED (Plan 03-02)` for these when `--wave 1` flag is passed.
    - Do NOT run the full Astro build inside this script; shell out only to `cat`, `grep`, `diff`, `test -f`, `node -e`. Keep runtime < 5 seconds.
    - Export `module.exports = { runAll, checks }` so other test harnesses could reuse.

    Mark `test/validate-phase-03.js` as executable via `fs.chmodSync` or leave as node-invoked.

    Add a script entry to `package.json` IS OUT OF SCOPE — do not touch package.json in Phase 3 (Phase 4 owns package.json edits per Phase 2 CONTEXT constraint). Invoke the script via `node test/validate-phase-03.js`.
  </action>
  <verify>
    <automated>grep -q 'banner-bmad-method.png' .planning/REQUIREMENTS.md && grep -q 'Credits footer section' .planning/REQUIREMENTS.md && grep -q 'per CONTEXT D-05' .planning/ROADMAP.md && test -f test/fixtures/license-bmad-baseline.txt && test -f test/fixtures/contributors-bmad-baseline.txt && test -f test/validate-phase-03.js && diff test/fixtures/license-bmad-baseline.txt LICENSE && node test/validate-phase-03.js --wave 1 --dry-run 2>&1 | grep -q 'invariant'</automated>
  </verify>
  <done>
    - REQUIREMENTS.md CREDIT-02, BRAND-01, DOCS-05 lines reflect CONTEXT deltas verbatim (grep confirms)
    - ROADMAP.md Phase 3 success criteria 2, 4, 5 reflect CONTEXT deltas verbatim (grep confirms)
    - test/fixtures/license-bmad-baseline.txt is byte-identical to current LICENSE (`diff` exits 0)
    - test/fixtures/contributors-bmad-baseline.txt is byte-identical to current CONTRIBUTORS.md
    - test/validate-phase-03.js exists, runs without runtime errors, and prints 21 invariant check results
    - Running the checker NOW will show many FAIL results (expected); subsequent tasks make them PASS. VALIDATION.md Invariants #1, #2, #19 → addressed in this task.
  </done>
</task>

<task type="auto">
  <name>Task 2: Compose LICENSE + rewrite TRADEMARK.md + append to CONTRIBUTORS.md</name>
  <files>
    LICENSE,
    TRADEMARK.md,
    CONTRIBUTORS.md
  </files>
  <read_first>
    - LICENSE (current file — MUST preserve lines 1-31 byte-identical)
    - CONTRIBUTORS.md (current file — MUST preserve lines 1-32 byte-identical)
    - TRADEMARK.md (current file — for reference only; will be fully overwritten)
    - .planning/phases/03-credit-branding-docs/03-CONTEXT.md D-21, D-22, D-23 (TRADEMARK posture and CONTRIBUTORS append pattern)
    - This plan's `<canonical_disclaimer>`, `<license_composition_template>`, `<trademark_md_template>`, `<contributors_md_append>` context blocks (source of truth)
    - .planning/phases/03-credit-branding-docs/03-RESEARCH.md §"LICENSE Composition Pattern" (lines 146-183) and §"CONTRIBUTORS.md Composition Pattern" (lines 185-199)
  </read_first>
  <action>
    **LICENSE — append-only edit:**

    1. Read the current `LICENSE` file into a variable `bmadBlock` using `fs.readFileSync('LICENSE', 'utf8')`. Do NOT modify `bmadBlock` in any way.
    2. Build the new LICENSE content by string concatenation:
       ```
       bmadBlock + '\n----------------------------------------------------------------\n\n' + gomadBlock
       ```
       where `gomadBlock` is (EXACT verbatim, newline-terminated):
       ```
       GoMad is a hard fork of BMAD Method, distributed under the MIT License.

       Copyright (c) 2026 Rockie Guo / xgent-ai

       Modifications and additions made by xgent-ai are likewise released under the
       MIT License above. The original BMAD Method copyright notice and license terms
       are preserved in their entirety as required by the MIT License.

       xgent-ai is not affiliated with, endorsed by, or sponsored by BMad Code, LLC.
       BMad™, BMad Method™, and BMad Core™ remain trademarks of BMad Code, LLC and are
       referenced here solely for attribution purposes.
       ```
    3. Write the concatenated content back to `LICENSE` with `fs.writeFileSync('LICENSE', content)`. No Prettier, no markdownlint, no trailing-whitespace normalization — the file has no extension and must not be touched by editor auto-format.
    4. Verify: `diff test/fixtures/license-bmad-baseline.txt <(head -c $(wc -c < test/fixtures/license-bmad-baseline.txt) LICENSE)` exits 0.

    **TRADEMARK.md — full rewrite:**

    Replace the entire contents of `TRADEMARK.md` with the `<trademark_md_template>` block from this plan's context (the five-section structure: Nominative acknowledgment / Non-affiliation / GoMad as trademark / Prohibited uses / Permitted uses / Contact). Use the verbatim template text provided. This template embeds the canonical non-affiliation disclaimer sentence in Section 2.

    **CONTRIBUTORS.md — append-only edit:**

    1. Read the current `CONTRIBUTORS.md` into `bmadContribBlock`. Do NOT modify it.
    2. Build new content as `bmadContribBlock + '\n----------------------------------------------------------------\n\n' + gomadContribBlock` where `gomadContribBlock` is:
       ```
       ## GoMad Contributors

       GoMad is maintained by xgent-ai. Initial maintainer: Rockie Guo
       <rockie@kitmi.com.au>.

       New contributors are acknowledged via git history and future editions of this
       file.
       ```
    3. Write back to `CONTRIBUTORS.md`.
    4. Verify preservation: `diff test/fixtures/contributors-bmad-baseline.txt <(head -c $(wc -c < test/fixtures/contributors-bmad-baseline.txt) CONTRIBUTORS.md)` exits 0.
  </action>
  <verify>
    <automated>diff test/fixtures/license-bmad-baseline.txt <(head -c $(wc -c < test/fixtures/license-bmad-baseline.txt) LICENSE) && grep -q 'Copyright (c) 2026 Rockie Guo' LICENSE && grep -q 'not affiliated with, endorsed by, or sponsored by BMad Code' LICENSE && grep -q '^# GoMad Trademark Policy' TRADEMARK.md && grep -q 'GoMad as a trademark' TRADEMARK.md && grep -q 'not affiliated with, endorsed by, or sponsored by BMad Code' TRADEMARK.md && diff test/fixtures/contributors-bmad-baseline.txt <(head -c $(wc -c < test/fixtures/contributors-bmad-baseline.txt) CONTRIBUTORS.md) && grep -q '^## GoMad Contributors' CONTRIBUTORS.md && node test/validate-phase-03.js --only 1,2,6</automated>
  </verify>
  <done>
    - LICENSE lines 1-31 byte-identical to baseline fixture (Inv #1 ✓)
    - LICENSE contains GoMad copyright block, re-license statement, canonical disclaimer
    - TRADEMARK.md is the GoMad five-point template verbatim, contains canonical disclaimer
    - CONTRIBUTORS.md first 32 lines byte-identical to baseline (Inv #2 ✓)
    - CONTRIBUTORS.md has `## GoMad Contributors` section (Inv #6 partial ✓ — this task covers 2 of the 4 files containing the disclaimer)
    - `node test/validate-phase-03.js --only 1,2,6` shows these invariants PASS
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite CONTRIBUTING.md + delete SECURITY.md/AGENTS.md + CHANGELOG truncate + CNAME fix + banner delete + .npmignore fix</name>
  <files>
    CONTRIBUTING.md,
    SECURITY.md,
    AGENTS.md,
    CHANGELOG.md,
    CNAME,
    .npmignore,
    banner-bmad-method.png
  </files>
  <read_first>
    - CONTRIBUTING.md (current — 182 lines with Discord links and BMAD community content)
    - SECURITY.md (current — for inbound link grep before delete, per Pitfall 7)
    - AGENTS.md (current — minimal file)
    - CHANGELOG.md (current — 1889 lines, ALL deleted then replaced)
    - CNAME (current — contains `docs.bmad-method.org`, must become `gomad.xgent.ai\n`)
    - .npmignore (current — line 29 references `banner-bmad-method.png`, must be deleted)
    - .planning/phases/03-credit-branding-docs/03-CONTEXT.md D-09, D-10, D-11, D-12, D-20, D-24, D-25
    - .planning/phases/03-credit-branding-docs/03-RESEARCH.md §"CHANGELOG v1.1.0 entry scaffold" (lines 413-457) — VERBATIM template
    - This plan's `<changelog_v110_template>` and `<contributing_md_outline>` context blocks
  </read_first>
  <action>
    **Step 1 — CONTRIBUTING.md full rewrite:**

    Overwrite `CONTRIBUTING.md` entirely with the `<contributing_md_outline>` template from this plan's context. The rewrite MUST NOT contain any of: `discord.gg`, `BMad Code`, `BMAD Method`, `bmadcode.com`, `buymeacoffee.com/bmad`. It MUST contain: `rockie@kitmi.com.au`, `https://github.com/xgent-ai/gomad`, `gm-*`, `1-analysis`.

    **Step 2 — Pre-delete grep for inbound links (Pitfall 7):**

    Before deleting SECURITY.md and AGENTS.md, run:
    ```
    grep -rn 'SECURITY\.md\|AGENTS\.md' --include='*.md' --include='*.mjs' --include='*.js' --include='*.json' --include='*.yaml' --include='*.astro' . 2>/dev/null | grep -v '^\./\.planning/' | grep -v '^\./node_modules/' | grep -v '^\./test/validate-phase-03.js' | grep -v '^\./\.github/'
    ```
    For each inbound link hit outside `.planning/`, `node_modules/`, `.github/`, and `test/validate-phase-03.js`:
    - If in CONTRIBUTING.md (already rewritten in Step 1, should have zero hits) — verify clean.
    - If in README.md or README_CN.md — DEFER to Plan 03-02 (note in summary).
    - If in docs/ — delete the inbound link line or replace with a plain-text mention.
    - If anywhere else — fix or flag.

    Document all inbound-link fixes in the task summary.

    **Step 3 — Delete SECURITY.md and AGENTS.md:**

    `git rm SECURITY.md` and `git rm AGENTS.md`. Do NOT replace with stubs (D-24 prefers delete).

    **Step 4 — CHANGELOG.md truncate + v1.1.0 entry:**

    Overwrite `CHANGELOG.md` entirely with the `<changelog_v110_template>` text (the full markdown content starting with `# Changelog` and ending with the "Version numbering note" paragraph). This deletes lines 3-1889 of the old file and replaces the whole file with the v1.1.0 entry.

    **Step 5 — CNAME fix:**

    Overwrite `CNAME` file with exactly `gomad.xgent.ai\n` (17 characters including trailing newline). Use `fs.writeFileSync('CNAME', 'gomad.xgent.ai\n')`. Verify: `[ "$(cat CNAME)" = "gomad.xgent.ai" ]` exits 0 (cat strips trailing newline from display but file has one).

    **Step 6 — banner-bmad-method.png delete + .npmignore paired edit:**

    1. `git rm banner-bmad-method.png` (PNG file at repo root).
    2. Edit `.npmignore`: find line containing `banner-bmad-method.png` (research says line 29) and delete that entire line. Verify no other `.npmignore` lines reference it.
    3. Grep the whole repo for remaining references: `grep -rn 'banner-bmad-method' --include='*.md' --include='*.json' --include='*.yaml' --include='*.mjs' --include='*.npmignore' .` — must return zero hits (README.md/README_CN.md references will still exist and are handled in Plan 03-02 task 1 top-of-file deletion).

    Note: If the grep in step 3 surfaces README.md line 1 as still referencing the banner, that is expected — Plan 03-02 deletes that line. Do NOT touch README.md in this task.
  </action>
  <verify>
    <automated>test ! -f SECURITY.md && test ! -f AGENTS.md && test ! -f banner-bmad-method.png && [ "$(cat CNAME)" = "gomad.xgent.ai" ] && ! grep -q 'discord.gg' CONTRIBUTING.md && ! grep -q 'BMad Code' CONTRIBUTING.md && grep -q 'rockie@kitmi.com.au' CONTRIBUTING.md && grep -q '^## \[1\.1\.0\]' CHANGELOG.md && grep -q '^### BREAKING CHANGES' CHANGELOG.md && ! grep -qE '^## \[6\.|^## v6\.' CHANGELOG.md && ! grep -q 'banner-bmad-method.png' .npmignore && node test/validate-phase-03.js --only 3,5,9,10,13,14,19</automated>
  </verify>
  <done>
    - CONTRIBUTING.md rewritten — zero Discord/BMAD community references, contains maintainer contact (Inv #5 enforcement of D-25)
    - SECURITY.md deleted (Inv #10 partial)
    - AGENTS.md deleted (Inv #10 partial)
    - CHANGELOG.md starts at v1.1.0 with BREAKING CHANGES subsection (Inv #13, #14)
    - CNAME equals `gomad.xgent.ai\n` (Inv #9)
    - banner-bmad-method.png deleted (Inv #3, #10 partial)
    - .npmignore has no reference to banner-bmad-method.png (Inv #3)
    - test/validate-phase-03.js shows invariants 3, 5 (partial, README half still pending for Plan 03-02), 9, 10, 13, 14, 19 all PASS
    - Plan summary exports: (a) canonical non-affiliation disclaimer sentence verbatim, (b) list of any inbound-link fixes made during Pitfall 7 grep, (c) confirmation that Plan 03-02 must delete README.md line 1 to fully close Invariant #3
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → legal text | Byte-identical claims on LICENSE/CONTRIBUTORS must be provable via diff; drift = false legal claim |
| repo → external trademark holder (BMad Code LLC) | Nominative fair use language boundary; over-claim = infringement risk, under-claim = loss of common-law GoMad protection |
| repo → npm tarball | `.npmignore` desync from on-disk files = brand leak or unused-reference lint failure |
| repo → DNS | CNAME content = public routing claim for `gomad.xgent.ai` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Repudiation / Tampering | LICENSE BMAD block | mitigate | Wave 0 baseline fixture `test/fixtures/license-bmad-baseline.txt` snapshot; Invariant #1 in `test/validate-phase-03.js` runs `diff` on every commit. Any drift fails CI and blocks merge. Append-only edit pattern in Task 2 (read file, concat, write — never modify prefix). |
| T-03-01-02 | Spoofing / Information Disclosure | TRADEMARK.md language | mitigate (+ manual review) | Five-point defensive template in `<trademark_md_template>` is fixed wording reviewed by planner. Includes canonical disclaimer + nominative acknowledgment. Manual verification row in 03-VALIDATION.md (manual legal review) remains as human gate. |
| T-03-01-03 | Tampering / Information Disclosure | CHANGELOG truncation losing upstream attribution trail | accept with mitigation | LICENSE preserves BMAD copyright byte-identical (Invariant #1); CHANGELOG v1.1.0 entry includes explicit "Removed: All BMAD Method upstream changelog entries … Lineage preserved via LICENSE, README Credits, and this entry" line (template verbatim). Explicit acknowledgment + legal preservation in a different file is sufficient. |
| T-03-01-04 | Data leak / Information Disclosure | `banner-bmad-method.png` shipped via dangling `.npmignore` | mitigate | Task 3 pairs the file delete with the `.npmignore` line delete in the same commit; verification grep ensures zero remaining references in `.npmignore` / `*.md` / `*.json` / `*.yaml`. Phase 4 re-verifies via `npm pack --dry-run`. |
| T-03-01-05 | Spoofing (DNS) | CNAME pointing at wrong or hijacked domain | mitigate | Task 3 step 5 sets exact byte content `gomad.xgent.ai\n`; Invariant #9 asserts exact match; GitHub Pages deploy deferred per PROJECT.md so no live DNS exposure from this plan. |
| T-03-01-06 | Tampering (link rot) | Dead inbound links to deleted SECURITY.md / AGENTS.md | mitigate | Task 3 step 2 pre-delete grep for inbound link references; fix or remove before committing the deletion. |

Severity: all LOW. No HIGH/CRITICAL threats — phase blocks on `high` severity per config default, so no security gate triggered.
</threat_model>

<verification>
After all three tasks complete, run:
```
node test/validate-phase-03.js --wave 1
```
Expected result: invariants #1, #2, #3 (partial — requires README.md line 1 deletion in Plan 03-02 for full close), #5 (partial — README/README_CN halves pending in Plan 03-02), #6 (partial — LICENSE + TRADEMARK halves complete; README + README_CN halves pending), #9, #10, #13, #14, #19 all PASS. Others remain PENDING or SKIPPED (will close in Plan 03-02).

Also run:
```
npm run lint:md
```
to ensure markdownlint does not object to the rewritten CONTRIBUTING.md, CHANGELOG.md, TRADEMARK.md, CONTRIBUTORS.md. Fix any lint hits that involve style rather than content.

Do NOT run `npm run docs:build` in this plan — it depends on files Plan 03-02 modifies (website/, docs/index.md).
</verification>

<success_criteria>
- REQUIREMENTS.md + ROADMAP.md deltas applied and committed (Wave 0 pre-task)
- `test/validate-phase-03.js` exists and runs without error
- `test/fixtures/license-bmad-baseline.txt` + `test/fixtures/contributors-bmad-baseline.txt` committed
- LICENSE preserves BMAD block byte-identical, contains GoMad block + canonical disclaimer
- TRADEMARK.md is full GoMad rewrite with five-section defensive posture
- CONTRIBUTORS.md preserves BMAD block byte-identical, has `## GoMad Contributors` section
- CONTRIBUTING.md rewritten with no Discord/BMAD community content, has maintainer contact
- SECURITY.md deleted, AGENTS.md deleted
- CHANGELOG.md truncated + fresh v1.1.0 entry with all six subsections (Summary/Added/Changed/Removed/BREAKING CHANGES/Version numbering note)
- CNAME contains exactly `gomad.xgent.ai\n`
- banner-bmad-method.png deleted + .npmignore cleaned of reference
- `npm run lint:md` passes on all edited markdown files
- Plan summary exports: canonical disclaimer sentence (for Plan 03-02 reuse), any inbound-link fixes made during Pitfall 7, list of open Invariant closures that Plan 03-02 will complete
</success_criteria>

<output>
After completion, create `.planning/phases/03-credit-branding-docs/03-01-SUMMARY.md` with:
- Canonical non-affiliation disclaimer sentence (for Plan 03-02 to reuse verbatim in README Credits)
- List of files modified/created/deleted
- State of all 21 Phase 3 Invariants (✓ / pending-03-02 / manual-only)
- Any Pitfall 7 inbound-link fixes made
- Baseline fixture paths (so Plan 03-02 doesn't need to re-snapshot)
</output>