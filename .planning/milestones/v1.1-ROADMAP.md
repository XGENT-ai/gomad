# Milestone v1.1: Rename & Rebrand

**Status:** ✅ SHIPPED 2026-04-18
**Phases:** 1-4
**Total Plans:** 10

## Overview

Transformed the raw BMAD Method fork into `@xgent-ai/gomad@1.1.0` — a properly-named, slim, credited, and publishable package. Delivered across four phases: package identity + dead-code removal, full file-system + content rename (`bmad`/`BMAD`/`bmm` → `gomad`/`GOMAD`/`gm-*`), credit + branding + bilingual documentation, and verification + npm publish with v1.0.0 deprecation.

## Phases

### Phase 1: Foundation

**Goal:** The package declares itself as `@xgent-ai/gomad@1.1.0` with correct metadata, all dead code and unused assets are removed, and the codebase is slim enough to rename cleanly
**Depends on:** Nothing (first phase)
**Requirements:** PKG-01, PKG-02, PKG-03, PKG-04, PKG-05, SLIM-01, SLIM-02, SLIM-03, SLIM-04
**Plans:** 2 plans

Plans:

- [x] 01-01: Package identity, bin, scripts, and dependency cleanup
- [x] 01-02: ExternalModuleManager removal, dead docs/translations, bundler cleanup

**Details:** Renamed `bmad-method@6.2.2` → `@xgent-ai/gomad@1.1.0`, installed GoMad identity across all `package.json` metadata, collapsed `bin` to a single `gomad` entry, renamed `bmad:*` npm scripts to `gomad:*`, deleted the dead `rebundle` script, and dropped three unused production dependencies (`xml2js`, `@kayvan/markdown-tree-parser`, `ignore`). Removed the entire ExternalModuleManager subsystem (3 source files, multiple call sites, test mocks), the dead Vietnamese README, 3 abandoned doc translations, and stale `.bundler-temp/**` ignore patterns from 3 config files.

### Phase 2: Rename

**Goal:** Every file path, directory name, manifest entry, skill ID, and content reference has been transformed from `bmad`/`BMAD`/`bmm` to `gomad`/`GOMAD`/`gm-*`, with no rename-related breakage in installer or tests
**Depends on:** Phase 1
**Requirements:** FS-01, FS-02, FS-03, FS-04, FS-05, TXT-01, TXT-02, TXT-03, TXT-04
**Plans:** 3 plans

Plans:

- [x] 02-01: FS renames, atomic CLI/artifacts/manifest handoff, drop mobmad-plan (FS-01..05)
- [x] 02-02: Content sweep script + execute sweep, reconcile module.yaml/module-help.csv (TXT-01..03)
- [x] 02-03: Rename test fixture and run full phase-gate validation (TXT-04)

**Details:** Atomic filesystem handoff across the `bmm-skills/` tree, ~41 `bmad-*` skill directory renames to `gm-*`, manifest filename prefix drop (`bmad-skill-manifest.yaml` → `skill-manifest.yaml`, `bmad-manifest.json` → `manifest.json`), CLI entry rename (`bmad-cli.js` → `gomad-cli.js`), artifacts helper rename, and deletion of `docs/mobmad-plan.md`. Idempotent content-sweep script applied across source, configs, tests, docs, and website content — excluding Phase 3-owned files (LICENSE, CHANGELOG, TRADEMARK, CONTRIBUTORS, README, README_CN). Test fixtures renamed (`bmm-style.csv` → `gomad-style.csv`).

### Phase 3: Credit, Branding & Docs

**Goal:** GoMad is properly credited as a hard fork of BMAD Method with correct legal files, new visual branding, and complete English + Chinese documentation
**Depends on:** Phase 2
**Requirements:** CREDIT-01, CREDIT-02, CREDIT-03, CREDIT-04, BRAND-01, BRAND-02, DOCS-01, DOCS-02, DOCS-03, DOCS-04, DOCS-05, DOCS-06, DOCS-07
**Plans:** 2 plans

Plans:

- [x] 03-01: Legal + Identity — LICENSE composition, TRADEMARK rewrite, CONTRIBUTORS append, CONTRIBUTING rewrite, SECURITY/AGENTS delete, CHANGELOG truncate, CNAME, banner delete
- [x] 03-02: Surface + Assets — README + README_CN surgical edit, CLI banner, Wordmark + favicon, website stub + i18n, docs landing + style guide + spot-check sweep

**Details:** Composed `LICENSE` with byte-identical BMAD MIT preservation + GoMad copyright block + non-affiliation disclaimer. Rewrote `TRADEMARK.md` with defensive posture (nominative fair use). Appended to `CONTRIBUTORS.md` byte-identical. Surgically rewrote `README.md` + `README_CN.md` with verbatim canonical disclaimer Credits footer. Replaced BMAD CLI ASCII banner with hand-authored GoMad ASCII art (no figlet dep). Regenerated `Wordmark.png` + favicon via a committed sharp reproducibility script. Stubbed the Astro website as an under-construction one-pager, deleted orphaned fr-FR/vi-VN i18n JSON. Rewrote `docs/index.md` + `docs/zh-cn/index.md` landing pages in GoMad voice. Updated both `STYLE_GUIDE.md` files. 21 Phase 3 invariants all PASS.

### Phase 4: Verification & Release

**Goal:** The package passes all quality gates, installs cleanly from a local tarball, and is published to npm as `@xgent-ai/gomad@1.1.0` with v1.0.0 deprecated
**Depends on:** Phase 3
**Requirements:** VFY-01, VFY-02, VFY-03, REL-01, REL-02
**Plans:** 3 plans

Plans:

- [x] 04-01: Package hygiene — files allowlist, delete .npmignore/publish.yaml, fix residual bmad refs, tarball verification script
- [x] 04-02: E2E fresh-install test, full quality gate validation
- [x] 04-03: Merge next→main, npm publish, deprecate v1.0.0, tag v1.1.0

**Details:** `files` allowlist in `package.json` limits tarball to 320 shipped files. `verify-tarball.js` asserts no forbidden paths and no `bmad`/`bmm` residuals in shipped code. `test/test-e2e-install.js` packs the tarball, installs in an isolated temp dir, verifies `src/` and `tools/installer/` structure, and confirms all 52 `gm-*` skill directories contain `SKILL.md` files. Full quality gate (7 sub-checks) plus tarball and E2E tests all exit 0. Manual publish sequence: merge `next` → `main`, `npm publish`, confirm installability, `npm deprecate "@xgent-ai/gomad@1.0.0" "Use @xgent-ai/gomad@latest instead."`, `git tag v1.1.0 && git push origin main --tags`.

---

## Milestone Summary

**Key Decisions:**

- **Version `1.1.0` (not `2.0.0`)** — User explicitly chose minor bump despite major-pivot semantics; v1.0 had effectively no users so blast radius is near-zero ✓
- **Skill prefix `gm-*` (not `gomad-*`)** — Shorter; less verbose in command invocations and file paths ✓
- **Top-level dir `bmm-skills/` → `gomad-skills/`** — Full `gomad` at module level, short `gm-` only for individual skill names ✓
- **Manifest filenames: drop `bmad-` prefix entirely** — Directory namespace already provides scoping ✓
- **`package.json author`: `Rockie Guo <rockie@kitmi.com.au>` only; BMAD attribution lives in LICENSE/README** — Avoids confusing npm registry metadata ✓
- **`LICENSE` strategy: append GoMad block to BMAD's MIT, single file, no `NOTICE`** — OpenTofu/HashiCorp precedent; NOTICE is Apache-2.0 not MIT ✓
- **Casing: "GoMad" (display) / `gomad` (paths, CLI) / `GOMAD` (acronym expansion only)** ✓
- **Drop `tools/installer/external-official-modules.yaml` + consumer code entirely** — We are not a frontend for BMAD's external modules ecosystem ✓
- **Drop `docs/cs/`, `docs/fr/`, `docs/vi-vn/`; keep default (en) + `zh-cn/` only** — Same EN+CN policy as README; no maintainer for others ✓
- **Deprecate `@xgent-ai/gomad@1.0.0` (not `bmad-method`)** — v1.0.0 was a wrong product direction; deprecation prevents accidental installs ✓
- **Keep `core-skills/` (rename only)** — It's the foundation bmm workflows depend on, not "BMad builder" cruft ✓
- **Defer all new agents/skills to milestone 2** — Keeps v1.1 diff focused on rename/credit; clean baseline for future work ✓
- **Canonical non-affiliation disclaimer reused verbatim** — Single source of truth across LICENSE, TRADEMARK, README, README_CN ✓
- **CLI banner via hand-authored ASCII art in `cli-utils.js displayLogo()`, no figlet dep** ✓
- **E2E test verifies tarball structurally (not via interactive `gomad install`)** — Avoids `@clack/prompts` hang ✓

**Issues Resolved:**

- `validate-skills.js NAME_REGEX` hardcoded `bmad-` prefix — updated during Phase 2 rename
- `TARGET_GLOB` missed five text extensions in content sweep — extended
- Dangling `ExternalModuleManager` requires in two unlisted files — cleaned up in Phase 1
- `findBmadDir` function references in additional files beyond Plan 04-01 scope — renamed

**Issues Deferred:**

- GitHub Pages deployment — CNAME set now; actual deploy deferred until stable
- New gomad-specific agents and skills — deferred to milestone 2

**Technical Debt Incurred:**

- Phase 3 dropped cs-CZ locale and Ecosystem sidebar section beyond plan scope (dead links / no content) — acceptable scope creep

---

*For current project status, see .planning/ROADMAP.md*
