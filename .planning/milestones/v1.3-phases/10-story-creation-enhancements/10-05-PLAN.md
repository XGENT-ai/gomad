---
phase: 10-story-creation-enhancements
plan: 05
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - src/domain-kb/testing/SKILL.md
  - src/domain-kb/testing/anti-patterns.md
  - src/domain-kb/testing/reference/unit-tests.md
  - src/domain-kb/testing/reference/integration-tests.md
  - src/domain-kb/testing/reference/e2e-tests.md
  - src/domain-kb/testing/reference/mocking.md
  - src/domain-kb/testing/reference/coverage.md
  - src/domain-kb/testing/examples/jest-setup.md
  - src/domain-kb/testing/examples/playwright-e2e.md
  - src/domain-kb/architecture/SKILL.md
  - src/domain-kb/architecture/anti-patterns.md
  - src/domain-kb/architecture/reference/layered-architecture.md
  - src/domain-kb/architecture/reference/hexagonal.md
  - src/domain-kb/architecture/reference/modular-monolith.md
  - src/domain-kb/architecture/reference/event-driven.md
  - src/domain-kb/architecture/reference/api-design.md
  - src/domain-kb/architecture/examples/layered-node-app.md
  - src/domain-kb/architecture/examples/hexagonal-port-adapter.md
autonomous: true
requirements:
  - STORY-08
  - STORY-09
tags:
  - kb
  - content
  - seed-pack
must_haves:
  truths:
    - "Developer running `ls src/domain-kb/testing/` sees SKILL.md + reference/ + examples/ + anti-patterns.md (STORY-08 canonical format per D-12)"
    - "Developer running `ls src/domain-kb/architecture/` sees SKILL.md + reference/ + examples/ + anti-patterns.md (STORY-09 same format)"
    - "Every .md file under src/domain-kb/**/ has frontmatter with `source: original`, `license: MIT`, `last_reviewed: YYYY-MM-DD`"
    - "Every .md file has an H1 heading immediately after the frontmatter (required for gm-domain-skill D-09 catalog listing)"
    - "`npm run validate:kb-licenses` exits 0 with all files under `src/domain-kb/**` present"
    - "`npm run test:domain-kb-install` exits 0 — installer copies both packs to `<installRoot>/_config/kb/testing/**` and `<installRoot>/_config/kb/architecture/**`"
    - "Content is authored from scratch (not copied or paraphrased from external sources) per PITFALLS §4-J to avoid IP/licensing risk"
  artifacts:
    - path: "src/domain-kb/testing/SKILL.md"
      provides: "Testing pack overview + when-to-use"
      contains: "# Testing"
    - path: "src/domain-kb/testing/anti-patterns.md"
      provides: "Common testing mistakes to avoid"
      contains: "# Testing Anti-Patterns"
    - path: "src/domain-kb/testing/reference/*.md"
      provides: "5 subtopic references (unit, integration, e2e, mocking, coverage)"
      exports: ["5 files"]
    - path: "src/domain-kb/testing/examples/*.md"
      provides: "2 concrete code examples (jest, playwright)"
      exports: ["2 files"]
    - path: "src/domain-kb/architecture/SKILL.md"
      provides: "Architecture pack overview + when-to-use"
      contains: "# Architecture"
    - path: "src/domain-kb/architecture/anti-patterns.md"
      provides: "Common architectural mistakes"
      contains: "# Architecture Anti-Patterns"
    - path: "src/domain-kb/architecture/reference/*.md"
      provides: "5 subtopic references (layered, hexagonal, modular-monolith, event-driven, api-design)"
      exports: ["5 files"]
    - path: "src/domain-kb/architecture/examples/*.md"
      provides: "2 concrete code examples (layered node app, hexagonal port-adapter)"
      exports: ["2 files"]
  key_links:
    - from: "every src/domain-kb/**/*.md frontmatter"
      to: "tools/validate-kb-licenses.js KB-02/KB-03/KB-04"
      via: "source: / license: / last_reviewed: required keys"
      pattern: "source:"
    - from: "every src/domain-kb/**/*.md H1"
      to: "gm-domain-skill D-09 catalog listing"
      via: "/^#\\s+.+/ regex"
      pattern: "^# "
---

<objective>
Author two seed knowledge-base packs — `src/domain-kb/testing/**` and `src/domain-kb/architecture/**` — conforming to the D-12 broad-shallow format (1 SKILL.md + 5 reference/ + 2 examples/ + 1 anti-patterns.md = 9 files per pack). Implements STORY-08 (testing pack) and STORY-09 (architecture pack).

Purpose: Give `gm-domain-skill` (Plan 04) actual content to retrieve and rank. Plan 01's `validate-kb-licenses.js` MUST be in place first (this plan's `depends_on: [10-01]`) so every KB file's `source: original` + `license: MIT` + `last_reviewed:` frontmatter passes the release gate the moment it's committed. Plan 02's installer machinery copies these packs into `<installRoot>/_config/kb/` at install time — verified by `test/test-domain-kb-install.js`.

IP/licensing hard gate (PITFALLS §4-J, CRITICAL): every file MUST be authored from scratch. No paraphrasing of external sources. No copying from Claude-Cortex, vercel-labs, or any other repo. `source: original` + `license: MIT` accurately reflects authorship.

Output: 18 new `.md` files under `src/domain-kb/testing/` (9) and `src/domain-kb/architecture/` (9). All pass Plan 01's KB-01..KB-07 rules. The installer test (Plan 02) passes end-to-end with real packs.
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-story-creation-enhancements/10-CONTEXT.md
@.planning/phases/10-story-creation-enhancements/10-PATTERNS.md
@.planning/phases/10-story-creation-enhancements/10-01-PLAN.md
@.planning/research/PITFALLS.md
@.planning/research/FEATURES.md

<interfaces>
<!-- Pack shape spec (D-12) + frontmatter contract (STORY-08/09, validated by Plan 01). -->

**Pack shape per D-12** (broad-shallow, ~10 files):
```
src/domain-kb/<slug>/
├── SKILL.md                # overview + "when to use this pack"
├── anti-patterns.md        # common mistakes
├── reference/              # 4-6 subtopic refs, 200-500 words each
│   ├── <topic-1>.md
│   ├── ...
│   └── <topic-5>.md
└── examples/               # 2-3 concrete code examples
    ├── <example-1>.md
    └── <example-2>.md
```

**Required frontmatter (KB-01..KB-06, per PATTERNS §7):**
```yaml
---
source: original
license: MIT
last_reviewed: 2026-04-24
---
```

**H1 heading (KB-07):** First non-empty line after frontmatter MUST be `# <Title>`. The H1 is consumed by gm-domain-skill (Plan 04) for the D-09 catalog listing.

**LICENSE_ALLOWLIST from Plan 01:** `MIT`, `Apache-2.0`, `BSD-3-Clause`, `CC-BY-4.0`, `CC0-1.0`, `original`. Use `MIT` since GoMad is MIT-licensed.

**Content length targets (D-12):**
- SKILL.md: ~300-500 words (pack overview + when-to-use)
- anti-patterns.md: ~500-800 words (5-10 anti-patterns with 1-2 sentence explanations)
- reference/*.md: 200-500 words each (concise subtopic reference)
- examples/*.md: 300-800 words each (concrete code + walk-through)

**Ban list (PITFALLS §4-J):** NO content that could be traced to:
- vercel-labs/agent-skills
- NickCrew/Claude-Cortex
- Anthropic's skills repo
- Any framework's official docs (React, Vue, Next.js, etc.) — paraphrase is NOT sufficient; author from scratch with your own examples
- Any Stack Overflow answer
- Any blog post under a non-MIT license

**Tone/style target:** Direct, concrete, practical. No marketing language. No "best practices" generic prose. Each reference file should answer ONE specific question that a coding agent would ask.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author `src/domain-kb/testing/` pack (9 files)</name>
  <files>src/domain-kb/testing/SKILL.md, src/domain-kb/testing/anti-patterns.md, src/domain-kb/testing/reference/unit-tests.md, src/domain-kb/testing/reference/integration-tests.md, src/domain-kb/testing/reference/e2e-tests.md, src/domain-kb/testing/reference/mocking.md, src/domain-kb/testing/reference/coverage.md, src/domain-kb/testing/examples/jest-setup.md, src/domain-kb/testing/examples/playwright-e2e.md</files>
  <read_first>
    - .planning/phases/10-story-creation-enhancements/10-CONTEXT.md section Decisions D-12 (pack shape)
    - .planning/phases/10-story-creation-enhancements/10-PATTERNS.md section 7 (KB pack shape + frontmatter + per-file H1 contract)
    - .planning/research/PITFALLS.md sections 4-J (IP/licensing, CRITICAL) and 4-I (KB pack version drift — last_reviewed discipline)
    - tools/validate-kb-licenses.js (post-Plan-01 state — KB-01..KB-07 rules)
    - .planning/research/FEATURES.md (testing + architecture rationale — confirm pack topic choices align with seeds chosen from candidate list)
  </read_first>
  <behavior>
    - 9 files created at the exact paths listed in files_modified.
    - Every file has YAML frontmatter starting on line 1 with `source: original`, `license: MIT`, `last_reviewed: <today-ISO>`.
    - Every file's first non-empty line after `---\n...\n---` is an H1 heading (e.g., `# Testing Pack Overview`).
    - All content is authored from scratch. No verbatim or near-verbatim text from external sources. Examples use generic code patterns (e.g., `function add(a, b)`) rather than pulled-from-internet snippets.
    - `npm run validate:kb-licenses` exits 0 after this task.
  </behavior>
  <action>
    Create 9 files under `src/domain-kb/testing/`. For each: start with the 3-key frontmatter, then H1 title, then body.

    **Common frontmatter (every file):**
    ```
    ---
    source: original
    license: MIT
    last_reviewed: 2026-04-24
    ---
    ```
    (Use today's date in ISO YYYY-MM-DD format at authoring time.)

    **File 1 — `src/domain-kb/testing/SKILL.md`** (~400 words):
    - H1: `# Testing Pack Overview`
    - Sections: "When to use this pack" (3-5 concrete scenarios: e.g., "deciding between unit and integration tests for new code", "picking a mocking strategy", "diagnosing flaky tests"), "What's in this pack" (list the 8 other files with one-sentence descriptions), "Conventions" (test file naming, directory layout).

    **File 2 — `src/domain-kb/testing/anti-patterns.md`** (~600 words):
    - H1: `# Testing Anti-Patterns`
    - 6-8 anti-patterns in `## Anti-pattern N: <name>` subsections: e.g., "Testing implementation details", "Mocking what you own", "Shared mutable fixtures", "Sleep-based waits in async tests", "Snapshot tests covering volatile output", "Single large E2E suite blocking CI", "Assertion-less tests that only check for no-throw".
    - Each anti-pattern: 2-3 sentences on why it's bad + one line on the replacement.

    **File 3 — `src/domain-kb/testing/reference/unit-tests.md`** (~350 words):
    - H1: `# Unit Tests`
    - Sections: "Scope" (single function/class, no I/O), "Structure" (Arrange-Act-Assert), "When to write" (every pure function with branching logic), "When NOT to write" (trivial getters, framework boilerplate), "Naming" (`describe('X').it('does Y when Z')`).

    **File 4 — `src/domain-kb/testing/reference/integration-tests.md`** (~400 words):
    - H1: `# Integration Tests`
    - Sections: "Scope" (module boundary, real dependencies where cheap), "Database strategy" (per-test transaction rollback vs per-suite seed), "External API strategy" (record/replay vs sandbox), "Fixtures vs factories", "When to prefer over E2E".

    **File 5 — `src/domain-kb/testing/reference/e2e-tests.md`** (~400 words):
    - H1: `# End-to-End Tests`
    - Sections: "Scope" (user journey, browser for web, CLI invocation for tools), "Framework picks" (Playwright for web, execSync harness for CLI per test-e2e-install.js pattern), "How many" (critical paths only; 5-20 E2E max for most apps), "Flakiness mitigation" (explicit waits, deterministic seeds, retry-once-on-fail not retry-until-pass).

    **File 6 — `src/domain-kb/testing/reference/mocking.md`** (~350 words):
    - H1: `# Mocking Strategies`
    - Sections: "When to mock" (external I/O, time, randomness), "When NOT to mock" (code you own, pure functions, trivial objects), "Mock vs stub vs fake" (definitions + examples), "Verification" (call-count assertions are over-specification; prefer state assertions).

    **File 7 — `src/domain-kb/testing/reference/coverage.md`** (~300 words):
    - H1: `# Coverage Metrics`
    - Sections: "What coverage measures" (lines executed, not correctness), "Targets" (~80% line for most modules, 100% for security-sensitive logic), "Pitfalls" (high coverage with weak assertions is worse than low coverage with strong assertions), "Per-module thresholds" (tests should be stricter for domain logic than for UI glue).

    **File 8 — `src/domain-kb/testing/examples/jest-setup.md`** (~500 words):
    - H1: `# Jest Setup for a CommonJS Node Project`
    - Sections: "Minimal config" (package.json jest key, testMatch, testEnvironment), "Example test file" (2-3 tests of a hypothetical `add(a,b)` or `validateEmail(s)` function — ORIGINAL examples, not from docs), "Running a single test", "Coverage reporting" (jest --coverage flags).

    **File 9 — `src/domain-kb/testing/examples/playwright-e2e.md`** (~600 words):
    - H1: `# Playwright E2E Test Example`
    - Sections: "Install + config" (@playwright/test as devDep, playwright.config.js skeleton), "Example test" (a hypothetical login-flow test against `http://localhost:3000` — ORIGINAL scenario, not copy-pasted from docs), "Running headed vs headless", "Screenshot on failure", "Parallelism + sharding".

    **Content authorship discipline:**
    - Use your own wording. If you recognize a phrase as commonly seen online, rephrase.
    - Use generic function names (`add`, `validateEmail`, `Calculator`) in examples, not names from well-known projects.
    - For any code block, invent the scenario; do not reproduce real-world code from a blog or repo.
    - Keep the tone direct and practical. No "In this guide, you will learn..." or "Let's dive in..." filler.

    After all 9 files are created, run:
    - `npx prettier --write "src/domain-kb/testing/**/*.md"`
    - `npm run lint:md -- "src/domain-kb/testing/**/*.md"`
    - `npm run validate:kb-licenses`
  </action>
  <verify>
    <automated>node -e "const {globSync} = require('glob'); const files = globSync('src/domain-kb/testing/**/*.md', {absolute: false}); if (files.length !== 9) { console.error('expected 9 files, got', files.length, files); process.exit(1); } const fs=require('fs'); const yaml=require('yaml'); for (const f of files) { const c = fs.readFileSync(f,'utf8'); const m = c.match(/^---\n([\s\S]*?)\n---/); if (!m) { console.error(f, 'no frontmatter'); process.exit(1); } const fm = yaml.parse(m[1]); if (fm.source !== 'original') { console.error(f, 'source != original'); process.exit(1); } if (fm.license !== 'MIT') { console.error(f, 'license != MIT'); process.exit(1); } if (!/^\\d{4}-\\d{2}-\\d{2}$/.test(fm.last_reviewed)) { console.error(f, 'last_reviewed not ISO date:', fm.last_reviewed); process.exit(1); } const body = c.slice(m[0].length); if (!/^\\s*#\\s+\\S/.test(body)) { console.error(f, 'no H1 after frontmatter'); process.exit(1); } } console.log('OK 9 testing files'); process.exit(0);" && npm run validate:kb-licenses</automated>
  </verify>
  <done>
    - 9 files exist at the exact paths listed in files_modified for the testing pack.
    - Every file has YAML frontmatter with `source: original`, `license: MIT`, `last_reviewed: <today-ISO>`.
    - Every file has an H1 heading immediately after the frontmatter.
    - Content is authored from scratch (no copy-paste from external sources — author self-certifies in SUMMARY).
    - `npm run validate:kb-licenses` exits 0.
    - `npm run lint:md -- "src/domain-kb/testing/**/*.md"` exits 0.
  </done>
  <acceptance_criteria>
    - `find src/domain-kb/testing -name '*.md' | wc -l` = 9
    - `test -f src/domain-kb/testing/SKILL.md` returns 0
    - `test -f src/domain-kb/testing/anti-patterns.md` returns 0
    - `find src/domain-kb/testing/reference -name '*.md' | wc -l` = 5
    - `find src/domain-kb/testing/examples -name '*.md' | wc -l` = 2
    - For every file under `src/domain-kb/testing/**/*.md`:
      - `grep -c "^source: original$" FILE` >= 1
      - `grep -c "^license: MIT$" FILE` >= 1
      - `grep -cE "^last_reviewed: [0-9]{4}-[0-9]{2}-[0-9]{2}$" FILE` >= 1
      - `head -20 FILE | grep -cE "^# " FILE` >= 1 (H1 present early)
    - `npm run validate:kb-licenses` exits 0
    - `npm run lint:md -- "src/domain-kb/testing/**/*.md"` exits 0
    - Authorship self-certification recorded in 10-05-SUMMARY.md (see <output>)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author `src/domain-kb/architecture/` pack (9 files)</name>
  <files>src/domain-kb/architecture/SKILL.md, src/domain-kb/architecture/anti-patterns.md, src/domain-kb/architecture/reference/layered-architecture.md, src/domain-kb/architecture/reference/hexagonal.md, src/domain-kb/architecture/reference/modular-monolith.md, src/domain-kb/architecture/reference/event-driven.md, src/domain-kb/architecture/reference/api-design.md, src/domain-kb/architecture/examples/layered-node-app.md, src/domain-kb/architecture/examples/hexagonal-port-adapter.md</files>
  <read_first>
    - Same as Task 1 (CONTEXT.md D-12, PATTERNS.md §7, PITFALLS.md §4-J/§4-I, validate-kb-licenses.js)
    - Task 1 output as reference for pack shape consistency (testing pack must be done first or this task must mirror the shape independently)
  </read_first>
  <behavior>
    - Same structural + frontmatter requirements as Task 1, applied to the architecture pack.
    - Content topics: 5 references covering common architecture styles (layered, hexagonal, modular monolith, event-driven, API design) + 2 concrete examples + 1 SKILL.md + 1 anti-patterns.md.
  </behavior>
  <action>
    Create 9 files under `src/domain-kb/architecture/`. Same frontmatter template as Task 1.

    **File 1 — `src/domain-kb/architecture/SKILL.md`** (~400 words):
    - H1: `# Architecture Pack Overview`
    - Sections: "When to use this pack" (3-5 scenarios: e.g., "structuring a new service", "picking a boundary for a new module", "deciding between sync and async service-to-service calls"), "What's in this pack" (list other 8 files with one-sentence descriptions), "Principles" (dependency inversion, explicit boundaries, avoid speculative abstraction).

    **File 2 — `src/domain-kb/architecture/anti-patterns.md`** (~700 words):
    - H1: `# Architecture Anti-Patterns`
    - 6-8 anti-patterns: e.g., "Distributed monolith (micro-services with sync cross-calls)", "Ambiguous module boundaries", "Premature event-driven design", "God services", "Leaky abstractions in API design", "Sharing a database across services", "Inheritance for code reuse across unrelated concepts", "Synchronous call chains deeper than 3 hops".

    **File 3 — `src/domain-kb/architecture/reference/layered-architecture.md`** (~350 words):
    - H1: `# Layered Architecture`
    - Sections: "Typical layers" (presentation → application → domain → infrastructure), "Dependency rule" (outer depends on inner, never reverse), "When to use" (CRUD-dominant apps, well-understood domains), "When to avoid" (highly decoupled components, plugin-style extensibility).

    **File 4 — `src/domain-kb/architecture/reference/hexagonal.md`** (~400 words):
    - H1: `# Hexagonal Architecture (Ports and Adapters)`
    - Sections: "Concept" (core surrounded by ports; adapters implement ports), "Ports" (interfaces owned by the core), "Adapters" (database drivers, HTTP handlers, message subscribers), "When it pays off" (swappable infrastructure, tests via fakes), "Overhead" (indirection cost for trivial apps).

    **File 5 — `src/domain-kb/architecture/reference/modular-monolith.md`** (~400 words):
    - H1: `# Modular Monolith`
    - Sections: "Concept" (single deployable, strict internal module boundaries), "Module contract" (public API + private impl), "Why not microservices" (network latency, transaction boundaries, operational overhead), "Evolution path" (extract module to service when boundary is stable).

    **File 6 — `src/domain-kb/architecture/reference/event-driven.md`** (~400 words):
    - H1: `# Event-Driven Architecture`
    - Sections: "Concept" (producers emit events, consumers react asynchronously), "Event shape" (past-tense, domain-vocabulary, self-contained payload), "Ordering + idempotency", "Debugging cost", "When to use" (high-fan-out writes, cross-domain integration).

    **File 7 — `src/domain-kb/architecture/reference/api-design.md`** (~450 words):
    - H1: `# API Design Fundamentals`
    - Sections: "Resource vs RPC shape" (REST nouns vs gRPC verbs vs GraphQL queries), "Versioning" (URL vs header vs field), "Error contract" (status code + machine-readable type + human message), "Pagination" (cursor vs offset tradeoffs), "Idempotency for POST" (client-provided idempotency key).

    **File 8 — `src/domain-kb/architecture/examples/layered-node-app.md`** (~600 words):
    - H1: `# Example: Layered Node.js App`
    - Sections: "Directory layout" (routes/, services/, repositories/, models/), "Example flow" (GET /users/:id → route calls service → service calls repository → repository queries DB), "Where validation lives" (route entrypoint + domain invariants), "What NOT to put in the service layer" (HTTP status codes — belong in the route adapter).

    **File 9 — `src/domain-kb/architecture/examples/hexagonal-port-adapter.md`** (~700 words):
    - H1: `# Example: Hexagonal Port + Adapter in Node.js`
    - Sections: "Port interface" (`UserRepository` interface), "Two adapters" (`PostgresUserRepository` for production, `InMemoryUserRepository` for tests), "Core service uses the port" (sample service method calling the port, not a concrete class), "Wiring at the edge" (DI container or manual wiring in entry-point file), "Test benefit" (swap PostgresUserRepository for InMemory in tests, no DB required).

    Same authorship discipline as Task 1: invent scenarios, use generic names, paraphrase any commonly-seen phrasing.

    After all 9 files are created, run:
    - `npx prettier --write "src/domain-kb/architecture/**/*.md"`
    - `npm run lint:md -- "src/domain-kb/architecture/**/*.md"`
    - `npm run validate:kb-licenses`
    - `npm run test:domain-kb-install` (end-to-end — confirms Plan 02 installer copies both packs).
  </action>
  <verify>
    <automated>node -e "const {globSync} = require('glob'); const files = globSync('src/domain-kb/architecture/**/*.md', {absolute: false}); if (files.length !== 9) { console.error('expected 9 files, got', files.length); process.exit(1); } const fs=require('fs'); const yaml=require('yaml'); for (const f of files) { const c = fs.readFileSync(f,'utf8'); const m = c.match(/^---\n([\s\S]*?)\n---/); if (!m) { console.error(f,'no frontmatter'); process.exit(1); } const fm = yaml.parse(m[1]); if (fm.source !== 'original' || fm.license !== 'MIT' || !/^\\d{4}-\\d{2}-\\d{2}$/.test(fm.last_reviewed)) { console.error(f,'bad frontmatter'); process.exit(1); } const body = c.slice(m[0].length); if (!/^\\s*#\\s+\\S/.test(body)) { console.error(f,'no H1'); process.exit(1); } } console.log('OK 9 architecture files'); process.exit(0);" && npm run validate:kb-licenses && npm run test:domain-kb-install</automated>
  </verify>
  <done>
    - 9 files exist at the exact paths listed in files_modified for the architecture pack.
    - All files pass KB-01..KB-07 validation.
    - `npm run validate:kb-licenses` exits 0 (testing + architecture packs validated together).
    - `npm run test:domain-kb-install` exits 0 (end-to-end: installer copies both packs, manifest tracks them).
    - `npm run lint:md` exits 0 for both packs.
  </done>
  <acceptance_criteria>
    - `find src/domain-kb/architecture -name '*.md' | wc -l` = 9
    - `test -f src/domain-kb/architecture/SKILL.md` returns 0
    - `test -f src/domain-kb/architecture/anti-patterns.md` returns 0
    - `find src/domain-kb/architecture/reference -name '*.md' | wc -l` = 5
    - `find src/domain-kb/architecture/examples -name '*.md' | wc -l` = 2
    - For every file under `src/domain-kb/architecture/**/*.md`:
      - `grep -c "^source: original$" FILE` >= 1
      - `grep -c "^license: MIT$" FILE` >= 1
      - `grep -cE "^last_reviewed: [0-9]{4}-[0-9]{2}-[0-9]{2}$" FILE` >= 1
      - `head -20 FILE | grep -cE "^# " FILE` >= 1
    - Total KB files across both packs: `find src/domain-kb -name '*.md' | wc -l` = 18
    - `npm run validate:kb-licenses` exits 0
    - `npm run test:domain-kb-install` exits 0 (end-to-end from Plan 02)
    - `npm run lint:md -- "src/domain-kb/architecture/**/*.md"` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo content → public npm tarball | `src/domain-kb/**` ships to users via `gomad install`. IP/licensing compliance is load-bearing. |
| KB file → gm-domain-skill retrieval | Content becomes LLM context when retrieved by gm-domain-skill (Plan 04); content must be safe to feed to an agent (no prompt injection-style payloads). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05-01 | Information Disclosure (I) | IP/licensing violation — copied content from external source | mitigate | PITFALLS §4-J hard gate; author-from-scratch rule stated explicitly in both tasks; Plan 01's validate-kb-licenses.js enforces `source: original` OR a URL with compliant license via KB-02/KB-05/KB-06 rules. **CRITICAL severity** — violating this ships un-attributed content. Authorship self-certification in SUMMARY.md is the final checkpoint. |
| T-10-05-02 | Tampering (T) | Stale `last_reviewed` date | mitigate | Plan 01 enforces ISO-date shape (KB-04) but not freshness. PITFALLS §4-I (deferred to v1.4+) — for v1.3, all files land with today's date. Staleness review is a future concern. LOW. |
| T-10-05-03 | Tampering (T) | Prompt injection via KB content | mitigate | Content is author-committed (not user-input at install time). Plan 04 feeds content to LLM via `gm-domain-skill`. Mitigation: keep content domain-technical (testing/architecture); avoid meta-instructions like "Ignore previous context" or "Act as X". Task authorship discipline covers this. LOW. |
| T-10-05-04 | Denial of Service (D) | Oversized KB pack bloats install | accept | D-12 caps scope; 18 files × avg 500 words × 8 bytes/char ≈ 70KB total. No DoS. LOW. |
| T-10-05-05 | Repudiation (R) | Who authored which file | accept | Git blame + authorship self-certification in SUMMARY.md. LOW. |

**HIGH/CRITICAL findings (requiring mitigation plan in this plan):** T-10-05-01 (IP/licensing) — CRITICAL. Mitigated by: (a) Plan 01 release gate, (b) explicit authorship-from-scratch rule in both task actions, (c) required self-certification in SUMMARY.md, (d) task reviewer's paraphrase check. This gate landed BEFORE content per depends_on: [10-01].
</threat_model>

<verification>
Phase-level checks for this plan:

1. `find src/domain-kb -name '*.md' | wc -l` = 18 (9 per pack × 2 packs).
2. `npm run validate:kb-licenses` exits 0 — every file passes KB-01..KB-07 rules.
3. `npm run test:domain-kb-install` exits 0 — end-to-end: installer copies testing/ + architecture/ to `<installRoot>/_config/kb/` with manifest v2 rows.
4. `npm run lint:md -- "src/domain-kb/**/*.md"` exits 0.
5. `npm run validate:kb-licenses --strict` — promotes HIGH findings (KB-05/KB-06/KB-07) to failing. Expect exit 0 — no HIGH findings in author-from-scratch content.
6. Authorship self-certification recorded in 10-05-SUMMARY.md (affirmative statement: "All 18 files authored from scratch by [author/claude]; no verbatim or near-verbatim copying from external sources.")
</verification>

<success_criteria>
- 18 new `.md` files under `src/domain-kb/testing/` (9) and `src/domain-kb/architecture/` (9).
- Every file has frontmatter `source: original` + `license: MIT` + `last_reviewed: <ISO-today>`.
- Every file has an H1 heading after frontmatter.
- All files pass `npm run validate:kb-licenses`.
- `npm run test:domain-kb-install` passes end-to-end — installer round-trips both packs.
- Zero copied content from external sources (authorship self-certification in SUMMARY.md).
- `npm run lint:md` passes for all new files.
- Zero new runtime deps added.
</success_criteria>

<output>
After completion, create `.planning/phases/10-story-creation-enhancements/10-05-SUMMARY.md` documenting:
- File counts per pack (9 + 9 = 18)
- Topic list per pack with one-line descriptions
- **Authorship self-certification** — explicit statement: "All 18 files authored from scratch; no verbatim or near-verbatim copying from external sources per PITFALLS §4-J"
- Any content that drew inspiration from external concepts but was reworded (list them)
- Confirm `npm run validate:kb-licenses && npm run test:domain-kb-install` both exit 0
</output>
