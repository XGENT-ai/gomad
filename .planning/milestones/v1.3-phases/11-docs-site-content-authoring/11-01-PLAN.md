---
phase: 11-docs-site-content-authoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/tutorials/getting-started.md
  - docs/how-to/customize-gomad.md
  - docs/how-to/established-projects.md
  - docs/how-to/get-answers-about-gomad.md
  - docs/how-to/install-gomad.md
  - docs/how-to/non-interactive-installation.md
  - docs/how-to/project-context.md
  - docs/how-to/quick-fixes.md
  - docs/how-to/shard-large-documents.md
  - docs/how-to/upgrade-to-v6.md
  - docs/explanation/advanced-elicitation.md
  - docs/explanation/adversarial-review.md
  - docs/explanation/analysis-phase.md
  - docs/explanation/brainstorming.md
  - docs/explanation/checkpoint-preview.md
  - docs/explanation/established-projects-faq.md
  - docs/explanation/party-mode.md
  - docs/explanation/preventing-agent-conflicts.md
  - docs/explanation/project-context.md
  - docs/explanation/quick-dev.md
  - docs/explanation/why-solutioning-matters.md
  - docs/reference/agents.md
  - docs/reference/commands.md
  - docs/reference/core-tools.md
  - docs/reference/modules.md
  - docs/reference/testing.md
  - docs/reference/workflow-map.md
  - docs/zh-cn/tutorials/getting-started.md
  - docs/zh-cn/how-to/customize-gomad.md
  - docs/zh-cn/how-to/established-projects.md
  - docs/zh-cn/how-to/get-answers-about-gomad.md
  - docs/zh-cn/how-to/install-gomad.md
  - docs/zh-cn/how-to/non-interactive-installation.md
  - docs/zh-cn/how-to/project-context.md
  - docs/zh-cn/how-to/quick-fixes.md
  - docs/zh-cn/how-to/shard-large-documents.md
  - docs/zh-cn/how-to/upgrade-to-v6.md
  - docs/zh-cn/explanation/advanced-elicitation.md
  - docs/zh-cn/explanation/adversarial-review.md
  - docs/zh-cn/explanation/brainstorming.md
  - docs/zh-cn/explanation/established-projects-faq.md
  - docs/zh-cn/explanation/party-mode.md
  - docs/zh-cn/explanation/preventing-agent-conflicts.md
  - docs/zh-cn/explanation/project-context.md
  - docs/zh-cn/explanation/quick-dev.md
  - docs/zh-cn/explanation/why-solutioning-matters.md
  - docs/zh-cn/reference/agents.md
  - docs/zh-cn/reference/commands.md
  - docs/zh-cn/reference/core-tools.md
  - docs/zh-cn/reference/modules.md
  - docs/zh-cn/reference/testing.md
  - docs/zh-cn/reference/workflow-map.md
  - docs/index.md
  - docs/zh-cn/index.md
  - docs/roadmap.mdx
  - docs/zh-cn/roadmap.mdx
  - website/astro.config.mjs
  - tools/build-docs.mjs
  - .planning/phases/11-docs-site-content-authoring/PHASE-NOTE.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04
  - DOCS-05
  - DOCS-06

must_haves:
  truths:
    - "All 53 BMAD-era files under docs/{tutorials,how-to,reference,explanation}/ and zh-cn siblings are deleted from the working tree."
    - "docs/index.md and docs/zh-cn/index.md no longer link to deleted BMAD pages; rewritten files exist with the v1.3 landing-page structure (cross-links to Wave 3 pages are tolerated until Wave 4 integration plan 11-07 closes the link-check gate)."
    - "docs/roadmap.mdx and docs/zh-cn/roadmap.mdx are deleted; the slug: 'roadmap' sidebar entry in website/astro.config.mjs is removed."
    - "tools/build-docs.mjs LLM_EXCLUDE_PATTERNS no longer contains stale BMAD entries (bmgd/, v4-to-v6-upgrade, explanation/game-dev/)."
    - "PHASE-NOTE.md exists with the Phase 12 merge-coordination flag (D-11)."
  artifacts:
    - path: "docs/index.md"
      provides: "Rewritten gomad landing page (no BMAD references)"
      contains: "title: GoMad"
    - path: "docs/zh-cn/index.md"
      provides: "Rewritten zh-cn landing page mirror"
      contains: "title:"
    - path: "website/astro.config.mjs"
      provides: "Sidebar config without slug: 'roadmap' entry"
      contains: "starlight"
    - path: "tools/build-docs.mjs"
      provides: "LLM_EXCLUDE_PATTERNS without stale BMAD entries"
      contains: "LLM_EXCLUDE_PATTERNS"
    - path: ".planning/phases/11-docs-site-content-authoring/PHASE-NOTE.md"
      provides: "Cross-phase coordination note for Phase 12 release planner"
      contains: "Phase 12"
  key_links:
    - from: "docs/index.md"
      to: "docs/tutorials/install.md (authored in Plan 04)"
      via: "relative markdown link — link-check resolution deferred to Wave 4 integration plan 11-07"
      pattern: "(tutorials/install|/tutorials/install)"
    - from: "tools/build-docs.mjs"
      to: "validate-doc-links.js"
      via: "execSync at line ~459"
      pattern: "validate-doc-links"
---

<objective>
Delete all 53 inherited BMAD-era doc pages, rewrite docs/index.md (+ zh-cn) as the gomad landing page, delete roadmap.mdx (+ zh-cn) and its sidebar entry, scrub stale LLM_EXCLUDE_PATTERNS in tools/build-docs.mjs, and write a cross-phase coordination note (PHASE-NOTE.md) flagging that Phase 12's release plan must merge this branch alongside `npm publish @xgent-ai/gomad@1.3.0`.

Purpose: Prepare a clean docs tree before authoring plans (03-06) land their pages. Cleanup is intentionally separate from authoring (per CONTEXT D-04) so cleanup is reversible without blocking partial authoring landings, and so the link-check gate stays green throughout.

Output: 53 file deletions + 4 rewrites (index EN/zh-cn + delete roadmap EN/zh-cn) + 1 sidebar config edit + 1 build-docs.mjs scrub + 1 phase-note artifact.

Wave 1 isolation note: Task 3 verify deliberately does NOT run `npm run docs:build`. The rewritten `docs/index.md` cross-links to 6 future pages (`tutorials/install.md`, `tutorials/quick-start.md`, `reference/agents.md`, `reference/skills.md`, `explanation/architecture.md`, `how-to/contributing.md`) authored in Wave 3 plans; the link-check stage would fail on those forward refs. Wave 4 integration plan `11-07-PLAN.md` runs the full `npm run docs:build` once all Wave 3 plans complete. This plan asserts cleanup correctness via isolated checks (file existence, content greps, syntax checks) — no build invocation.
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-docs-site-content-authoring/11-CONTEXT.md
@.planning/phases/11-docs-site-content-authoring/11-RESEARCH.md
@.planning/phases/11-docs-site-content-authoring/11-PATTERNS.md
@docs/_STYLE_GUIDE.md
@docs/index.md
@docs/404.md
@website/astro.config.mjs
@tools/build-docs.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete 53 inherited BMAD-era pages and remove roadmap sidebar entry</name>
  <files>
    Delete (53 EN + zh-cn pages — exact list from RESEARCH.md "Existing BMAD Content Audit"):
    - docs/tutorials/getting-started.md
    - docs/how-to/customize-gomad.md
    - docs/how-to/established-projects.md
    - docs/how-to/get-answers-about-gomad.md
    - docs/how-to/install-gomad.md
    - docs/how-to/non-interactive-installation.md
    - docs/how-to/project-context.md
    - docs/how-to/quick-fixes.md
    - docs/how-to/shard-large-documents.md
    - docs/how-to/upgrade-to-v6.md
    - docs/explanation/advanced-elicitation.md
    - docs/explanation/adversarial-review.md
    - docs/explanation/analysis-phase.md
    - docs/explanation/brainstorming.md
    - docs/explanation/checkpoint-preview.md
    - docs/explanation/established-projects-faq.md
    - docs/explanation/party-mode.md
    - docs/explanation/preventing-agent-conflicts.md
    - docs/explanation/project-context.md
    - docs/explanation/quick-dev.md
    - docs/explanation/why-solutioning-matters.md
    - docs/reference/agents.md
    - docs/reference/commands.md
    - docs/reference/core-tools.md
    - docs/reference/modules.md
    - docs/reference/testing.md
    - docs/reference/workflow-map.md
    - docs/zh-cn/tutorials/getting-started.md
    - docs/zh-cn/how-to/customize-gomad.md
    - docs/zh-cn/how-to/established-projects.md
    - docs/zh-cn/how-to/get-answers-about-gomad.md
    - docs/zh-cn/how-to/install-gomad.md
    - docs/zh-cn/how-to/non-interactive-installation.md
    - docs/zh-cn/how-to/project-context.md
    - docs/zh-cn/how-to/quick-fixes.md
    - docs/zh-cn/how-to/shard-large-documents.md
    - docs/zh-cn/how-to/upgrade-to-v6.md
    - docs/zh-cn/explanation/advanced-elicitation.md
    - docs/zh-cn/explanation/adversarial-review.md
    - docs/zh-cn/explanation/brainstorming.md
    - docs/zh-cn/explanation/established-projects-faq.md
    - docs/zh-cn/explanation/party-mode.md
    - docs/zh-cn/explanation/preventing-agent-conflicts.md
    - docs/zh-cn/explanation/project-context.md
    - docs/zh-cn/explanation/quick-dev.md
    - docs/zh-cn/explanation/why-solutioning-matters.md
    - docs/zh-cn/reference/agents.md
    - docs/zh-cn/reference/commands.md
    - docs/zh-cn/reference/core-tools.md
    - docs/zh-cn/reference/modules.md
    - docs/zh-cn/reference/testing.md
    - docs/zh-cn/reference/workflow-map.md
    - docs/roadmap.mdx
    - docs/zh-cn/roadmap.mdx
    Modify:
    - website/astro.config.mjs (remove slug: 'roadmap' sidebar block at lines ~96-100)
  </files>
  <read_first>
    - .planning/phases/11-docs-site-content-authoring/11-RESEARCH.md (lines 653-735 — full deletion list + which 5 files MUST be kept)
    - .planning/phases/11-docs-site-content-authoring/11-CONTEXT.md (D-01, D-02, D-03, D-04 — disposition rules)
    - website/astro.config.mjs (sidebar block at lines ~96-100; the slug: 'roadmap' block must be removed when roadmap.mdx is deleted, otherwise Starlight emits a warning about missing slug)
    - docs/_STYLE_GUIDE.md (frozen — DO NOT delete)
    - docs/404.md (KEEP — DO NOT delete)
    - docs/upgrade-recovery.md (KEEP — still relevant for v1.2 backup-restore)
    - docs/zh-cn/_STYLE_GUIDE.md (KEEP — frozen mirror)
    - docs/zh-cn/404.md (KEEP)
  </read_first>
  <action>
    Delete all 53 files listed in `<files>` using `git rm` so the deletions land as proper git operations (not just unlinks). Use the exact paths from RESEARCH.md "Files to DELETE" — do not modify the deletion list. After deletions, the directories `docs/tutorials/`, `docs/how-to/`, `docs/reference/`, `docs/explanation/`, and their zh-cn mirrors should be empty (until Plans 03-06 author new pages). Note: zh-cn/explanation has 9 files (not 11 — `analysis-phase.md` and `checkpoint-preview.md` were never translated).

    After deletions, edit `website/astro.config.mjs`:
    - Locate the sidebar config block that contains `slug: 'roadmap'` (around lines 96-100, inside the Starlight `sidebar:` array).
    - Remove the entire object that has `slug: 'roadmap'`. Leave surrounding sidebar entries intact.
    - The autogenerate entries for `tutorials`, `how-to`, `reference`, `explanation` MUST stay — Starlight will repopulate them as Plans 03-06 author new pages.

    Per D-03 (planner picks delete-vs-rewrite for roadmap.mdx), this plan picks DELETE — the live `.planning/ROADMAP.md` is one click away on GitHub and a single-pointer page adds maintenance burden for negligible audience value (RESEARCH.md "Open Question 1" recommends DELETE).

    Do NOT touch:
    - docs/_STYLE_GUIDE.md, docs/zh-cn/_STYLE_GUIDE.md (FROZEN per CONTEXT.md "Specifics")
    - docs/404.md, docs/zh-cn/404.md (custom 404 splash)
    - docs/upgrade-recovery.md (v1.2 backup-restore docs, still current)
    - docs/index.md, docs/zh-cn/index.md (rewritten in Task 2)

    Do NOT touch `.github/workflows/docs.yaml` — branch isolation (D-11) handles deploy gating without CI surgery.
  </action>
  <verify>
    <automated>
      # All 53 EN + zh-cn deleted files are gone:
      [ ! -f docs/tutorials/getting-started.md ] && \
      [ ! -f docs/how-to/install-gomad.md ] && \
      [ ! -f docs/how-to/upgrade-to-v6.md ] && \
      [ ! -f docs/explanation/quick-dev.md ] && \
      [ ! -f docs/reference/agents.md ] && \
      [ ! -f docs/reference/commands.md ] && \
      [ ! -f docs/zh-cn/tutorials/getting-started.md ] && \
      [ ! -f docs/zh-cn/how-to/install-gomad.md ] && \
      [ ! -f docs/zh-cn/explanation/quick-dev.md ] && \
      [ ! -f docs/zh-cn/reference/agents.md ] && \
      [ ! -f docs/roadmap.mdx ] && \
      [ ! -f docs/zh-cn/roadmap.mdx ] && \
      # docs/{tutorials,how-to,reference,explanation}/ are empty (no .md remaining):
      [ "$(find docs/tutorials -maxdepth 1 -name '*.md' | wc -l | tr -d ' ')" = "0" ] && \
      [ "$(find docs/how-to -maxdepth 1 -name '*.md' | wc -l | tr -d ' ')" = "0" ] && \
      [ "$(find docs/reference -maxdepth 1 -name '*.md' | wc -l | tr -d ' ')" = "0" ] && \
      [ "$(find docs/explanation -maxdepth 1 -name '*.md' | wc -l | tr -d ' ')" = "0" ] && \
      # KEEP files survived:
      [ -f docs/_STYLE_GUIDE.md ] && \
      [ -f docs/404.md ] && \
      [ -f docs/upgrade-recovery.md ] && \
      [ -f docs/zh-cn/_STYLE_GUIDE.md ] && \
      [ -f docs/zh-cn/404.md ] && \
      # Sidebar entry removed:
      ! grep -q "slug: 'roadmap'" website/astro.config.mjs && \
      ! grep -q 'slug: "roadmap"' website/astro.config.mjs
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - `find docs/tutorials docs/how-to docs/reference docs/explanation -maxdepth 1 -name '*.md' | wc -l` outputs `0`.
    - `find docs/zh-cn/tutorials docs/zh-cn/how-to docs/zh-cn/reference docs/zh-cn/explanation -maxdepth 1 -name '*.md' | wc -l` outputs `0`.
    - `[ ! -f docs/roadmap.mdx ]` and `[ ! -f docs/zh-cn/roadmap.mdx ]` both succeed.
    - `[ -f docs/_STYLE_GUIDE.md ] && [ -f docs/404.md ] && [ -f docs/upgrade-recovery.md ]` succeeds (KEEP files preserved).
    - `! grep -E "slug:\s*['\"]roadmap['\"]" website/astro.config.mjs` succeeds (no roadmap sidebar entry).
    - `git status --porcelain | grep '^D ' | wc -l` outputs at least `53` (53 deleted files staged).
  </acceptance_criteria>
  <done>53 BMAD pages deleted, roadmap.mdx + zh-cn sibling deleted, slug: 'roadmap' sidebar entry removed from astro.config.mjs. KEEP files (_STYLE_GUIDE.md, 404.md, upgrade-recovery.md and zh-cn siblings) untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite docs/index.md and docs/zh-cn/index.md as the gomad landing page</name>
  <files>
    - docs/index.md (REWRITE)
    - docs/zh-cn/index.md (REWRITE)
  </files>
  <read_first>
    - docs/index.md (current state — note frontmatter `title: GoMad`, `description:`; replace BMAD-era body)
    - docs/zh-cn/index.md (current state — same shape in Chinese)
    - docs/404.md (frontmatter analog if planner uses `template: splash`)
    - docs/_STYLE_GUIDE.md (header budget 8-12 `##`, no `####`, admonition syntax `:::tip[]`/`:::note[]`/`:::caution[]`/`:::danger[]`, no horizontal rules outside frontmatter, no "Related"/"Next:" sections)
    - .planning/phases/11-docs-site-content-authoring/11-PATTERNS.md (lines 254-281 — index.md REWRITE pattern, frontmatter, link-check constraint)
    - .planning/phases/11-docs-site-content-authoring/11-CONTEXT.md (D-03, D-10 path layout, D-13 EN-first then zh-cn)
  </read_first>
  <action>
    Rewrite `docs/index.md` as the gomad landing page. Use this exact frontmatter shape:

    ```markdown
    ---
    title: GoMad
    description: Agentic workflow framework for AI-driven agile development — opinionated agents, skills, and an installer that ships them into your AI IDE.
    ---
    ```

    Body MUST contain (in order, with these exact `##` section headers):
    1. A 1-2 sentence hook paragraph after the frontmatter (no header) describing what gomad is.
    2. `## What is GoMad?` — 2-3 paragraphs covering: a lean fork of BMAD Method, owned end-to-end, 4-phase agile workflow (Analysis → Planning → Solutioning → Implementation), zero new runtime deps. Mention the 8 agent personas (`gm-agent-*`).
    3. `## Get started` — link to the install tutorial as a relative markdown link `[Install GoMad](./tutorials/install.md)` (the file is authored in Plan 04 — link will resolve once that plan lands; the link-check gate runs in Wave 4 integration plan `11-07-PLAN.md` after all Wave 3 plans complete). Also link `[Quick start](./tutorials/quick-start.md)`.
    4. `## Browse the docs` — bullet list with 4 links: `[Agents reference](./reference/agents.md)`, `[Skills reference](./reference/skills.md)`, `[Architecture](./explanation/architecture.md)`, `[Contributing](./how-to/contributing.md)`.
    5. `## Project status` — single paragraph noting the current milestone (v1.3) and pointing to `https://github.com/xgent-ai/gomad/blob/main/.planning/ROADMAP.md` for the live roadmap. Use admonition `:::note[Live roadmap]`.

    Constraints (style-guide enforced):
    - No `####` headers anywhere.
    - No `---` horizontal rules outside frontmatter.
    - No "Related" / "Next:" sections.
    - 5 `##` headers total (within budget 8-12 — landing pages can be lighter).
    - Admonitions: at most 1 (in Project status section).
    - DO NOT use `template: splash` — keep the default `doc` template so the sidebar renders alongside the landing content.
    - DO NOT mention BMAD persona names (Mary/John/Winston/Bob/Sally/Amelia/Paige/Barry are gomad personas — reference them by `gm-agent-*` short names).
    - DO NOT mention BMM, BMB, or BMGD modules.
    - DO NOT mention v1.2 paths (no `<installRoot>/gomad/agents/`); if any path appears, use `<installRoot>/_config/agents/<shortName>.md` per D-10.
    - DO NOT link to deleted pages (`./tutorials/getting-started.md`, `./how-to/install-gomad.md`, `./explanation/analysis-phase.md`, `./explanation/quick-dev.md`, `./roadmap` etc.).

    Then mirror the structure in `docs/zh-cn/index.md`. Translation rules per D-13/D-14:
    - Translate `title:` (e.g., `title: GoMad` → keep brand as `GoMad`) and `description:` to Simplified Chinese.
    - Translate `##` section headers and prose body to Simplified Chinese.
    - Code blocks, file paths, slash commands, npm package names stay in English.
    - Same 5-section structure as EN (Project status admonition translated).
    - Preserve relative markdown links unchanged (`./tutorials/install.md` etc. — the zh-cn pages live at `docs/zh-cn/tutorials/install.md` and Starlight resolves locale-aware routing automatically; relative paths point to EN siblings, which is the existing convention — verify against current `docs/zh-cn/index.md`).

    Critical: This rewrite MUST land in the same plan as the deletions in Task 1, otherwise `docs/index.md` carries stale BMAD body content into the working tree. The link-check gate is NOT run in this plan (forward links to Wave 3 pages would fail); plan `11-07` (Wave 4) runs the full build after all Wave 3 plans land their cross-linked siblings.
  </action>
  <verify>
    <automated>
      # Frontmatter present with title:
      grep -q '^title: GoMad' docs/index.md && \
      grep -q '^title:' docs/zh-cn/index.md && \
      # Required section headers in EN:
      grep -q '^## What is GoMad?' docs/index.md && \
      grep -q '^## Get started' docs/index.md && \
      grep -q '^## Browse the docs' docs/index.md && \
      grep -q '^## Project status' docs/index.md && \
      # No BMAD references:
      ! grep -iE '\b(BMM|BMB|BMGD|getting-started|install-gomad)\b' docs/index.md && \
      ! grep -iE '\b(BMM|BMB|BMGD|getting-started|install-gomad)\b' docs/zh-cn/index.md && \
      # No v1.2 install path:
      ! grep -F '<installRoot>/gomad/agents/' docs/index.md && \
      ! grep -F '<installRoot>/gomad/agents/' docs/zh-cn/index.md && \
      # No #### headers:
      ! grep -E '^####' docs/index.md && \
      ! grep -E '^####' docs/zh-cn/index.md && \
      # No horizontal rules outside frontmatter (rough check — first two `---` are frontmatter; any further is a rule):
      [ "$(grep -c '^---$' docs/index.md)" -le 2 ] && \
      [ "$(grep -c '^---$' docs/zh-cn/index.md)" -le 2 ] && \
      # No "Related"/"Next:" sections:
      ! grep -E '^## (Related|Next:?)' docs/index.md && \
      ! grep -E '^## (Related|Next:?)' docs/zh-cn/index.md
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## ' docs/index.md` outputs `5` (five `##` sections).
    - `grep -c '^## ' docs/zh-cn/index.md` outputs `5`.
    - `grep -F '[Install GoMad](./tutorials/install.md)' docs/index.md` finds the relative link.
    - `grep -F '[Agents reference](./reference/agents.md)' docs/index.md` finds the relative link.
    - `grep -F '.planning/ROADMAP.md' docs/index.md` finds the live-roadmap pointer.
    - `! grep -F 'getting-started' docs/index.md` succeeds (no link to deleted page).
    - `! grep -F '<installRoot>/gomad/agents/' docs/index.md` succeeds (no v1.2 path).
    - `! grep -E '^####' docs/index.md` succeeds (no `####` headers).
    - Same set of grep assertions pass for `docs/zh-cn/index.md` (titles + section headers translated; structure mirrors EN).
  </acceptance_criteria>
  <done>docs/index.md and docs/zh-cn/index.md rewritten as gomad landing pages with 5 `##` sections, relative links to all 6 future pages, no BMAD references, no v1.2 paths, style-guide compliant. Forward-link resolution against Wave 3 siblings is intentionally deferred to Wave 4 plan 11-07.</done>
</task>

<task type="auto">
  <name>Task 3: Scrub stale LLM_EXCLUDE_PATTERNS in tools/build-docs.mjs and write PHASE-NOTE.md</name>
  <files>
    - tools/build-docs.mjs (PATCH lines ~34-43 — drop 3 stale entries)
    - .planning/phases/11-docs-site-content-authoring/PHASE-NOTE.md (NEW)
  </files>
  <read_first>
    - tools/build-docs.mjs (lines 34-43 — current LLM_EXCLUDE_PATTERNS array)
    - .planning/phases/11-docs-site-content-authoring/11-RESEARCH.md (lines 813-837 "LLM_EXCLUDE_PATTERNS Audit" — exact entries to drop, exact entries to KEEP)
    - .planning/phases/11-docs-site-content-authoring/11-CONTEXT.md (D-11 branch isolation, "Claude's Discretion" item 9)
  </read_first>
  <action>
    Edit `tools/build-docs.mjs` at lines ~34-43 — replace the current `LLM_EXCLUDE_PATTERNS` array with the cleaned version. Drop these three stale entries (all tied to deleted BMAD content):
    - `'v4-to-v6-upgrade'` (matched the deleted `docs/how-to/upgrade-to-v6.md`)
    - `'explanation/game-dev/'` (gomad has no game-dev module)
    - `'bmgd/'` (BMM Game Dev module, never existed in gomad)

    KEEP these entries (harmless future-proofing):
    - `'changelog'`
    - `'ide-info/'`
    - `'faq'`
    - `'reference/glossary/'`

    Final array shape:
    ```javascript
    const LLM_EXCLUDE_PATTERNS = [
      'changelog',
      'ide-info/',
      'faq',
      'reference/glossary/',
    ];
    ```

    Then create `.planning/phases/11-docs-site-content-authoring/PHASE-NOTE.md` with this exact content (cross-phase coordination flag for D-11):

    ```markdown
    # Phase 11 → Phase 12 Coordination Note

    **Created:** during Phase 11 Plan 01 (cleanup)
    **Branch:** {current branch — e.g., gsd/phase-11-docs-site-content-authoring}

    ## Why this exists

    Per CONTEXT.md D-10/D-11, Phase 11 docs reference the v1.3 install layout
    (`<installRoot>/_config/agents/<shortName>.md`) but the npm registry still
    serves `@xgent-ai/gomad@1.2.x` which uses the v1.2 layout
    (`<installRoot>/gomad/agents/`). To prevent docs from advertising paths
    that don't match the shipped npm package, deploy is gated by branch
    isolation: this branch does NOT auto-deploy.

    ## Required Phase 12 action

    Phase 12's release plan (Plans TBD) MUST schedule the merge of this
    Phase 11 branch into `main` AS PART OF the v1.3.0 release sequence:

    1. Phase 12 lands AGENT-* relocation + REL-* release tasks on its own branch.
    2. Phase 12 publishes `@xgent-ai/gomad@1.3.0` to npm (REL-03).
    3. Phase 12 merges its own branch to `main`, triggering `.github/workflows/docs.yaml`
       which auto-deploys docs to `gomad.xgent.ai`.
    4. **At step 3, this Phase 11 branch must already be merged to main** (or
       merged in the same flight) so the live docs reflect v1.3 paths
       simultaneously with the npm release.

    ## Why this matters

    If Phase 11 merges before Phase 12 ships v1.3.0: live docs advertise
    v1.3 paths over a v1.2 npm install. Users hit "command not found"
    confusion.

    If Phase 12 ships v1.3.0 but Phase 11 sits unmerged: live docs still
    show stale BMAD content and never reflect the v1.3 reality.

    ## Verification

    DOCS-07 in Phase 12 (`tools/validate-doc-paths.js`) is the final gate
    that catches v1.2 path leaks in docs. It runs as part of `npm run quality`
    on the release commit. Phase 11 declares "authored to v1.3 spec"; Phase 12
    enforces.
    ```

    Replace `{current branch — e.g., gsd/phase-11-docs-site-content-authoring}` with the actual current branch name (capture from `git rev-parse --abbrev-ref HEAD` at execution time).

    Commit message for this task should include the line:
    `note: Phase 12 release plan must schedule merge of this branch alongside npm publish @xgent-ai/gomad@1.3.0 (see .planning/phases/11-docs-site-content-authoring/PHASE-NOTE.md)`
  </action>
  <verify>
    <automated>
      # Stale entries dropped:
      ! grep -F "'v4-to-v6-upgrade'" tools/build-docs.mjs && \
      ! grep -F "'explanation/game-dev/'" tools/build-docs.mjs && \
      ! grep -F "'bmgd/'" tools/build-docs.mjs && \
      # KEEP entries still present:
      grep -F "'changelog'" tools/build-docs.mjs && \
      grep -F "'ide-info/'" tools/build-docs.mjs && \
      grep -F "'faq'" tools/build-docs.mjs && \
      grep -F "'reference/glossary/'" tools/build-docs.mjs && \
      # PHASE-NOTE.md exists with required markers:
      [ -f .planning/phases/11-docs-site-content-authoring/PHASE-NOTE.md ] && \
      grep -q 'Phase 11 → Phase 12 Coordination Note' .planning/phases/11-docs-site-content-authoring/PHASE-NOTE.md && \
      grep -q '@xgent-ai/gomad@1.3.0' .planning/phases/11-docs-site-content-authoring/PHASE-NOTE.md && \
      grep -q 'gomad.xgent.ai' .planning/phases/11-docs-site-content-authoring/PHASE-NOTE.md && \
      grep -q '_config/agents' .planning/phases/11-docs-site-content-authoring/PHASE-NOTE.md && \
      # Mechanical BMAD-pattern check (substantive content; full link-check deferred to Wave 4 plan 11-07):
      [ "$(find docs -path 'docs/zh-cn' -prune -o -type f -name '*.md' -print 2>/dev/null | xargs grep -l 'BMAD' 2>/dev/null | wc -l | tr -d ' ')" = "0" ] && \
      # Rewritten landing pages exist with v1.3 frontmatter shape:
      [ -f docs/index.md ] && grep -q '^title: GoMad' docs/index.md && \
      [ -f docs/zh-cn/index.md ] && grep -q '^title:' docs/zh-cn/index.md && \
      # build-docs.mjs is syntactically valid (Wave 4 plan 11-07 runs the full pipeline):
      node --check tools/build-docs.mjs
      echo OK
      # NOTE: `npm run docs:build` is INTENTIONALLY NOT run here. The rewritten docs/index.md
      # cross-links to 6 Wave 3 pages that don't exist yet, so the link-check stage would fail.
      # Wave 4 plan 11-07-PLAN.md runs the full build once all Wave 3 plans complete.
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LLM_EXCLUDE_PATTERNS" tools/build-docs.mjs` outputs at least `1` (the constant still exists).
    - `! grep -F "'v4-to-v6-upgrade'" tools/build-docs.mjs` succeeds.
    - `! grep -F "'bmgd/'" tools/build-docs.mjs` succeeds.
    - `! grep -F "'explanation/game-dev/'" tools/build-docs.mjs` succeeds.
    - `[ -f .planning/phases/11-docs-site-content-authoring/PHASE-NOTE.md ]` succeeds.
    - `grep -q '@xgent-ai/gomad@1.3.0' .planning/phases/11-docs-site-content-authoring/PHASE-NOTE.md` succeeds.
    - `node --check tools/build-docs.mjs` exits 0 (syntax valid after the LLM_EXCLUDE_PATTERNS edit).
    - `[ -f docs/index.md ] && [ -f docs/zh-cn/index.md ]` succeed (rewritten landing pages exist from Task 2).
    - Full `npm run docs:build` is NOT asserted in this plan — Wave 4 integration plan `11-07-PLAN.md` owns end-to-end build verification.
  </acceptance_criteria>
  <done>tools/build-docs.mjs has stale BMAD patterns removed, KEEP patterns preserved. PHASE-NOTE.md created with the Phase 12 merge-coordination flag. Cleanup correctness asserted via isolated checks; full `npm run docs:build` is deferred to Wave 4 plan 11-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → public docs site | Branch isolation gates this; only `main` deploys via `.github/workflows/docs.yaml` (D-11). Phase 11 commits land on a feature branch and do not deploy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Information Disclosure | Public `gomad.xgent.ai` site | accept | Branch isolation per D-11 — feature branch does NOT trigger deploy (verified: docs.yaml line 5-6 `push: branches: [main]`). PHASE-NOTE.md flags Phase 12 must merge this branch only after `npm publish 1.3.0` to keep docs and shipped npm package aligned. |
| T-11-01-02 | Tampering | Sidebar config (`website/astro.config.mjs`) | mitigate | Removing the `slug: 'roadmap'` block in the same commit as the roadmap.mdx deletion prevents Starlight build warnings about a missing slug. Verified by acceptance criterion `! grep -E "slug:\s*['\"]roadmap['\"]" website/astro.config.mjs`. |
| T-11-01-03 | Tampering | `tools/build-docs.mjs` LLM_EXCLUDE_PATTERNS | mitigate | Dropping only entries explicitly tied to deleted content (`v4-to-v6-upgrade`, `explanation/game-dev/`, `bmgd/`); keeping forward-looking patterns (`changelog`, `ide-info/`, `faq`, `reference/glossary/`). Acceptance criteria assert exact membership before/after. Risk if wrong (Assumption A2): llms-full.txt size warning. Verified by `node --check tools/build-docs.mjs` syntax check + Wave 4 plan 11-07 full build. |
| T-11-01-04 | Denial of Service | `npm run docs:build` link-check stage | accept | Cleanup commit MUST include the index.md rewrite in the same plan (Task 2 lands in same plan as Task 1) so the working tree is internally consistent. Forward links from docs/index.md to Wave 3 pages will fail link-check until Wave 3 completes — that's why this plan does NOT run `npm run docs:build`. Wave 4 integration plan `11-07-PLAN.md` enforces the link-check gate after all Wave 3 plans land. |
</threat_model>

<verification>
- All 53 BMAD-era files deleted; KEEP files preserved.
- docs/index.md and docs/zh-cn/index.md rewritten with no BMAD refs, no `####` headers, no horizontal rules outside frontmatter.
- roadmap.mdx + zh-cn sibling deleted; `slug: 'roadmap'` sidebar entry removed from astro.config.mjs.
- tools/build-docs.mjs LLM_EXCLUDE_PATTERNS scrubbed; `node --check tools/build-docs.mjs` exits 0.
- PHASE-NOTE.md exists with the Phase 12 merge-coordination flag.
- Full `npm run docs:build` is intentionally NOT run in this plan; Wave 4 plan `11-07-PLAN.md` handles end-to-end build verification once Wave 3 plans land.
</verification>

<success_criteria>
1. `find docs/{tutorials,how-to,reference,explanation} docs/zh-cn/{tutorials,how-to,reference,explanation} -maxdepth 1 -name '*.md' | wc -l` outputs `0`.
2. `[ ! -f docs/roadmap.mdx ] && [ ! -f docs/zh-cn/roadmap.mdx ]` succeeds.
3. `! grep -E "slug:\s*['\"]roadmap['\"]" website/astro.config.mjs` succeeds.
4. `! grep -F "'bmgd/'" tools/build-docs.mjs` succeeds (and the other two stale patterns).
5. `[ -f .planning/phases/11-docs-site-content-authoring/PHASE-NOTE.md ]` AND it mentions `@xgent-ai/gomad@1.3.0`.
6. `node --check tools/build-docs.mjs` exits 0 (syntax check after the LLM_EXCLUDE_PATTERNS edit; full build is Wave 4 plan 11-07's responsibility).
</success_criteria>

<output>
After completion, create `.planning/phases/11-docs-site-content-authoring/11-01-SUMMARY.md` documenting: deletion list (53 files), index rewrite shape, sidebar edit, LLM_EXCLUDE_PATTERNS delta, PHASE-NOTE.md location, and the deferral of `npm run docs:build` to Wave 4 plan 11-07.
</output>
