---
phase: 11-docs-site-content-authoring
plan: 09
type: execute
wave: 5
gap_closure: true
depends_on:
  - 11-07
files_modified:
  - docs/zh-cn/upgrade-recovery.md
autonomous: true
requirements:
  - DOCS-04
  - DOCS-06

must_haves:
  truths:
    - "docs/zh-cn/upgrade-recovery.md exists as a translation of docs/upgrade-recovery.md, mirroring the EN structure (frontmatter + 7 H2 sections covering backup directory layout, metadata.json schema, restore steps, version compat, identifying which backup, pruning, when backups are NOT produced)."
    - "The relative link `../upgrade-recovery.md` from docs/zh-cn/explanation/architecture.md:65 now resolves cleanly (target file exists at docs/zh-cn/upgrade-recovery.md)."
    - "Translation honors D-13 (EN-first then translate) and D-14 (Claude drafts and commits zh-cn without human pre-review) — no glossary lock, no review window, ship-and-iterate."
    - "Per D-14 zh-cn convention from siblings: prose translated to Simplified Chinese; English commands, file paths, code blocks, bash snippets, JSON, and the literal `_gomad/_backups/<YYYYMMDD-HHMMSS>/` token preserved verbatim. Frontmatter `title:` and `description:` translated."
  artifacts:
    - path: "docs/zh-cn/upgrade-recovery.md"
      provides: "zh-cn translation of upgrade-recovery guide referenced by docs/zh-cn/explanation/architecture.md:65"
      min_lines: 100
      contains: "_gomad/_backups"
  key_links:
    - from: "docs/zh-cn/explanation/architecture.md line 65"
      to: "docs/zh-cn/upgrade-recovery.md"
      via: "relative markdown link `../upgrade-recovery.md`"
      pattern: "升级恢复"
---

<objective>
Close Gap B by translating `docs/upgrade-recovery.md` (English, kept by Plan 01 cleanup) into `docs/zh-cn/upgrade-recovery.md`. The current zh-cn architecture explainer (Plan 05 output) cross-links to `../upgrade-recovery.md` from line 65, but no zh-cn sibling exists, causing the link-checker to fail with `File not found anywhere` for `docs/zh-cn/upgrade-recovery.md`.

Purpose: Phase 11 D-13 + D-14 mandate full EN/zh-cn parity for every page that EN authors. Plan 05 already translated the architecture explainer end-to-end, including the cross-link to upgrade-recovery, on the assumption that a zh-cn upgrade-recovery sibling existed. Plan 01 left `docs/upgrade-recovery.md` in place (D-04: kept as part of v1.2 baseline) but did NOT translate it because Phase 11's spec-locked page list (DOCS-01..05) didn't include upgrade-recovery as an authored page. The zh-cn cross-link orphan is the result. The fix per 11-07-SUMMARY's preferred path: translate the EN page so the link resolves, matching the EN+zh-cn lockstep pattern from the rest of Phase 11.

Output: 1 new file (`docs/zh-cn/upgrade-recovery.md`). No other files modified. Plan 11-10's integration gate confirms the patch.

Why a separate plan from Plan 11-08: Different files (Plan 11-08 touches `tools/`, Plan 11-09 touches `docs/zh-cn/`); no file overlap means they can run in parallel in Wave 5. Different concerns (tooling correctness vs. translation parity) means easier code review and surgical revert if either patch needs adjustment.
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-docs-site-content-authoring/11-CONTEXT.md
@.planning/phases/11-docs-site-content-authoring/11-VERIFICATION.md
@.planning/phases/11-docs-site-content-authoring/11-07-SUMMARY.md
@.planning/phases/11-docs-site-content-authoring/11-05-SUMMARY.md
@docs/upgrade-recovery.md
@docs/zh-cn/explanation/architecture.md
@docs/_STYLE_GUIDE.md

<interfaces>
<!-- Translation contract derived from CONTEXT.md D-13/D-14 and Plan 05's actual zh-cn architecture output. -->

**EN source structure (`docs/upgrade-recovery.md`):**

```yaml
# (frontmatter delimiters omitted in this snippet to keep the surrounding plan frontmatter parser happy)
title: "Upgrade Recovery"
description: How manifest-driven cleanup snapshots work and how to restore from them
```

7 H2 sections:
1. (preamble paragraph — no header)
2. `## Backup directory layout`
3. `## metadata.json schema`
4. `## How to restore` (with H3 subsections: `### Quick recovery`, `### Restore file content only (skip metadata.json and README.md)`, `### Before restoring — version compat check`)
5. `## Identifying which backup to restore`
6. `## Pruning backups`
7. `## When backups are NOT produced`

Total ~107 lines. Heavy use of fenced code blocks (4-5 bash + 1 directory tree + 1 JSON + 1 markdown table).

**zh-cn translation contract** — derived from `docs/zh-cn/explanation/architecture.md` (Plan 05 output) and 11-CONTEXT.md D-14:

- Translate `title:` to Simplified Chinese (e.g., `"升级恢复"`).
- Translate `description:` to Simplified Chinese.
- Keep all H2/H3 anchor structure — translate the heading TEXT but match the EN heading order/depth exactly.
- Keep ALL code blocks, paths, commands, variable names verbatim:
  - `_gomad/_backups/<YYYYMMDD-HHMMSS>/` (literal)
  - `metadata.json`, `README.md`, `files-manifest.csv`, `gomad install`, `npx gomad --version`, `cp -R`, `rsync -a --exclude=...`, `cat ... | grep gomad_version`, `rm -rf _gomad/_backups/20260401-*`
  - JSON schema fields (`gomad_version`, `created_at`, `reason`, `files`, `install_root`, `relative_path`, `orig_hash`, `was_modified`, `recovery_hint`)
  - Directory-tree comments stay in English (`# machine-readable backup manifest`, `# auto-generated recovery instructions (co-located)`, etc.) — Plan 05 set this precedent.
- Translate the prose around code blocks.
- Translate the table headers + the explanatory text in the table cells in the metadata.json field-purpose table (`gomad_version` row purpose: "The gomad version that produced this backup..." → translated; field name itself stays English).
- Keep enum literals in English: `manifest_cleanup`, `legacy_v1_cleanup` are config values, not prose.
- Per CONTEXT.md style-guide notes: NO horizontal rules (`---` outside frontmatter), 8-12 H2 budget OK with 7 H2s, Starlight admonition syntax `:::tip[]` etc. (none currently used in EN — preserve EN's choice of plain prose vs admonitions).

**Cross-link to verify works after this plan lands:**

The link in `docs/zh-cn/explanation/architecture.md:65` reads (Chinese):
```
如果某次重装出现意外结果，快照目录里就有可逆的恢复记录 —— 恢复流程见[升级恢复指南](../upgrade-recovery.md)。
```

This relative path `../upgrade-recovery.md` resolves from `docs/zh-cn/explanation/architecture.md` to `docs/zh-cn/upgrade-recovery.md` (one level up from `explanation/`, then `upgrade-recovery.md`). Plan 09 creates that target file.

**Why translate vs. cross-locale link (option 1 vs. option 2 from 11-07-SUMMARY):**

11-07-SUMMARY documented two paths:
1. Translate `docs/upgrade-recovery.md` → `docs/zh-cn/upgrade-recovery.md` (parallels Plan 01-style EN+zh-cn lockstep).
2. Cross-locale link from zh-cn architecture page to EN page (`/upgrade-recovery/`) with translator note.

Per D-13 + D-14 + the consistent EN+zh-cn parity pattern across all 6 Phase 11 page sets: option 1 is preferred. Option 2 would create the only EN-only page in the zh-cn navigation tree, breaking the parity invariant that already exists for tutorials/install, tutorials/quick-start, reference/agents, reference/skills, explanation/architecture, how-to/contributing. Option 2 was deliberately NOT chosen.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate docs/upgrade-recovery.md → docs/zh-cn/upgrade-recovery.md</name>
  <files>
    - docs/zh-cn/upgrade-recovery.md (NEW)
  </files>
  <read_first>
    - docs/upgrade-recovery.md (full EN source — ~107 lines; this is the byte-for-byte reference for structure, heading order, code blocks)
    - docs/zh-cn/explanation/architecture.md (Plan 05 output — same translator's voice, set the convention for code-block / path / command preservation; lines 53-69 reference upgrade-recovery and define the section's tone in zh-cn)
    - docs/zh-cn/tutorials/install.md (Plan 04 zh-cn output — second reference for translation conventions, especially install-root / `_gomad` / `manifest` terminology)
    - docs/_STYLE_GUIDE.md (project doc conventions — style guide is FROZEN per CONTEXT.md "specifics" section; do not deviate)
    - .planning/phases/11-docs-site-content-authoring/11-CONTEXT.md D-13 + D-14 + style notes
  </read_first>
  <action>
    Create `docs/zh-cn/upgrade-recovery.md` as a Simplified Chinese translation of `docs/upgrade-recovery.md`. Follow the translation contract spelled out in `<interfaces>` above.

    **Frontmatter:**
    ```yaml
    ---
    title: "升级恢复"
    description: 介绍 manifest 驱动的清理快照如何工作，以及如何从快照中恢复
    ---
    ```

    Do NOT add `sidebar:` keys to frontmatter — the EN source doesn't have one and Starlight autogenerates upgrade-recovery's position from filesystem ordering (no need to override). Mirroring "no sidebar key" matches EN exactly.

    **Body structure (mirror EN section-for-section):**

    1. **Preamble paragraph** (no header): Translate the opening line "Starting with gomad v1.2, every `gomad install` that removes files takes a snapshot of those files into a timestamped directory under `_gomad/_backups/<YYYYMMDD-HHMMSS>/`. This document explains how those snapshots are structured and how to restore from them." Keep `gomad install` and the path token verbatim.

    2. **`## 备份目录布局`** (translates "Backup directory layout"). Translate the prose lead-in. Keep the directory-tree code block VERBATIM (the EN-language comments inside the tree like `# machine-readable backup manifest` and `# auto-generated recovery instructions (co-located)` STAY in English per Plan 05 precedent). Translate the closing paragraph about each top-level dir corresponding to an `install_root`.

    3. **`## metadata.json 结构`** (translates "metadata.json schema"). Translate the prose. Keep the JSON code block COMPLETELY verbatim (every field name, value, comment-free JSON exactly as EN). Translate the markdown table:
       - Translate column headers: "Field" → "字段", "Purpose" → "用途".
       - Field names in the first column STAY English (`gomad_version`, `created_at`, `reason`, `files[]`, `recovery_hint`).
       - Purpose-column descriptions are translated. Keep enum literals in code-fence: `manifest_cleanup`, `legacy_v1_cleanup`. Keep `was_modified` field name English.

    4. **`## 如何恢复`** (translates "How to restore"). Translate the lead-in.
       - **`### 快速恢复`** (translates "Quick recovery"). Translate the "From the workspace root:" lead-in. Keep the bash code block VERBATIM. Translate the closing line about replacing `<YYYYMMDD-HHMMSS>` with the backup directory name.
       - **`### 仅恢复文件内容（跳过 metadata.json 和 README.md）`** (translates "Restore file content only (skip metadata.json and README.md)"). Keep the rsync command verbatim. No prose around it (EN has none).
       - **`### 恢复前 — 版本兼容性检查`** (translates "Before restoring — version compat check"). Translate the lead-in. Keep both bash commands verbatim. Translate the closing paragraph about version diff implications, keeping `metadata.json.gomad_version`, `v1.2`, `v1.3+`, and `install_root` literal.

    5. **`## 如何识别要恢复哪个备份`** (translates "Identifying which backup to restore"). Translate both prose paragraphs. Keep `_gomad/_backups/<YYYYMMDD-HHMMSS>/`, `<ts>`, `<ts>-2`, `D-37`, and `README.md` literal.

    6. **`## 修剪备份`** (translates "Pruning backups"). Translate the prose. Keep the bash code block (`rm -rf _gomad/_backups/20260401-*` and its EN comment "# remove all April-1st backups") VERBATIM — Plan 05 set the precedent that inline-code-block comments stay English.

    7. **`## 何时不会产生备份`** (translates "When backups are NOT produced"). Translate the lead-in and the bullet list:
       - "`gomad install` runs on a fresh workspace (nothing to snapshot)" → keep `gomad install` literal; translate parenthetical.
       - "The new install set is identical to the prior — idempotent re-install" → translate fully.
       - "A manifest corruption is detected (`MANIFEST_CORRUPT` logged) — cleanup is skipped entirely as a safety measure" → keep `MANIFEST_CORRUPT` literal; translate the rest.
       Then translate the closing paragraph "In these cases, `_gomad/_backups/` may remain empty or contain only earlier backups."

    **Translation tone — match Plan 05's voice (do NOT deviate):**

    - Use 你 (informal) for second-person address (consistent with Plan 05).
    - Translate "manifest" as "manifest"（保留英文 + 短语化解释，e.g., "manifest（清单）"）only on first occurrence in each major section if needed for clarity, then plain "manifest" after. Plan 05 used the bare English term consistently — follow that precedent (`manifest 驱动`, `读取上一次的 manifest`).
    - Translate "snapshot" as "快照".
    - Translate "backup" as "备份".
    - Translate "installer" as "安装器" (consistent with Plan 05).
    - Translate "workspace" as "工作区" (consistent with Plan 05).
    - Use 中文全角 punctuation for prose (，。；："" 等), but ASCII inside code blocks and inline code spans (` `).
    - Use proper Chinese dash/em-dash conventions: `——` for em-dash (matches Plan 05 line 8, 16, 22, 32, 36, etc.).

    **Style guide constraints (D-_STYLE_GUIDE.md is FROZEN):**
    - NO horizontal rules in the body (no `---` outside frontmatter).
    - NO H4 (`####`) headers — EN uses H2 + H3 only.
    - Keep header budget within 8-12 H2 (EN has 7 H2 — within budget).
    - NO Starlight admonition syntax (`:::tip[]` etc.) unless EN has them — EN does not, so zh-cn does not either.

    **Do NOT:**
    - Add new sections that aren't in EN.
    - Drop sections from EN.
    - Translate code-block content (bash/JSON/directory-tree).
    - Add a "translator's note" or "machine-translated" disclaimer (D-14 ship-and-iterate; no review-gate language).
    - Add a `sidebar.order:` frontmatter key (EN doesn't have one).
    - Modify any other file. This task creates exactly one new file.
  </action>
  <verify>
    <automated>
      # 1) File exists.
      [ -f docs/zh-cn/upgrade-recovery.md ] && \
      # 2) Has Starlight frontmatter with translated title + description.
      head -5 docs/zh-cn/upgrade-recovery.md | grep -q '^title:' && \
      head -5 docs/zh-cn/upgrade-recovery.md | grep -q '升级恢复' && \
      head -5 docs/zh-cn/upgrade-recovery.md | grep -q '^description:' && \
      # 3) Section count matches EN (7 H2 sections).
      [ "$(grep -c '^## ' docs/zh-cn/upgrade-recovery.md)" = "7" ] && \
      # 4) H3 subsection count matches EN (3 H3 under "How to restore").
      [ "$(grep -c '^### ' docs/zh-cn/upgrade-recovery.md)" = "3" ] && \
      # 5) Critical literals preserved verbatim from EN.
      grep -q '_gomad/_backups' docs/zh-cn/upgrade-recovery.md && \
      grep -q 'metadata.json' docs/zh-cn/upgrade-recovery.md && \
      grep -q 'files-manifest.csv' docs/zh-cn/upgrade-recovery.md && \
      grep -q 'gomad_version' docs/zh-cn/upgrade-recovery.md && \
      grep -q 'manifest_cleanup' docs/zh-cn/upgrade-recovery.md && \
      grep -q 'legacy_v1_cleanup' docs/zh-cn/upgrade-recovery.md && \
      grep -q 'MANIFEST_CORRUPT' docs/zh-cn/upgrade-recovery.md && \
      grep -q 'gomad install' docs/zh-cn/upgrade-recovery.md && \
      grep -q 'rsync -a --exclude=metadata.json' docs/zh-cn/upgrade-recovery.md && \
      grep -q 'rm -rf _gomad/_backups' docs/zh-cn/upgrade-recovery.md && \
      # 6) Chinese characters present (CJK Unified Ideographs U+4E00 to U+9FFF). Use perl/python; macOS grep -P unreliable.
      [ "$(perl -ne 'print if /[\x{4e00}-\x{9fff}]/' docs/zh-cn/upgrade-recovery.md | wc -l | tr -d ' ')" -gt "20" ] && \
      # 7) NO horizontal rules in body (style guide).
      [ "$(awk 'NR>5' docs/zh-cn/upgrade-recovery.md | grep -c '^---$')" = "0" ] && \
      # 8) NO H4 headers (style guide).
      ! grep -q '^#### ' docs/zh-cn/upgrade-recovery.md && \
      # 9) The cross-link from zh-cn architecture now resolves.
      # docs/zh-cn/explanation/architecture.md:65 references ../upgrade-recovery.md
      # which resolves to docs/zh-cn/upgrade-recovery.md.
      grep -q '../upgrade-recovery.md' docs/zh-cn/explanation/architecture.md && \
      # 10) Validator agrees the link no longer flags as broken.
      # Standalone link-check; the only remaining failures should be the 4 https URLs (Gap A — closed by Plan 11-08, not 11-09).
      ! (node tools/validate-doc-links.js 2>&1 | grep -B1 'upgrade-recovery' | grep -q 'zh-cn/explanation/architecture\.md') && \
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - `docs/zh-cn/upgrade-recovery.md` exists.
    - Frontmatter has translated `title:` (containing 升级恢复) and `description:` (containing CJK characters).
    - Body has exactly 7 H2 sections (`grep -c '^## '` = 7), matching EN section count.
    - Body has exactly 3 H3 sections (`grep -c '^### '` = 3), matching EN's "How to restore" subsections.
    - All preserved-English literals present: `_gomad/_backups`, `metadata.json`, `files-manifest.csv`, `gomad_version`, `manifest_cleanup`, `legacy_v1_cleanup`, `MANIFEST_CORRUPT`, `gomad install`, `rsync -a --exclude=metadata.json`, `rm -rf _gomad/_backups`.
    - At least 20 lines contain CJK Unified Ideographs (U+4E00 to U+9FFF) — proves substantive translation, not stub.
    - No horizontal rule (`---`) appears in the body (line 6 onward).
    - No H4 (`#### `) headers anywhere in the file.
    - The relative link `../upgrade-recovery.md` already exists in `docs/zh-cn/explanation/architecture.md` (no edit needed there) and now resolves to the newly-created file.
    - Running `node tools/validate-doc-links.js` does NOT flag `docs/zh-cn/explanation/architecture.md` for the upgrade-recovery link any more (Gap B closed). Other unrelated link issues (Gap A — closed by Plan 11-08, not Plan 11-09) may still appear in validator output if Plan 11-08 hasn't landed yet, but the upgrade-recovery line specifically must not appear in the issue list.
    - Only `docs/zh-cn/upgrade-recovery.md` is added; no other files modified by this task.
  </acceptance_criteria>
  <done>docs/zh-cn/upgrade-recovery.md exists as a fluent Simplified Chinese translation of the EN source, mirrors EN structure section-for-section, preserves all English code blocks/paths/commands per D-14, honors the frozen style guide, and resolves the orphan cross-link from zh-cn architecture explainer. zh-cn parity restored across the full Phase 11 + v1.2-baseline page surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EN content → zh-cn translation | EN-source prose flows to zh-cn output via the translator (Claude). Risk surface: technical terms drift across pages, code-block content gets accidentally translated, structural divergence. |
| zh-cn content → site rendering pipeline | New file gets picked up by Starlight's i18n routing (`docs/zh-cn/<path>.md` → `/zh-cn/<path>`); link-check pipeline validates relative refs from sibling zh-cn pages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09-01 | Tampering | Translator accidentally translates code-block content (bash, JSON, paths) and breaks the documented commands | mitigate | Action explicitly enumerates code blocks that must stay verbatim. Acceptance criteria grep for 10 specific English literals (`_gomad/_backups`, `metadata.json`, etc.). |
| T-11-09-02 | Information Disclosure | Translator drifts terminology vs Plan 05 (zh-cn architecture explainer), introducing two different Chinese terms for "manifest" / "installer" / "workspace" across the site | mitigate | Action explicitly references Plan 05's voice and lists the canonical Chinese terms (安装器, 工作区, 备份, 快照). Verifier (CONTEXT.md D-14) accepts ship-and-iterate; minor terminology drift is patched post-publish. |
| T-11-09-03 | Denial of Service | Style guide violation — extra H4 headers or horizontal rules — breaks Starlight build | mitigate | Acceptance criteria assert `grep -c '^#### '` = 0 and `awk 'NR>5'` `grep -c '^---$'` = 0. Plan 11-10 integration gate catches any leftover Astro build issues. |
| T-11-09-04 | Repudiation | Plan 11-09 ships a translation but the cross-link from architecture is still typo-broken or mis-pathed | mitigate | The link in `docs/zh-cn/explanation/architecture.md:65` is `../upgrade-recovery.md` (Plan 05 already authored this; verified in 11-07-SUMMARY Gap B docs). This plan creates the target at `docs/zh-cn/upgrade-recovery.md` which resolves the relative path. The 10th acceptance criterion runs the link validator and asserts the architecture-page link no longer flags. |
| T-11-09-05 | Elevation of Privilege | New file unintentionally registers a sidebar entry that conflicts with EN sidebar | accept | Starlight i18n autogenerates zh-cn sidebar from filesystem; EN already has `docs/upgrade-recovery.md` rendering at `/upgrade-recovery/`; adding `docs/zh-cn/upgrade-recovery.md` mirrors at `/zh-cn/upgrade-recovery/`. No conflict — i18n routing keeps the trees separate. Frontmatter omits `sidebar.order:` deliberately to inherit EN ordering. |
</threat_model>

<verification>
- `docs/zh-cn/upgrade-recovery.md` exists with translated frontmatter.
- Section structure matches EN (7 H2, 3 H3).
- All 10 critical English literals preserved verbatim.
- CJK content density confirms substantive translation.
- No style guide violations (no `---` in body, no H4).
- `node tools/validate-doc-links.js` no longer flags the architecture-page upgrade-recovery link.
- Plan 11-10 integration gate confirms by running `npm run docs:build` end-to-end.
</verification>

<success_criteria>
1. `docs/zh-cn/upgrade-recovery.md` exists as a complete Simplified Chinese translation of `docs/upgrade-recovery.md`.
2. Frontmatter `title:` and `description:` translated; structure mirrors EN exactly (7 H2 + 3 H3).
3. All English code blocks, paths, commands, JSON fields, and enum literals preserved verbatim.
4. CJK character density indicates substantive translation (≥20 CJK-containing lines).
5. Style guide compliance: no horizontal rule in body, no H4 headers.
6. Validator no longer flags `docs/zh-cn/explanation/architecture.md` for the upgrade-recovery link.
7. No files modified outside the new `docs/zh-cn/upgrade-recovery.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/11-docs-site-content-authoring/11-09-SUMMARY.md` documenting:
- File created (`docs/zh-cn/upgrade-recovery.md`).
- Section count parity (7 H2 + 3 H3 in both EN and zh-cn).
- Translation conventions followed (D-13 EN-first, D-14 ship-and-iterate, code blocks preserved, terminology aligned with Plan 05's zh-cn architecture).
- Confirmation that running `node tools/validate-doc-links.js` no longer flags the zh-cn architecture-page upgrade-recovery link.
- Pointer to Plan 11-10 for the end-to-end integration gate that proves Gap B closure in context.
</output>
