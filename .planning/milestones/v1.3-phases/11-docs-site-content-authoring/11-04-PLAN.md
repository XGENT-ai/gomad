---
phase: 11-docs-site-content-authoring
plan: 04
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - docs/tutorials/install.md
  - docs/tutorials/quick-start.md
  - docs/zh-cn/tutorials/install.md
  - docs/zh-cn/tutorials/quick-start.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-06

must_haves:
  truths:
    - "Visitor opening /tutorials/install sees step-by-step guidance covering `npm install @xgent-ai/gomad` + `npx @xgent-ai/gomad install` with working commands (DOCS-01 part 1)."
    - "Visitor opening /tutorials/quick-start sees a 5-minute walkthrough invoking a gm-agent-* persona and producing a tangible artifact (DOCS-01 part 2)."
    - "Both EN tutorials have zh-cn siblings authored in the same plan (D-13 EN-first per page; DOCS-06)."
    - "All path references use the v1.3 layout `<installRoot>/_config/agents/<shortName>.md` (D-10) — zero v1.2 leaks."
    - "tutorials/install.md sidebar.order = 1, tutorials/quick-start.md sidebar.order = 2."
  artifacts:
    - path: "docs/tutorials/install.md"
      provides: "Install tutorial — npm install + interactive install + verify"
      contains: "npx @xgent-ai/gomad install"
      min_lines: 80
    - path: "docs/tutorials/quick-start.md"
      provides: "Quick-start tutorial — first persona invocation + first artifact"
      contains: "/gm:agent-"
      min_lines: 80
    - path: "docs/zh-cn/tutorials/install.md"
      provides: "Install tutorial zh-cn translation"
      contains: "npx @xgent-ai/gomad install"
    - path: "docs/zh-cn/tutorials/quick-start.md"
      provides: "Quick-start tutorial zh-cn translation"
      contains: "/gm:agent-"
  key_links:
    - from: "docs/tutorials/install.md"
      to: "docs/tutorials/quick-start.md"
      via: "prose link in 'What's next' paragraph (sidebar handles formal navigation)"
      pattern: "quick-start"
    - from: "docs/tutorials/quick-start.md"
      to: "docs/reference/agents.md (Plan 03)"
      via: "prose link in persona-selection step"
      pattern: "../reference/agents"
---

<objective>
Author the two tutorial pages (`docs/tutorials/install.md` and `docs/tutorials/quick-start.md`) plus zh-cn siblings. Cover the canonical install flow (`npm install @xgent-ai/gomad` then `npx @xgent-ai/gomad install`) and the first end-to-end persona invocation that produces a tangible artifact.

Purpose: Deliver DOCS-01 (install + quick-start tutorials) and DOCS-06 (zh-cn parity for these pages).

Output: 4 markdown pages (~120-180 lines each EN, similar zh-cn).

Wave 3: Depends on Plan 01 (cleanup) and Plan 02 (injector pipeline patch — without it, `npm run docs:build` fails because `tools/inject-reference-tables.cjs` is referenced from build-docs.mjs main()). Plans 03/04/05/06 can run in parallel within Wave 3 (different files).
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-docs-site-content-authoring/11-CONTEXT.md
@.planning/phases/11-docs-site-content-authoring/11-RESEARCH.md
@.planning/phases/11-docs-site-content-authoring/11-PATTERNS.md
@docs/_STYLE_GUIDE.md
@docs/upgrade-recovery.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/tutorials/install.md (EN) — npm install + interactive install + verify</name>
  <files>
    - docs/tutorials/install.md (NEW)
  </files>
  <read_first>
    - docs/_STYLE_GUIDE.md (lines 88-119 — Tutorial Structure 15-section template + 8-12 `##` budget + admonition cap 3-4 per major section in tutorials)
    - docs/upgrade-recovery.md (shipped prose tone reference for code blocks + tables + admonitions)
    - README.md (canonical entry command — `npx @xgent-ai/gomad install` per RESEARCH.md DOCS-01 row)
    - .planning/phases/11-docs-site-content-authoring/11-PATTERNS.md (lines 38-110 — tutorial frontmatter, structure pattern, admonition pattern)
    - .planning/phases/11-docs-site-content-authoring/11-CONTEXT.md (D-10 v1.3 paths, D-13 EN-first)
    - .planning/phases/11-docs-site-content-authoring/11-RESEARCH.md (lines 491-518 — install tutorial frontmatter + body skeleton)
  </read_first>
  <action>
    Create `docs/tutorials/install.md`. Use this EXACT frontmatter:

    ```yaml
    ---
    title: "Install GoMad"
    description: Install @xgent-ai/gomad and run gomad install in 5 minutes.
    sidebar:
      order: 1
    ---
    ```

    Body MUST contain (exact section headers — adapted from style guide tutorial template):

    1. Hook (no header) — 1-2 sentences: "Install GoMad in your project in five minutes using `npx`."

    2. `## What you'll learn` — bullet list of 3-4 outcomes:
       - Install `@xgent-ai/gomad` (or run via `npx`)
       - Run the interactive `gomad install` flow
       - Understand the directory layout it produces (`<installRoot>/_config/`)
       - Verify the install via the manifest

    3. `## Prerequisites` — admonition:
       ```markdown
       :::note[Prerequisites]
       Node.js 20+, npm 10+, and an AI IDE that supports slash commands (Claude Code, Cursor, etc.).
       :::
       ```

    4. `## Quick path` — admonition with the TL;DR command:
       ```markdown
       :::tip[Quick path]
       `npx @xgent-ai/gomad install` — accept defaults — done.
       :::
       ```

    5. `## Step 1: Choose how to install` — 2 short paragraphs covering:
       - Option A: One-shot via `npx` — for trying GoMad without committing.
         ```bash
         npx @xgent-ai/gomad install
         ```
       - Option B: Add as a dev dependency — for projects that pin the version.
         ```bash
         npm install --save-dev @xgent-ai/gomad
         npx gomad install
         ```

    6. `## Step 2: Run the interactive installer` — describe the prompts:
       - Choose install root (default `_gomad`).
       - Confirm IDE target (Claude Code, Cursor, Codex, etc.).
       - Confirm agent selection (default = all 8 personas).
       The installer writes:
       - Persona body files at `<installRoot>/_config/agents/<shortName>.md` (D-10).
       - Domain knowledge packs at `<installRoot>/_config/kb/<slug>/`.
       - Slash command stubs at `.claude/commands/gm/agent-*.md`.
       - The install manifest at `<installRoot>/_config/files-manifest.csv`.

    7. `## Step 3: Verify the install` — show the expected directory layout via a code block:
       ```text
       _gomad/
       ├── _config/
       │   ├── agents/
       │   │   ├── analyst.md
       │   │   ├── architect.md
       │   │   ├── dev.md
       │   │   ├── pm.md
       │   │   ├── sm.md
       │   │   ├── solo-dev.md
       │   │   ├── tech-writer.md
       │   │   └── ux-designer.md
       │   ├── kb/
       │   │   ├── architecture/
       │   │   └── testing/
       │   └── files-manifest.csv
       └── _backups/   # populated on subsequent installs
       ```
       Single sentence after: "If your IDE supports slash commands, type `/gm:` to see the launcher list."

    8. `## Common questions` — short FAQ (3 entries):
       - "Where do persona files live?" — `<installRoot>/_config/agents/<shortName>.md`.
       - "Can I customize a persona without losing changes?" — yes via `.customize.yaml`; details in the contributing guide.
       - "How do I upgrade?" — re-run `npx @xgent-ai/gomad install`; the cleanup planner snapshots replaced files under `<installRoot>/_backups/<timestamp>/`.

    9. `## Where to next` — single paragraph (NOT a "Next:" section header — phrase as prose link): "Once the install completes, run a real workflow in the [quick start tutorial](./quick-start.md)."

    Constraints:
    - 7 `##` headers total (within budget 8-12 — tutorials may use 3-4 admonitions per major section).
    - 2 admonitions (`:::note[Prerequisites]`, `:::tip[Quick path]`).
    - No `####` headers.
    - No horizontal rules outside frontmatter.
    - No "Related" section header (use prose link in "Where to next").
    - Every install path uses v1.3 layout. Specifically `<installRoot>/_config/agents/<shortName>.md`, `<installRoot>/_config/kb/<slug>/`, `<installRoot>/_config/files-manifest.csv`, `.claude/commands/gm/agent-*.md`. NO `<installRoot>/gomad/agents/`.
    - DO NOT mention BMM/BMB/BMGD modules.
    - DO NOT mention the Claude Code plugin marketplace (`.claude-plugin/marketplace.json`) — dropped 2026-04-24 (RESEARCH.md State of the Art line 632).
  </action>
  <verify>
    <automated>
      [ -f docs/tutorials/install.md ] && \
      grep -q '^title: "Install GoMad"' docs/tutorials/install.md && \
      grep -A1 '^sidebar:' docs/tutorials/install.md | grep -q 'order: 1' && \
      # 7 ## headers:
      [ "$(grep -c '^## ' docs/tutorials/install.md)" = "7" ] && \
      grep -q "^## What you'll learn" docs/tutorials/install.md && \
      grep -q '^## Prerequisites' docs/tutorials/install.md && \
      grep -q '^## Quick path' docs/tutorials/install.md && \
      grep -q '^## Step 1:' docs/tutorials/install.md && \
      grep -q '^## Step 2:' docs/tutorials/install.md && \
      grep -q '^## Step 3:' docs/tutorials/install.md && \
      grep -q '^## Common questions' docs/tutorials/install.md && \
      grep -q '^## Where to next' docs/tutorials/install.md && \
      # Canonical command present:
      grep -F 'npx @xgent-ai/gomad install' docs/tutorials/install.md && \
      # v1.3 paths used:
      grep -F '<installRoot>/_config/agents/' docs/tutorials/install.md && \
      grep -F '<installRoot>/_config/kb/' docs/tutorials/install.md && \
      grep -F '<installRoot>/_config/files-manifest.csv' docs/tutorials/install.md && \
      grep -F '.claude/commands/gm/agent-' docs/tutorials/install.md && \
      # No v1.2 leak:
      ! grep -F '<installRoot>/gomad/agents/' docs/tutorials/install.md && \
      # No BMAD modules:
      ! grep -wE '\bBMM\b|\bBMB\b|\bBMGD\b' docs/tutorials/install.md && \
      # No marketplace:
      ! grep -i 'marketplace' docs/tutorials/install.md && \
      # Style guide compliance:
      ! grep -E '^####' docs/tutorials/install.md && \
      [ "$(grep -c '^---$' docs/tutorials/install.md)" = "2" ] && \
      ! grep -E '^## (Related|Next:?)' docs/tutorials/install.md && \
      # Cross-link to quick-start (prose, not "Next:" header):
      grep -F 'quick-start.md' docs/tutorials/install.md
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - `[ -f docs/tutorials/install.md ]` succeeds.
    - `grep -c '^## ' docs/tutorials/install.md` outputs `7`.
    - `grep -F 'npx @xgent-ai/gomad install' docs/tutorials/install.md` succeeds.
    - `grep -F '<installRoot>/_config/agents/' docs/tutorials/install.md` succeeds.
    - `! grep -F '<installRoot>/gomad/agents/' docs/tutorials/install.md` succeeds.
    - `! grep -i 'marketplace' docs/tutorials/install.md` succeeds (no marketplace mentions).
    - `! grep -wE '\bBMM\b|\bBMB\b|\bBMGD\b' docs/tutorials/install.md` succeeds.
    - `! grep -E '^####' docs/tutorials/install.md` succeeds.
    - `grep -F './quick-start.md' docs/tutorials/install.md` succeeds (prose cross-link).
    - `wc -l < docs/tutorials/install.md` outputs at least `80`.
  </acceptance_criteria>
  <done>EN install tutorial authored with 7 `##` sections, 2 admonitions, canonical npm + npx command, v1.3 install paths, prose link to quick-start, style-guide compliant.</done>
</task>

<task type="auto">
  <name>Task 2: Author docs/tutorials/quick-start.md (EN) — first persona invocation + first artifact</name>
  <files>
    - docs/tutorials/quick-start.md (NEW)
  </files>
  <read_first>
    - docs/tutorials/install.md (Task 1 — same plan; tone + frontmatter shape match)
    - docs/_STYLE_GUIDE.md (tutorial template + admonition cap)
    - .planning/phases/11-docs-site-content-authoring/11-CONTEXT.md (D-10, D-13)
    - .planning/phases/11-docs-site-content-authoring/11-PATTERNS.md (lines 38-110 — tutorial pattern)
    - src/gomad-skills/2-plan-workflows/gm-agent-pm/SKILL.md (sample persona for the walkthrough — pm produces a PRD draft, a small tangible artifact suitable for a 5-minute tutorial)
  </read_first>
  <action>
    Create `docs/tutorials/quick-start.md`. Use this EXACT frontmatter:

    ```yaml
    ---
    title: "Quick Start"
    description: Run your first GoMad workflow — invoke a persona, produce a draft PRD in 5 minutes.
    sidebar:
      order: 2
    ---
    ```

    Body MUST contain (in this exact order):

    1. Hook (no header) — 1-2 sentences: "Run your first GoMad workflow end-to-end. You'll invoke the Product Manager persona, draft a PRD, and see how the launcher-form slash command + persona body resolution actually works."

    2. `## What you'll learn` — 4 bullets:
       - Invoke a `gm-agent-*` persona via slash command.
       - Understand how the launcher loads the persona body at runtime.
       - Produce your first artifact (a draft PRD).
       - Know where to find the agents reference and where to go next.

    3. `## Before you start` — admonition:
       ```markdown
       :::note[Before you start]
       You've completed the [install tutorial](./install.md). The `_gomad/` directory exists with `_config/agents/` populated.
       :::
       ```

    4. `## Step 1: Pick a persona` — 1-2 paragraphs:
       - Explain the 4-phase grouping briefly.
       - For this walkthrough, pick `gm-agent-pm` (John). Show the file layout:
         ```text
         <installRoot>/_config/agents/pm.md       # persona body (loaded at runtime)
         .claude/commands/gm/agent-pm.md          # launcher stub (slash command target)
         ```
       - Cross-link in prose: "Browse the full catalog in the [agents reference](../reference/agents.md)."

    5. `## Step 2: Invoke the persona` — show the slash command:
       ```text
       /gm:agent-pm
       ```
       - Explain that the launcher stub at `.claude/commands/gm/agent-pm.md` loads the body from `<installRoot>/_config/agents/pm.md` (D-10) on each invocation.
       - One sentence on what John will say back when invoked (greets, lists capabilities).

    6. `## Step 3: Drive your first workflow` — 2-3 short paragraphs:
       - Ask John to draft a PRD for a small example feature (e.g., "Add a daily-summary email"). Show the prompt: "Use the `gm-create-prd` skill to draft a PRD for a daily-summary email feature."
       - Note that John will load the `gm-create-prd` skill (a task skill, no slash command of its own per D-09 — invoked through the persona).
       - The output lands at the path the persona indicates (project-relative path; depends on the project's planning convention).

    7. `## Step 4: Inspect the artifact` — 1 paragraph:
       - Open the produced PRD draft. Note that it's a regular markdown file under your project's planning directory.
       - The persona-body's instructions, the skill's template, and the user-prompt input combine to produce the artifact — see `[architecture explainer](../explanation/architecture.md)` for the runtime model.

    8. `## What you got` — bullet list of 3 things produced:
       - First persona invocation (slash command resolved to persona body).
       - First task skill loaded (`gm-create-prd`).
       - First artifact written (a draft PRD in markdown).

    9. `## Where to next` — single paragraph (prose, not "Next:" header):
       - Browse `[skills reference](../reference/skills.md)` for the full catalog.
       - Read `[architecture explainer](../explanation/architecture.md)` to understand the 4-phase lifecycle.
       - When you're ready to extend GoMad, see `[contributing](../how-to/contributing.md)`.

    Constraints:
    - 8 `##` headers (within budget).
    - 1 admonition (`:::note[Before you start]`).
    - No `####`, no horizontal rules outside frontmatter, no "Related"/"Next:" section headers (prose links instead).
    - Every install path uses v1.3 layout.
    - Cross-links to: `./install.md`, `../reference/agents.md`, `../reference/skills.md`, `../explanation/architecture.md`, `../how-to/contributing.md`.
  </action>
  <verify>
    <automated>
      [ -f docs/tutorials/quick-start.md ] && \
      grep -q '^title: "Quick Start"' docs/tutorials/quick-start.md && \
      grep -A1 '^sidebar:' docs/tutorials/quick-start.md | grep -q 'order: 2' && \
      [ "$(grep -c '^## ' docs/tutorials/quick-start.md)" = "8" ] && \
      grep -q "^## What you'll learn" docs/tutorials/quick-start.md && \
      grep -q '^## Before you start' docs/tutorials/quick-start.md && \
      grep -q '^## Step 1:' docs/tutorials/quick-start.md && \
      grep -q '^## Step 2:' docs/tutorials/quick-start.md && \
      grep -q '^## Step 3:' docs/tutorials/quick-start.md && \
      grep -q '^## Step 4:' docs/tutorials/quick-start.md && \
      grep -q '^## What you got' docs/tutorials/quick-start.md && \
      grep -q '^## Where to next' docs/tutorials/quick-start.md && \
      # Slash command syntax shown:
      grep -F '/gm:agent-pm' docs/tutorials/quick-start.md && \
      # v1.3 paths:
      grep -F '<installRoot>/_config/agents/pm.md' docs/tutorials/quick-start.md && \
      grep -F '.claude/commands/gm/agent-pm.md' docs/tutorials/quick-start.md && \
      ! grep -F '<installRoot>/gomad/agents/' docs/tutorials/quick-start.md && \
      # Cross-links to the 5 expected pages:
      grep -F './install.md' docs/tutorials/quick-start.md && \
      grep -F '../reference/agents.md' docs/tutorials/quick-start.md && \
      grep -F '../reference/skills.md' docs/tutorials/quick-start.md && \
      grep -F '../explanation/architecture.md' docs/tutorials/quick-start.md && \
      grep -F '../how-to/contributing.md' docs/tutorials/quick-start.md && \
      # Style guide compliance:
      ! grep -E '^####' docs/tutorials/quick-start.md && \
      [ "$(grep -c '^---$' docs/tutorials/quick-start.md)" = "2" ] && \
      ! grep -E '^## (Related|Next:?)' docs/tutorials/quick-start.md && \
      ! grep -wE '\bBMM\b|\bBMB\b|\bBMGD\b' docs/tutorials/quick-start.md && \
      ! grep -i 'marketplace' docs/tutorials/quick-start.md
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - `[ -f docs/tutorials/quick-start.md ]` succeeds.
    - `grep -c '^## ' docs/tutorials/quick-start.md` outputs `8`.
    - `grep -F '/gm:agent-pm' docs/tutorials/quick-start.md` succeeds.
    - `grep -F '<installRoot>/_config/agents/pm.md' docs/tutorials/quick-start.md` succeeds.
    - `! grep -F '<installRoot>/gomad/agents/' docs/tutorials/quick-start.md` succeeds.
    - All 5 cross-links present (install.md, agents.md, skills.md, architecture.md, contributing.md).
    - `! grep -E '^####' docs/tutorials/quick-start.md` succeeds.
    - `wc -l < docs/tutorials/quick-start.md` outputs at least `80`.
  </acceptance_criteria>
  <done>EN quick-start tutorial authored with 8 `##` sections covering persona pick → invoke → drive workflow → inspect artifact. v1.3 paths, slash command syntax, cross-links to 5 sibling pages, style-guide compliant.</done>
</task>

<task type="auto">
  <name>Task 3: Translate to zh-cn — docs/zh-cn/tutorials/install.md and quick-start.md (D-13 EN-first)</name>
  <files>
    - docs/zh-cn/tutorials/install.md (NEW)
    - docs/zh-cn/tutorials/quick-start.md (NEW)
  </files>
  <read_first>
    - docs/tutorials/install.md (Task 1 EN sibling — primary translation source)
    - docs/tutorials/quick-start.md (Task 2 EN sibling)
    - .planning/phases/11-docs-site-content-authoring/11-CONTEXT.md (D-13 EN-first per page; D-14 Claude commits zh-cn without pre-review)
    - .planning/phases/11-docs-site-content-authoring/11-PATTERNS.md (lines 567-590 — translation rules: translate prose + frontmatter title/description; KEEP code blocks/paths/slash commands/npm package names in English)
  </read_first>
  <action>
    Translate the two EN tutorial pages to Simplified Chinese.

    For `docs/zh-cn/tutorials/install.md`:
    - Frontmatter: translate `title:` (e.g., `title: "安装 GoMad"`) and `description:`. Keep `sidebar.order: 1`.
    - Translate every `##` section header to Chinese (e.g., `## What you'll learn` → `## 你将学到什么`, `## Prerequisites` → `## 先决条件`, `## Quick path` → `## 快速路径`, `## Step 1: Choose how to install` → `## 步骤 1：选择安装方式`, etc.).
    - Translate every prose paragraph + bullet list item to Chinese.
    - Translate admonition labels (e.g., `:::note[Prerequisites]` → `:::note[先决条件]`).
    - DO NOT translate:
      - Code blocks (shell commands like `npm install --save-dev @xgent-ai/gomad`, paths like `<installRoot>/_config/agents/<shortName>.md`).
      - npm package names (`@xgent-ai/gomad`).
      - Slash commands (`/gm:agent-*`).
      - The directory tree code block under Step 3 (file paths stay in English).
    - Translate the prose link label: `[quick start tutorial](./quick-start.md)` → `[快速入门教程](./quick-start.md)`.

    For `docs/zh-cn/tutorials/quick-start.md`:
    - Same translation rules.
    - `title: "Quick Start"` → `title: "快速入门"`.
    - All 8 `##` headers translated; admonition `:::note[Before you start]` → `:::note[开始之前]`.
    - All slash commands (`/gm:agent-pm`), paths (`<installRoot>/_config/agents/pm.md`, `.claude/commands/gm/agent-pm.md`), and skill names (`gm-create-prd`) remain in English.
    - Translate the link labels: `[install tutorial](./install.md)` → `[安装教程](./install.md)`; `[agents reference](../reference/agents.md)` → `[Agents 参考](../reference/agents.md)`; etc.

    Constraints:
    - Same `##` header count as EN siblings (7 for install, 8 for quick-start).
    - Same admonition count.
    - No `####`, no horizontal rules outside frontmatter.
    - Per D-14: terminology drift across pages is acceptable (no glossary lock).
    - sidebar.order MUST mirror EN (install=1, quick-start=2).

    After writing both files, run `npm run docs:build` and confirm:
    - Link-check passes (cross-links to `./install.md`, `../reference/*.md`, `../explanation/*.md`, `../how-to/*.md` resolve — but NOTE: those siblings are authored in Plans 03/05/06 within Wave 2, and may not yet have shipped depending on plan ordering. Acceptance criterion accommodates this — see "Wave 3 sequencing note" below).
    - Astro build succeeds.

    Wave 3 sequencing note: Plans 03/04/05/06 run in parallel within Wave 3. Cross-links between pages may briefly resolve to "not-yet-authored" pages mid-wave. The link-check gate is a hard fail — so the executor MUST run `npm run docs:build` only AFTER all four Wave 3 plans complete (not after each plan). The acceptance criterion below uses `! grep` checks (file content) rather than `npm run docs:build` for THIS task; the wave-level integration test runs as part of phase verification.
  </action>
  <verify>
    <automated>
      [ -f docs/zh-cn/tutorials/install.md ] && \
      [ -f docs/zh-cn/tutorials/quick-start.md ] && \
      # Same ## counts as EN siblings:
      [ "$(grep -c '^## ' docs/zh-cn/tutorials/install.md)" = "7" ] && \
      [ "$(grep -c '^## ' docs/zh-cn/tutorials/quick-start.md)" = "8" ] && \
      # Frontmatter title differs from EN (translated):
      ! grep -q '^title: "Install GoMad"' docs/zh-cn/tutorials/install.md && \
      ! grep -q '^title: "Quick Start"' docs/zh-cn/tutorials/quick-start.md && \
      # sidebar.order matches EN:
      grep -A1 '^sidebar:' docs/zh-cn/tutorials/install.md | grep -q 'order: 1' && \
      grep -A1 '^sidebar:' docs/zh-cn/tutorials/quick-start.md | grep -q 'order: 2' && \
      # CJK in body:
      grep -P '[\x{4e00}-\x{9fff}]' docs/zh-cn/tutorials/install.md && \
      grep -P '[\x{4e00}-\x{9fff}]' docs/zh-cn/tutorials/quick-start.md && \
      # Code blocks / paths / slash commands stay in English:
      grep -F 'npx @xgent-ai/gomad install' docs/zh-cn/tutorials/install.md && \
      grep -F '<installRoot>/_config/agents/' docs/zh-cn/tutorials/install.md && \
      grep -F '/gm:agent-pm' docs/zh-cn/tutorials/quick-start.md && \
      grep -F '<installRoot>/_config/agents/pm.md' docs/zh-cn/tutorials/quick-start.md && \
      # No v1.2 leak:
      ! grep -F '<installRoot>/gomad/agents/' docs/zh-cn/tutorials/install.md && \
      ! grep -F '<installRoot>/gomad/agents/' docs/zh-cn/tutorials/quick-start.md && \
      # Style guide compliance:
      ! grep -E '^####' docs/zh-cn/tutorials/install.md && \
      ! grep -E '^####' docs/zh-cn/tutorials/quick-start.md && \
      [ "$(grep -c '^---$' docs/zh-cn/tutorials/install.md)" = "2" ] && \
      [ "$(grep -c '^---$' docs/zh-cn/tutorials/quick-start.md)" = "2" ] && \
      # Cross-links preserved (relative paths unchanged):
      grep -F './quick-start.md' docs/zh-cn/tutorials/install.md && \
      grep -F './install.md' docs/zh-cn/tutorials/quick-start.md && \
      grep -F '../reference/agents.md' docs/zh-cn/tutorials/quick-start.md
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - Both files exist.
    - `grep -c '^## '` matches EN sibling counts (7 + 8).
    - `grep -P '[\x{4e00}-\x{9fff}]'` finds CJK characters in both files (not copies of EN).
    - English code blocks / paths / slash commands preserved.
    - `! grep -F '<installRoot>/gomad/agents/'` succeeds in both.
    - sidebar.order matches EN siblings.
    - Cross-link relative paths unchanged from EN siblings.
  </acceptance_criteria>
  <done>zh-cn tutorial siblings authored — translated frontmatter + section headers + prose; English code/paths/slash commands preserved per D-14 policy; cross-links unchanged. Per Wave 3 sequencing note, full `npm run docs:build` verification runs only after all Wave 2 plans complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tutorial command examples → user shell | Examples use `npx @xgent-ai/gomad install` which is the canonical command. A typo or stale flag could mislead a real user. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Information Disclosure | v1.2 install path leak in tutorial prose | mitigate | Acceptance criteria assert `! grep -F '<installRoot>/gomad/agents/' docs/tutorials/*.md docs/zh-cn/tutorials/*.md`. DOCS-07 in Phase 12 is the enforcement gate; this plan prevents leaks. |
| T-11-04-02 | Tampering | Stale marketplace mention in quick-start | mitigate | Acceptance criterion `! grep -i 'marketplace' docs/tutorials/install.md` succeeds. RESEARCH State of the Art line 632: marketplace was dropped 2026-04-24. |
| T-11-04-03 | Denial of Service | Wave 2 cross-link breakage | accept | Plans 03/04/05/06 author cross-linked sibling pages within the same wave. Cross-link link-check gate is hard-fail — full `npm run docs:build` runs as a wave-level verification step (post Wave 3 completion), not per-plan. Each plan asserts file content; integration assertion is wave-level. |
| T-11-04-04 | Tampering | Tutorial command typo (e.g., wrong npm package name) | mitigate | Acceptance criterion `grep -F 'npx @xgent-ai/gomad install'` enforces exact canonical command from README.md. |
</threat_model>

<verification>
- 4 tutorial pages authored (install + quick-start, EN + zh-cn).
- Sidebar order: install=1, quick-start=2.
- v1.3 install paths everywhere; no v1.2 leak; no marketplace mention; no BMAD modules.
- Cross-links to install/quick-start/agents/skills/architecture/contributing all use relative paths.
- Wave 3 cross-page link-check is a wave-level integration test (run after all Wave 2 plans complete).
</verification>

<success_criteria>
1. `docs/tutorials/install.md` exists with 7 `##` sections, canonical npm + npx commands, v1.3 paths, prose link to quick-start.
2. `docs/tutorials/quick-start.md` exists with 8 `##` sections covering persona pick → invoke → workflow → artifact, with cross-links to 5 sibling pages.
3. `docs/zh-cn/tutorials/install.md` and `docs/zh-cn/tutorials/quick-start.md` exist with translated prose + section headers; English code/paths/slash commands preserved.
4. No v1.2 path leaks (`! grep -F '<installRoot>/gomad/agents/' docs/tutorials/*.md docs/zh-cn/tutorials/*.md`).
5. No marketplace mentions; no BMAD module names (BMM/BMB/BMGD).
6. Style-guide compliant: no `####`, exactly 2 `---` lines (frontmatter only), no "Related"/"Next:" section headers.
</success_criteria>

<output>
After completion, create `.planning/phases/11-docs-site-content-authoring/11-04-SUMMARY.md` documenting: 4 tutorial paths, sidebar.order assignment, the canonical command (`npx @xgent-ai/gomad install`), the persona walkthrough chosen for quick-start (`/gm:agent-pm` + `gm-create-prd`), and the cross-link map.
</output>