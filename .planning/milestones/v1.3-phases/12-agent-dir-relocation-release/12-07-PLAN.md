---
phase: 12-agent-dir-relocation-release
plan: 07
type: execute
wave: 5
depends_on: ["12-04", "12-05", "12-06"]
files_modified:
  - docs/upgrade-recovery.md
  - docs/zh-cn/upgrade-recovery.md
autonomous: false
requirements: [AGENT-11, DOCS-07]
tags: [docs, upgrade-recovery, phase-11-merge, validation]

must_haves:
  truths:
    - "docs/upgrade-recovery.md has a new top-level section '## v1.2 → v1.3 recovery' explaining what relocates, where snapshots land, and the rollback recipe"
    - "docs/zh-cn/upgrade-recovery.md mirrors the new section in Simplified Chinese, with code blocks (cp -R, gomad install) untranslated"
    - "Phase 11 docs branch (gsd/phase-11-docs-site-content-authoring) is merged into the Phase 12 working branch BEFORE this plan's checkpoint, so validate:doc-paths runs against the combined tree"
    - "Running `npm run validate:doc-paths` on the combined tree exits 0"
    - "The new heading slugifies to v12--v13-recovery for CHANGELOG cross-link consumption (Plan 08)"
  artifacts:
    - path: "docs/upgrade-recovery.md"
      provides: "v1.2→v1.3 recovery section with rollback recipe and forward-recovery notes"
      contains: "v1.2 → v1.3 recovery"
    - path: "docs/zh-cn/upgrade-recovery.md"
      provides: "Chinese translation of v1.2→v1.3 recovery section"
      contains: "v1.2 → v1.3"
  key_links:
    - from: "docs/upgrade-recovery.md new section heading"
      to: "CHANGELOG.md v1.3.0 BREAKING entry (Plan 08)"
      via: "cross-link anchor #v12--v13-recovery"
      pattern: "v12--v13-recovery"
---

<objective>
Author the v1.2→v1.3 migration recovery content for `docs/upgrade-recovery.md` (English) and translate to `docs/zh-cn/upgrade-recovery.md` (Simplified Chinese parity). Also: confirm Phase 11 docs branch is merged into the Phase 12 working branch BEFORE this plan finalizes, then run `npm run validate:doc-paths` against the combined tree to prove DOCS-07 is satisfied.

Purpose: Implements AGENT-11 (upgrade-recovery doc updated with v1.2→v1.3 instructions) and finalizes DOCS-07 sweep (Pitfall 8 — Phase 11 branch must merge before release; validate-doc-paths must be green on combined tree). The new heading anchor `v12--v13-recovery` is consumed by Plan 08's CHANGELOG cross-link.

Output: Modified docs/upgrade-recovery.md (new section appended), modified docs/zh-cn/upgrade-recovery.md (parity translation). One human checkpoint before finalization to confirm Phase 11 merge state.
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-agent-dir-relocation-release/12-CONTEXT.md
@.planning/phases/12-agent-dir-relocation-release/12-RESEARCH.md
@.planning/phases/12-agent-dir-relocation-release/12-PATTERNS.md
@.planning/phases/12-agent-dir-relocation-release/12-04-SUMMARY.md
@.planning/phases/11-docs-site-content-authoring/11-09-SUMMARY.md

<interfaces>
<!-- docs/upgrade-recovery.md current content. -->

From docs/upgrade-recovery.md:
- File documents v1.2 backup-tree layout, metadata.json schema, restore commands, version-compat check, identifying which backup to use, pruning, when no backup is produced
- Line ~19 contains the literal `gomad/agents/pm.md` inside an example backup tree (legitimate v1.2-era reference; Plan 04 added this file to validate-doc-paths.js ALLOWLIST)
- Last existing section is "When backups are NOT produced" (around line 99 in the prior snapshot)
- File uses Markdown headings; new section title `## v1.2 → v1.3 recovery` slugifies via the project's slugifier in `tools/validate-doc-links.js:74-82` (`headingToAnchor`) to `v12--v13-recovery` (em-dash and arrow collapse to double-dash; spaces collapse to single-dash; lowercase)

Phase 11 branch coordination:
- Phase 11 was authored on branch `gsd/phase-11-docs-site-content-authoring`
- STATE.md lists Phase 11 as "Complete" but does NOT assert merge-to-main status
- Per Pitfall 8: Phase 11 MUST merge into the Phase 12 working branch (or main, then Phase 12 rebases) BEFORE release
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Confirm Phase 11 docs branch merge state</name>
  <what-built>
    Before finalizing the v1.2→v1.3 docs content, confirm that the Phase 11 docs branch (`gsd/phase-11-docs-site-content-authoring`) is merged into the current Phase 12 working branch (or that Phase 11 was already merged to main and Phase 12 has rebased on it). This is a coordination checkpoint per RESEARCH §Pitfall 8 — the release commit MUST contain Phase 11 docs content + Phase 12 changes together.
  </what-built>
  <how-to-verify>
1. Check current branch: `git branch --show-current` — should be the Phase 12 working branch (e.g. `gsd/phase-12-...`)
2. Check Phase 11 merge state into current branch: `git log --oneline | grep -E "phase[- ]?11|docs-site-content" | head -5`
3. If no Phase 11 commits visible:
   - Run `git log origin/main --oneline | grep -E "phase[- ]?11|docs-site-content" | head -5` — confirm Phase 11 is merged to main
   - Then `git merge origin/main` (or `git rebase origin/main`) to bring Phase 11 content into the Phase 12 branch
4. After merge/rebase, verify Phase 11 docs files exist:
   - `ls docs/reference/agents.md docs/reference/skills.md docs/tutorials/install.md docs/tutorials/quick-start.md docs/explanation/architecture.md docs/how-to/contributing.md` (all should exist)
   - `ls docs/zh-cn/reference/agents.md docs/zh-cn/tutorials/install.md` (zh-cn parity)
5. Run combined-tree lint: `npm run validate:doc-paths`
   - MUST exit 0 — proves Phase 11 docs reference canonical `_config/agents/` and Plan 04's linter is satisfied
6. If validate:doc-paths fails: fix the offending file (likely a legacy path leak in Phase 11 content) before proceeding
  </how-to-verify>
  <resume-signal>Type "approved — phase-11-merged-and-lint-green" once `git log` shows Phase 11 commits in the current branch and `npm run validate:doc-paths` exits 0.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 1: Add "v1.2 → v1.3 recovery" section to docs/upgrade-recovery.md</name>
  <files>docs/upgrade-recovery.md</files>
  <read_first>
    - docs/upgrade-recovery.md (FULL FILE — understand existing section structure: backup layout, metadata.json schema, restore commands, version-compat check, "When backups are NOT produced")
    - tools/validate-doc-links.js (lines 74-82 — `headingToAnchor` slugifier semantics; confirm `v1.2 → v1.3 recovery` slugifies to `v12--v13-recovery`)
    - tools/validate-doc-paths.js (verify Plan 04's ALLOWLIST has `'upgrade-recovery.md'` so legacy-path examples in this file remain legitimate)
    - .planning/phases/12-agent-dir-relocation-release/12-PATTERNS.md (§ "docs/upgrade-recovery.md" — exact content to add)
  </read_first>
  <action>
**Locate** the last existing section in docs/upgrade-recovery.md (likely "When backups are NOT produced" around line 99 of the prior snapshot, but verify exact line by reading the file).

**Append** a new top-level section AFTER the last existing section. Per PATTERNS § docs/upgrade-recovery.md content:

```markdown
## v1.2 → v1.3 recovery

When upgrading from v1.2.0 to v1.3.0, the installer relocates persona body
files from `_gomad/gomad/agents/<shortName>.md` to
`_gomad/_config/agents/<shortName>.md`. The 8 old files are snapshotted
into `_gomad/_backups/<YYYYMMDD-HHMMSS>/_gomad/gomad/agents/` before removal.

The v1.3 cleanup planner detects v1.2 layouts via the presence of both a
`_gomad/_config/files-manifest.csv` (v2 schema) AND at least one persona
file at `_gomad/gomad/agents/`. When detected, the migration emits a
verbose banner naming the move, the backup location, and this recovery
document.

### Rollback to v1.2 layout

If `/gm:agent-*` invocation fails after upgrade, or you need to roll back to
v1.2.0:

1. Re-pin the v1.2 release globally:

   ```bash
   npm install -g @xgent-ai/gomad@1.2.0
   ```

2. Restore the snapshotted persona files (substitute the actual timestamp
   from `_gomad/_backups/`):

   ```bash
   cp -R _gomad/_backups/<YYYYMMDD-HHMMSS>/_gomad/gomad/agents/ _gomad/gomad/
   ```

3. Re-run `gomad install` against the v1.2 binary — the installer will
   regenerate launcher stubs (`/gm:agent-*`) pointing at the restored
   `_gomad/gomad/agents/` location:

   ```bash
   gomad install
   ```

### Forward recovery

If only the launcher stubs are stale (the persona body files are at the
new `_config/agents/` location but `/gm:agent-*` 404s), re-run
`gomad install`. The `writeAgentLaunchers` step overwrites launcher stubs
unconditionally and will regenerate them with the v1.3 path.

```bash
gomad install
```

### What the .customize.yaml semantics look like

User overrides at `_gomad/_config/agents/.customize.yaml` are preserved
across the v1.2→v1.3 upgrade. The custom-file detector treats the 8
generated persona `.md` files as installer-managed and leaves
`.customize.yaml` and any non-matching `.md` files untouched.
```

**CRITICAL formatting requirements:**
1. The heading is exactly `## v1.2 → v1.3 recovery` (with a literal Unicode arrow → between `v1.2` and `v1.3`). The slugifier converts this to `v12--v13-recovery` (Plan 08's CHANGELOG cross-link uses this anchor).
2. Code blocks use triple-backtick fenced syntax. Do NOT translate the bash commands or paths inside code blocks.
3. The `cp -R` command intentionally uses `<YYYYMMDD-HHMMSS>` placeholder — leave this literal so the user understands they must substitute.
4. The example backup-tree paths inside this section reference `_gomad/gomad/agents/` — that is allowlisted in validate-doc-paths.js per Plan 04.

**DO NOT:**
- Modify any existing section in docs/upgrade-recovery.md
- Reorder existing headings
- Change the file's frontmatter (if present)
- Touch any other docs file in this task (Task 2 handles zh-cn translation)

After saving, run `npm run validate:doc-paths` to confirm the file remains within the allowlist (it must exit 0).
  </action>
  <verify>
    <automated>set -e; grep -q "## v1.2 → v1.3 recovery" docs/upgrade-recovery.md && grep -q "_gomad/_backups/<YYYYMMDD-HHMMSS>/_gomad/gomad/agents/" docs/upgrade-recovery.md && grep -q "writeAgentLaunchers" docs/upgrade-recovery.md && npm run validate:doc-paths && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## v1.2 → v1.3 recovery" docs/upgrade-recovery.md` returns exactly 1
    - `grep -c "### Rollback to v1.2 layout" docs/upgrade-recovery.md` returns 1
    - `grep -c "### Forward recovery" docs/upgrade-recovery.md` returns 1
    - `grep -c "_gomad/_backups/<YYYYMMDD-HHMMSS>/_gomad/gomad/agents/" docs/upgrade-recovery.md` returns at least 1
    - `grep -c "npm install -g @xgent-ai/gomad@1.2.0" docs/upgrade-recovery.md` returns 1
    - `grep -c "writeAgentLaunchers" docs/upgrade-recovery.md` returns 1
    - `grep -c ".customize.yaml" docs/upgrade-recovery.md` returns at least 1 (preservation note)
    - The pre-existing line 19 with `gomad/agents/pm.md` is byte-for-byte unchanged: `git diff docs/upgrade-recovery.md | grep -E "^-.*gomad/agents/pm\\.md" | wc -l` returns 0
    - `npm run validate:doc-paths` exits 0 (allowlist correctly exempts upgrade-recovery.md)
    - `npm run lint:md -- docs/upgrade-recovery.md` exits 0 (markdown lint clean)
    - `npm run docs:build` does not regress (the build output should still succeed)
  </acceptance_criteria>
  <done>
docs/upgrade-recovery.md has the v1.2→v1.3 recovery section appended; existing content preserved; validate:doc-paths still green; markdown lint clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Translate the v1.2→v1.3 recovery section to docs/zh-cn/upgrade-recovery.md</name>
  <files>docs/zh-cn/upgrade-recovery.md</files>
  <read_first>
    - docs/zh-cn/upgrade-recovery.md (FULL FILE — verify Phase 11's gap-closure plan 09 created this file; understand existing translation conventions, what's translated vs left in English)
    - docs/upgrade-recovery.md (Task 1 output — the English source for this translation)
    - .planning/phases/12-agent-dir-relocation-release/12-PATTERNS.md (§ "docs/zh-cn/upgrade-recovery.md" — translation parity guidance)
  </read_first>
  <action>
**Locate** the last existing section in docs/zh-cn/upgrade-recovery.md (mirrors the structure of the English file — Phase 11 plan 09 should have established the section parity).

**Append** a Simplified Chinese translation of the new section:

```markdown
## v1.2 → v1.3 升级恢复

从 v1.2.0 升级到 v1.3.0 时，安装程序会将代理人格主体文件从
`_gomad/gomad/agents/<shortName>.md` 迁移到
`_gomad/_config/agents/<shortName>.md`。这 8 个旧文件会在删除前被快照备份到
`_gomad/_backups/<YYYYMMDD-HHMMSS>/_gomad/gomad/agents/` 目录。

v1.3 清理计划程序通过同时检测以下两个条件来识别 v1.2 布局：
存在 `_gomad/_config/files-manifest.csv`（v2 模式），并且
`_gomad/gomad/agents/` 目录下至少存在一个人格文件。一旦检测到，
迁移过程会输出醒目的横幅，说明迁移内容、备份位置以及指向本恢复文档的链接。

### 回滚到 v1.2 布局

如果升级后 `/gm:agent-*` 调用失败，或者你需要回滚到 v1.2.0：

1. 重新全局固定 v1.2 版本：

   ```bash
   npm install -g @xgent-ai/gomad@1.2.0
   ```

2. 还原快照中的人格文件（请把示例中的时间戳替换为
   `_gomad/_backups/` 目录下的实际值）：

   ```bash
   cp -R _gomad/_backups/<YYYYMMDD-HHMMSS>/_gomad/gomad/agents/ _gomad/gomad/
   ```

3. 在 v1.2 二进制下重新运行 `gomad install`，安装程序会重新生成
   指向已恢复的 `_gomad/gomad/agents/` 路径的启动器存根
   （`/gm:agent-*`）：

   ```bash
   gomad install
   ```

### 前向恢复

如果只是启动器存根失效（人格主体文件已位于新的 `_config/agents/` 路径，
但 `/gm:agent-*` 仍然 404），重新运行 `gomad install`。
`writeAgentLaunchers` 步骤会无条件覆盖启动器存根，使用 v1.3 的新路径
重新生成它们：

```bash
gomad install
```

### .customize.yaml 的保留语义

位于 `_gomad/_config/agents/.customize.yaml` 的用户自定义配置会在
v1.2→v1.3 升级中得到保留。自定义文件检测器把 8 个生成的人格 `.md`
文件视为安装程序管理的文件，不会触碰 `.customize.yaml` 以及任何
不匹配上述名单的 `.md` 文件。
```

**Translation rules:**
1. Heading `## v1.2 → v1.3 recovery` → `## v1.2 → v1.3 升级恢复` (preserve the literal arrow → so the slug remains compatible; note that zh-cn slugifier may produce a different anchor — Plan 08 cross-link is to the EN file specifically)
2. Code blocks: `cp -R`, `gomad install`, `npm install -g @xgent-ai/gomad@1.2.0` are NOT translated
3. Path literals (`_gomad/...`, `<shortName>.md`, `<YYYYMMDD-HHMMSS>`) are NOT translated
4. File names (`.customize.yaml`, `files-manifest.csv`) are NOT translated
5. Function names (`writeAgentLaunchers`) are NOT translated
6. Heading levels match the English file (`##` for top-level new section, `###` for sub-sections)

The example backup-tree paths inside this section reference `gomad/agents/` — Plan 04's allowlist exempts `zh-cn/upgrade-recovery.md` per D-12.

DO NOT:
- Modify any existing section of docs/zh-cn/upgrade-recovery.md
- Touch the English source file in this task (Task 1 handled it)
- Add a translator's note or any non-content frontmatter field

After saving, run `npm run validate:doc-paths` to confirm zh-cn allowlist exemption is honored (must exit 0).
  </action>
  <verify>
    <automated>set -e; grep -q "## v1.2 → v1.3 升级恢复" docs/zh-cn/upgrade-recovery.md && grep -q "writeAgentLaunchers" docs/zh-cn/upgrade-recovery.md && grep -q "回滚到 v1.2 布局" docs/zh-cn/upgrade-recovery.md && npm run validate:doc-paths && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## v1.2 → v1.3 升级恢复" docs/zh-cn/upgrade-recovery.md` returns exactly 1
    - `grep -c "### 回滚到 v1.2 布局" docs/zh-cn/upgrade-recovery.md` returns 1
    - `grep -c "### 前向恢复" docs/zh-cn/upgrade-recovery.md` returns 1
    - `grep -c ".customize.yaml" docs/zh-cn/upgrade-recovery.md` returns at least 2 (filename in heading + body)
    - `grep -c "npm install -g @xgent-ai/gomad@1.2.0" docs/zh-cn/upgrade-recovery.md` returns 1 (untranslated bash)
    - `grep -c "writeAgentLaunchers" docs/zh-cn/upgrade-recovery.md` returns 1 (untranslated function name)
    - `grep -c "_gomad/_backups/<YYYYMMDD-HHMMSS>/_gomad/gomad/agents/" docs/zh-cn/upgrade-recovery.md` returns at least 1 (untranslated path literal)
    - The pre-existing v1.2-era backup-tree example referencing `gomad/agents/pm.md` (or similar) is byte-for-byte unchanged
    - `npm run validate:doc-paths` exits 0 (zh-cn/upgrade-recovery.md is in allowlist)
    - `npm run lint:md -- docs/zh-cn/upgrade-recovery.md` exits 0
    - `npm run docs:build` succeeds (build catches malformed markdown)
  </acceptance_criteria>
  <done>
zh-cn translation appended; English file untouched; validate:doc-paths green; lint clean; build green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs site → user (visitor) | Public docs; no untrusted input — content is authored, deployed via Starlight pipeline |
| Allowlist-exempted file → linter | Allowlist is data-trust: validate-doc-paths.js skips this file entirely; ensure no malicious content slips through (review at PR time) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07-01 | Information Disclosure | Recovery doc accidentally leaks secret env vars in code blocks | accept | Section is hand-authored; reviewer confirms no env-var values in commands |
| T-12-07-02 | Tampering | User follows the rollback recipe with wrong timestamp and overwrites current install | mitigate | Recipe step 2 explicitly says "substitute the actual timestamp"; `<YYYYMMDD-HHMMSS>` placeholder makes the substitution requirement obvious |
| T-12-07-03 | Spoofing | Translation drift between EN and zh-cn introduces incorrect rollback steps | mitigate | Section structure mirrors EN line-for-line; code blocks identical (untranslated); reviewer compares at checkpoint |
| T-12-07-04 | Denial of Service | Markdown lint fails build on malformed syntax | mitigate | acceptance_criteria runs lint:md and docs:build before completion |
</threat_model>

<verification>
- Both `docs/upgrade-recovery.md` and `docs/zh-cn/upgrade-recovery.md` have the new v1.2→v1.3 section
- `npm run validate:doc-paths` exits 0 on the combined Phase 11 + Phase 12 tree
- `npm run lint:md` and `npm run docs:build` both exit 0
- The Phase 11 branch is merged into the current Phase 12 working branch (per checkpoint)
</verification>

<success_criteria>
- Phase 11 branch confirmed merged into Phase 12 working branch (checkpoint approval)
- docs/upgrade-recovery.md has the new v1.2 → v1.3 recovery section with rollback + forward-recovery + .customize.yaml notes
- docs/zh-cn/upgrade-recovery.md has the parity translation (untranslated commands/paths, translated prose)
- New heading slugifies to `v12--v13-recovery` for CHANGELOG cross-link consumption
- validate:doc-paths exits 0 on the combined tree
- markdownlint + docs:build still pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-agent-dir-relocation-release/12-07-SUMMARY.md` documenting:
- Phase 11 merge commit hash brought into Phase 12 working branch
- Sections added to upgrade-recovery.md (English + zh-cn) — exact heading text + LOC count
- Result of npm run validate:doc-paths on combined tree (exit 0 expected)
- Note for Plan 08: CHANGELOG cross-link uses anchor `#v12--v13-recovery` (slugified from `## v1.2 → v1.3 recovery`)
</output>
