---
phase: 12-agent-dir-relocation-release
plan: 08
type: execute
wave: 6
depends_on: ["12-04", "12-05", "12-06", "12-07"]
files_modified:
  - package.json
  - CHANGELOG.md
  - .planning/PROJECT.md
  - .planning/MILESTONES.md
  - .planning/STATE.md
autonomous: false
requirements: [REL-01, REL-02, REL-03, REL-04]
tags: [release, changelog, npm-publish, git-tag, planning-state]

user_setup:
  - service: npm
    why: "Publish @xgent-ai/gomad@1.3.0 to the npm registry with the latest dist-tag"
    env_vars:
      - name: NPM_TOKEN
        source: "npmjs.com → Account → Access Tokens → Generate New Token (Granular, 2FA enabled, 90-day max). REL-03 accepts granular token; OIDC NOT required per D-06."
    dashboard_config:
      - task: "Confirm 2FA is enabled on npm account (Granular tokens require it)"
        location: "https://www.npmjs.com/settings/<username>/profile"
      - task: "Verify @xgent-ai/gomad maintainer permissions"
        location: "https://www.npmjs.com/package/@xgent-ai/gomad"

must_haves:
  truths:
    - "package.json scripts.quality chains the FULL release-gate matrix: format:check, lint, lint:md, docs:build, validate:refs, validate:skills, validate:kb-licenses, validate:doc-paths, test:inject-reference-tables, test:orphan-refs, test:install, test:integration, test:gm-surface, test:tarball, test:domain-kb-install, test:legacy-v12-upgrade, test:v13-agent-relocation"
    - "package.json has prepublishOnly = 'npm run quality' so npm publish is gated"
    - "package.json scripts has test:legacy-v12-upgrade and test:v13-agent-relocation entries pointing at Plan 05's test files"
    - "package.json version is bumped from 1.2.0 to 1.3.0"
    - "CHANGELOG.md has a new ## [1.3.0] entry with Summary / Added / Changed / Removed / BREAKING CHANGES sections per the v1.2.0 template"
    - "BREAKING CHANGES section explicitly documents the agent-dir relocation with old-path → new-path mapping AND a cross-link to docs/upgrade-recovery.md#v12--v13-recovery"
    - "npm run quality exits 0 BEFORE the version bump commit"
    - "After publish: npm view @xgent-ai/gomad@1.3.0 returns metadata; npm view @xgent-ai/gomad dist-tags shows latest=1.3.0"
    - "Git tag v1.3.0 exists and is pushed to origin/main"
    - ".planning/PROJECT.md, MILESTONES.md, STATE.md updated in the same release commit range to reflect v1.3.0 ship"
  artifacts:
    - path: "package.json"
      provides: "v1.3.0 version + extended quality script + prepublishOnly + new test scripts"
      contains: "1.3.0"
    - path: "CHANGELOG.md"
      provides: "v1.3.0 entry with explicit BREAKING CHANGES section"
      contains: "## [1.3.0]"
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions row for v1.3 BREAKING-callout pattern + manual prepublishOnly gate"
      contains: "v1.3"
    - path: ".planning/MILESTONES.md"
      provides: "Phase 12 marked complete + v1.3.0 release link"
      contains: "v1.3.0"
    - path: ".planning/STATE.md"
      provides: "Phase 12 exclusive lock cleared + v1.3.0 release fact logged"
      contains: "v1.3.0"
  key_links:
    - from: "package.json scripts.prepublishOnly"
      to: "package.json scripts.quality"
      via: "npm run quality"
      pattern: "npm run quality"
    - from: "CHANGELOG.md v1.3.0 BREAKING section"
      to: "docs/upgrade-recovery.md#v12--v13-recovery"
      via: "cross-link"
      pattern: "v12--v13-recovery"
---

<objective>
The release commit. Wires the full quality-gate matrix (REL-02), authors the v1.3.0 CHANGELOG entry with BREAKING callout (REL-01), bumps the version, runs `npm publish` to the npm registry as `latest` (REL-03), tags `v1.3.0` and pushes to `origin/main`, and updates planning state files (REL-04).

Purpose: Implements REL-01 (CHANGELOG BREAKING entry), REL-02 (quality gate composition), REL-03 (manual npm publish with prepublishOnly safety per D-07), REL-04 (git tag + planning state). Gated by THREE human checkpoints: (1) quality green BEFORE version bump per Pitfall 4, (2) reviewer approval of CHANGELOG content + visual confirmation of `npm pack --dry-run` file list per Pitfall 4, (3) post-publish smoke test confirmation.

Output: package.json (version + scripts), CHANGELOG.md (v1.3.0 entry), three planning files (PROJECT/MILESTONES/STATE). One npm publish operation. One git tag pushed.
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/MILESTONES.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-agent-dir-relocation-release/12-CONTEXT.md
@.planning/phases/12-agent-dir-relocation-release/12-RESEARCH.md
@.planning/phases/12-agent-dir-relocation-release/12-PATTERNS.md
@.planning/phases/12-agent-dir-relocation-release/12-04-SUMMARY.md
@.planning/phases/12-agent-dir-relocation-release/12-05-SUMMARY.md
@.planning/phases/12-agent-dir-relocation-release/12-06-SUMMARY.md
@.planning/phases/12-agent-dir-relocation-release/12-07-SUMMARY.md

<interfaces>
<!-- Reference release artifacts. -->

Current package.json scripts (verified at planning time):
- `quality` chain: format:check && lint && lint:md && docs:build && test:install && test:integration && validate:refs && validate:skills && test:inject-reference-tables && validate:kb-licenses && test:orphan-refs
- Existing test:* scripts: test:domain-kb-install, test:gm-surface, test:tarball, test:install, test:integration, test:inject-reference-tables, test:orphan-refs, test:refs, test:e2e, test:cleanup-all, test:self-install-guard
- NO prepublishOnly currently
- Version: 1.2.0

Current CHANGELOG.md v1.2.0 entry pattern (lines 7-76):
- `## [1.2.0] - 2026-04-23`
- `### Summary` (2-3 paragraph narrative)
- `### Added` (bullets)
- `### Changed` (bullets)
- `### Removed` (bullets)
- `### BREAKING CHANGES` (paragraph with old → new + installer behavior + user action)

Plan 04 SUMMARY: validate:doc-paths npm script exists; runs against current docs tree exit 0.
Plan 05 SUMMARY: test/test-legacy-v12-upgrade.js + test/test-v13-agent-relocation.js exist; npm scripts NOT yet wired.
Plan 06 SUMMARY: test:gm-surface + test:tarball still pass (extended).
Plan 07 SUMMARY: docs/upgrade-recovery.md has #v12--v13-recovery anchor.

Phase 11 status: per Plan 07 checkpoint, Phase 11 branch is merged into Phase 12 working branch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend package.json scripts (quality matrix + prepublishOnly + new test entries)</name>
  <files>package.json</files>
  <read_first>
    - package.json (FULL FILE — verify current scripts block; confirm Plan 04's `validate:doc-paths` script entry is present; confirm version is still 1.2.0)
    - test/test-legacy-v12-upgrade.js (Plan 05) and test/test-v13-agent-relocation.js (Plan 05) — confirm files exist
    - .planning/phases/12-agent-dir-relocation-release/12-PATTERNS.md (§ "package.json")
    - .planning/phases/12-agent-dir-relocation-release/12-CONTEXT.md (D-07, D-08)
    - .planning/phases/12-agent-dir-relocation-release/12-RESEARCH.md (§ "Code Examples Example 1" — full quality chain)
  </read_first>
  <action>
**Edit 1 — Add new test script entries**, alphabetically among existing `test:*` scripts:

```json
"test:legacy-v12-upgrade": "node test/test-legacy-v12-upgrade.js",
"test:v13-agent-relocation": "node test/test-v13-agent-relocation.js",
```

**Edit 2 — Add prepublishOnly script** (D-07; placed alphabetically near `prepare`):

```json
"prepublishOnly": "npm run quality",
```

Per D-07 + Open Q4: `prepublishOnly` runs ONLY on `npm publish` (NOT on `npm install`). Use `prepublishOnly` (NOT the deprecated `prepublish`).

**Edit 3 — REPLACE the entire `quality` script** with the extended chain per D-08 + RESEARCH §Code Examples Example 1. Order: cheap fast-fail first, expensive E2E last.

Replace:
```json
"quality": "npm run format:check && npm run lint && npm run lint:md && npm run docs:build && npm run test:install && npm run test:integration && npm run validate:refs && npm run validate:skills && npm run test:inject-reference-tables && npm run validate:kb-licenses && npm run test:orphan-refs"
```

With:
```json
"quality": "npm run format:check && npm run lint && npm run lint:md && npm run docs:build && npm run validate:refs && npm run validate:skills && npm run validate:kb-licenses && npm run validate:doc-paths && npm run test:inject-reference-tables && npm run test:orphan-refs && npm run test:install && npm run test:integration && npm run test:gm-surface && npm run test:tarball && npm run test:domain-kb-install && npm run test:legacy-v12-upgrade && npm run test:v13-agent-relocation"
```

Order rationale (cheap fast-fail first):
1. format:check (sub-second)
2. lint (~10s)
3. lint:md (~5s)
4. docs:build (~30s) — catches docs syntax / link breakage
5. validate:* lints (validate:refs, validate:skills, validate:kb-licenses, validate:doc-paths) — fast greps
6. test:inject-reference-tables (~10s)
7. test:orphan-refs (~5s)
8. test:install (~3min) — slowest pure-installer test
9. test:integration (prd-chain, ~3min)
10. test:gm-surface (~3min cold) — extended Phase C
11. test:tarball (~30s) — extended Phase 4
12. test:domain-kb-install (~1min) — Phase 10 test
13. test:legacy-v12-upgrade (~3min) — NEW
14. test:v13-agent-relocation (~2min) — NEW

**DO NOT** bump the version field in this task — Task 2 (CHANGELOG) is authored first, then Task 3 runs `npm run quality` to confirm GREEN, then Task 4 bumps version. Pitfall 4: "Don't bump version BEFORE quality green."

**DO NOT** modify any field outside `scripts`. JSON-validity check after edit: `node -e "JSON.parse(require('fs').readFileSync('package.json'))"`.
  </action>
  <verify>
    <automated>set -e; node -e "const p = JSON.parse(require('fs').readFileSync('package.json', 'utf8')); for (const s of ['prepublishOnly','test:legacy-v12-upgrade','test:v13-agent-relocation','validate:doc-paths','test:gm-surface','test:tarball','test:domain-kb-install']) { if (!p.scripts[s]) { console.error('missing script:', s); process.exit(1); } } if (!p.scripts.quality.includes('test:legacy-v12-upgrade')) process.exit(1); if (!p.scripts.quality.includes('validate:doc-paths')) process.exit(1); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json')).scripts.prepublishOnly)"` outputs `npm run quality`
    - `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json')).scripts['test:legacy-v12-upgrade'])"` outputs `node test/test-legacy-v12-upgrade.js`
    - `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json')).scripts['test:v13-agent-relocation'])"` outputs `node test/test-v13-agent-relocation.js`
    - The `quality` script string contains every required gate: `for s in format:check lint lint:md docs:build validate:refs validate:skills validate:kb-licenses validate:doc-paths test:inject-reference-tables test:orphan-refs test:install test:integration test:gm-surface test:tarball test:domain-kb-install test:legacy-v12-upgrade test:v13-agent-relocation; do node -e "if (!JSON.parse(require('fs').readFileSync('package.json')).scripts.quality.includes('npm run $s')) process.exit(1)" || { echo missing $s; exit 1; }; done`
    - Version field is STILL `"version": "1.2.0"` (Task 4 bumps it)
    - JSON valid: `node -e "JSON.parse(require('fs').readFileSync('package.json'))"` exits 0
    - `npm run format:check -- package.json` exits 0
  </acceptance_criteria>
  <done>
quality script wires the full release matrix; prepublishOnly gate added; new test scripts wired; version still 1.2.0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author CHANGELOG.md v1.3.0 entry with BREAKING CHANGES section</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (lines 1-120 — confirm v1.2.0 entry structure: Summary / Added / Changed / Removed / BREAKING CHANGES; understand date format and version-link footnotes if present)
    - .planning/phases/12-agent-dir-relocation-release/12-PATTERNS.md (§ "CHANGELOG.md" — section structure)
    - .planning/phases/12-agent-dir-relocation-release/12-RESEARCH.md (§ "Code Examples Example 3" — sketch of v1.3.0 entry)
    - docs/upgrade-recovery.md (Plan 07 output — confirm `## v1.2 → v1.3 recovery` heading exists; the cross-link anchor is `#v12--v13-recovery`)
  </read_first>
  <action>
**Insert** a new `## [1.3.0]` section IMMEDIATELY ABOVE the existing `## [1.2.0]` section in CHANGELOG.md. Preserve all existing entries verbatim.

Use today's date (the agent should query `date +%Y-%m-%d` via shell at execution time and substitute).

```markdown
## [1.3.0] - YYYY-MM-DD

### Summary

v1.3.0 ships the docs/story-creation/relocation milestone. Persona body files
relocate from `<installRoot>/_gomad/gomad/agents/` to
`<installRoot>/_gomad/_config/agents/`. The `gm-discuss-story` and
`gm-domain-skill` skills land under `4-implementation/`, two seed KB packs
ship at `<installRoot>/_gomad/_config/kb/`, and initial docs content goes
live at `gomad.xgent.ai`. v1.2 → v1.3 upgrades are handled automatically by
the cleanup planner with backup snapshots.

### Added

- `gm-discuss-story` skill (`src/gomad-skills/4-implementation/gm-discuss-story/`) — pre-story context-gathering with locked output sections.
- `gm-domain-skill` (`src/gomad-skills/4-implementation/gm-domain-skill/`) — hand-rolled BM25-style retrieval with Levenshtein typo fallback.
- `src/domain-kb/testing/` and `src/domain-kb/architecture/` — seed KB packs with attribution frontmatter (`source`, `license`, `last_reviewed`).
- `tools/validate-kb-licenses.js` — release gate for KB attribution.
- `tools/validate-doc-paths.js` — DOCS-07 linter forbidding legacy `_gomad/gomad/agents/` paths in shipped docs.
- `tools/verify-tarball.js` Phase 4 — grep-clean for residual `gomad/agents/` references in shipped source (allowlisted via `tools/fixtures/tarball-grep-allowlist.json`).
- `test/test-domain-kb-install.js`, `test/test-legacy-v12-upgrade.js`, `test/test-v13-agent-relocation.js` — new E2E test coverage.
- `prepublishOnly` script gating publish on the full `quality` matrix.
- Initial docs content at `gomad.xgent.ai` (English + Simplified Chinese): tutorials/install, tutorials/quick-start, reference/agents, reference/skills, explanation/architecture, how-to/contributing.
- `docs/upgrade-recovery.md` — new "v1.2 → v1.3 recovery" section with rollback recipe.

### Changed

- Persona body install path: `<installRoot>/_gomad/gomad/agents/<shortName>.md` → `<installRoot>/_gomad/_config/agents/<shortName>.md`.
- Launcher stubs (`/gm:agent-*`) now reference `_gomad/_config/agents/`; regenerated unconditionally on every install.
- `tools/installer/core/cleanup-planner.js` — new `isV12LegacyAgentsDir` detector + parallel branch in `buildCleanupPlan` that snapshots and removes the 8 legacy persona files (reason `manifest_cleanup`).
- `tools/installer/core/installer.js` — `detectCustomFiles` whitelist treats the 8 known persona `.md` files at `_config/agents/` as generated (preserves `.customize.yaml` user-override semantics).
- `npm run quality` matrix extended with `validate:doc-paths`, `test:gm-surface`, `test:tarball`, `test:domain-kb-install`, `test:legacy-v12-upgrade`, `test:v13-agent-relocation`.
- `tools/installer/ide/shared/path-utils.js` — new `AGENTS_PERSONA_SUBPATH` and `LEGACY_AGENTS_PERSONA_SUBPATH` constants as the sole source of truth for the persona subpath.

### Removed

- (none — v1.2 → v1.3 is a relocation, not a removal)

### BREAKING CHANGES

The 8 persona body files have moved from
`<installRoot>/_gomad/gomad/agents/<shortName>.md` to
`<installRoot>/_gomad/_config/agents/<shortName>.md`.

Upgrading from v1.2.0: run `gomad install` in your existing v1.2 workspace.
The installer will:

1. Snapshot the old `_gomad/gomad/agents/` files into
   `_gomad/_backups/<YYYYMMDD-HHMMSS>/_gomad/gomad/agents/`.
2. Remove the old files from `_gomad/gomad/agents/`.
3. Write the new files at `_gomad/_config/agents/<shortName>.md`.
4. Regenerate launcher stubs (`/gm:agent-*`) to point at the new location.

A verbose migration banner prints during the upgrade naming the relocation,
the backup location, and the recovery doc cross-reference.

If `/gm:agent-pm` (or any persona) invocation fails after upgrade, see
[`docs/upgrade-recovery.md`](./docs/upgrade-recovery.md#v12--v13-recovery)
for the rollback recipe (re-pin v1.2 globally → restore from snapshot →
re-run `gomad install`).

If you scripted the literal path `_gomad/gomad/agents/`, update to
`_gomad/_config/agents/`. Your `.customize.yaml` files at `_config/agents/`
are preserved across the upgrade.
```

**CRITICAL formatting requirements:**
1. Date in YYYY-MM-DD format — substitute with `date +%Y-%m-%d` output at execution time
2. The cross-link uses `#v12--v13-recovery` (slugified from `## v1.2 → v1.3 recovery` in docs/upgrade-recovery.md per Plan 07)
3. Section order MUST match the v1.2.0 template: Summary → Added → Changed → Removed → BREAKING CHANGES
4. The `_gomad/gomad/agents/` literal appears ~5 times in this entry — that's fine; CHANGELOG.md is OUTSIDE Plan 04 validate-doc-paths scope (linter walks `docs/` only)
5. All 4 BREAKING-section requirements per Pitfall 5 are present:
   - State what changed (path move)
   - State what installer does automatically
   - State what user does (run `gomad install`)
   - State rollback recipe + cross-link to upgrade-recovery.md

**DO NOT** modify the v1.2.0, v1.1.0, v1.0.0, or any other existing CHANGELOG entry.

**Footer/links:** if the v1.2.0 entry uses `[1.2.0]: <git-compare-url>` reference-style links at the bottom of the file, add a parallel `[1.3.0]: https://github.com/xgent-ai/gomad/compare/v1.2.0...v1.3.0` entry. If no such footer exists, no changes needed.

After saving: `npm run lint:md -- CHANGELOG.md` exits 0.
  </action>
  <verify>
    <automated>set -e; grep -q "## \[1.3.0\]" CHANGELOG.md && grep -q "### BREAKING CHANGES" CHANGELOG.md && grep -q "_gomad/_config/agents/" CHANGELOG.md && grep -q "v12--v13-recovery" CHANGELOG.md && grep -q "## \[1.2.0\]" CHANGELOG.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## \\[1.3.0\\]" CHANGELOG.md` returns exactly 1
    - `grep -c "## \\[1.2.0\\]" CHANGELOG.md` returns exactly 1 (v1.2.0 entry preserved)
    - `grep -c "### Summary" CHANGELOG.md` returns at least 2 (v1.3.0 + v1.2.0)
    - `grep -c "### Added" CHANGELOG.md` returns at least 2
    - `grep -c "### Changed" CHANGELOG.md` returns at least 2
    - `grep -c "### Removed" CHANGELOG.md` returns at least 2
    - `grep -c "### BREAKING CHANGES" CHANGELOG.md` returns at least 2 (v1.3.0 has one; v1.2.0 has one)
    - `grep -c "v12--v13-recovery" CHANGELOG.md` returns at least 1
    - `grep -c "_gomad/_config/agents/" CHANGELOG.md` returns at least 2 (Changed + BREAKING sections)
    - `grep -c "_gomad/_backups/<YYYYMMDD-HHMMSS>/_gomad/gomad/agents/" CHANGELOG.md` returns at least 1
    - The new entry sits ABOVE the v1.2.0 entry: `awk '/## \\[1\\.3\\.0\\]/ {f=NR} /## \\[1\\.2\\.0\\]/ {if (f && NR > f) print "OK"}' CHANGELOG.md` outputs `OK`
    - Date placeholder replaced with actual date: `grep -c "## \\[1.3.0\\] - 20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]" CHANGELOG.md` returns 1 (no `YYYY-MM-DD` literal remains)
    - `npm run lint:md -- CHANGELOG.md` exits 0
  </acceptance_criteria>
  <done>
v1.3.0 entry inserted above v1.2.0 with full Summary/Added/Changed/Removed/BREAKING sections; cross-link anchor matches Plan 07's heading slug; markdown lint clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Run npm run quality + visually inspect npm pack --dry-run BEFORE version bump</name>
  <what-built>
    Per Pitfall 4 ("Don't bump version BEFORE quality green") and Pitfall 4 ("Verify dist-tag explicitly", "Run npm pack --dry-run and visually inspect file list before npm publish"), the user must verify the release-gate matrix is GREEN and the tarball contents are correct before any version-bumping commit happens.

    What was automated up to this point:
    - Plans 01-07 complete (relocation + tests + tooling + docs)
    - package.json scripts.quality is the full matrix (Task 1 of this plan)
    - CHANGELOG.md v1.3.0 entry authored (Task 2 of this plan)
    - Version is STILL 1.2.0 (NOT yet bumped)
  </what-built>
  <how-to-verify>
1. Confirm working tree is clean of unstaged work (other than this plan's commit-pending files):

   ```bash
   git status
   ```

2. Run the FULL quality matrix:

   ```bash
   npm run quality
   ```

   Expected: exit 0. This may take 15-20 minutes per RESEARCH §Validation Architecture estimate.

3. If quality FAILS: investigate the failing gate, fix the underlying code (NOT the test), re-run quality. Do NOT proceed past this checkpoint until exit 0.

4. After quality green, inspect the planned tarball file list:

   ```bash
   npm pack --dry-run 2>&1 | grep -E "^npm notice"
   ```

   Confirm:
   - `package.json`, `README.md`, `README_CN.md`, `LICENSE`, `CHANGELOG.md`, `TRADEMARK.md`, `CONTRIBUTORS.md`, `CONTRIBUTING.md` all listed
   - `src/` files listed
   - `tools/installer/` files listed
   - `.planning/`, `.claude/`, `test/`, `docs/`, `website/` are NOT listed (out of scope per `package.json` `files:` array)
   - No `.env*`, `.npmrc`, secrets, or build artifacts unexpectedly present

5. If the tarball file list looks wrong: STOP, fix `package.json` `files:` array, re-run quality + npm pack --dry-run. Do NOT publish from a broken tarball.
  </how-to-verify>
  <resume-signal>Type "approved — quality-green-and-tarball-clean" once `npm run quality` exits 0 AND `npm pack --dry-run` file list visually matches the expected v1.2 baseline (with the new test files and CHANGELOG.md entry shipped, but no leakage of .planning/ test/ docs/ etc.).</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Bump version 1.2.0 → 1.3.0 in package.json + commit release-gate-green state</name>
  <files>package.json</files>
  <read_first>
    - package.json (verify Task 1 changes landed; verify version is currently 1.2.0)
    - Output of npm run quality from previous checkpoint (must have been green)
  </read_first>
  <action>
**Edit:** Change the version field in package.json from `"version": "1.2.0"` to `"version": "1.3.0"`.

After saving, run a sanity gate that does NOT take 20 min — confirm parse-validity + version reads correctly:

```bash
node -e "const v = JSON.parse(require('fs').readFileSync('package.json','utf8')).version; if (v !== '1.3.0') process.exit(1); console.log('version OK:', v);"
```

Then commit ALL pending Plan 08 changes so far in a single release commit:

```bash
git add package.json CHANGELOG.md
# (Planning state files committed in Task 6 alongside the tag/publish)
git commit -m "release(12): bump @xgent-ai/gomad to v1.3.0 (BREAKING — agent dir relocation)

- package.json version 1.2.0 → 1.3.0
- quality gate matrix extended (validate:doc-paths, test:gm-surface,
  test:tarball, test:domain-kb-install, test:legacy-v12-upgrade,
  test:v13-agent-relocation)
- prepublishOnly = npm run quality
- CHANGELOG v1.3.0 entry with BREAKING CHANGES section + upgrade-recovery
  cross-link

Implements REL-01 + REL-02 + part of REL-03/REL-04. Tag and publish in
follow-up tasks."
```

DO NOT touch any field other than `version` in this task. DO NOT add or remove dependencies. DO NOT touch the `quality` script (Task 1 already extended it).

The commit message follows project convention. After commit, verify with `git log -1 --name-only`.
  </action>
  <verify>
    <automated>set -e; node -e "if (JSON.parse(require('fs').readFileSync('package.json')).version !== '1.3.0') process.exit(1)" && git log -1 --pretty=%B | grep -q "release(12)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json')).version)"` outputs exactly `1.3.0`
    - Last commit message contains "release(12)" and "v1.3.0": `git log -1 --pretty=%B | grep -E "release.*12.*1\\.3\\.0"` exits 0
    - Last commit touched at least package.json and CHANGELOG.md: `git log -1 --name-only --pretty=format:"" | grep -E "package\\.json|CHANGELOG\\.md" | wc -l` returns at least 2
    - Working tree is clean of those files: `git status --porcelain | grep -E "package\\.json|CHANGELOG\\.md"` returns nothing
    - `npm run lint && npm run format:check` exits 0 against package.json (re-confirm post-bump)
  </acceptance_criteria>
  <done>
Version bumped to 1.3.0; release commit created with package.json + CHANGELOG.md; lint/format clean.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: User publishes @xgent-ai/gomad@1.3.0 to npm</name>
  <what-built>
    Per REL-03 + D-06: manual `npm publish` from local machine using a granular access token. Claude cannot publish to npm — this requires the user's authenticated npm session + 2FA (OTP code from authenticator app).

    Pre-publish state (verified):
    - Working tree clean
    - Quality matrix green (Task 3 checkpoint)
    - Version bumped to 1.3.0 (Task 3)
    - prepublishOnly = `npm run quality` is wired (Task 1) — will auto-run on `npm publish`
  </what-built>
  <how-to-verify>
1. Confirm npm authentication state:

   ```bash
   npm whoami
   ```

   Should output your npm username. If not logged in: `npm login` (or set NPM_TOKEN env var per dashboard config in `user_setup`).

2. Confirm 2FA is enabled and you have an OTP code ready (TOTP authenticator app or hardware key).

3. From the repo root, publish:

   ```bash
   npm publish --access public --tag latest
   ```

   This triggers `prepublishOnly` (which runs `npm run quality` again — expected exit 0 since the same gate just passed at Task 3 checkpoint).

   When prompted for OTP, enter the code from your authenticator.

   Expected output: `+ @xgent-ai/gomad@1.3.0` with metadata.

4. **Smoke verify** the publish succeeded:

   ```bash
   npm view @xgent-ai/gomad@1.3.0 version
   # Expected: 1.3.0

   npm view @xgent-ai/gomad dist-tags
   # Expected: { latest: '1.3.0', ...other tags... }

   npm view @xgent-ai/gomad versions --json
   # Expected: includes "1.0.0" (deprecated), "1.1.0", "1.2.0", "1.3.0"
   ```

5. **Optional sanity install** in a fresh tempdir:

   ```bash
   TMP=$(mktemp -d) && cd "$TMP" && npm init -y > /dev/null && npm install @xgent-ai/gomad@1.3.0 && ls node_modules/@xgent-ai/gomad/package.json && cat node_modules/@xgent-ai/gomad/package.json | grep '"version"'
   ```

   Expected: shows `"version": "1.3.0"`.

6. If publish FAILS (e.g. 403 OTP wrong, version conflict, prepublishOnly red):
   - 403 OTP wrong: re-run with fresh OTP
   - Version conflict (npm refuses overwrite): the version already exists — investigate; do NOT bump again, do NOT use --force
   - prepublishOnly red: investigate the failing test (likely network or environmental); fix; re-run

   Do NOT use `--force`. Do NOT re-publish the same version after a partial failure (npm immutability).
  </how-to-verify>
  <resume-signal>Type "approved — npm-publish-1.3.0-confirmed" once `npm view @xgent-ai/gomad@1.3.0 version` returns `1.3.0` and `npm view @xgent-ai/gomad dist-tags` shows `latest: 1.3.0`.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Tag v1.3.0 and push to origin/main + update planning state files</name>
  <files>.planning/PROJECT.md, .planning/MILESTONES.md, .planning/STATE.md</files>
  <read_first>
    - .planning/PROJECT.md (FULL — find Key Decisions table; understand the row format used for v1.2 entries)
    - .planning/MILESTONES.md (FULL — find the Phase 12 row in the v1.3 milestone section; understand the completion-status conventions)
    - .planning/STATE.md (FULL — find the milestone field, status field, current-position section, accumulated-context decisions section)
    - User's confirmation that npm publish succeeded (previous checkpoint approval)
  </read_first>
  <action>
**Edit 1 — Tag v1.3.0 and push:**

```bash
git tag -a v1.3.0 -m "Release v1.3.0 — Docs, Story Context & Agent Relocation

BREAKING: Persona body files relocated from <installRoot>/_gomad/gomad/agents/
to <installRoot>/_gomad/_config/agents/. v1.2 → v1.3 upgrade is automated
with backup snapshots under _gomad/_backups/<ts>/. See CHANGELOG.md and
docs/upgrade-recovery.md for the full BREAKING entry and rollback recipe.

Includes:
- DOCS-01..06: docs site content (tutorials, references, architecture, contributing) EN + zh-cn
- STORY-01..12: gm-discuss-story + gm-domain-skill + 2 seed KB packs + license validator
- DOCS-07: validate:doc-paths linter wired into npm run quality
- AGENT-01..11: agent dir relocation + isV12LegacyAgentsDir cleanup branch +
  detector whitelist + extended Phase C body regex + verify-tarball Phase 4
- REL-01..04: CHANGELOG BREAKING + extended quality gate + npm publish + tag"

git push origin v1.3.0
```

After tag push, verify:

```bash
git ls-remote --tags origin v1.3.0
# Expected: SHA + refs/tags/v1.3.0 listed
```

**Edit 2 — Update .planning/PROJECT.md:**

Add a new Key Decisions row (per the table format used for v1.2 entries — verify the table column structure by reading the file). The row should document:
- Decision: "v1.3 BREAKING-callout pattern + manual npm publish gated by prepublishOnly"
- Date: today's YYYY-MM-DD
- Phase: 12
- Rationale: brief — "Reused v1.2 BREAKING-section template for CHANGELOG; added prepublishOnly = npm run quality gate (D-07) to prevent accidental publish on un-tested state. OIDC migration deferred to v1.4+ (D-06)."

If PROJECT.md has a "Current focus" or "Active milestone" line near the top, update v1.3 → "Shipped v1.3.0 on YYYY-MM-DD" and shift current focus to v1.4 planning placeholder (or whatever the next-phase convention is).

**Edit 3 — Update .planning/MILESTONES.md:**

Find the Phase 12 row in the v1.3 section. Mark it complete:
- Status: `Complete` (matching v1.2 row conventions)
- Completed: today's YYYY-MM-DD
- Plans: `8/8 plans complete` (or whatever count this plan set produces)

If MILESTONES.md has a top-level "Shipped Milestones" summary line for v1.3, populate it with the ship date.

If MILESTONES.md has a v1.3 milestone-status section (similar to v1.1 / v1.2 rows), set v1.3 status to "Shipped YYYY-MM-DD".

**Edit 4 — Update .planning/STATE.md:**

In the YAML frontmatter:
- `status: planning` → `status: shipped` (or `idle`, matching v1.2 close convention — re-read v1.2 close commits if uncertain)
- `stopped_at: Phase 12 context gathered` → `stopped_at: v1.3.0 shipped to npm + tagged on origin/main`
- `last_updated:` → today's ISO timestamp
- `last_activity:` → today's YYYY-MM-DD
- `progress.completed_phases: 2` → `progress.completed_phases: 3`
- `progress.completed_plans: 16` → `progress.completed_plans: 24` (16 + 8 from Phase 12; verify exact count post-merge)
- `progress.percent: 100` (already at 100)

In the body:
- "Current Position" → set Phase: 12 → "v1.3.0 shipped"; Status: Ready to plan → "Shipped"
- "Decisions" section: add a Phase 12 close note: "Phase 12 closed YYYY-MM-DD — v1.3.0 published to npm with `latest` dist-tag; v1.2/v1.1 retained; v1.0.0 deprecation unchanged. CHANGELOG BREAKING + upgrade-recovery cross-link in place. prepublishOnly = npm run quality wired."
- "Quick Tasks Completed" — no new entry
- "Session Continuity" — set Last session and Resume to "v1.3 closed; next milestone planning runs `/gsd-roadmap-phase` for v1.4"

**Edit 5 — Commit all three planning files:**

```bash
git add .planning/PROJECT.md .planning/MILESTONES.md .planning/STATE.md
git commit -m "docs(state): close v1.3.0 — Phase 12 complete, agent dir relocation shipped

- PROJECT.md: Key Decisions row for v1.3 BREAKING-callout + prepublishOnly gate
- MILESTONES.md: Phase 12 marked complete; v1.3.0 shipped YYYY-MM-DD
- STATE.md: status shipped; completed_phases 2 → 3; v1.3.0 publish fact logged

Implements REL-04 (planning state updated in same release commit range)."

git push origin main
```

**Notes:**
- Substitute YYYY-MM-DD with `date +%Y-%m-%d` output at execution time
- The plan count "8/8" in MILESTONES.md should match the actual plan count for Phase 12 (this plan is plan 08 — verify the directory contents at execution time: `ls .planning/phases/12-agent-dir-relocation-release/12-*-PLAN.md | wc -l`)
- DO NOT touch any other planning file (ROADMAP.md, REQUIREMENTS.md, RETROSPECTIVE.md). Those updates (if any) belong to a separate `/gsd-evolve-project` cycle after this plan completes.
  </action>
  <verify>
    <automated>set -e; git ls-remote --tags origin v1.3.0 | grep -q "refs/tags/v1.3.0" && grep -q "v1.3.0" .planning/STATE.md && grep -q "Phase 12" .planning/MILESTONES.md && grep -q "v1.3" .planning/PROJECT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-remote --tags origin v1.3.0` returns the v1.3.0 tag SHA (proves tag pushed)
    - `git tag --list v1.3.0` returns `v1.3.0` locally
    - `git tag -l --format='%(contents)' v1.3.0 | grep -q "BREAKING"` exits 0 (annotated tag has the message)
    - `.planning/STATE.md` contains `v1.3.0` (publish fact): `grep -c "v1.3.0" .planning/STATE.md` returns at least 1
    - `.planning/STATE.md` `progress.completed_phases` is now 3 (or matches the actual phase-completion count): `grep -c "completed_phases: 3" .planning/STATE.md` returns at least 1 (or the correct count)
    - `.planning/MILESTONES.md` contains "Phase 12" with completion status: `grep -E "Phase 12.*Complete" .planning/MILESTONES.md` exits 0
    - `.planning/PROJECT.md` has a Key Decisions row mentioning v1.3: `grep -c "v1.3" .planning/PROJECT.md` increases (compared to before this plan)
    - Last 2 commits are: (a) release bump (Task 3), (b) docs(state) close (Task 4): `git log --oneline -2 | head -1 | grep -q "docs(state)" && git log --oneline -2 | tail -1 | grep -q "release(12)"`
    - `npm view @xgent-ai/gomad version` returns `1.3.0` (post-publish smoke confirms persistence)
    - `npm run lint && npm run lint:md && npm run format:check` exits 0 (planning files lint clean)
  </acceptance_criteria>
  <done>
v1.3.0 tag pushed; PROJECT.md/MILESTONES.md/STATE.md updated and committed; npm view confirms publish; planning state reflects shipped milestone.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → npm registry | npm publish uploads tarball; granular token + 2FA OTP authenticate the publisher |
| Local repo → origin/main | git push tag uploads to GitHub; user's git credentials authenticate |
| package.json scripts → arbitrary npm hooks | prepublishOnly runs quality gate which executes installer code in tempdirs (test:install, test:legacy-v12-upgrade) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08-01 | Spoofing | Wrong npm account publishes @xgent-ai/gomad@1.3.0 | mitigate | npm whoami check at checkpoint; granular token scoped to @xgent-ai org; 2FA OTP required per token policy |
| T-12-08-02 | Tampering | Tarball polluted with .planning/ or test/ shipped to users | mitigate | npm pack --dry-run inspection at human-verify checkpoint; package.json files: array enumerates allowed paths |
| T-12-08-03 | Tampering | Pre-publish state divergence (publish unbuilt code) | mitigate | prepublishOnly = npm run quality blocks publish on any test failure; quality gate green confirmed at checkpoint BEFORE version bump |
| T-12-08-04 | Spoofing | Wrong dist-tag (e.g. publish to next instead of latest) | mitigate | Explicit `--tag latest` flag in npm publish command; smoke-verify step asserts dist-tags shows latest=1.3.0 |
| T-12-08-05 | Tampering | Force-push v1.3.0 over a previously-deprecated version | mitigate | npm registry refuses immutable-version overwrites by design; v1.0.0 deprecation NOT touched; no --force flag in any command |
| T-12-08-06 | Information Disclosure | npm token leaked in console output or git history | mitigate | NPM_TOKEN consumed via env var (not committed); checkpoint instructs user to use `npm login` interactively rather than passing token on CLI; .npmrc not committed |
| T-12-08-07 | Repudiation | User publishes wrong version and cannot prove which commit was published | mitigate | git tag v1.3.0 contains release notes; npm registry stores publish metadata with timestamp; tarball SHA recorded by npm |
| T-12-08-08 | Denial of Service | npm publish hangs / rate-limits | accept | Human-driven step; user retries with fresh OTP if needed; no auto-retry logic |
</threat_model>

<verification>
- `npm view @xgent-ai/gomad@1.3.0 version` returns `1.3.0`
- `npm view @xgent-ai/gomad dist-tags` shows `{ latest: '1.3.0', ... }`
- `git ls-remote --tags origin v1.3.0` returns the tag SHA
- `git log --oneline -3` shows: docs(state) commit, release(12) commit, and pre-release work
- `.planning/STATE.md`, `MILESTONES.md`, `PROJECT.md` reflect v1.3.0 ship
- `npm run quality` exit 0 was confirmed at checkpoint BEFORE version bump (Pitfall 4)
- `npm pack --dry-run` file list was visually approved at checkpoint
</verification>

<success_criteria>
- package.json: version 1.3.0, scripts.prepublishOnly = npm run quality, scripts.quality includes the full release matrix, scripts.test:legacy-v12-upgrade and scripts.test:v13-agent-relocation present
- CHANGELOG.md: ## [1.3.0] entry with Summary/Added/Changed/Removed/BREAKING CHANGES; cross-link to upgrade-recovery.md#v12--v13-recovery
- npm: @xgent-ai/gomad@1.3.0 published with latest dist-tag; v1.1.0 + v1.2.0 retained; v1.0.0 deprecation unchanged
- git: v1.3.0 annotated tag pushed to origin/main
- planning: PROJECT.md + MILESTONES.md + STATE.md committed with v1.3.0 ship status
- All three checkpoints (quality-green, npm-publish-success, tag-push) approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/12-agent-dir-relocation-release/12-08-SUMMARY.md` documenting:
- Final quality run timestamp + exit code
- npm publish output (paste the `+ @xgent-ai/gomad@1.3.0` line)
- npm view @xgent-ai/gomad dist-tags output
- git tag v1.3.0 SHA
- Commit SHAs of the release-bump commit and docs(state) close commit
- Confirmation that PROJECT.md / MILESTONES.md / STATE.md reflect shipped status
- Note for next milestone: v1.4 planning starts with `/gsd-roadmap-phase` (no carry-over blockers)
</output>
