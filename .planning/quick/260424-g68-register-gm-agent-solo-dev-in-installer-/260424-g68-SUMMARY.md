---
phase: 260424-g68
plan: 01
subsystem: installer / agent-registration
tags:
  - installer
  - agent-registration
  - quick-task
  - solo-dev
requirements:
  - G68-REG-01
  - G68-REG-02
  - G68-REG-03
requires: []
provides:
  - solo-dev-agent-in-install-surface
  - gm:agent-solo-dev-launcher
affects:
  - tools/installer/ide/shared/agent-command-generator.js
  - tools/installer/core/installer.js
  - tools/installer/core/cleanup-planner.js
  - tools/fixtures/tarball-grep-allowlist.json
  - test/fixtures/orphan-refs/allowlist.json
  - test/test-gm-command-surface.js
tech-stack:
  added: []
  patterns:
    - "Registry-driven agent registration (single source of truth in AGENT_SOURCES, consumed by writeAgentLaunchers, extractPersonas, cleanup-planner's LEGACY_AGENT_SHORT_NAMES)"
key-files:
  created: []
  modified:
    - tools/installer/ide/shared/agent-command-generator.js
    - tools/installer/core/installer.js
    - tools/installer/core/cleanup-planner.js
    - tools/fixtures/tarball-grep-allowlist.json
    - test/fixtures/orphan-refs/allowlist.json
    - test/test-gm-command-surface.js
decisions:
  - "Preserve LEGACY_AGENTS at 7 entries across three test files — solo-dev never existed in v1.1, so it has no legacy upgrade semantics"
  - "Use multi-line object form for the solo-dev registry entry, matching the style of 5-of-6 sibling entries (the single-line `dev` entry is the outlier)"
metrics:
  duration: ~3 minutes
  completed-date: 2026-04-24
  commits: 2
  tasks: 3
  assertions-verified: 47
---

# Quick Task 260424-g68: Register gm-agent-solo-dev in Installer Summary

Registered `gm-agent-solo-dev` as the 8th canonical agent in the installer's source-of-truth registry, updated all "7-means-current-agents" references to "8", and explicitly preserved the three v1.1-LEGACY "7-agent" arrays that encode pre-solo-dev upgrade semantics. Full install-smoke roundtrip now asserts all 8 launchers with valid frontmatter.

## Objective Recap

Without registry registration, the new `gm-agent-solo-dev` skill dir added by quick task 260424-fvb was dead code — the installer never read it, no launcher or persona was written, and `/gm:agent-solo-dev` did not appear in Claude's command palette after install. This task wires solo-dev into `AgentCommandGenerator.AGENT_SOURCES` (the single source of truth) so a fresh install emits the launcher + persona.

## Implementation

### AGENT_SOURCES entry added

In `tools/installer/ide/shared/agent-command-generator.js` — appended as the 8th entry after `dev`:

```js
{
  shortName: 'solo-dev',
  dir: '4-implementation/gm-agent-solo-dev',
  purpose: 'Quick flow spec-to-implementation, lean artifacts, minimum ceremony',
},
```

The multi-line object form was chosen to match 5-of-6 sibling entries in the registry (the single-line `dev` entry at the bottom is the stylistic outlier).

### Magic-7 → magic-8 comment/reason-field updates

Each of the following files had exactly one "7" reference that described CURRENT agents, updated to "8":

| File | Location | Change |
|------|----------|--------|
| `tools/installer/ide/shared/agent-command-generator.js` | Line 11 comment | "The 7 canonical agent personas" → "The 8 canonical agent personas" |
| `tools/installer/core/installer.js` | Line 294 comment | "extract 7 agent personas" → "extract 8 agent personas" |
| `tools/installer/core/cleanup-planner.js` | Line 37 JSDoc order list | Appended `, 'solo-dev'` to the JSDoc-shown canonical order |
| `tools/fixtures/tarball-grep-allowlist.json` | Line 68 reason | "7 agent paths" → "8 agent paths" |
| `test/fixtures/orphan-refs/allowlist.json` | Line 89 reason | "7 agent paths" → "8 agent paths" |
| `test/test-gm-command-surface.js` | Line 16, 212, 216 comments | "all 7"/"1-of-7" → "all 8"/"1-of-8" |
| `test/test-gm-command-surface.js` | Line 217 `EXPECTED_AGENTS` array | 7 entries → 8 entries (`'solo-dev'` appended) |

### v1.1-LEGACY arrays intentionally NOT modified

These three arrays encode pre-solo-dev upgrade semantics and MUST remain at 7 entries:

| File | Line | Reason |
|------|------|--------|
| `test/test-gm-command-surface.js` | 244 (`LEGACY_AGENTS`) | Asserts fresh install leaves no v1.1-era `.claude/skills/gm-agent-<name>/` dir. solo-dev never shipped in v1.1 — adding it would assert something tautological. |
| `test/test-legacy-v11-upgrade.js` | 53 (`LEGACY_AGENTS`) | v1.1 upgrade-path test — describes the agents that existed in v1.1. |
| `test/test-dry-run.js` | 55 (`LEGACY_AGENTS`) | Dry-run assertion over v1.1 skill dirs — same semantic constraint. |

Confirmed via `grep -c "'solo-dev'" test/test-legacy-v11-upgrade.js test/test-dry-run.js` — both return `0`. And `LEGACY_AGENTS` in `test-gm-command-surface.js` remains at 7 elements.

### `cleanup-planner.js` propagation

No code change needed — the file already derives `LEGACY_AGENT_SHORT_NAMES` dynamically:

```js
const LEGACY_AGENT_SHORT_NAMES = AgentCommandGenerator.AGENT_SOURCES.map((a) => a.shortName);
```

Cleanup will now also consider `.claude/skills/gm-agent-solo-dev/` as a removal candidate; this is harmless because that directory never exists in real workspaces (solo-dev never shipped as a v1.1 skill-dir), and the `if (exists) remove` pattern is idempotent.

## Verification

### Sanity check

```bash
node -e "const {AgentCommandGenerator} = require('./tools/installer/ide/shared/agent-command-generator'); const s=AgentCommandGenerator.AGENT_SOURCES; if(s.length!==8||s[s.length-1].shortName!=='solo-dev'){console.error('FAIL'); process.exit(1)}; console.log('OK length=8 last=solo-dev');"
```

Output: `OK length=8 last=solo-dev` ✓

### Full install-smoke (authoritative roundtrip)

```bash
node test/test-gm-command-surface.js
```

Result: **47 passed, 0 failed** — exit 0. Includes:

- Tarball pack + install + `gomad install` roundtrip succeeds
- `.claude/commands/gm/agent-solo-dev.md` present
- `agent-solo-dev.md` has valid YAML frontmatter
- `agent-solo-dev.md` frontmatter `name: gm:agent-solo-dev` matches expected
- `agent-solo-dev.md` has non-empty description
- All 7 legacy v1.1 skill dirs confirmed absent after fresh install (LEGACY_AGENTS negative assertion, unchanged at 7 entries)

### Combined verification

```bash
node test/test-gm-command-surface.js \
  && test "$(grep -c "'solo-dev'" test/test-legacy-v11-upgrade.js)" = "0" \
  && test "$(grep -c "'solo-dev'" test/test-dry-run.js)" = "0" \
  && echo "ALL PASS"
```

Output: `ALL PASS` ✓

### Pre-commit test suite (ran automatically on each commit)

Both `feat(...)` and `test(...)` commits triggered the full pre-commit pipeline:

- `test:refs` — 7/7 passed
- `test:orphan-refs` — 2/2 passed, 186 allowlisted hits, 0 unallowlisted
- `test:install` (Installation Component Tests) — **205 passed, 0 failed**
- `lint` (ESLint, max-warnings=0) — clean
- `lint:md` (markdownlint) — 0 errors across 395 files
- `format:check` (Prettier) — all files pass

## Commits

| # | Hash | Type | Description |
|---|------|------|-------------|
| 1 | `d239ccc` | feat | register solo-dev as 8th canonical agent in installer registry (5 files) |
| 2 | `8c754b1` | test | append solo-dev to EXPECTED_AGENTS in install-smoke (1 file) |

Task 3 was verification-only — no code changes, no commit (per plan).

## Deviations from Plan

None — plan executed exactly as written. All edits landed at the expected locations, all verifications passed on first attempt, no auto-fixes or architectural deviations were triggered. Manifest fields (`displayName`, `title`, `icon`, `capabilities`) for solo-dev were verified during planning and confirmed present/non-empty at execution; the `generateLauncherContent` fail-fast guard passed cleanly.

## Manifest-Field Surprises

None. The `src/gomad-skills/4-implementation/gm-agent-solo-dev/skill-manifest.yaml` file contained all six required fields (shortName from registry; displayName, title, icon, capabilities from manifest; purpose from registry) as non-empty strings — exactly as verified during planning.

## Success Criteria — All Met

1. ✓ `AgentCommandGenerator.AGENT_SOURCES.length === 8` and last entry `.shortName === 'solo-dev'`
2. ✓ Fresh install emits `.claude/commands/gm/agent-solo-dev.md` with `name: gm:agent-solo-dev` and non-empty description (asserted in install-smoke Phase C)
3. ✓ `node test/test-gm-command-surface.js` exits 0 (47/47 assertions)
4. ✓ `test/test-legacy-v11-upgrade.js` and `test/test-dry-run.js` contain zero `solo-dev` occurrences (LEGACY_AGENTS arrays unchanged)
5. ✓ All six magic-7-means-current-agents references updated to "8"; all magic-7-means-v1.1-legacy references unchanged

## Self-Check: PASSED

Files modified (confirmed via `git show --stat d239ccc 8c754b1`):

- `tools/installer/ide/shared/agent-command-generator.js` FOUND
- `tools/installer/core/installer.js` FOUND
- `tools/installer/core/cleanup-planner.js` FOUND
- `tools/fixtures/tarball-grep-allowlist.json` FOUND
- `test/fixtures/orphan-refs/allowlist.json` FOUND
- `test/test-gm-command-surface.js` FOUND

Commits exist in git log:

- `d239ccc` FOUND
- `8c754b1` FOUND
