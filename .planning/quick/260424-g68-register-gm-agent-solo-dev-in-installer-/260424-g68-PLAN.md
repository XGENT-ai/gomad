---
phase: 260424-g68
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/installer/ide/shared/agent-command-generator.js
  - tools/installer/core/installer.js
  - tools/installer/core/cleanup-planner.js
  - tools/fixtures/tarball-grep-allowlist.json
  - test/fixtures/orphan-refs/allowlist.json
  - test/test-gm-command-surface.js
autonomous: true
requirements:
  - G68-REG-01
  - G68-REG-02
  - G68-REG-03
tags:
  - installer
  - agent-registration
  - quick-task

must_haves:
  truths:
    - "AgentCommandGenerator.AGENT_SOURCES has 8 entries with solo-dev appended last"
    - "A fresh install emits .claude/commands/gm/agent-solo-dev.md with valid frontmatter (name: gm:agent-solo-dev, non-empty description)"
    - "A fresh install emits _gomad/gomad/agents/solo-dev.md with skill-manifest.yaml as frontmatter and SKILL.md body"
    - "test-gm-command-surface.js hard-asserts all 8 agents and still passes"
    - "LEGACY_AGENTS arrays in test-legacy-v11-upgrade.js and test-dry-run.js remain at 7 entries (v1.1 upgrade semantics preserved)"
  artifacts:
    - path: "tools/installer/ide/shared/agent-command-generator.js"
      provides: "AGENT_SOURCES registry"
      contains: "shortName: 'solo-dev'"
    - path: "test/test-gm-command-surface.js"
      provides: "Install-smoke assertion over 8 agents"
      contains: "'solo-dev'"
  key_links:
    - from: "tools/installer/ide/shared/agent-command-generator.js"
      to: "src/gomad-skills/4-implementation/gm-agent-solo-dev/{skill-manifest.yaml,SKILL.md}"
      via: "extractPersonas() iterates AGENT_SOURCES and reads both files"
      pattern: "4-implementation/gm-agent-solo-dev"
    - from: "test/test-gm-command-surface.js"
      to: ".claude/commands/gm/agent-solo-dev.md"
      via: "Phase C install-smoke EXPECTED_AGENTS loop"
      pattern: "EXPECTED_AGENTS.*solo-dev"
---

<objective>
Register `gm-agent-solo-dev` as the 8th canonical agent in the installer's source-of-truth registry and update the fixed "7 agent" assertions/comments that refer to CURRENT agents. Leave v1.1-legacy "7 agent" arrays untouched (they describe pre-solo-dev history).

Purpose: Without registry registration, the new skill dir added by task 260424-fvb is dead code — the installer never reads it, no launcher or persona is written, and `/gm:agent-solo-dev` does not appear in Claude's command palette after install.

Output: After install, `.claude/commands/gm/agent-solo-dev.md` (launcher) and `_gomad/gomad/agents/solo-dev.md` (persona) exist in the target workspace, and `node test/test-gm-command-surface.js` passes end-to-end.
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/gomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260424-fvb-bmad-agent-solo-dev-gm-agent-solo-dev-bm/260424-fvb-SUMMARY.md
@src/gomad-skills/4-implementation/gm-agent-solo-dev/skill-manifest.yaml

<interfaces>
<!-- Verified from the codebase during planning. Executor: use these directly; do not re-scout. -->

AGENT_SOURCES entry shape (from agent-command-generator.js:14-46):
```js
// Each entry:
{
  shortName: string,  // short identifier used for filenames (agent-<short>.md, <short>.md)
  dir: string,        // path under src/gomad-skills/ to the gm-agent-* directory
  purpose: string,    // one-line purpose for D-17 description formatting
}
```

Required manifest fields consumed by generateLauncherContent (agent-command-generator.js:114-119 fail-fast guard):
  shortName, displayName, title, icon, capabilities, purpose — ALL must be non-empty strings.

Verified manifest at src/gomad-skills/4-implementation/gm-agent-solo-dev/skill-manifest.yaml:
```yaml
type: agent
name: gm-agent-solo-dev
displayName: Barry
title: Quick Flow Solo Dev
icon: "🚀"
capabilities: "rapid spec creation, lean implementation, minimum ceremony"
role: Elite Full-Stack Developer + Quick Flow Specialist
identity: "Barry handles Quick Flow - from tech spec creation through implementation. ..."
communicationStyle: "Direct, confident, and implementation-focused. ..."
principles: "Planning and execution are two sides of the same coin. ..."
module: gomad
```
All six required fields (shortName from registry, displayName/title/icon/capabilities from manifest, purpose from registry) are present and non-empty — the fail-fast guard will pass.

cleanup-planner.js:41 inherits the list automatically:
```js
const LEGACY_AGENT_SHORT_NAMES = AgentCommandGenerator.AGENT_SOURCES.map((a) => a.shortName);
```
No code change in cleanup-planner.js — only a comment-level order-list update at line 37.

test-gm-command-surface.js current assertions (lines 217, 244):
```js
const EXPECTED_AGENTS = ['analyst', 'tech-writer', 'pm', 'ux-designer', 'architect', 'sm', 'dev'];  // UPDATE: append 'solo-dev'
const LEGACY_AGENTS   = ['analyst', 'tech-writer', 'pm', 'ux-designer', 'architect', 'sm', 'dev'];  // DO NOT TOUCH — v1.1 legacy semantics
```
</interfaces>

<semantic_distinction>
<!-- CRITICAL: not all "7" references mean the same thing -->

**UPDATE to 8** (refers to CURRENT canonical agents):
  - agent-command-generator.js:11 comment ("7 canonical agent personas")
  - installer.js:294 comment ("extract 7 agent personas")
  - cleanup-planner.js:37 comment (hardcoded order list shown in JSDoc)
  - tarball-grep-allowlist.json:68 reason ("7 agent paths")
  - orphan-refs/allowlist.json:89 reason ("7 agent paths")
  - test-gm-command-surface.js:16 comment ("all 7 .claude/commands/...")
  - test-gm-command-surface.js:212 comment ("all 7 agent launchers")
  - test-gm-command-surface.js:216 comment ("all 7 explicitly" + "silently-missing 1-of-7")
  - test-gm-command-surface.js:217 EXPECTED_AGENTS array (append 'solo-dev')
  - test-gm-command-surface.js:244 LEGACY_AGENTS array — **DO NOT CHANGE** (see below)

**LEAVE AT 7** (refers to v1.1-LEGACY agents — solo-dev never existed in v1.1):
  - test-gm-command-surface.js:244 LEGACY_AGENTS array (validates fresh install leaves no v1.1 legacy .claude/skills/gm-agent-<name>/ dirs)
  - test-legacy-v11-upgrade.js:53 LEGACY_AGENTS array
  - test-dry-run.js:55 LEGACY_AGENTS array
  - cleanup-planner.js:180, :238 "7 legacy" comments
</semantic_distinction>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register solo-dev in AGENT_SOURCES and update magic-7 comments/allowlist reasons</name>
  <files>
    tools/installer/ide/shared/agent-command-generator.js,
    tools/installer/core/installer.js,
    tools/installer/core/cleanup-planner.js,
    tools/fixtures/tarball-grep-allowlist.json,
    test/fixtures/orphan-refs/allowlist.json
  </files>
  <action>
Make these edits exactly as listed. Each bullet is a single Edit call.

1. **tools/installer/ide/shared/agent-command-generator.js**
   - Line 11 comment: change `The 7 canonical agent personas` → `The 8 canonical agent personas`.
   - Append a new entry to `AGENT_SOURCES` immediately after the `dev` entry (current line 45), preserving the inline-object style used by `dev` (NOT the multi-line style used by the first six entries — keep the mixed style the file already has, single-line is fine for a leaf entry). Insert before the closing `];`:
     ```js
     {
       shortName: 'solo-dev',
       dir: '4-implementation/gm-agent-solo-dev',
       purpose: 'Quick flow spec-to-implementation, lean artifacts, minimum ceremony',
     },
     ```
     Note: use the multi-line form shown above for consistency with five of the six sibling entries. The trailing comma is required (the `dev` entry on line 45 already has one implicit by being last, so add a comma to the `dev` entry too if not present, then add the new entry after it).

2. **tools/installer/core/installer.js**
   - Line 294 comment: change `extract 7 agent personas` → `extract 8 agent personas`. No code changes in this file — the loop reads AGENT_SOURCES dynamically.

3. **tools/installer/core/cleanup-planner.js**
   - Line 37 JSDoc comment: change
     `Order: ['analyst', 'tech-writer', 'pm', 'ux-designer', 'architect', 'sm', 'dev']`
     →
     `Order: ['analyst', 'tech-writer', 'pm', 'ux-designer', 'architect', 'sm', 'dev', 'solo-dev']`
   - DO NOT change comments at lines 180 or 238 — those correctly describe v1.1-LEGACY semantics (the 7 pre-solo-dev legacy dirs). cleanup-planner will now also consider `.claude/skills/gm-agent-solo-dev/` as a cleanup candidate, which is harmless: that dir never exists in real workspaces because solo-dev never shipped as a v1.1-era skill dir. The `if (exists) remove` pattern is idempotent.

4. **tools/fixtures/tarball-grep-allowlist.json**
   - Line 68 reason field: change `7 agent paths` → `8 agent paths`.
   - DO NOT add any new `path:` entry here — the prior quick task (260424-fvb) already added solo-dev SKILL.md and skill-manifest.yaml entries.

5. **test/fixtures/orphan-refs/allowlist.json**
   - Line 89 reason field: change `7 agent paths` → `8 agent paths`.
   - DO NOT add any new `path:` entry — prior quick task already covered solo-dev paths.

After edits, run a sanity check (MUST print `8`):
```
node -e "const {AgentCommandGenerator} = require('./tools/installer/ide/shared/agent-command-generator'); console.log(AgentCommandGenerator.AGENT_SOURCES.length); console.log(AgentCommandGenerator.AGENT_SOURCES.map(a => a.shortName).join(','))"
```
Expected stdout: two lines — `8` and `analyst,tech-writer,pm,ux-designer,architect,sm,dev,solo-dev`.
  </action>
  <verify>
    <automated>node -e "const {AgentCommandGenerator} = require('./tools/installer/ide/shared/agent-command-generator'); const s = AgentCommandGenerator.AGENT_SOURCES; if (s.length !== 8) { console.error('FAIL length=' + s.length); process.exit(1); } const last = s[s.length - 1]; if (last.shortName !== 'solo-dev' || last.dir !== '4-implementation/gm-agent-solo-dev' || !last.purpose) { console.error('FAIL last entry ' + JSON.stringify(last)); process.exit(1); } console.log('OK'); "</automated>
  </verify>
  <done>
    - AGENT_SOURCES has exactly 8 entries, with solo-dev as the 8th.
    - The sanity-check command prints `OK` and exits 0.
    - All six magic-7 comment/reason-field updates are applied; all v1.1-legacy references are UNCHANGED.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update test-gm-command-surface.js assertions and comments from 7 → 8 (current) while preserving v1.1-legacy "7"</name>
  <files>test/test-gm-command-surface.js</files>
  <action>
Make these edits to `test/test-gm-command-surface.js`. Each is a single targeted Edit.

1. Line 16 comment (Phase C install-smoke docstring):
   `- Hard assertion: all 7 .claude/commands/gm/agent-<name>.md launchers present`
   →
   `- Hard assertion: all 8 .claude/commands/gm/agent-<name>.md launchers present`

2. Line 212 comment:
   `// Installer MUST now emit .claude/commands/gm/ with all 7 agent launchers.`
   →
   `// Installer MUST now emit .claude/commands/gm/ with all 8 agent launchers.`

3. Line 216 comment:
   `// D-69: enumerate all 7 explicitly (not globSync) so a silently-missing 1-of-7 fails.`
   →
   `// D-69: enumerate all 8 explicitly (not globSync) so a silently-missing 1-of-8 fails.`

4. Line 217 array (the **hard assertion** — this is the load-bearing change):
   `const EXPECTED_AGENTS = ['analyst', 'tech-writer', 'pm', 'ux-designer', 'architect', 'sm', 'dev'];`
   →
   `const EXPECTED_AGENTS = ['analyst', 'tech-writer', 'pm', 'ux-designer', 'architect', 'sm', 'dev', 'solo-dev'];`

5. **LEAVE UNCHANGED**:
   - Line 244 `LEGACY_AGENTS` array — this asserts that a fresh install leaves no v1.1-LEGACY `.claude/skills/gm-agent-<name>/` directory behind. solo-dev never existed in v1.1, so it does not belong in this list. Adding it would assert something tautological and could mask real regressions.
   - Any other "7" references not enumerated above. (grep to confirm none remain that refer to current agents.)

Self-check after edits:
```
grep -n "agent" test/test-gm-command-surface.js | grep -E "(all 7|7 agent|1-of-7|EXPECTED_AGENTS|LEGACY_AGENTS)"
```
Expected matches: the three bullets above should now say "8"; `EXPECTED_AGENTS` should include `'solo-dev'`; `LEGACY_AGENTS` should still be the 7-element list.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const src = fs.readFileSync('test/test-gm-command-surface.js', 'utf8'); const expMatch = src.match(/const EXPECTED_AGENTS = (\[[^\]]+\])/); const legMatch = src.match(/const LEGACY_AGENTS = (\[[^\]]+\])/); if (!expMatch || !legMatch) { console.error('FAIL: could not locate arrays'); process.exit(1); } const exp = eval(expMatch[1]); const leg = eval(legMatch[1]); if (exp.length !== 8 || !exp.includes('solo-dev')) { console.error('FAIL EXPECTED_AGENTS=' + JSON.stringify(exp)); process.exit(1); } if (leg.length !== 7 || leg.includes('solo-dev')) { console.error('FAIL LEGACY_AGENTS=' + JSON.stringify(leg)); process.exit(1); } if (/1-of-7|all 7 \\.claude|all 7 agent/.test(src)) { console.error('FAIL: stale 7-references remain'); process.exit(1); } console.log('OK');"</automated>
  </verify>
  <done>
    - EXPECTED_AGENTS contains all 8 current agents including solo-dev.
    - LEGACY_AGENTS still contains exactly 7 v1.1-era names (NO solo-dev).
    - No current-agent "7" references remain in comments.
    - The self-check node script prints `OK` and exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: End-to-end install-smoke — verify launcher, persona, and test suite all pass</name>
  <files>(no writes — verification only)</files>
  <action>
Run the full install-smoke test. This is the authoritative check: it packs the tarball, installs into a temp dir, runs `gomad install`, and asserts all 8 launchers exist with valid frontmatter plus the negative v1.1-legacy assertion.

```
node test/test-gm-command-surface.js
```

Expected: exits 0 with all assertions passing. Runtime ~2-3 min (npm pack + npm install + gomad install + filesystem asserts).

If it fails:
- Read the failure line. If it says `agent-solo-dev.md` is missing → AGENT_SOURCES edit didn't land. Re-check Task 1.
- If it says `frontmatter name mismatch` for solo-dev → the launcher template substitution failed; most likely cause is a missing/empty manifest field. Re-read `src/gomad-skills/4-implementation/gm-agent-solo-dev/skill-manifest.yaml` and confirm `displayName`, `title`, `icon`, `capabilities` are all non-empty strings (they should be — verified during planning).
- If LEGACY_AGENTS assertion fails → the test was mis-edited in Task 2. `LEGACY_AGENTS` must remain the 7-element v1.1 list.

Also spot-check the two related-but-unrelated legacy-upgrade tests still have LEGACY_AGENTS=7 (these should NOT have been edited):
```
grep -c "'solo-dev'" test/test-legacy-v11-upgrade.js test/test-dry-run.js
```
Expected: `test/test-legacy-v11-upgrade.js:0` and `test/test-dry-run.js:0` (zero occurrences — solo-dev must NOT appear in these).
  </action>
  <verify>
    <automated>node test/test-gm-command-surface.js && test "$(grep -c "'solo-dev'" test/test-legacy-v11-upgrade.js)" = "0" && test "$(grep -c "'solo-dev'" test/test-dry-run.js)" = "0" && echo "ALL PASS"</automated>
  </verify>
  <done>
    - `node test/test-gm-command-surface.js` exits 0.
    - Both legacy-upgrade test files contain zero occurrences of `'solo-dev'`.
    - The combined verification command prints `ALL PASS`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| src/gomad-skills/ → installer | Installer reads skill-manifest.yaml and SKILL.md as trusted source content |
| installer → target workspace | Installer writes to user's .claude/ and _gomad/ directories |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-g68-01 | Tampering | skill-manifest.yaml fields (displayName/title/icon/capabilities) | mitigate | Existing fail-fast guard in generateLauncherContent (agent-command-generator.js:114-119) throws if any required string is missing or non-string. No new surface introduced. |
| T-g68-02 | Information Disclosure | Launcher/persona file contents | accept | All content originates from repo-controlled files already shipped in the package. No user-supplied data enters the launcher. |
| T-g68-03 | Denial of Service | Installer crash on bad manifest | mitigate | Fail-fast is already the intended behavior; a malformed solo-dev manifest would block install loudly rather than silently producing a broken launcher — this is correct. |
</threat_model>

<verification>
- AGENT_SOURCES length sanity check (Task 1 verify).
- Test assertion array integrity check (Task 2 verify).
- Full install-smoke roundtrip (Task 3 verify) — packs, installs, asserts all 8 launchers present with valid frontmatter AND no v1.1-legacy dirs leaked.
- Legacy-test non-contamination check (Task 3 verify) — confirms solo-dev was NOT added to LEGACY_AGENTS arrays.
</verification>

<success_criteria>
1. `AgentCommandGenerator.AGENT_SOURCES.length === 8` and last entry `.shortName === 'solo-dev'`.
2. After `gomad install`, the target workspace contains:
   - `.claude/commands/gm/agent-solo-dev.md` with `name: gm:agent-solo-dev` and non-empty description.
   - `_gomad/gomad/agents/solo-dev.md` with skill-manifest.yaml frontmatter and SKILL.md body.
3. `node test/test-gm-command-surface.js` exits 0.
4. `test/test-legacy-v11-upgrade.js` and `test/test-dry-run.js` contain zero occurrences of `solo-dev` (LEGACY_AGENTS arrays unchanged).
5. All six magic-7-that-means-current-agents references in comments/reason fields now say `8`; all magic-7-that-means-v1.1-legacy references unchanged.
</success_criteria>

<output>
After completion, create `.planning/quick/260424-g68-register-gm-agent-solo-dev-in-installer-/260424-g68-SUMMARY.md` describing:
- The exact AGENT_SOURCES entry added (the 3-field object).
- Which comment/reason-field updates were applied (1 file each: generator, installer, cleanup-planner, tarball-allowlist, orphan-refs-allowlist, test-gm-command-surface).
- Explicit confirmation that LEGACY_AGENTS arrays in test-gm-command-surface.js:244, test-legacy-v11-upgrade.js:53, and test-dry-run.js:55 were NOT modified.
- Test result from `node test/test-gm-command-surface.js` (expected: 0 exit code).
- Any manifest-field surprises encountered (none expected — manifest verified during planning).
</output>
