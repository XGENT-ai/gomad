---
quick_task: 260510-ql6
title: Raise the quality bar inside gm-create-story so dev agents cannot self-lower it
type: execute
target_skill: src/gomad-skills/4-implementation/gm-create-story/
out_of_scope:
  - src/gomad-skills/4-implementation/gm-dev-story/
  - src/gomad-skills/4-implementation/gm-code-review/
  - src/gomad-skills/4-implementation/gm-checkpoint-preview/
  - src/gomad-skills/4-implementation/gm-qa-generate-e2e-tests/
files_modified:
  - src/gomad-skills/4-implementation/gm-create-story/template.md
  - src/gomad-skills/4-implementation/gm-create-story/workflow.md
  - src/gomad-skills/4-implementation/gm-create-story/checklist.md
autonomous: true
---

<objective>
Close the "mocks pass = done" loophole at the contract layer (the story file itself).
The story is the only artifact gm-dev-story consults, so the cheap interpretation must
be made syntactically impossible inside the story format. We add three small, tightly
coupled artifacts: a Real-World Verification block, an Anti-Acceptance (off-limits) list,
and a checklist gate that refuses to ship a story missing either.
</objective>

<diagnosis>

## Diagnosis: Where the bar leaks today

All citations are line numbers in the current files (read 2026-05-10).

### Leak 1 — Template has zero real-world verification slot
**File:** `src/gomad-skills/4-implementation/gm-create-story/template.md`
**Lines:** 1–50 (entire file)

The template's only proof-of-done sections are `## Acceptance Criteria` (line 13–15, free-form bullets) and `## Tasks / Subtasks` (line 17–22, checkboxes). There is **no field that demands evidence the feature works against the real system** — no curl/CLI/UI/screenshot/log slot, no "human-demoable smoke test" slot, no anti-acceptance list. A dev agent reading this template can satisfy AC#1 with a unit test asserting a mocked response and check the box. The format itself permits it.

Confirmed via grep: `mock`, `real`, `smoke`, `manual`, `curl`, `browser`, `usable` — **zero occurrences** anywhere in template.md or workflow.md.

### Leak 2 — Workflow's "DEV AGENT GUARDRAILS" extracts architecture, never proof-of-done
**File:** `src/gomad-skills/4-implementation/gm-create-story/workflow.md`
**Lines:** 303–364 (Step 5: Create comprehensive story file)

Step 5 emits seven `<template-output>` blocks (`technical_requirements`, `architecture_compliance`, `library_framework_requirements`, `file_structure_requirements`, `testing_requirements`, etc.). None of these slots is "real-world verification evidence". The closest, `testing_requirements`, leans into testing *standards* (frameworks/coverage), not *what real-system behavior the dev agent must demonstrate before claiming done*. There is no instruction telling the SM to elicit and write down a concrete demo command/URL/flow.

### Leak 3 — Checklist optimises for "completeness", never disallows mock-only acceptance
**File:** `src/gomad-skills/4-implementation/gm-create-story/checklist.md`
**Lines:** 9–18 ("Critical Mistakes to Prevent") and 161–167 ("Implementation DISASTERS")

The list at line 16 — *"Lying about completion - Implementing incorrectly or incompletely"* — is the closest signal, but it's vague. "Vague implementations" (line 162) and "Completion lies" (line 165) are gestured at but never operationalised into a check that says *"reject the story unless it specifies a real-system verification step that cannot be satisfied by mocks"*. The SM agent self-validates against this checklist (workflow.md line 367) but the checklist gives it no failure criterion for our exact leak.

### Leak 4 — Dev-story DoD treats "tests pass" as the universal evidence
**File:** `src/gomad-skills/4-implementation/gm-dev-story/checklist.md`
**Lines:** 41–48 ("Testing & Quality Assurance" block)

Every quality gate is testing-shaped: unit tests, integration tests, E2E tests, regression tests, lint. **There is no checkbox of the form** *"Real-system verification evidence captured (curl output / screenshot / log / human-run smoke command)"*. So even if gm-create-story emitted such a section into the story, gm-dev-story has no slot in its own DoD that ties back to it. The contract is one-sided.

This is the **out-of-scope follow-up** flagged below — we cannot fix it in this quick task per scope rules — but `gm-create-story` can still anchor the contract on its end so the story carries the slot, and an explicit "the dev agent must fill this section before status=review" instruction is embedded *in the story body itself* (which gm-dev-story will read as authoritative per dev-story workflow.md line 172).

### Leak 5 — Workflow never tells the dev agent it is forbidden from softening the bar
**File:** `src/gomad-skills/4-implementation/gm-create-story/template.md` (entire file)

The story is the only thing gm-dev-story will read. Nowhere in the template does the story say to the dev agent: *"if you cannot meet this bar, STOP and report — do NOT redefine done."* The dev-story workflow has HALT conditions for missing config (line 297) and 3 consecutive failures (line 296), but **no HALT for "I cannot demonstrate real-world behavior so I will declare mocks sufficient"**. That instruction must come from the story itself, because the story is the contract.

## Worst leak (single biggest behavioral driver)

**Leak 1.** The template has no real-world verification slot at all. Every other leak feeds off this one — the workflow has nothing to populate, the checklist has nothing to gate on, the dev agent has nothing to fill. Fix the template structure and the rest follows.

</diagnosis>

<tasks>

<task type="auto">
  <name>Task 1: Add Real-World Verification + Anti-Acceptance blocks to story template</name>
  <files>src/gomad-skills/4-implementation/gm-create-story/template.md</files>
  <action>
Insert two new H2 sections into `template.md`, immediately after `## Acceptance Criteria` (after line 15) and before `## Tasks / Subtasks`. Surgical insert — do not rewrite or "improve" any existing section.

Section A — Real-World Verification (REQUIRED, non-skippable):

```markdown
## Real-World Verification

**This section is the proof-of-done contract. Mocks-only test passage is NOT acceptance.**
The dev agent MUST execute every command/flow below against the real system (real DB, real upstream service, real browser, real network) and paste the actual observed output/screenshot path/log excerpt into "Dev Agent Record → Real-World Verification Evidence" before setting Status to `review`.

For each Acceptance Criterion above, fill at least one row:

| AC# | What a human/operator runs | Against what (real system) | Expected observable result |
|-----|----------------------------|-----------------------------|----------------------------|
| 1   | e.g. `curl -X POST localhost:3000/api/orders -d '{...}'` | local dev server + real Postgres | HTTP 201 + new row visible in `orders` table via `psql` |
| 2   | e.g. open https://localhost:3000/orders in browser, click "New" | local dev server + real browser session | new order appears in list within 2s |

Rules:
- Each row MUST name a concrete command, URL, or UI flow — no "verify it works".
- Mocks, stubs, fakes, in-memory replacements, and `console.log` of expected values are NOT acceptable evidence here.
- If a row cannot be satisfied without a real external dependency the dev agent cannot reach (e.g. third-party prod API), the dev agent MUST HALT and report — NOT substitute a mock and declare done.
```

Section B — Anti-Acceptance (off-limits proofs):

```markdown
## Anti-Acceptance (what does NOT count as done)

The following are explicitly NOT acceptance, regardless of test suite color:

- ❌ Unit/integration tests that pass only because dependencies are mocked, stubbed, or replaced with in-memory fakes
- ❌ `console.log` / `print` of an expected value instead of producing it
- ❌ TODO / FIXME / "wire later" comments in code paths reachable by the AC
- ❌ Hardcoded fixture responses returned from a route in lieu of real logic
- ❌ Disabled, skipped, or `.only`'d tests in changed files
- ❌ "Compiles / type-checks" without running against the real system per Real-World Verification above
- ❌ A green CI run when Real-World Verification rows are unfilled

If the dev agent cannot meet the Real-World Verification bar, the correct action is **HALT and report**, not redefine "done". Lowering the bar to fit the implementation is a contract violation.
```

Also append one new bullet to `## Dev Agent Record` (after the existing `### File List` heading, line 49):

```markdown
### Real-World Verification Evidence

<!-- Dev agent MUST paste real observed output for each Real-World Verification row before Status=review.
     Format: "AC#1: <command run> → <actual output excerpt>". Empty section blocks Status=review. -->
```

Do not modify any other line. Match existing markdown style (single blank line between H2 sections, no trailing whitespace).
  </action>
  <verify>
1. After edit, run `grep -c "^## Real-World Verification$" template.md` → must equal `1`.
2. Run `grep -c "^## Anti-Acceptance" template.md` → must equal `1`.
3. Run `grep -c "### Real-World Verification Evidence" template.md` → must equal `1`.
4. Run `grep -nE "mock|stub|fake|console\.log|TODO" template.md` → must show ≥4 hits inside the new Anti-Acceptance block (these words must literally appear so the dev agent reads them as forbidden).
5. **Behavioral verification (the real test):** simulate one fresh story generation from the updated template (manually copy template.md to a scratch file, fill `{{role}}/{{action}}/{{benefit}}` and 2 ACs). Confirm the resulting story file contains both new sections with non-removable language; confirm a dev agent reading only that story file would see explicit forbidden patterns and a real-system command table BEFORE reaching Tasks/Subtasks.
6. Diff size: `git diff --stat template.md` should show ~30–40 lines added, 0 removed.
  </verify>
  <done>
- template.md contains the two new H2 sections in the correct position (after AC, before Tasks).
- template.md contains the new `### Real-World Verification Evidence` slot under Dev Agent Record.
- No existing line in template.md was modified or reflowed.
- A story produced from this template forces the dev agent to encounter (a) a verification table that names real-system commands and (b) an explicit list of forbidden proofs, before they ever read the task list.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire workflow to populate the new template sections (no new ceremony)</name>
  <files>src/gomad-skills/4-implementation/gm-create-story/workflow.md</files>
  <action>
Two surgical edits to `workflow.md`. Do not touch any other step or instruction.

**Edit 2a — Step 5 template-outputs (around lines 312–325):**
After the existing `<template-output file="{default_output_file}">testing_requirements</template-output>` line (line 324), insert two new template-output directives:

```xml
  <!-- Real-world verification contract — see template.md "Real-World Verification" + "Anti-Acceptance" -->
  <template-output file="{default_output_file}">real_world_verification</template-output>
  <template-output file="{default_output_file}">anti_acceptance</template-output>
```

**Edit 2b — Step 5 critical instruction block (insert immediately after line 304, before the first `<action>` of step 5):**

```xml
  <critical>🚧 PROOF-OF-DONE CONTRACT (anti-mock-only): For every Acceptance Criterion you write, you MUST also populate the "Real-World Verification" table with at least one row naming a CONCRETE real-system command, URL, or UI flow (not "verify it works", not "run tests"). If you cannot name a real-system verification for an AC, that AC is malformed — split it, sharpen it, or flag it for the user. Do NOT emit the story until every AC has at least one Real-World Verification row.</critical>
  <critical>🚫 The "Anti-Acceptance" list in template.md is canonical. Do NOT soften, shorten, paraphrase, or remove its bullets when emitting the story — copy them VERBATIM. The dev agent will read this list as the off-limits register.</critical>
```

Do NOT add new steps. Do NOT add new <step> blocks. Do NOT modify the discover-inputs flow, KB pre-bake, or web research steps. The two new template-outputs piggyback on the existing template-rendering machinery.
  </action>
  <verify>
1. `grep -c "real_world_verification" workflow.md` → `1`. `grep -c "anti_acceptance" workflow.md` → `1`.
2. `grep -c "PROOF-OF-DONE CONTRACT" workflow.md` → `1`. `grep -c "Anti-Acceptance.*canonical" workflow.md` → `1`.
3. The two new `<critical>` blocks appear inside `<step n="5"` (verify by reading 10 lines before/after each match).
4. `git diff --stat workflow.md` shows ~6 lines added, 0 removed (Edit 2a = 3 lines incl. comment, Edit 2b = 3 lines incl. blank).
5. **Behavioral verification:** dry-run the workflow against a minimal fake epic (story 1-1 with 2 ACs). Confirm the SM agent now refuses to emit a story whose ACs lack matching real-world verification rows — i.e. the new `<critical>` block is read as a hard precondition, not advice.
  </verify>
  <done>
- workflow.md emits the two new template-output blocks in step 5 in the right position.
- The two new `<critical>` instructions are inside step 5, not at file scope.
- No existing step, output, or critical instruction was reworded.
- The story-creation pipeline now refuses to ship a story missing real-world verification or with a softened anti-acceptance list.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add checklist gate that fails any story missing the new sections</name>
  <files>src/gomad-skills/4-implementation/gm-create-story/checklist.md</files>
  <action>
Single insert. Find the existing section `### **3.5 Implementation DISASTERS**` (around line 162) and append exactly one new subsection immediately after its bullet list (i.e. after the line containing `- **Quality failures:**...`). Do not rewrite any other section.

Insert:

```markdown

#### **3.6 Mock-Only Acceptance DISASTERS** 🚨

The single most common failure mode of LLM dev agents: declaring "done" because mocked tests pass. The story format must make this impossible. **REJECT the story** if any of the following is true:

- [ ] **Missing Real-World Verification section** — the H2 section `## Real-World Verification` is absent or has zero filled rows
- [ ] **Verification rows name no real system** — a row says "run tests" / "verify it works" / "check the function" instead of a concrete command, URL, or UI flow against a real DB / real upstream / real browser
- [ ] **AC without a verification row** — any Acceptance Criterion has no corresponding Real-World Verification row mapped to it
- [ ] **Missing Anti-Acceptance section** — the H2 section `## Anti-Acceptance` is absent
- [ ] **Anti-Acceptance softened** — bullets reworded, removed, or hedged (e.g. "mocks discouraged" instead of "mocks are NOT acceptance")
- [ ] **Missing Real-World Verification Evidence slot** — the `### Real-World Verification Evidence` heading is absent under Dev Agent Record
- [ ] **Vague verification** — uses "should", "may", "could" instead of imperative real-system instructions

If any box above would be checked, the story is NOT ready-for-dev. Return to the workflow and force-populate the missing field — do NOT pass the story to gm-dev-story.

**Why this matters:** gm-dev-story reads only the story file. If the story does not literally name the forbidden patterns and the real-system commands, the dev agent will pick the cheapest path. The story IS the bar.
```

Do not change any other rule, mistake, optimisation, or success metric in checklist.md.
  </action>
  <verify>
1. `grep -c "Mock-Only Acceptance DISASTERS" checklist.md` → `1`.
2. `grep -c "REJECT the story" checklist.md` → `1`.
3. The new subsection appears AFTER `### **3.5 Implementation DISASTERS**` and BEFORE `### **Step 4:` (the "LLM-Dev-Agent Optimization Analysis" section). Verify by `grep -n "3\.5\|3\.6\|Step 4:" checklist.md | head -10` — line numbers must be in ascending order 3.5 < 3.6 < Step 4.
4. `git diff --stat checklist.md` shows ~18 lines added, 0 removed.
5. **Behavioral verification:** feed a deliberately bad story (no Real-World Verification section, ACs only) to the SM agent's checklist self-validation step (workflow.md line 367). Confirm the agent now flags it under 3.6 and refuses to mark the story `ready-for-dev`. Feed a good story with the new sections — confirm it passes 3.6.
  </verify>
  <done>
- checklist.md has a new 3.6 subsection placed correctly between 3.5 and Step 4.
- The 7 reject-the-story checkboxes literally name the failure modes; no soft language.
- No existing checklist item was edited or reordered.
- The SM agent's self-validation pass now blocks a story that lacks real-world verification or has a softened anti-acceptance list.
  </done>
</task>

</tasks>

<out_of_scope>

## Out of scope (flag for future quick task)

These follow-ups are needed for end-to-end closure but are explicitly outside this quick task:

1. **gm-dev-story DoD does not gate on Real-World Verification Evidence** (`gm-dev-story/checklist.md` lines 41–48 / lines 58–65). Today the dev agent's DoD has no checkbox of the form *"Real-World Verification Evidence section is filled with actual observed output, not 'tests pass'"*. The story body now demands it (Task 1 + Task 2), but the dev agent's own self-validation checklist won't fail on its absence. **Recommended follow-up quick task:** add to `gm-dev-story/checklist.md` a new "Real-World Verification" block under "🧪 Testing & Quality Assurance" with checkboxes mirroring template.md's section, and a HALT condition in `gm-dev-story/workflow.md` step 9 that refuses Status=review if the Real-World Verification Evidence slot is empty.

2. **gm-code-review does not currently re-execute Real-World Verification commands.** A natural extension would be: at code-review time, the reviewer agent re-runs each row of the story's Real-World Verification table and confirms the captured evidence matches reality. Out of scope here — flag as a separate quick task on the gm-code-review skill.

3. **Anti-acceptance bullets are static.** Some projects have project-specific forbidden patterns (e.g. "no SQL string concat", "no `// @ts-ignore`"). A future enhancement would let `_gomad/agile/config.yaml` declare additional anti-acceptance bullets that gm-create-story appends. Out of scope.

4. **Tangential issue noticed (per CLAUDE.md item 3 — not acted on):** `workflow.md` lines 156–207 are a near-duplicate of lines 95–154 (the same auto-discover logic appears twice, once inside `<check if="no user input provided">` and once at file scope). This is pre-existing dead code or a merge artifact. Flagging only — do not delete in this quick task.

</out_of_scope>

<verification>

## Combined verification — does the change actually move agent behavior?

1. **Negative test (the one that matters):** Ask a fresh Claude instance to run gm-dev-story against a story produced by the *old* template (no Real-World Verification, no Anti-Acceptance) vs. a story produced by the *new* template. Old template → agent will write mock tests, declare done at Status=review. New template → agent encounters the Real-World Verification table, the literal "❌ Unit/integration tests that pass only because dependencies are mocked … are NOT acceptance" line, and the explicit HALT-don't-soften instruction. The agent's only legal moves are (a) execute the real commands and paste output, or (b) HALT and report.

2. **Positive test:** Generate a real story for an existing epic (any one with ≥1 backlog story in sprint-status.yaml). Confirm the emitted story file:
   - Has `## Real-World Verification` populated with concrete commands per AC
   - Has `## Anti-Acceptance` copied verbatim
   - Has empty `### Real-World Verification Evidence` slot under Dev Agent Record awaiting dev-agent fill

3. **Regression test:** All other story sections (Story / Acceptance Criteria / Tasks / Dev Notes / References / Dev Agent Record / File List / Change Log) render unchanged. Diff shows additions only.

</verification>

<success_criteria>

- [ ] template.md gains exactly two new H2 sections + one new H3 slot, no other line touched
- [ ] workflow.md step 5 renders the two new sections via two new `<template-output>` directives, plus two new `<critical>` instructions inside step 5 only
- [ ] checklist.md gains exactly one new 3.6 subsection between 3.5 and Step 4, no other line touched
- [ ] Total skill text added across the three files: ~55–70 lines (template ~35, workflow ~6, checklist ~18)
- [ ] Total skill text removed: 0 lines
- [ ] Out-of-scope follow-ups documented (gm-dev-story DoD gap is the highest priority, listed first)
- [ ] A story produced from the updated skill physically cannot be silently satisfied by mock-only test passage — the format demands a real-system verification row per AC, names the forbidden proofs verbatim, and gates on those at SM-self-validation time

</success_criteria>

<output>
After completion, no SUMMARY.md is required for a quick task — commit the three file edits as a single commit with message `docs(quick-260510-ql6): close mock-only-done loophole in gm-create-story (template + workflow + checklist)`.
</output>
