---
phase: 260501-vy9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gomad-skills/4-implementation/gm-dev-story/workflow.md
  - src/gomad-skills/4-implementation/gm-code-review/workflow.md
  - src/gomad-skills/4-implementation/gm-code-review/steps/step-04-present.md
  - src/gomad-skills/4-implementation/gm-sprint-agent/workflow.md
autonomous: true
requirements:
  - VY9-A
  - VY9-B
  - VY9-C
must_haves:
  truths:
    - "gm-dev-story appends a per-story summary entry to {output_folder}/epic-{epic-num}-done.md after marking the story status to review and before user communication."
    - "gm-code-review updates the existing summary entry only when code modifications occurred during review (option 0 or option 1 was taken)."
    - "gm-sprint-agent Phase 4 no longer writes the summary; it only announces, commits, updates sprint-status, and announces completion."
    - "The summary markdown template in gm-dev-story matches the template currently in gm-sprint-agent Phase 4 step 3 verbatim."
  artifacts:
    - path: "src/gomad-skills/4-implementation/gm-dev-story/workflow.md"
      provides: "INITIALIZATION resolves output_folder; new step appends summary entry to epic-{epic-num}-done.md after status→review."
      contains: "output_folder"
    - path: "src/gomad-skills/4-implementation/gm-code-review/workflow.md"
      provides: "INITIALIZATION resolves output_folder for use by step-04-present."
      contains: "output_folder"
    - path: "src/gomad-skills/4-implementation/gm-code-review/steps/step-04-present.md"
      provides: "New 'Update summary' section between patch handling and status update; runs only when patches applied."
      contains: "epic-{epic-num}-done.md"
    - path: "src/gomad-skills/4-implementation/gm-sprint-agent/workflow.md"
      provides: "Phase 4 trimmed: announce → commit → sprint-status update → announce. Summary write removed."
      contains: "Phase 4"
  key_links:
    - from: "gm-dev-story/workflow.md (new summary step)"
      to: "{output_folder}/epic-{epic-num}-done.md"
      via: "Append entry using exact template copied from gm-sprint-agent Phase 4 step 3"
      pattern: "epic-.*-done\\.md"
    - from: "gm-code-review/steps/step-04-present.md (new update section)"
      to: "{output_folder}/epic-{epic-num}-done.md"
      via: "Locate {story_key} entry and update Work Done / Known Issues; skip if entry missing or no patches applied"
      pattern: "epic-.*-done\\.md"
    - from: "gm-sprint-agent/workflow.md Phase 4"
      to: "(removed link)"
      via: "Summary write deleted; note added pointing to gm-dev-story as new owner"
      pattern: "Append summary"
---

<objective>
Move the per-story summary write from gm-sprint-agent Phase 4 into gm-dev-story (as a new step after status→review), and add a complementary "Update summary" section to gm-code-review/step-04-present that runs only when review-time code patches were applied.

Purpose: Decouple summary authorship from the sprint-agent's commit/announce phase so that summaries are written at the natural authorship point (story completion) and updated at the natural amendment point (post-review patches), not coupled to the orchestration agent.

Output: Four edited skill files. No new files. Behavior change is documentation-only (skill workflow markdown).
</objective>

<context>
@CLAUDE.md
@src/gomad-skills/module.yaml
@src/gomad-skills/4-implementation/gm-sprint-agent/workflow.md
@src/gomad-skills/4-implementation/gm-dev-story/workflow.md
@src/gomad-skills/4-implementation/gm-code-review/workflow.md
@src/gomad-skills/4-implementation/gm-code-review/steps/step-04-present.md

<key_facts>
- output_folder is a top-level config key (parent of implementation_artifacts).
- story_key format: "1-2-user-auth" — epic-num = first hyphen-separated segment ("1").
- Story file lives at {implementation_artifacts}/{story_key}.md in both gm-dev-story and gm-code-review.
- gm-sprint-agent Phase 4 currently has 6 steps; after this change it should have 4 (announce, commit, sprint-status update, announce).
- gm-dev-story currently ends at step 10 (communicate completion); the new summary step inserts between step 9 (status→review) and step 10.
- gm-code-review/step-04-present has 7 sections; after this change section 6 (update summary) inserts between current section 5 (patches) and current section 6 (status), shifting status→7 and next-steps→8.
</key_facts>

<summary_template>
The exact template (verbatim) from gm-sprint-agent Phase 4 step 3 to be copied into gm-dev-story:

```markdown
---

## {story-key} — {story title}

**Date:** {current date}

### Story
{One-line description}

### Work Done
- {Bullet list of completed FEATURES/CAPABILITIES}

### Known Issues
- {Deferred issues, or "None"}
```
</summary_template>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update gm-dev-story to load output_folder and append summary after status→review</name>
  <files>src/gomad-skills/4-implementation/gm-dev-story/workflow.md</files>
  <action>
Make two edits to this file:

(1) In the INITIALIZATION / Configuration Loading section (the block that already resolves project_name, user_name, communication_language, document_output_language, user_skill_level, implementation_artifacts, date), add resolution of `output_folder` from config.yaml. Match the surrounding style exactly (same bullet/line format the existing entries use). Place the new entry adjacent to `implementation_artifacts` since they share a parent.

(2) Insert a NEW step between the existing step 9 (which marks story status to "review" and updates sprint-status.yaml) and step 10 (communicate completion). The new step:
   - Uses the same `<step n="..." goal="..." tag="...">` XML-ish block style as the surrounding steps in this workflow.
   - Numbered as the new step 10; renumber the existing step 10 to 11.
   - Goal: append a summary entry to `{output_folder}/epic-{epic-num}-done.md` (create file if not exists).
   - Substeps:
     1. Compute `epic-num` = first hyphen-separated segment of `{story_key}` (e.g., story_key "1-2-user-auth" → epic-num "1").
     2. Resolve target file path: `{output_folder}/epic-{epic-num}-done.md`. If file does not exist, create it (empty contents are fine; the appended entry begins with the `---` divider so a fresh file reads correctly).
     3. Extract from the story file `{implementation_artifacts}/{story_key}.md`:
        - story title (from the story file's H1 / title field — use the same source step 9 already references)
        - one-line description (from the story file's description/goal section)
        - completed tasks (the FEATURES/CAPABILITIES, not implementation minutiae)
        - deferred items from `deferred-work.md` (or "None" if none for this story)
     4. Append the following markdown block VERBATIM (copied from gm-sprint-agent Phase 4 step 3, with placeholders filled):

        ```markdown
        ---

        ## {story-key} — {story title}

        **Date:** {current date}

        ### Story
        {One-line description}

        ### Work Done
        - {Bullet list of completed FEATURES/CAPABILITIES}

        ### Known Issues
        - {Deferred issues, or "None"}
        ```

   - Constraints noted in the step body:
     - This step runs AFTER status is set to "review" and BEFORE user communication.
     - Do NOT modify any pre-existing entries in the file; only append.
     - Use {current date} from the INITIALIZATION-loaded `date` value.

(3) Renumber the former step 10 (communicate completion) to step 11. Update any internal cross-references in the workflow that point to "step 10" to point to "step 11" as appropriate. Do NOT alter step content; only the step number changes.

Do not touch any other steps or sections. Match existing indentation and tag style precisely.
  </action>
  <verify>
    <automated>grep -n "output_folder" src/gomad-skills/4-implementation/gm-dev-story/workflow.md && grep -n "epic-.*-done.md" src/gomad-skills/4-implementation/gm-dev-story/workflow.md && grep -c '<step n=' src/gomad-skills/4-implementation/gm-dev-story/workflow.md</automated>
  </verify>
  <done>
- INITIALIZATION block resolves `output_folder`.
- A new step appears between the existing status→review step and the existing communicate-completion step.
- The new step contains the verbatim summary template (matching gm-sprint-agent Phase 4 step 3).
- Total step count increased by exactly 1; the former step 10 is now step 11; all other steps unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add 'Update summary' section to gm-code-review/step-04-present and load output_folder in parent workflow</name>
  <files>src/gomad-skills/4-implementation/gm-code-review/workflow.md, src/gomad-skills/4-implementation/gm-code-review/steps/step-04-present.md</files>
  <action>
Make two edits:

(1) `src/gomad-skills/4-implementation/gm-code-review/workflow.md` — In the INITIALIZATION / Configuration Loading section (the block at lines ~38-48 that loads project_name, planning_artifacts, implementation_artifacts, user_name, communication_language, document_output_language, user_skill_level, date, sprint_status, project_context), add resolution of `output_folder` from config.yaml. Place adjacent to `implementation_artifacts`. Match existing style exactly.

(2) `src/gomad-skills/4-implementation/gm-code-review/steps/step-04-present.md` — Insert a new numbered section between the current section 5 (Handle patch findings — Options 0/1/2/3) and the current section 6 (Update story status and sync sprint tracking). The new section becomes section 6; renumber current section 6 → 7 and current section 7 (Next steps) → 8. Update any internal cross-references that point to those section numbers.

The new section 6 has the following content:

   - Title: "Update summary (only if review applied code patches)"
   - Trigger condition (state explicitly at top of section): run this section ONLY IF the user selected Option 0 (batch-apply) or Option 1 (fix automatically) in section 5 AND at least one patch was applied. Skip otherwise (Options 2/3 with no fixes, or clean review with zero findings).
   - Substeps:
     1. Compute `epic-num` = first hyphen-separated segment of `{story_key}`.
     2. Target file: `{output_folder}/epic-{epic-num}-done.md`.
     3. Locate the existing entry for `{story_key}` in that file (the H2 heading `## {story-key} — ...`).
        - If the entry is NOT found, log a one-line note ("summary entry for {story_key} not found in epic-{epic-num}-done.md; skipping update — likely standalone code review run") and skip the rest of this section. Do NOT create a new entry; entry creation is gm-dev-story's responsibility.
     4. If the entry IS found, update it in place:
        - Append/extend the "Work Done" subsection with bullets describing the post-review patches that were applied (use the patch findings from section 5 as the source).
        - Update the "Known Issues" subsection to include any newly-deferred items added during review (from deferred-work.md). If the subsection currently reads "None" and new deferrals exist, replace "None" with the bullet list.
     5. Do NOT touch other entries or other sections of the file.

Do not touch the section 4 (resolve decision-needed findings) or section 5 (handle patch findings) content. Only insert the new section and renumber subsequent sections.
  </action>
  <verify>
    <automated>grep -n "output_folder" src/gomad-skills/4-implementation/gm-code-review/workflow.md && grep -n "Update summary" src/gomad-skills/4-implementation/gm-code-review/steps/step-04-present.md && grep -n "epic-.*-done.md" src/gomad-skills/4-implementation/gm-code-review/steps/step-04-present.md && grep -nE '^### [0-9]+\.|^## [0-9]+\.|^[0-9]+\.' src/gomad-skills/4-implementation/gm-code-review/steps/step-04-present.md | head -20</automated>
  </verify>
  <done>
- gm-code-review/workflow.md INITIALIZATION resolves `output_folder`.
- step-04-present.md has a new section titled "Update summary" inserted between patch-handling and status-update sections.
- The new section explicitly states its trigger condition (only when patches were applied) and the skip path when the entry doesn't exist.
- Subsequent sections (status update, next steps) are correctly renumbered.
- No edits to unrelated sections.
  </done>
</task>

<task type="auto">
  <name>Task 3: Trim gm-sprint-agent Phase 4 — remove summary write, keep only commit + status + announces</name>
  <files>src/gomad-skills/4-implementation/gm-sprint-agent/workflow.md</files>
  <action>
Edit Phase 4 (SUMMARY & COMMIT, lines ~320-368). After this change Phase 4 must contain exactly four steps in order:

   1. Announce (current step 1, unchanged).
   2. Commit all changes (currently step 4).
   3. Update sprint-status.yaml (review → done) (currently step 5).
   4. Announce completion (currently step 6).

Remove:
   - Current step 2 (Read story file to extract title/completed tasks/known issues) — no longer needed because step 4 (commit) operates via git and step 5 (sprint-status update) does not depend on those extracted fields. Confirm by inspection that no later step references the extracted variables; if any does, leave the read in place and only remove step 3.
   - Current step 3 (Append summary to epic-{epic-num}-done.md) — moved to gm-dev-story.

Renumber the remaining steps to 1, 2, 3, 4 sequentially. Update the Phase 4 header section: directly under the Phase 4 title, add a single-line note (no more, no less) explaining the relocation. Suggested wording (adjust to match the file's tone):

> Note: The per-story summary entry in `{output_folder}/epic-{epic-num}-done.md` is now written by `gm-dev-story` upon story completion and updated by `gm-code-review` when review-time patches are applied. Phase 4 here is intentionally lighter: commit + sprint-status update + announce.

Also rename Phase 4 if its title implies summary-writing (e.g., "SUMMARY & COMMIT" → "COMMIT & STATUS" or similar) ONLY if the existing title would be misleading after the change. If the existing title still reads sensibly (e.g., a "summary" announcement still happens), keep it.

Do NOT change other Phases. Do NOT alter the announce wording, the commit command, or the sprint-status mechanics — only remove the two steps and renumber.

Verify by inspection that Phase 4 still meets its original intent: final commit + status transition to done + final user-facing announcement.
  </action>
  <verify>
    <automated>! grep -n "Append summary" src/gomad-skills/4-implementation/gm-sprint-agent/workflow.md && grep -n "Phase 4" src/gomad-skills/4-implementation/gm-sprint-agent/workflow.md && grep -nE "^[[:space:]]*(Step|step) [0-9]+|^[0-9]+\." src/gomad-skills/4-implementation/gm-sprint-agent/workflow.md | head -30</automated>
  </verify>
  <done>
- "Append summary" no longer appears in gm-sprint-agent/workflow.md.
- Phase 4 contains exactly four steps: Announce, Commit, Update sprint-status, Announce.
- A one-line note under the Phase 4 header points to gm-dev-story / gm-code-review as the new summary owners.
- No other Phases were modified.
  </done>
</task>

</tasks>

<verification>
After all three tasks:

1. `grep -rn "epic-.*-done.md" src/gomad-skills/4-implementation/` should show occurrences in:
   - gm-dev-story/workflow.md (new summary step)
   - gm-code-review/steps/step-04-present.md (new update section)
   - gm-sprint-agent/workflow.md (only in any preserved announce/note text, NOT in an action step)

2. `grep -rn "output_folder" src/gomad-skills/4-implementation/gm-dev-story/workflow.md src/gomad-skills/4-implementation/gm-code-review/workflow.md` should show output_folder resolved in both INITIALIZATION blocks.

3. `! grep -n "Append summary" src/gomad-skills/4-implementation/gm-sprint-agent/workflow.md` should pass (returns 0 — phrase removed).

4. Manual read-through: the four files together describe a coherent flow:
   - gm-dev-story creates the entry on completion.
   - gm-code-review amends the entry only when patches were applied.
   - gm-sprint-agent commits and flips status without touching the summary.
</verification>

<success_criteria>
- All three changes (A, B, C) implemented in the four target files.
- Summary template in gm-dev-story is byte-identical to the template currently in gm-sprint-agent Phase 4 step 3 (modulo placeholder substitution at runtime).
- gm-code-review's update step is gated on "patches were applied" and skips gracefully when the entry doesn't exist.
- gm-sprint-agent Phase 4 reduced to four steps with a one-line relocation note.
- No new files created. No unrelated content edited. Existing style of each file preserved.
</success_criteria>

<output>
After completion, create `.planning/quick/260501-vy9-gm-sprint-agent-skill-append-summary-gm-/260501-vy9-01-SUMMARY.md` summarizing the diffs applied to each of the four files.
</output>
